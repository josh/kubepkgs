# This file was generated with nixidy resource generator, do not edit.
{
  lib,
  options,
  config,
  ...
}:

with lib;

let
  hasAttrNotNull = attr: set: hasAttr attr set && set.${attr} != null;

  attrsToList =
    values:
    if values != null then
      sort (
        a: b:
        if (hasAttrNotNull "_priority" a && hasAttrNotNull "_priority" b) then
          a._priority < b._priority
        else
          false
      ) (mapAttrsToList (n: v: v) values)
    else
      values;

  getDefaults =
    resource: group: version: kind:
    catAttrs "default" (
      filter (
        default:
        (default.resource == null || default.resource == resource)
        && (default.group == null || default.group == group)
        && (default.version == null || default.version == version)
        && (default.kind == null || default.kind == kind)
      ) config.defaults
    );

  types = lib.types // rec {
    str = mkOptionType {
      name = "str";
      description = "string";
      check = isString;
      merge = mergeEqualOption;
    };

    # Either value of type `finalType` or `coercedType`, the latter is
    # converted to `finalType` using `coerceFunc`.
    coercedTo =
      coercedType: coerceFunc: finalType:
      mkOptionType rec {
        inherit (finalType) getSubOptions getSubModules;

        name = "coercedTo";
        description = "${finalType.description} or ${coercedType.description}";
        check = x: finalType.check x || coercedType.check x;
        merge =
          loc: defs:
          let
            coerceVal =
              val:
              if finalType.check val then
                val
              else
                let
                  coerced = coerceFunc val;
                in
                assert finalType.check coerced;
                coerced;

          in
          finalType.merge loc (map (def: def // { value = coerceVal def.value; }) defs);
        substSubModules = m: coercedTo coercedType coerceFunc (finalType.substSubModules m);
        typeMerge = t1: t2: null;
        functor = (defaultFunctor name) // {
          wrapped = finalType;
        };
      };

    # Numeric bounds.
    withMinimum =
      min: base:
      lib.types.addCheck base (x: x >= min)
      // {
        description = "${base.description} (minimum ${toString min})";
      };
    withMaximum =
      max: base:
      lib.types.addCheck base (x: x <= max)
      // {
        description = "${base.description} (maximum ${toString max})";
      };
    withExclusiveMinimum =
      min: base:
      lib.types.addCheck base (x: x > min)
      // {
        description = "${base.description} (exclusive minimum ${toString min})";
      };
    withExclusiveMaximum =
      max: base:
      lib.types.addCheck base (x: x < max)
      // {
        description = "${base.description} (exclusive maximum ${toString max})";
      };
    withMultipleOf =
      m: base:
      lib.types.addCheck base (x: mod x m == 0)
      // {
        description = "${base.description} (multiple of ${toString m})";
      };

    # String constraints.
    withMinLength =
      n: base:
      lib.types.addCheck base (x: stringLength x >= n)
      // {
        description = "${base.description} (min length ${toString n})";
      };
    withMaxLength =
      n: base:
      lib.types.addCheck base (x: stringLength x <= n)
      // {
        description = "${base.description} (max length ${toString n})";
      };
  };

  mkOptionDefault = mkOverride 1001;

  mergeValuesByKey =
    attrMergeKey: listMergeKeys: values:
    listToAttrs (
      imap0 (
        i: value:
        nameValuePair (
          if hasAttr attrMergeKey value then
            if isAttrs value.${attrMergeKey} then
              toString value.${attrMergeKey}.content
            else
              (toString value.${attrMergeKey})
          else
            # generate merge key for list elements if it's not present
            "__kubenix_list_merge_key_"
            + (concatStringsSep "" (
              map (
                key: if isAttrs value.${key} then toString value.${key}.content else (toString value.${key})
              ) listMergeKeys
            ))
        ) (value // { _priority = i; })
      ) values
    );

  submoduleOf =
    ref:
    types.submodule (
      { name, ... }:
      {
        options = definitions."${ref}".options or { };
        config = definitions."${ref}".config or { };
      }
    );

  globalSubmoduleOf =
    ref:
    types.submodule (
      { name, ... }:
      {
        options = config.definitions."${ref}".options or { };
        config = config.definitions."${ref}".config or { };
      }
    );

  submoduleWithMergeOf =
    ref: mergeKey:
    types.submodule (
      { name, ... }:
      let
        convertName =
          name: if definitions."${ref}".options.${mergeKey}.type == types.int then toInt name else name;
      in
      {
        options = definitions."${ref}".options // {
          # position in original array
          _priority = mkOption {
            type = types.nullOr types.int;
            default = null;
            internal = true;
          };
        };
        config = definitions."${ref}".config // {
          ${mergeKey} = mkOverride 1002 (
            # use name as mergeKey only if it is not coming from mergeValuesByKey
            if (!hasPrefix "__kubenix_list_merge_key_" name) then convertName name else null
          );
        };
      }
    );

  submoduleForDefinition =
    ref: resource: kind: group: version:
    let
      apiVersion = if group == "core" then version else "${group}/${version}";
    in
    types.submodule (
      { name, ... }:
      {
        inherit (definitions."${ref}") options;

        imports = getDefaults resource group version kind;
        config = mkMerge [
          definitions."${ref}".config
          {
            kind = mkOptionDefault kind;
            apiVersion = mkOptionDefault apiVersion;

            # metdata.name cannot use option default, due deep config
            metadata.name = mkOptionDefault name;
          }
        ];
      }
    );

  coerceAttrsOfSubmodulesToListByKey =
    ref: attrMergeKey: listMergeKeys:
    (types.coercedTo (types.listOf (submoduleOf ref)) (mergeValuesByKey attrMergeKey listMergeKeys) (
      types.attrsOf (submoduleWithMergeOf ref attrMergeKey)
    ));

  definitions = {
    "ceph.rook.io.v1.CephBlockPool" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "NamedBlockPoolSpec allows a block pool to be created with a non-default name.\nThis is more specific than the NamedPoolSpec so we get schema validation on the\nallowed pool names that can be specified.";
          type = (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpec");
        };
        "status" = mkOption {
          description = "CephBlockPoolStatus represents the mirroring status of Ceph Storage Pool";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespace" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "Spec represents the specification of a Ceph BlockPool Rados Namespace";
          type = (submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceSpec");
        };
        "status" = mkOption {
          description = "Status represents the status of a CephBlockPool Rados Namespace";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceSpec" = {

      options = {
        "blockPoolName" = mkOption {
          description = "BlockPoolName is the name of Ceph BlockPool. Typically it's the name of\nthe CephBlockPool CR.";
          type = types.str;
        };
        "clusterID" = mkOption {
          description = "ClusterID to be used for this RadosNamespace in the CSI configuration.\nIt must be unique among all Ceph clusters managed by Rook.\nIf not specified, the clusterID will be generated and can be found in the CR status.";
          type = (types.nullOr (types.withMaxLength 36 (types.withMinLength 1 types.str)));
        };
        "mirroring" = mkOption {
          description = "Mirroring configuration of CephBlockPoolRadosNamespace";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceSpecMirroring"));
        };
        "name" = mkOption {
          description = "The name of the CephBlockPoolRadosNamespaceSpec namespace. If not set, the default is the name of the CR.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "clusterID" = mkOverride 1002 null;
        "mirroring" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceSpecMirroring" = {

      options = {
        "mode" = mkOption {
          description = "Mode is the mirroring mode; either pool or image.";
          type = (
            types.enum [
              ""
              "pool"
              "image"
            ]
          );
        };
        "remoteNamespace" = mkOption {
          description = "RemoteNamespace is the name of the CephBlockPoolRadosNamespace on the secondary cluster CephBlockPool";
          type = (types.nullOr types.str);
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the scheduling of snapshot for mirrored images";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceSpecMirroringSnapshotSchedules"
              )
            )
          );
        };
      };

      config = {
        "remoteNamespace" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceSpecMirroringSnapshotSchedules" = {

      options = {
        "interval" = mkOption {
          description = "Interval represent the periodicity of the snapshot.";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot, only valid for CephFS";
          type = (types.nullOr types.str);
        };
        "startTime" = mkOption {
          description = "StartTime indicates when to start the snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "startTime" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatus" = {

      options = {
        "conditions" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusConditions")
            )
          );
        };
        "info" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "mirroringInfo" = mkOption {
          description = "MirroringInfoSpec is the status of the pool/radosnamespace mirroring";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringInfo")
          );
        };
        "mirroringStatus" = mkOption {
          description = "MirroringStatusSpec is the status of the pool/radosNamespace mirroring";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatus")
          );
        };
        "phase" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
        "snapshotScheduleStatus" = mkOption {
          description = "SnapshotScheduleStatusSpec is the status of the snapshot schedule";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusSnapshotScheduleStatus")
          );
        };
      };

      config = {
        "conditions" = mkOverride 1002 null;
        "info" = mkOverride 1002 null;
        "mirroringInfo" = mkOverride 1002 null;
        "mirroringStatus" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
        "snapshotScheduleStatus" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringInfo" = {

      options = {
        "details" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastChanged" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastChecked" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "mode" = mkOption {
          description = "Mode is the mirroring mode";
          type = (types.nullOr types.str);
        };
        "peers" = mkOption {
          description = "Peers are the list of peer sites connected to that cluster";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringInfoPeers")
            )
          );
        };
        "site_name" = mkOption {
          description = "SiteName is the current site name";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "details" = mkOverride 1002 null;
        "lastChanged" = mkOverride 1002 null;
        "lastChecked" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "site_name" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringInfoPeers" = {

      options = {
        "client_name" = mkOption {
          description = "ClientName is the CephX user used to connect to the peer";
          type = (types.nullOr types.str);
        };
        "direction" = mkOption {
          description = "Direction is the peer mirroring direction";
          type = (types.nullOr types.str);
        };
        "mirror_uuid" = mkOption {
          description = "MirrorUUID is the mirror UUID";
          type = (types.nullOr types.str);
        };
        "site_name" = mkOption {
          description = "SiteName is the current site name";
          type = (types.nullOr types.str);
        };
        "uuid" = mkOption {
          description = "UUID is the peer UUID";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "client_name" = mkOverride 1002 null;
        "direction" = mkOverride 1002 null;
        "mirror_uuid" = mkOverride 1002 null;
        "site_name" = mkOverride 1002 null;
        "uuid" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatus" = {

      options = {
        "details" = mkOption {
          description = "Details contains potential status errors";
          type = (types.nullOr types.str);
        };
        "lastChanged" = mkOption {
          description = "LastChanged is the last time the status last changed";
          type = (types.nullOr types.str);
        };
        "lastChecked" = mkOption {
          description = "LastChecked is the last time the status was checked";
          type = (types.nullOr types.str);
        };
        "summary" = mkOption {
          description = "Summary is the mirroring status summary";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatusSummary")
          );
        };
      };

      config = {
        "details" = mkOverride 1002 null;
        "lastChanged" = mkOverride 1002 null;
        "lastChecked" = mkOverride 1002 null;
        "summary" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatusSummary" = {

      options = {
        "daemon_health" = mkOption {
          description = "DaemonHealth is the health of the mirroring daemon";
          type = (types.nullOr types.str);
        };
        "group_health" = mkOption {
          description = "GroupHealth is the health of the mirrored image group";
          type = (types.nullOr types.str);
        };
        "group_states" = mkOption {
          description = "GroupStates is the various state for all mirrored image groups";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatusSummaryGroup_states"
            )
          );
        };
        "health" = mkOption {
          description = "Health is the mirroring health";
          type = (types.nullOr types.str);
        };
        "image_health" = mkOption {
          description = "ImageHealth is the health of the mirrored image";
          type = (types.nullOr types.str);
        };
        "image_states" = mkOption {
          description = "ImageStates is the various state for all mirrored images";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatusSummaryImage_states"
            )
          );
        };
        "states" = mkOption {
          description = "States is the various state for all mirrored images";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatusSummaryStates"
            )
          );
        };
      };

      config = {
        "daemon_health" = mkOverride 1002 null;
        "group_health" = mkOverride 1002 null;
        "group_states" = mkOverride 1002 null;
        "health" = mkOverride 1002 null;
        "image_health" = mkOverride 1002 null;
        "image_states" = mkOverride 1002 null;
        "states" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatusSummaryGroup_states" = {

      options = {
        "error" = mkOption {
          description = "Error is when the mirroring state is errored";
          type = (types.nullOr types.int);
        };
        "replaying" = mkOption {
          description = "Replaying is when the replay of the mirroring journal is on-going";
          type = (types.nullOr types.int);
        };
        "starting_replay" = mkOption {
          description = "StartingReplay is when the replay of the mirroring journal starts";
          type = (types.nullOr types.int);
        };
        "stopped" = mkOption {
          description = "Stopped is when the mirroring state is stopped";
          type = (types.nullOr types.int);
        };
        "stopping_replay" = mkOption {
          description = "StopReplaying is when the replay of the mirroring journal stops";
          type = (types.nullOr types.int);
        };
        "syncing" = mkOption {
          description = "Syncing is when the image is syncing";
          type = (types.nullOr types.int);
        };
        "unknown" = mkOption {
          description = "Unknown is when the mirroring state is unknown";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "error" = mkOverride 1002 null;
        "replaying" = mkOverride 1002 null;
        "starting_replay" = mkOverride 1002 null;
        "stopped" = mkOverride 1002 null;
        "stopping_replay" = mkOverride 1002 null;
        "syncing" = mkOverride 1002 null;
        "unknown" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatusSummaryImage_states" = {

      options = {
        "error" = mkOption {
          description = "Error is when the mirroring state is errored";
          type = (types.nullOr types.int);
        };
        "replaying" = mkOption {
          description = "Replaying is when the replay of the mirroring journal is on-going";
          type = (types.nullOr types.int);
        };
        "starting_replay" = mkOption {
          description = "StartingReplay is when the replay of the mirroring journal starts";
          type = (types.nullOr types.int);
        };
        "stopped" = mkOption {
          description = "Stopped is when the mirroring state is stopped";
          type = (types.nullOr types.int);
        };
        "stopping_replay" = mkOption {
          description = "StopReplaying is when the replay of the mirroring journal stops";
          type = (types.nullOr types.int);
        };
        "syncing" = mkOption {
          description = "Syncing is when the image is syncing";
          type = (types.nullOr types.int);
        };
        "unknown" = mkOption {
          description = "Unknown is when the mirroring state is unknown";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "error" = mkOverride 1002 null;
        "replaying" = mkOverride 1002 null;
        "starting_replay" = mkOverride 1002 null;
        "stopped" = mkOverride 1002 null;
        "stopping_replay" = mkOverride 1002 null;
        "syncing" = mkOverride 1002 null;
        "unknown" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusMirroringStatusSummaryStates" = {

      options = {
        "error" = mkOption {
          description = "Error is when the mirroring state is errored";
          type = (types.nullOr types.int);
        };
        "replaying" = mkOption {
          description = "Replaying is when the replay of the mirroring journal is on-going";
          type = (types.nullOr types.int);
        };
        "starting_replay" = mkOption {
          description = "StartingReplay is when the replay of the mirroring journal starts";
          type = (types.nullOr types.int);
        };
        "stopped" = mkOption {
          description = "Stopped is when the mirroring state is stopped";
          type = (types.nullOr types.int);
        };
        "stopping_replay" = mkOption {
          description = "StopReplaying is when the replay of the mirroring journal stops";
          type = (types.nullOr types.int);
        };
        "syncing" = mkOption {
          description = "Syncing is when the image is syncing";
          type = (types.nullOr types.int);
        };
        "unknown" = mkOption {
          description = "Unknown is when the mirroring state is unknown";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "error" = mkOverride 1002 null;
        "replaying" = mkOverride 1002 null;
        "starting_replay" = mkOverride 1002 null;
        "stopped" = mkOverride 1002 null;
        "stopping_replay" = mkOverride 1002 null;
        "syncing" = mkOverride 1002 null;
        "unknown" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusSnapshotScheduleStatus" = {

      options = {
        "details" = mkOption {
          description = "Details contains potential status errors";
          type = (types.nullOr types.str);
        };
        "lastChanged" = mkOption {
          description = "LastChanged is the last time the status last changed";
          type = (types.nullOr types.str);
        };
        "lastChecked" = mkOption {
          description = "LastChecked is the last time the status was checked";
          type = (types.nullOr types.str);
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the list of snapshots scheduled";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusSnapshotScheduleStatusSnapshotSchedules"
              )
            )
          );
        };
      };

      config = {
        "details" = mkOverride 1002 null;
        "lastChanged" = mkOverride 1002 null;
        "lastChecked" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusSnapshotScheduleStatusSnapshotSchedules" = {

      options = {
        "image" = mkOption {
          description = "Image is the mirrored image";
          type = (types.nullOr types.str);
        };
        "items" = mkOption {
          description = "Items is the list schedules times for a given snapshot";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusSnapshotScheduleStatusSnapshotSchedulesItems"
              )
            )
          );
        };
        "namespace" = mkOption {
          description = "Namespace is the RADOS namespace the image is part of";
          type = (types.nullOr types.str);
        };
        "pool" = mkOption {
          description = "Pool is the pool name";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "image" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
        "pool" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolRadosNamespaceStatusSnapshotScheduleStatusSnapshotSchedulesItems" = {

      options = {
        "interval" = mkOption {
          description = "Interval is the interval in which snapshots will be taken";
          type = (types.nullOr types.str);
        };
        "start_time" = mkOption {
          description = "StartTime is the snapshot starting time";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "start_time" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolSpec" = {

      options = {
        "application" = mkOption {
          description = "The application name to set on the pool. Only expected to be set for rgw pools.";
          type = (types.nullOr types.str);
        };
        "compressionMode" = mkOption {
          description = "DEPRECATED: use Parameters instead, e.g., Parameters[\"compression_mode\"] = \"force\"\nThe inline compression mode in Bluestore OSD to set to (options are: none, passive, aggressive, force)\nDo NOT set a default value for kubebuilder as this will override the Parameters";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "passive"
                "aggressive"
                "force"
                ""
              ]
            )
          );
        };
        "crushRoot" = mkOption {
          description = "The root of the crush hierarchy utilized by the pool";
          type = (types.nullOr types.str);
        };
        "deviceClass" = mkOption {
          description = "The device class the OSD should set to for use in the pool";
          type = (types.nullOr types.str);
        };
        "enableCrushUpdates" = mkOption {
          description = "Allow rook operator to change the pool CRUSH tunables once the pool is created";
          type = (types.nullOr types.bool);
        };
        "enableRBDStats" = mkOption {
          description = "EnableRBDStats is used to enable gathering of statistics for all RBD images in the pool";
          type = (types.nullOr types.bool);
        };
        "erasureCoded" = mkOption {
          description = "The erasure code settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpecErasureCoded"));
        };
        "failureDomain" = mkOption {
          description = "The failure domain: osd/host/(region or zone if available) - technically also any type in the crush map";
          type = (types.nullOr types.str);
        };
        "mirroring" = mkOption {
          description = "The mirroring settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpecMirroring"));
        };
        "name" = mkOption {
          description = "The desired name of the pool if different from the CephBlockPool CR name.";
          type = (
            types.nullOr (
              types.enum [
                ".rgw.root"
                ".nfs"
                ".mgr"
              ]
            )
          );
        };
        "parameters" = mkOption {
          description = "Parameters is a list of properties to enable on a given pool";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "quotas" = mkOption {
          description = "The quota settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpecQuotas"));
        };
        "replicated" = mkOption {
          description = "The replication settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpecReplicated"));
        };
        "statusCheck" = mkOption {
          description = "The mirroring statusCheck";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpecStatusCheck"));
        };
      };

      config = {
        "application" = mkOverride 1002 null;
        "compressionMode" = mkOverride 1002 null;
        "crushRoot" = mkOverride 1002 null;
        "deviceClass" = mkOverride 1002 null;
        "enableCrushUpdates" = mkOverride 1002 null;
        "enableRBDStats" = mkOverride 1002 null;
        "erasureCoded" = mkOverride 1002 null;
        "failureDomain" = mkOverride 1002 null;
        "mirroring" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "parameters" = mkOverride 1002 null;
        "quotas" = mkOverride 1002 null;
        "replicated" = mkOverride 1002 null;
        "statusCheck" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolSpecErasureCoded" = {

      options = {
        "algorithm" = mkOption {
          description = "The algorithm for erasure coding.\nIf absent, defaults to the plugin specified in osd_pool_default_erasure_code_profile.";
          type = (
            types.nullOr (
              types.enum [
                "isa"
                "jerasure"
              ]
            )
          );
        };
        "codingChunks" = mkOption {
          description = "Number of coding chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThis is the number of OSDs that can be lost simultaneously before data cannot be recovered.";
          type = (types.withMinimum 0 types.int);
        };
        "crushNumFailureDomains" = mkOption {
          description = "Number of failure domains to use for erasure coded chunk placement.\nWhen specified along with crushOSDsPerFailureDomain, a CRUSH MSR rule will be created\nthat distributes chunks across this many failure domains.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "crushOSDsPerFailureDomain" = mkOption {
          description = "Number of OSDs allowed per failure domain for erasure coded chunk placement.\nWhen specified along with crushNumFailureDomains, a CRUSH MSR rule will be created\nthat allows up to this many chunks on OSDs within each failure domain.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "dataChunks" = mkOption {
          description = "Number of data chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThe number of chunks required to recover an object when any single OSD is lost is the same\nas dataChunks so be aware that the larger the number of data chunks, the higher the cost of recovery.";
          type = (types.withMinimum 0 types.int);
        };
        "stripeUnit" = mkOption {
          description = "Erasure code stripe size in bytes. Ceph default is 4096 bytes (4 KiB).\nValue must be a multiple of 4096 (4Ki).";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "algorithm" = mkOverride 1002 null;
        "crushNumFailureDomains" = mkOverride 1002 null;
        "crushOSDsPerFailureDomain" = mkOverride 1002 null;
        "stripeUnit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolSpecMirroring" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled whether this pool is mirrored or not";
          type = (types.nullOr types.bool);
        };
        "mode" = mkOption {
          description = "Mode is the mirroring mode: pool, image or init-only.";
          type = (
            types.nullOr (
              types.enum [
                "pool"
                "image"
                "init-only"
              ]
            )
          );
        };
        "peers" = mkOption {
          description = "Peers represents the peers spec";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpecMirroringPeers"));
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the scheduling of snapshot for mirrored images/pools";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpecMirroringSnapshotSchedules")
            )
          );
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolSpecMirroringPeers" = {

      options = {
        "secretNames" = mkOption {
          description = "SecretNames represents the Kubernetes Secret names to add rbd-mirror or cephfs-mirror peers";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "secretNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolSpecMirroringSnapshotSchedules" = {

      options = {
        "interval" = mkOption {
          description = "Interval represent the periodicity of the snapshot.";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot, only valid for CephFS";
          type = (types.nullOr types.str);
        };
        "startTime" = mkOption {
          description = "StartTime indicates when to start the snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "startTime" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolSpecQuotas" = {

      options = {
        "maxBytes" = mkOption {
          description = "MaxBytes represents the quota in bytes\nDeprecated in favor of MaxSize";
          type = (types.nullOr types.int);
        };
        "maxObjects" = mkOption {
          description = "MaxObjects represents the quota in objects";
          type = (types.nullOr types.int);
        };
        "maxSize" = mkOption {
          description = "MaxSize represents the quota in bytes as a string";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "maxBytes" = mkOverride 1002 null;
        "maxObjects" = mkOverride 1002 null;
        "maxSize" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolSpecReplicated" = {

      options = {
        "hybridStorage" = mkOption {
          description = "HybridStorage represents hybrid storage tier settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpecReplicatedHybridStorage"));
        };
        "replicasPerFailureDomain" = mkOption {
          description = "ReplicasPerFailureDomain the number of replica in the specified failure domain";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "requireSafeReplicaSize" = mkOption {
          description = "RequireSafeReplicaSize if false allows you to set replica 1";
          type = (types.nullOr types.bool);
        };
        "size" = mkOption {
          description = "Size - Number of copies per object in a replicated storage pool, including the object itself (required for replicated pool type)";
          type = (types.withMinimum 0 types.int);
        };
        "subFailureDomain" = mkOption {
          description = "SubFailureDomain the name of the sub-failure domain";
          type = (types.nullOr types.str);
        };
        "targetSizeRatio" = mkOption {
          description = "TargetSizeRatio gives a hint (%) to Ceph in terms of expected consumption of the total cluster capacity";
          type = (types.nullOr (types.withMinimum 0 (types.either types.int types.float)));
        };
      };

      config = {
        "hybridStorage" = mkOverride 1002 null;
        "replicasPerFailureDomain" = mkOverride 1002 null;
        "requireSafeReplicaSize" = mkOverride 1002 null;
        "subFailureDomain" = mkOverride 1002 null;
        "targetSizeRatio" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolSpecReplicatedHybridStorage" = {

      options = {
        "primaryDeviceClass" = mkOption {
          description = "PrimaryDeviceClass represents high performance tier (for example SSD or NVME) for Primary OSD";
          type = (types.withMinLength 1 types.str);
        };
        "secondaryDeviceClass" = mkOption {
          description = "SecondaryDeviceClass represents low performance tier (for example HDDs) for remaining OSDs";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephBlockPoolSpecStatusCheck" = {

      options = {
        "mirror" = mkOption {
          description = "HealthCheckSpec represents the health check of an object store bucket";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolSpecStatusCheckMirror"));
        };
      };

      config = {
        "mirror" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolSpecStatusCheckMirror" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatus" = {

      options = {
        "cephx" = mkOption {
          description = "PeerTokenCephxStatus represents the cephx key rotation status for peer tokens";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusCephx"));
        };
        "conditions" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusConditions")));
        };
        "info" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "mirroringInfo" = mkOption {
          description = "MirroringInfoSpec is the status of the pool/radosnamespace mirroring";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusMirroringInfo"));
        };
        "mirroringStatus" = mkOption {
          description = "MirroringStatusSpec is the status of the pool/radosNamespace mirroring";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatus"));
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
        "poolID" = mkOption {
          description = "optional";
          type = (types.nullOr types.int);
        };
        "snapshotScheduleStatus" = mkOption {
          description = "SnapshotScheduleStatusSpec is the status of the snapshot schedule";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusSnapshotScheduleStatus"));
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
        "conditions" = mkOverride 1002 null;
        "info" = mkOverride 1002 null;
        "mirroringInfo" = mkOverride 1002 null;
        "mirroringStatus" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
        "poolID" = mkOverride 1002 null;
        "snapshotScheduleStatus" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusCephx" = {

      options = {
        "peerToken" = mkOption {
          description = "PeerToken shows the rotation status of the peer token associated with the `rbd-mirror-peer` user.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusCephxPeerToken"));
        };
      };

      config = {
        "peerToken" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusCephxPeerToken" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusMirroringInfo" = {

      options = {
        "details" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastChanged" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastChecked" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "mode" = mkOption {
          description = "Mode is the mirroring mode";
          type = (types.nullOr types.str);
        };
        "peers" = mkOption {
          description = "Peers are the list of peer sites connected to that cluster";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusMirroringInfoPeers"))
          );
        };
        "site_name" = mkOption {
          description = "SiteName is the current site name";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "details" = mkOverride 1002 null;
        "lastChanged" = mkOverride 1002 null;
        "lastChecked" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "site_name" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusMirroringInfoPeers" = {

      options = {
        "client_name" = mkOption {
          description = "ClientName is the CephX user used to connect to the peer";
          type = (types.nullOr types.str);
        };
        "direction" = mkOption {
          description = "Direction is the peer mirroring direction";
          type = (types.nullOr types.str);
        };
        "mirror_uuid" = mkOption {
          description = "MirrorUUID is the mirror UUID";
          type = (types.nullOr types.str);
        };
        "site_name" = mkOption {
          description = "SiteName is the current site name";
          type = (types.nullOr types.str);
        };
        "uuid" = mkOption {
          description = "UUID is the peer UUID";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "client_name" = mkOverride 1002 null;
        "direction" = mkOverride 1002 null;
        "mirror_uuid" = mkOverride 1002 null;
        "site_name" = mkOverride 1002 null;
        "uuid" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatus" = {

      options = {
        "details" = mkOption {
          description = "Details contains potential status errors";
          type = (types.nullOr types.str);
        };
        "lastChanged" = mkOption {
          description = "LastChanged is the last time the status last changed";
          type = (types.nullOr types.str);
        };
        "lastChecked" = mkOption {
          description = "LastChecked is the last time the status was checked";
          type = (types.nullOr types.str);
        };
        "summary" = mkOption {
          description = "Summary is the mirroring status summary";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatusSummary"));
        };
      };

      config = {
        "details" = mkOverride 1002 null;
        "lastChanged" = mkOverride 1002 null;
        "lastChecked" = mkOverride 1002 null;
        "summary" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatusSummary" = {

      options = {
        "daemon_health" = mkOption {
          description = "DaemonHealth is the health of the mirroring daemon";
          type = (types.nullOr types.str);
        };
        "group_health" = mkOption {
          description = "GroupHealth is the health of the mirrored image group";
          type = (types.nullOr types.str);
        };
        "group_states" = mkOption {
          description = "GroupStates is the various state for all mirrored image groups";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatusSummaryGroup_states")
          );
        };
        "health" = mkOption {
          description = "Health is the mirroring health";
          type = (types.nullOr types.str);
        };
        "image_health" = mkOption {
          description = "ImageHealth is the health of the mirrored image";
          type = (types.nullOr types.str);
        };
        "image_states" = mkOption {
          description = "ImageStates is the various state for all mirrored images";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatusSummaryImage_states")
          );
        };
        "states" = mkOption {
          description = "States is the various state for all mirrored images";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatusSummaryStates")
          );
        };
      };

      config = {
        "daemon_health" = mkOverride 1002 null;
        "group_health" = mkOverride 1002 null;
        "group_states" = mkOverride 1002 null;
        "health" = mkOverride 1002 null;
        "image_health" = mkOverride 1002 null;
        "image_states" = mkOverride 1002 null;
        "states" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatusSummaryGroup_states" = {

      options = {
        "error" = mkOption {
          description = "Error is when the mirroring state is errored";
          type = (types.nullOr types.int);
        };
        "replaying" = mkOption {
          description = "Replaying is when the replay of the mirroring journal is on-going";
          type = (types.nullOr types.int);
        };
        "starting_replay" = mkOption {
          description = "StartingReplay is when the replay of the mirroring journal starts";
          type = (types.nullOr types.int);
        };
        "stopped" = mkOption {
          description = "Stopped is when the mirroring state is stopped";
          type = (types.nullOr types.int);
        };
        "stopping_replay" = mkOption {
          description = "StopReplaying is when the replay of the mirroring journal stops";
          type = (types.nullOr types.int);
        };
        "syncing" = mkOption {
          description = "Syncing is when the image is syncing";
          type = (types.nullOr types.int);
        };
        "unknown" = mkOption {
          description = "Unknown is when the mirroring state is unknown";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "error" = mkOverride 1002 null;
        "replaying" = mkOverride 1002 null;
        "starting_replay" = mkOverride 1002 null;
        "stopped" = mkOverride 1002 null;
        "stopping_replay" = mkOverride 1002 null;
        "syncing" = mkOverride 1002 null;
        "unknown" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatusSummaryImage_states" = {

      options = {
        "error" = mkOption {
          description = "Error is when the mirroring state is errored";
          type = (types.nullOr types.int);
        };
        "replaying" = mkOption {
          description = "Replaying is when the replay of the mirroring journal is on-going";
          type = (types.nullOr types.int);
        };
        "starting_replay" = mkOption {
          description = "StartingReplay is when the replay of the mirroring journal starts";
          type = (types.nullOr types.int);
        };
        "stopped" = mkOption {
          description = "Stopped is when the mirroring state is stopped";
          type = (types.nullOr types.int);
        };
        "stopping_replay" = mkOption {
          description = "StopReplaying is when the replay of the mirroring journal stops";
          type = (types.nullOr types.int);
        };
        "syncing" = mkOption {
          description = "Syncing is when the image is syncing";
          type = (types.nullOr types.int);
        };
        "unknown" = mkOption {
          description = "Unknown is when the mirroring state is unknown";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "error" = mkOverride 1002 null;
        "replaying" = mkOverride 1002 null;
        "starting_replay" = mkOverride 1002 null;
        "stopped" = mkOverride 1002 null;
        "stopping_replay" = mkOverride 1002 null;
        "syncing" = mkOverride 1002 null;
        "unknown" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusMirroringStatusSummaryStates" = {

      options = {
        "error" = mkOption {
          description = "Error is when the mirroring state is errored";
          type = (types.nullOr types.int);
        };
        "replaying" = mkOption {
          description = "Replaying is when the replay of the mirroring journal is on-going";
          type = (types.nullOr types.int);
        };
        "starting_replay" = mkOption {
          description = "StartingReplay is when the replay of the mirroring journal starts";
          type = (types.nullOr types.int);
        };
        "stopped" = mkOption {
          description = "Stopped is when the mirroring state is stopped";
          type = (types.nullOr types.int);
        };
        "stopping_replay" = mkOption {
          description = "StopReplaying is when the replay of the mirroring journal stops";
          type = (types.nullOr types.int);
        };
        "syncing" = mkOption {
          description = "Syncing is when the image is syncing";
          type = (types.nullOr types.int);
        };
        "unknown" = mkOption {
          description = "Unknown is when the mirroring state is unknown";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "error" = mkOverride 1002 null;
        "replaying" = mkOverride 1002 null;
        "starting_replay" = mkOverride 1002 null;
        "stopped" = mkOverride 1002 null;
        "stopping_replay" = mkOverride 1002 null;
        "syncing" = mkOverride 1002 null;
        "unknown" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusSnapshotScheduleStatus" = {

      options = {
        "details" = mkOption {
          description = "Details contains potential status errors";
          type = (types.nullOr types.str);
        };
        "lastChanged" = mkOption {
          description = "LastChanged is the last time the status last changed";
          type = (types.nullOr types.str);
        };
        "lastChecked" = mkOption {
          description = "LastChecked is the last time the status was checked";
          type = (types.nullOr types.str);
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the list of snapshots scheduled";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusSnapshotScheduleStatusSnapshotSchedules"
              )
            )
          );
        };
      };

      config = {
        "details" = mkOverride 1002 null;
        "lastChanged" = mkOverride 1002 null;
        "lastChecked" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusSnapshotScheduleStatusSnapshotSchedules" = {

      options = {
        "image" = mkOption {
          description = "Image is the mirrored image";
          type = (types.nullOr types.str);
        };
        "items" = mkOption {
          description = "Items is the list schedules times for a given snapshot";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephBlockPoolStatusSnapshotScheduleStatusSnapshotSchedulesItems"
              )
            )
          );
        };
        "namespace" = mkOption {
          description = "Namespace is the RADOS namespace the image is part of";
          type = (types.nullOr types.str);
        };
        "pool" = mkOption {
          description = "Pool is the pool name";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "image" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
        "pool" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBlockPoolStatusSnapshotScheduleStatusSnapshotSchedulesItems" = {

      options = {
        "interval" = mkOption {
          description = "Interval is the interval in which snapshots will be taken";
          type = (types.nullOr types.str);
        };
        "start_time" = mkOption {
          description = "StartTime is the snapshot starting time";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "start_time" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketNotification" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "BucketNotificationSpec represent the spec of a Bucket Notification";
          type = (submoduleOf "ceph.rook.io.v1.CephBucketNotificationSpec");
        };
        "status" = mkOption {
          description = "Status represents the status of an object";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBucketNotificationStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketNotificationSpec" = {

      options = {
        "events" = mkOption {
          description = "List of events that should trigger the notification";
          type = (
            types.nullOr (
              types.listOf (
                types.enum [
                  "s3:ObjectCreated:*"
                  "s3:ObjectCreated:Put"
                  "s3:ObjectCreated:Post"
                  "s3:ObjectCreated:Copy"
                  "s3:ObjectCreated:CompleteMultipartUpload"
                  "s3:ObjectRemoved:*"
                  "s3:ObjectRemoved:Delete"
                  "s3:ObjectRemoved:DeleteMarkerCreated"
                  "s3:ObjectLifecycle:Expiration:Current"
                  "s3:ObjectLifecycle:Expiration:NonCurrent"
                  "s3:ObjectLifecycle:Expiration:DeleteMarker"
                  "s3:ObjectLifecycle:Expiration:AbortMultipartUpload"
                  "s3:ObjectLifecycle:Transition:Current"
                  "s3:ObjectLifecycle:Transition:NonCurrent"
                  "s3:LifecycleExpiration:*"
                  "s3:LifecycleExpiration:Delete"
                  "s3:LifecycleExpiration:DeleteMarkerCreated"
                  "s3:LifecycleTransition"
                  "s3:ObjectSynced:*"
                  "s3:ObjectSynced:Create"
                  "s3:ObjectSynced:Delete"
                  "s3:ObjectSynced:DeletionMarkerCreated"
                  "s3:Replication:*"
                  "s3:Replication:Create"
                  "s3:Replication:Delete"
                  "s3:Replication:DeletionMarkerCreated"
                  "s3:ObjectRestore:*"
                  "s3:ObjectRestore:Post"
                  "s3:ObjectRestore:Completed"
                  "s3:ObjectRestore:Delete"
                ]
              )
            )
          );
        };
        "filter" = mkOption {
          description = "Spec of notification filter";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBucketNotificationSpecFilter"));
        };
        "topic" = mkOption {
          description = "The name of the topic associated with this notification";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = {
        "events" = mkOverride 1002 null;
        "filter" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketNotificationSpecFilter" = {

      options = {
        "keyFilters" = mkOption {
          description = "Filters based on the object's key";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephBucketNotificationSpecFilterKeyFilters"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "metadataFilters" = mkOption {
          description = "Filters based on the object's metadata";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephBucketNotificationSpecFilterMetadataFilters"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "tagFilters" = mkOption {
          description = "Filters based on the object's tags";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephBucketNotificationSpecFilterTagFilters"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
      };

      config = {
        "keyFilters" = mkOverride 1002 null;
        "metadataFilters" = mkOverride 1002 null;
        "tagFilters" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketNotificationSpecFilterKeyFilters" = {

      options = {
        "name" = mkOption {
          description = "Name of the filter - prefix/suffix/regex";
          type = (
            types.enum [
              "prefix"
              "suffix"
              "regex"
            ]
          );
        };
        "value" = mkOption {
          description = "Value to filter on";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephBucketNotificationSpecFilterMetadataFilters" = {

      options = {
        "name" = mkOption {
          description = "Name of the metadata or tag";
          type = (types.withMinLength 1 types.str);
        };
        "value" = mkOption {
          description = "Value to filter on";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephBucketNotificationSpecFilterTagFilters" = {

      options = {
        "name" = mkOption {
          description = "Name of the metadata or tag";
          type = (types.withMinLength 1 types.str);
        };
        "value" = mkOption {
          description = "Value to filter on";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephBucketNotificationStatus" = {

      options = {
        "conditions" = mkOption {
          description = "";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephBucketNotificationStatusConditions"))
          );
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "conditions" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketNotificationStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopic" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "BucketTopicSpec represent the spec of a Bucket Topic";
          type = (submoduleOf "ceph.rook.io.v1.CephBucketTopicSpec");
        };
        "status" = mkOption {
          description = "BucketTopicStatus represents the Status of a CephBucketTopic";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBucketTopicStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopicSpec" = {

      options = {
        "endpoint" = mkOption {
          description = "Contains the endpoint spec of the topic";
          type = (submoduleOf "ceph.rook.io.v1.CephBucketTopicSpecEndpoint");
        };
        "objectStoreName" = mkOption {
          description = "The name of the object store on which to define the topic";
          type = (types.withMinLength 1 types.str);
        };
        "objectStoreNamespace" = mkOption {
          description = "The namespace of the object store on which to define the topic";
          type = (types.withMinLength 1 types.str);
        };
        "opaqueData" = mkOption {
          description = "Data which is sent in each event";
          type = (types.nullOr types.str);
        };
        "persistent" = mkOption {
          description = "Indication whether notifications to this endpoint are persistent or not";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "opaqueData" = mkOverride 1002 null;
        "persistent" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopicSpecEndpoint" = {

      options = {
        "amqp" = mkOption {
          description = "Spec of AMQP endpoint";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBucketTopicSpecEndpointAmqp"));
        };
        "http" = mkOption {
          description = "Spec of HTTP endpoint";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBucketTopicSpecEndpointHttp"));
        };
        "kafka" = mkOption {
          description = "Spec of Kafka endpoint";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBucketTopicSpecEndpointKafka"));
        };
      };

      config = {
        "amqp" = mkOverride 1002 null;
        "http" = mkOverride 1002 null;
        "kafka" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopicSpecEndpointAmqp" = {

      options = {
        "ackLevel" = mkOption {
          description = "The ack level required for this topic (none/broker/routable). The misspelled value\n\"routeable\" is deprecated, is sent to the RGW as \"routable\", and will be removed in a\nfuture release.";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "broker"
                "routable"
                "routeable"
              ]
            )
          );
        };
        "disableVerifySSL" = mkOption {
          description = "Indicate whether the server certificate is validated by the client or not";
          type = (types.nullOr types.bool);
        };
        "exchange" = mkOption {
          description = "Name of the exchange that is used to route messages based on topics";
          type = (types.withMinLength 1 types.str);
        };
        "uri" = mkOption {
          description = "The URI of the AMQP endpoint to push notification to";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = {
        "ackLevel" = mkOverride 1002 null;
        "disableVerifySSL" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopicSpecEndpointHttp" = {

      options = {
        "disableVerifySSL" = mkOption {
          description = "Indicate whether the server certificate is validated by the client or not";
          type = (types.nullOr types.bool);
        };
        "sendCloudEvents" = mkOption {
          description = "Send the notifications with the CloudEvents header: https://github.com/cloudevents/spec/blob/main/cloudevents/adapters/aws-s3.md";
          type = (types.nullOr types.bool);
        };
        "uri" = mkOption {
          description = "The URI of the HTTP endpoint to push notification to";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = {
        "disableVerifySSL" = mkOverride 1002 null;
        "sendCloudEvents" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopicSpecEndpointKafka" = {

      options = {
        "ackLevel" = mkOption {
          description = "The ack level required for this topic (none/broker)";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "broker"
              ]
            )
          );
        };
        "disableVerifySSL" = mkOption {
          description = "Indicate whether the server certificate is validated by the client or not";
          type = (types.nullOr types.bool);
        };
        "mechanism" = mkOption {
          description = "The authentication mechanism for this topic (PLAIN/SCRAM-SHA-512/SCRAM-SHA-256/GSSAPI/OAUTHBEARER)";
          type = (
            types.nullOr (
              types.enum [
                "PLAIN"
                "SCRAM-SHA-512"
                "SCRAM-SHA-256"
                "GSSAPI"
                "OAUTHBEARER"
              ]
            )
          );
        };
        "passwordSecretRef" = mkOption {
          description = "The kafka password to use for authentication";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephBucketTopicSpecEndpointKafkaPasswordSecretRef")
          );
        };
        "uri" = mkOption {
          description = "The URI of the Kafka endpoint to push notification to";
          type = (types.withMinLength 1 types.str);
        };
        "useSSL" = mkOption {
          description = "Indicate whether to use SSL when communicating with the broker";
          type = (types.nullOr types.bool);
        };
        "userSecretRef" = mkOption {
          description = "The kafka user name to use for authentication";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephBucketTopicSpecEndpointKafkaUserSecretRef"));
        };
      };

      config = {
        "ackLevel" = mkOverride 1002 null;
        "disableVerifySSL" = mkOverride 1002 null;
        "mechanism" = mkOverride 1002 null;
        "passwordSecretRef" = mkOverride 1002 null;
        "useSSL" = mkOverride 1002 null;
        "userSecretRef" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopicSpecEndpointKafkaPasswordSecretRef" = {

      options = {
        "key" = mkOption {
          description = "The key of the secret to select from.  Must be a valid secret key.";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "Specify whether the Secret or its key must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopicSpecEndpointKafkaUserSecretRef" = {

      options = {
        "key" = mkOption {
          description = "The key of the secret to select from.  Must be a valid secret key.";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "Specify whether the Secret or its key must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopicStatus" = {

      options = {
        "ARN" = mkOption {
          description = "The ARN of the topic generated by the RGW";
          type = (types.nullOr types.str);
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "secrets" = mkOption {
          description = "";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephBucketTopicStatusSecrets" "name" [ ]
            )
          );
          apply = attrsToList;
        };
      };

      config = {
        "ARN" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
        "secrets" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephBucketTopicStatusSecrets" = {

      options = {
        "name" = mkOption {
          description = "name is unique within a namespace to reference a secret resource.";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "namespace defines the space within which the secret name must be unique.";
          type = (types.nullOr types.str);
        };
        "resourceVersion" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "uid" = mkOption {
          description = "UID is a type that holds unique ID values, including UUIDs.  Because we\ndon't ONLY use UUIDs, this is an alias to string.  Being a type captures\nintent and helps make sure that UIDs and names do not get conflated.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
        "resourceVersion" = mkOverride 1002 null;
        "uid" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriver" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "Spec represents the specification of a Ceph COSI Driver";
          type = (submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpec");
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpec" = {

      options = {
        "deploymentStrategy" = mkOption {
          description = "DeploymentStrategy is the strategy to use to deploy the COSI driver.";
          type = (
            types.nullOr (
              types.enum [
                "Never"
                "Auto"
                "Always"
              ]
            )
          );
        };
        "image" = mkOption {
          description = "Image is the container image to run the Ceph COSI driver";
          type = (types.nullOr types.str);
        };
        "objectProvisionerImage" = mkOption {
          description = "ObjectProvisionerImage is the container image to run the COSI driver sidecar";
          type = (types.nullOr types.str);
        };
        "placement" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacement"));
        };
        "resources" = mkOption {
          description = "Resources is the resource requirements for the COSI driver";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecResources"));
        };
      };

      config = {
        "deploymentStrategy" = mkOverride 1002 null;
        "image" = mkOverride 1002 null;
        "objectProvisionerImage" = mkOverride 1002 null;
        "placement" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacement" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinity"));
        };
        "podAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinity"));
        };
        "podAntiAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinity"));
        };
        "tolerations" = mkOption {
          description = "";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementTolerations"))
          );
        };
        "topologySpreadConstraints" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementTopologySpreadConstraints")
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topologySpreadConstraints" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "";
            type = (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementTolerations" = {

      options = {
        "effect" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementTopologySpreadConstraints" = {

      options = {
        "labelSelector" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementTopologySpreadConstraintsLabelSelector"
            )
          );
        };
        "matchLabelKeys" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "maxSkew" = mkOption {
          description = "";
          type = types.int;
        };
        "minDomains" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "nodeAffinityPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "nodeTaintsPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "topologyKey" = mkOption {
          description = "";
          type = types.str;
        };
        "whenUnsatisfiable" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "matchLabelKeys" = mkOverride 1002 null;
        "minDomains" = mkOverride 1002 null;
        "nodeAffinityPolicy" = mkOverride 1002 null;
        "nodeTaintsPolicy" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementTopologySpreadConstraintsLabelSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephCOSIDriverSpecPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpecPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephCOSIDriverSpecResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephCOSIDriverSpecResourcesClaims" "name" [
                "name"
              ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCOSIDriverSpecResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClient" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "Spec represents the specification of a Ceph Client";
          type = (submoduleOf "ceph.rook.io.v1.CephClientSpec");
        };
        "status" = mkOption {
          description = "Status represents the status of a Ceph Client";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClientStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClientSpec" = {

      options = {
        "caps" = mkOption {
          description = "";
          type = (types.attrsOf types.str);
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "removeSecret" = mkOption {
          description = "RemoveSecret indicates whether the current secret for this ceph client should be removed or not.\nIf true, the K8s secret will be deleted, but the cephx keyring will remain until the CR is deleted.";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "SecretName is the name of the secret created for this ceph client.\nIf not specified, the default name is \"rook-ceph-client-\" as a prefix to the CR name.";
          type = (types.nullOr types.str);
        };
        "security" = mkOption {
          description = "Security represents security settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClientSpecSecurity"));
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "removeSecret" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
        "security" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClientSpecSecurity" = {

      options = {
        "cephx" = mkOption {
          description = "CephX configures CephX key settings. More: https://docs.ceph.com/en/latest/dev/cephx/";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClientSpecSecurityCephx"));
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClientSpecSecurityCephx" = {

      options = {
        "keyGeneration" = mkOption {
          description = "KeyGeneration specifies the desired CephX key generation. This is used when KeyRotationPolicy\nis KeyGeneration and ignored for other policies. If this is set to greater than the current\nkey generation, relevant keys will be rotated, and the generation value will be updated to\nthis new value (generation values are not necessarily incremental, though that is the\nintended use case). If this is set to less than or equal to the current key generation, keys\nare not rotated.";
          type = (types.nullOr (types.withMaximum 4294967295 (types.withMinimum 0 types.int)));
        };
        "keyRotationPolicy" = mkOption {
          description = "KeyRotationPolicy controls if and when CephX keys are rotated after initial creation.\nOne of Disabled, or KeyGeneration. Default Disabled.";
          type = (
            types.nullOr (
              types.enum [
                ""
                "Disabled"
                "KeyGeneration"
              ]
            )
          );
        };
        "keyType" = mkOption {
          description = "KeyType specifies the desired CephX key cipher type.\nIf unspecified, Ceph's default will be used.\nIf KeyRotationPolicy is Disabled or unspecified (default), modifying this value will never\ninitiate key rotation.\nIf KeyRotationPolicy is set to an enabled value (any other value), modifying this may\ninitiate key rotation.";
          type = (
            types.nullOr (
              types.enum [
                "aes"
                "aes256k"
              ]
            )
          );
        };
      };

      config = {
        "keyGeneration" = mkOverride 1002 null;
        "keyRotationPolicy" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClientStatus" = {

      options = {
        "cephx" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClientStatusCephx"));
        };
        "info" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
        "info" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClientStatusCephx" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephCluster" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "ClusterSpec represents the specification of Ceph Cluster";
          type = (submoduleOf "ceph.rook.io.v1.CephClusterSpec");
        };
        "status" = mkOption {
          description = "ClusterStatus represents the status of a Ceph cluster";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpec" = {

      options = {
        "annotations" = mkOption {
          description = "The annotations-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "cephConfig" = mkOption {
          description = "Ceph Config options";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "cephConfigFromSecret" = mkOption {
          description = "CephConfigFromSecret works exactly like CephConfig but takes config value from Secret Key reference.";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "cephVersion" = mkOption {
          description = "The version information that instructs Rook to orchestrate a particular version of Ceph.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecCephVersion"));
        };
        "cleanupPolicy" = mkOption {
          description = "Indicates user intent when deleting a cluster; blocks orchestration and should not be set if cluster\ndeletion is not imminent.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecCleanupPolicy"));
        };
        "continueUpgradeAfterChecksEvenIfNotHealthy" = mkOption {
          description = "ContinueUpgradeAfterChecksEvenIfNotHealthy defines if an upgrade should continue even if PGs are not clean";
          type = (types.nullOr types.bool);
        };
        "crashCollector" = mkOption {
          description = "A spec for the crash controller";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecCrashCollector"));
        };
        "csi" = mkOption {
          description = "CSI Driver Options applied per cluster.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecCsi"));
        };
        "dashboard" = mkOption {
          description = "Dashboard settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecDashboard"));
        };
        "dataDirHostPath" = mkOption {
          description = "The path on the host where config and data can be persisted";
          type = (types.nullOr types.str);
        };
        "disruptionManagement" = mkOption {
          description = "A spec for configuring disruption management.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecDisruptionManagement"));
        };
        "external" = mkOption {
          description = "Whether the Ceph Cluster is running external to this Kubernetes cluster\nmon, mgr, osd, mds, and discover daemons will not be created for external clusters.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecExternal"));
        };
        "healthCheck" = mkOption {
          description = "Internal daemon healthchecks and liveness probe";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecHealthCheck"));
        };
        "labels" = mkOption {
          description = "The labels-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "logCollector" = mkOption {
          description = "Logging represents loggings settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecLogCollector"));
        };
        "mgr" = mkOption {
          description = "A spec for mgr related options";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMgr"));
        };
        "mon" = mkOption {
          description = "A spec for mon related options";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMon"));
        };
        "monitoring" = mkOption {
          description = "Prometheus based Monitoring settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonitoring"));
        };
        "network" = mkOption {
          description = "Network related configuration";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecNetwork"));
        };
        "placement" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "priorityClassNames" = mkOption {
          description = "PriorityClassNames sets priority classes on components";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "removeOSDsIfOutAndSafeToRemove" = mkOption {
          description = "Remove the OSD that is out and safe to remove only if this option is true";
          type = (types.nullOr types.bool);
        };
        "resources" = mkOption {
          description = "Resources set resource requests and limits";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "security" = mkOption {
          description = "Security represents security settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecSecurity"));
        };
        "skipUpgradeChecks" = mkOption {
          description = "SkipUpgradeChecks defines if an upgrade should be forced even if one of the check fails";
          type = (types.nullOr types.bool);
        };
        "storage" = mkOption {
          description = "A spec for available storage in the cluster and how it should be used";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorage"));
        };
        "upgradeOSDRequiresHealthyPGs" = mkOption {
          description = "UpgradeOSDRequiresHealthyPGs defines if OSD upgrade requires PGs are clean. If set to `true` OSD upgrade process won't start until PGs are healthy.\nThis configuration will be ignored if `skipUpgradeChecks` is `true`.\nDefault is false.";
          type = (types.nullOr types.bool);
        };
        "waitTimeoutForHealthyOSDInMinutes" = mkOption {
          description = "WaitTimeoutForHealthyOSDInMinutes defines the time the operator would wait before an OSD can be stopped for upgrade or restart.\nIf the timeout exceeds and OSD is not ok to stop, then the operator would skip upgrade for the current OSD and proceed with the next one\nif `continueUpgradeAfterChecksEvenIfNotHealthy` is `false`. If `continueUpgradeAfterChecksEvenIfNotHealthy` is `true`, then operator would\ncontinue with the upgrade of an OSD even if it's not ok to stop after the timeout. This timeout won't be applied if `skipUpgradeChecks` is `true`.\nThe default wait timeout is 10 minutes.";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "cephConfig" = mkOverride 1002 null;
        "cephConfigFromSecret" = mkOverride 1002 null;
        "cephVersion" = mkOverride 1002 null;
        "cleanupPolicy" = mkOverride 1002 null;
        "continueUpgradeAfterChecksEvenIfNotHealthy" = mkOverride 1002 null;
        "crashCollector" = mkOverride 1002 null;
        "csi" = mkOverride 1002 null;
        "dashboard" = mkOverride 1002 null;
        "dataDirHostPath" = mkOverride 1002 null;
        "disruptionManagement" = mkOverride 1002 null;
        "external" = mkOverride 1002 null;
        "healthCheck" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "logCollector" = mkOverride 1002 null;
        "mgr" = mkOverride 1002 null;
        "mon" = mkOverride 1002 null;
        "monitoring" = mkOverride 1002 null;
        "network" = mkOverride 1002 null;
        "placement" = mkOverride 1002 null;
        "priorityClassNames" = mkOverride 1002 null;
        "removeOSDsIfOutAndSafeToRemove" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "security" = mkOverride 1002 null;
        "skipUpgradeChecks" = mkOverride 1002 null;
        "storage" = mkOverride 1002 null;
        "upgradeOSDRequiresHealthyPGs" = mkOverride 1002 null;
        "waitTimeoutForHealthyOSDInMinutes" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecCephVersion" = {

      options = {
        "allowUnsupported" = mkOption {
          description = "Whether to allow unsupported versions (do not set to true in production)";
          type = (types.nullOr types.bool);
        };
        "image" = mkOption {
          description = "Image is the container image used to launch the ceph daemons, such as quay.io/ceph/ceph:<tag>\nThe full list of images can be found at https://quay.io/repository/ceph/ceph?tab=tags";
          type = (types.nullOr types.str);
        };
        "imagePullPolicy" = mkOption {
          description = "ImagePullPolicy describes a policy for if/when to pull a container image\nOne of Always, Never, IfNotPresent.";
          type = (
            types.nullOr (
              types.enum [
                "IfNotPresent"
                "Always"
                "Never"
                ""
              ]
            )
          );
        };
      };

      config = {
        "allowUnsupported" = mkOverride 1002 null;
        "image" = mkOverride 1002 null;
        "imagePullPolicy" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecCleanupPolicy" = {

      options = {
        "allowUninstallWithVolumes" = mkOption {
          description = "AllowUninstallWithVolumes defines whether we can proceed with the uninstall if they are RBD images still present";
          type = (types.nullOr types.bool);
        };
        "confirmation" = mkOption {
          description = "Confirmation represents the cleanup confirmation";
          type = (types.nullOr types.str);
        };
        "sanitizeDisks" = mkOption {
          description = "SanitizeDisks represents way we sanitize disks";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecCleanupPolicySanitizeDisks"));
        };
        "wipeDevicesFromOtherClusters" = mkOption {
          description = "WipeDevicesFromOtherClusters wipes the OSD disks belonging to other clusters. This is useful in scenarios where ceph cluster\nwas reinstalled but OSD disk still contains the metadata from previous ceph cluster.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "allowUninstallWithVolumes" = mkOverride 1002 null;
        "confirmation" = mkOverride 1002 null;
        "sanitizeDisks" = mkOverride 1002 null;
        "wipeDevicesFromOtherClusters" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecCleanupPolicySanitizeDisks" = {

      options = {
        "dataSource" = mkOption {
          description = "DataSource is the data source to use to sanitize the disk with";
          type = (
            types.nullOr (
              types.enum [
                "zero"
                "random"
              ]
            )
          );
        };
        "iteration" = mkOption {
          description = "Iteration is the number of pass to apply the sanitizing";
          type = (types.nullOr types.int);
        };
        "method" = mkOption {
          description = "Method is the method we use to sanitize disks";
          type = (
            types.nullOr (
              types.enum [
                "complete"
                "quick"
              ]
            )
          );
        };
      };

      config = {
        "dataSource" = mkOverride 1002 null;
        "iteration" = mkOverride 1002 null;
        "method" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecCrashCollector" = {

      options = {
        "daysToRetain" = mkOption {
          description = "DaysToRetain represents the number of days to retain crash until they get pruned";
          type = (types.nullOr types.int);
        };
        "disable" = mkOption {
          description = "Disable determines whether we should enable the crash collector";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "daysToRetain" = mkOverride 1002 null;
        "disable" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecCsi" = {

      options = {
        "cephfs" = mkOption {
          description = "CephFS defines CSI Driver settings for CephFS driver.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecCsiCephfs"));
        };
        "readAffinity" = mkOption {
          description = "ReadAffinity defines the read affinity settings for CSI driver.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecCsiReadAffinity"));
        };
        "skipUserCreation" = mkOption {
          description = "SkipUserCreation determines whether CSI users and their associated secrets should be skipped.\nIf set to true, the user must manually manage these secrets.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "cephfs" = mkOverride 1002 null;
        "readAffinity" = mkOverride 1002 null;
        "skipUserCreation" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecCsiCephfs" = {

      options = {
        "fuseMountOptions" = mkOption {
          description = "FuseMountOptions defines the mount options for ceph fuse mounter.";
          type = (types.nullOr types.str);
        };
        "kernelMountOptions" = mkOption {
          description = "KernelMountOptions defines the mount options for kernel mounter.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fuseMountOptions" = mkOverride 1002 null;
        "kernelMountOptions" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecCsiReadAffinity" = {

      options = {
        "crushLocationLabels" = mkOption {
          description = "CrushLocationLabels defines which node labels to use\nas CRUSH location. This should correspond to the values set in\nthe CRUSH map.";
          type = (types.nullOr (types.listOf types.str));
        };
        "enabled" = mkOption {
          description = "Enables read affinity for CSI driver.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "crushLocationLabels" = mkOverride 1002 null;
        "enabled" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecDashboard" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled determines whether to enable the dashboard";
          type = (types.nullOr types.bool);
        };
        "port" = mkOption {
          description = "Port is the dashboard webserver port";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 0 types.int)));
        };
        "prometheusEndpoint" = mkOption {
          description = "Endpoint for the Prometheus host";
          type = (types.nullOr types.str);
        };
        "prometheusEndpointSSLVerify" = mkOption {
          description = "Whether to verify the ssl endpoint for prometheus. Set to false for a self-signed cert.";
          type = (types.nullOr types.bool);
        };
        "ssl" = mkOption {
          description = "SSL determines whether SSL should be used";
          type = (types.nullOr types.bool);
        };
        "urlPrefix" = mkOption {
          description = "URLPrefix is a prefix for all URLs to use the dashboard with a reverse proxy";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "port" = mkOverride 1002 null;
        "prometheusEndpoint" = mkOverride 1002 null;
        "prometheusEndpointSSLVerify" = mkOverride 1002 null;
        "ssl" = mkOverride 1002 null;
        "urlPrefix" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecDisruptionManagement" = {

      options = {
        "machineDisruptionBudgetNamespace" = mkOption {
          description = "Deprecated. Namespace to look for MDBs by the machineDisruptionBudgetController";
          type = (types.nullOr types.str);
        };
        "manageMachineDisruptionBudgets" = mkOption {
          description = "Deprecated. This enables management of machinedisruptionbudgets.";
          type = (types.nullOr types.bool);
        };
        "managePodBudgets" = mkOption {
          description = "This enables management of poddisruptionbudgets";
          type = (types.nullOr types.bool);
        };
        "osdMaintenanceTimeout" = mkOption {
          description = "OSDMaintenanceTimeout sets how many additional minutes the DOWN/OUT interval is for drained failure domains\nit only works if managePodBudgets is true.\nthe default is 30 minutes";
          type = (types.nullOr types.int);
        };
        "pgHealthCheckTimeout" = mkOption {
          description = "DEPRECATED: PGHealthCheckTimeout is no longer implemented";
          type = (types.nullOr types.int);
        };
        "pgHealthyRegex" = mkOption {
          description = "PgHealthyRegex is the regular expression that is used to determine which PG states should be considered healthy.\nThe default is `^active(\\+(clean|deep|scrubbing|snaptrim|snaptrim_wait))+$`";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "machineDisruptionBudgetNamespace" = mkOverride 1002 null;
        "manageMachineDisruptionBudgets" = mkOverride 1002 null;
        "managePodBudgets" = mkOverride 1002 null;
        "osdMaintenanceTimeout" = mkOverride 1002 null;
        "pgHealthCheckTimeout" = mkOverride 1002 null;
        "pgHealthyRegex" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecExternal" = {

      options = {
        "enable" = mkOption {
          description = "Enable determines whether external mode is enabled or not";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "enable" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecHealthCheck" = {

      options = {
        "daemonHealth" = mkOption {
          description = "DaemonHealth is the health check for a given daemon";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecHealthCheckDaemonHealth"));
        };
        "livenessProbe" = mkOption {
          description = "LivenessProbe allows changing the livenessProbe configuration for a given daemon";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "muteHealthWarning" = mkOption {
          description = "MuteHealthWarning configures muting of Ceph health warnings.";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "startupProbe" = mkOption {
          description = "StartupProbe allows changing the startupProbe configuration for a given daemon";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
      };

      config = {
        "daemonHealth" = mkOverride 1002 null;
        "livenessProbe" = mkOverride 1002 null;
        "muteHealthWarning" = mkOverride 1002 null;
        "startupProbe" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecHealthCheckDaemonHealth" = {

      options = {
        "mon" = mkOption {
          description = "Monitor represents the health check settings for the Ceph monitor";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecHealthCheckDaemonHealthMon"));
        };
        "osd" = mkOption {
          description = "ObjectStorageDaemon represents the health check settings for the Ceph OSDs";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecHealthCheckDaemonHealthOsd"));
        };
        "status" = mkOption {
          description = "Status represents the health check settings for the Ceph health";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecHealthCheckDaemonHealthStatus"));
        };
      };

      config = {
        "mon" = mkOverride 1002 null;
        "osd" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecHealthCheckDaemonHealthMon" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecHealthCheckDaemonHealthOsd" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecHealthCheckDaemonHealthStatus" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecLogCollector" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled represents whether the log collector is enabled";
          type = (types.nullOr types.bool);
        };
        "maxLogSize" = mkOption {
          description = "MaxLogSize is the maximum size of the log per ceph daemons. Must be at least 1M.";
          type = (types.nullOr (types.either types.int types.str));
        };
        "periodicity" = mkOption {
          description = "Periodicity is the periodicity of the log rotation.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "maxLogSize" = mkOverride 1002 null;
        "periodicity" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMgr" = {

      options = {
        "allowMultiplePerNode" = mkOption {
          description = "AllowMultiplePerNode allows to run multiple managers on the same node (not recommended)";
          type = (types.nullOr types.bool);
        };
        "count" = mkOption {
          description = "Count is the number of manager daemons to run";
          type = (types.nullOr (types.withMaximum 5 (types.withMinimum 0 types.int)));
        };
        "hostNetwork" = mkOption {
          description = "Whether host networking is enabled for the Ceph Mgr. If not set, the network settings from CephCluster.spec.networking will be applied.";
          type = (types.nullOr types.bool);
        };
        "modules" = mkOption {
          description = "Modules is the list of ceph manager modules to enable/disable";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephClusterSpecMgrModules" "name" [ ]
            )
          );
          apply = attrsToList;
        };
      };

      config = {
        "allowMultiplePerNode" = mkOverride 1002 null;
        "count" = mkOverride 1002 null;
        "hostNetwork" = mkOverride 1002 null;
        "modules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMgrModules" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled determines whether a module should be enabled or not";
          type = (types.nullOr types.bool);
        };
        "name" = mkOption {
          description = "Name is the name of the ceph manager module";
          type = (types.nullOr types.str);
        };
        "settings" = mkOption {
          description = "Settings to further configure the module";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMgrModulesSettings"));
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "settings" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMgrModulesSettings" = {

      options = {
        "balancerMode" = mkOption {
          description = "BalancerMode sets the `balancer` module with different modes like `upmap`, `crush-compact` etc";
          type = (
            types.nullOr (
              types.enum [
                ""
                "crush-compat"
                "upmap"
                "read"
                "upmap-read"
              ]
            )
          );
        };
      };

      config = {
        "balancerMode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMon" = {

      options = {
        "allowMultiplePerNode" = mkOption {
          description = "AllowMultiplePerNode determines if we can run multiple monitors on the same node (not recommended)";
          type = (types.nullOr types.bool);
        };
        "count" = mkOption {
          description = "Count is the number of Ceph monitors";
          type = (types.nullOr (types.withMaximum 9 (types.withMinimum 0 types.int)));
        };
        "externalMonIDs" = mkOption {
          description = "ExternalMonIDs - optional list of monitor IDs which are deployed externally and not managed by Rook.\nIf set, Rook will not remove mons with given IDs from quorum.\nThis parameter is used only for local Rook cluster running in normal mode\nand will be ignored if external or stretched mode is used.\nleading";
          type = (types.nullOr (types.listOf types.str));
        };
        "failureDomainLabel" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "floatingMon" = mkOption {
          description = "FloatingMon is the specification of the floating monitor for two-node clusters.\nThe floating mon is backed by a synchronously replicated store (e.g. DRBD)\nand can be scheduled on either node. Template variables are supplied via a ConfigMap.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonFloatingMon"));
        };
        "stretchCluster" = mkOption {
          description = "StretchCluster is the stretch cluster specification";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonStretchCluster"));
        };
        "volumeClaimTemplate" = mkOption {
          description = "VolumeClaimTemplate is the PVC definition";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplate"));
        };
        "zones" = mkOption {
          description = "Zones are specified when we want to provide zonal awareness to mons";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephClusterSpecMonZones" "name" [ ]
            )
          );
          apply = attrsToList;
        };
      };

      config = {
        "allowMultiplePerNode" = mkOverride 1002 null;
        "count" = mkOverride 1002 null;
        "externalMonIDs" = mkOverride 1002 null;
        "failureDomainLabel" = mkOverride 1002 null;
        "floatingMon" = mkOverride 1002 null;
        "stretchCluster" = mkOverride 1002 null;
        "volumeClaimTemplate" = mkOverride 1002 null;
        "zones" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonFloatingMon" = {

      options = {
        "configmapName" = mkOption {
          description = "ConfigMapName is the name of the ConfigMap containing key-value pairs\nof template variables for the floating mon deployment.";
          type = (types.withMaxLength 253 (types.withMinLength 1 types.str));
        };
        "name" = mkOption {
          description = "Name is the identifier for the floating monitor (recommended \"c\")";
          type = (types.withMaxLength 1 (types.withMinLength 1 types.str));
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchCluster" = {

      options = {
        "failureDomainLabel" = mkOption {
          description = "FailureDomainLabel the failure domain name (e,g: zone)";
          type = (types.nullOr types.str);
        };
        "subFailureDomain" = mkOption {
          description = "SubFailureDomain is the failure domain within a zone";
          type = (types.nullOr types.str);
        };
        "zones" = mkOption {
          description = "Zones is the list of zones";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZones" "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
      };

      config = {
        "failureDomainLabel" = mkOverride 1002 null;
        "subFailureDomain" = mkOverride 1002 null;
        "zones" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZones" = {

      options = {
        "arbiter" = mkOption {
          description = "Arbiter determines if the zone contains the arbiter used for stretch cluster mode";
          type = (types.nullOr types.bool);
        };
        "name" = mkOption {
          description = "Name is the name of the zone";
          type = (types.nullOr types.str);
        };
        "volumeClaimTemplate" = mkOption {
          description = "VolumeClaimTemplate is the PVC template";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplate"
            )
          );
        };
      };

      config = {
        "arbiter" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "volumeClaimTemplate" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplate" = {

      options = {
        "metadata" = mkOption {
          description = "Standard object's metadata.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateMetadata"
            )
          );
        };
        "spec" = mkOption {
          description = "spec defines the desired characteristics of a volume requested by a pod author.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpec"
            )
          );
        };
      };

      config = {
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateMetadata" = {

      options = {
        "annotations" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "finalizers" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "labels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "finalizers" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpec" = {

      options = {
        "accessModes" = mkOption {
          description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
          type = (types.nullOr (types.listOf types.str));
        };
        "dataSource" = mkOption {
          description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecDataSource"
            )
          );
        };
        "dataSourceRef" = mkOption {
          description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecDataSourceRef"
            )
          );
        };
        "resources" = mkOption {
          description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecResources"
            )
          );
        };
        "selector" = mkOption {
          description = "selector is a label query over volumes to consider for binding.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecSelector"
            )
          );
        };
        "storageClassName" = mkOption {
          description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
          type = (types.nullOr types.str);
        };
        "volumeAttributesClassName" = mkOption {
          description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
          type = (types.nullOr types.str);
        };
        "volumeMode" = mkOption {
          description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
          type = (types.nullOr types.str);
        };
        "volumeName" = mkOption {
          description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "accessModes" = mkOverride 1002 null;
        "dataSource" = mkOverride 1002 null;
        "dataSourceRef" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "selector" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
        "volumeAttributesClassName" = mkOverride 1002 null;
        "volumeMode" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecDataSource" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecDataSourceRef" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
        "namespace" = mkOption {
          description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecResources" = {

      options = {
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonStretchClusterZonesVolumeClaimTemplateSpecSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplate" = {

      options = {
        "metadata" = mkOption {
          description = "Standard object's metadata.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateMetadata"));
        };
        "spec" = mkOption {
          description = "spec defines the desired characteristics of a volume requested by a pod author.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpec"));
        };
      };

      config = {
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateMetadata" = {

      options = {
        "annotations" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "finalizers" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "labels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "finalizers" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpec" = {

      options = {
        "accessModes" = mkOption {
          description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
          type = (types.nullOr (types.listOf types.str));
        };
        "dataSource" = mkOption {
          description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecDataSource")
          );
        };
        "dataSourceRef" = mkOption {
          description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecDataSourceRef")
          );
        };
        "resources" = mkOption {
          description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecResources")
          );
        };
        "selector" = mkOption {
          description = "selector is a label query over volumes to consider for binding.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecSelector")
          );
        };
        "storageClassName" = mkOption {
          description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
          type = (types.nullOr types.str);
        };
        "volumeAttributesClassName" = mkOption {
          description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
          type = (types.nullOr types.str);
        };
        "volumeMode" = mkOption {
          description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
          type = (types.nullOr types.str);
        };
        "volumeName" = mkOption {
          description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "accessModes" = mkOverride 1002 null;
        "dataSource" = mkOverride 1002 null;
        "dataSourceRef" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "selector" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
        "volumeAttributesClassName" = mkOverride 1002 null;
        "volumeMode" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecDataSource" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecDataSourceRef" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
        "namespace" = mkOption {
          description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecResources" = {

      options = {
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonVolumeClaimTemplateSpecSelectorMatchExpressions" = {

      options = {
        "key" = mkOption {
          description = "key is the label key that the selector applies to.";
          type = types.str;
        };
        "operator" = mkOption {
          description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
          type = types.str;
        };
        "values" = mkOption {
          description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "values" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonZones" = {

      options = {
        "arbiter" = mkOption {
          description = "Arbiter determines if the zone contains the arbiter used for stretch cluster mode";
          type = (types.nullOr types.bool);
        };
        "name" = mkOption {
          description = "Name is the name of the zone";
          type = (types.nullOr types.str);
        };
        "volumeClaimTemplate" = mkOption {
          description = "VolumeClaimTemplate is the PVC template";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplate"));
        };
      };

      config = {
        "arbiter" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "volumeClaimTemplate" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplate" = {

      options = {
        "metadata" = mkOption {
          description = "Standard object's metadata.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateMetadata")
          );
        };
        "spec" = mkOption {
          description = "spec defines the desired characteristics of a volume requested by a pod author.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpec")
          );
        };
      };

      config = {
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateMetadata" = {

      options = {
        "annotations" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "finalizers" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "labels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "finalizers" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpec" = {

      options = {
        "accessModes" = mkOption {
          description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
          type = (types.nullOr (types.listOf types.str));
        };
        "dataSource" = mkOption {
          description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecDataSource"
            )
          );
        };
        "dataSourceRef" = mkOption {
          description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecDataSourceRef"
            )
          );
        };
        "resources" = mkOption {
          description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecResources")
          );
        };
        "selector" = mkOption {
          description = "selector is a label query over volumes to consider for binding.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecSelector")
          );
        };
        "storageClassName" = mkOption {
          description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
          type = (types.nullOr types.str);
        };
        "volumeAttributesClassName" = mkOption {
          description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
          type = (types.nullOr types.str);
        };
        "volumeMode" = mkOption {
          description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
          type = (types.nullOr types.str);
        };
        "volumeName" = mkOption {
          description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "accessModes" = mkOverride 1002 null;
        "dataSource" = mkOverride 1002 null;
        "dataSourceRef" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "selector" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
        "volumeAttributesClassName" = mkOverride 1002 null;
        "volumeMode" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecDataSource" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecDataSourceRef" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
        "namespace" = mkOption {
          description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecResources" = {

      options = {
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonZonesVolumeClaimTemplateSpecSelectorMatchExpressions" = {

      options = {
        "key" = mkOption {
          description = "key is the label key that the selector applies to.";
          type = types.str;
        };
        "operator" = mkOption {
          description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
          type = types.str;
        };
        "values" = mkOption {
          description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "values" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonitoring" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled determines whether to create the prometheus rules for the ceph cluster. If true, the prometheus\ntypes must exist or the creation will fail. Default is false.";
          type = (types.nullOr types.bool);
        };
        "exporter" = mkOption {
          description = "Ceph exporter configuration";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonitoringExporter"));
        };
        "externalMgrEndpoints" = mkOption {
          description = "ExternalMgrEndpoints points to an existing Ceph prometheus exporter endpoint";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonitoringExternalMgrEndpoints")
            )
          );
        };
        "externalMgrPrometheusPort" = mkOption {
          description = "ExternalMgrPrometheusPort Prometheus exporter port";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 0 types.int)));
        };
        "interval" = mkOption {
          description = "Interval determines prometheus scrape interval";
          type = (types.nullOr types.str);
        };
        "metricsDisabled" = mkOption {
          description = "Whether to disable the metrics reported by Ceph. If false, the prometheus mgr module and Ceph exporter are enabled.\nIf true, the prometheus mgr module and Ceph exporter are both disabled. Default is false.";
          type = (types.nullOr types.bool);
        };
        "port" = mkOption {
          description = "Port is the prometheus server port";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 0 types.int)));
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "exporter" = mkOverride 1002 null;
        "externalMgrEndpoints" = mkOverride 1002 null;
        "externalMgrPrometheusPort" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "metricsDisabled" = mkOverride 1002 null;
        "port" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonitoringExporter" = {

      options = {
        "hostNetwork" = mkOption {
          description = "Whether host networking is enabled for CephExporter. If not set, the network settings from CephCluster.spec.networking will be applied.";
          type = (types.nullOr types.bool);
        };
        "perfCountersPrioLimit" = mkOption {
          description = "Only performance counters greater than or equal to this option are fetched";
          type = (types.nullOr types.int);
        };
        "port" = mkOption {
          description = "Port is the listening port of the ceph-exporter process. Defaults to 9926.";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 1 types.int)));
        };
        "statsPeriodSeconds" = mkOption {
          description = "Time to wait before sending requests again to exporter server (seconds)";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "hostNetwork" = mkOverride 1002 null;
        "perfCountersPrioLimit" = mkOverride 1002 null;
        "port" = mkOverride 1002 null;
        "statsPeriodSeconds" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonitoringExternalMgrEndpoints" = {

      options = {
        "hostname" = mkOption {
          description = "The Hostname of this endpoint";
          type = (types.nullOr types.str);
        };
        "ip" = mkOption {
          description = "The IP of this endpoint.\nMay not be loopback (127.0.0.0/8 or ::1), link-local (169.254.0.0/16 or fe80::/10),\nor link-local multicast (224.0.0.0/24 or ff02::/16).";
          type = types.str;
        };
        "nodeName" = mkOption {
          description = "Optional: Node hosting this endpoint. This can be used to determine endpoints local to a node.";
          type = (types.nullOr types.str);
        };
        "targetRef" = mkOption {
          description = "Reference to object providing the endpoint.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecMonitoringExternalMgrEndpointsTargetRef")
          );
        };
      };

      config = {
        "hostname" = mkOverride 1002 null;
        "nodeName" = mkOverride 1002 null;
        "targetRef" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecMonitoringExternalMgrEndpointsTargetRef" = {

      options = {
        "apiVersion" = mkOption {
          description = "API version of the referent.";
          type = (types.nullOr types.str);
        };
        "fieldPath" = mkOption {
          description = "If referring to a piece of an object instead of an entire object, this string\nshould contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].\nFor example, if the object reference is to a container within a pod, this would take on a value like:\n\"spec.containers{name}\" (where \"name\" refers to the name of the container that triggered\nthe event) or if no container name is specified \"spec.containers[2]\" (container with\nindex 2 in this pod). This syntax is chosen only to have some well-defined way of\nreferencing a part of an object.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind of the referent.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "name" = mkOption {
          description = "Name of the referent.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "Namespace of the referent.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/";
          type = (types.nullOr types.str);
        };
        "resourceVersion" = mkOption {
          description = "Specific resourceVersion to which this reference is made, if any.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency";
          type = (types.nullOr types.str);
        };
        "uid" = mkOption {
          description = "UID of the referent.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "fieldPath" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
        "resourceVersion" = mkOverride 1002 null;
        "uid" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecNetwork" = {

      options = {
        "addressRanges" = mkOption {
          description = "AddressRanges specify a list of CIDRs that Rook will apply to Ceph's 'public_network' and/or\n'cluster_network' configurations. This config section may be used for the \"host\" or \"multus\"\nnetwork providers.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecNetworkAddressRanges"));
        };
        "connections" = mkOption {
          description = "Settings for network connections such as compression and encryption across the\nwire.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecNetworkConnections"));
        };
        "dualStack" = mkOption {
          description = "DualStack determines whether Ceph daemons should listen on both IPv4 and IPv6";
          type = (types.nullOr types.bool);
        };
        "hostNetwork" = mkOption {
          description = "HostNetwork to enable host network.\nIf host networking is enabled or disabled on a running cluster, then the operator will automatically fail over all the mons to\napply the new network settings.";
          type = (types.nullOr types.bool);
        };
        "ipFamily" = mkOption {
          description = "IPFamily is the single stack IPv6 or IPv4 protocol";
          type = (
            types.nullOr (
              types.enum [
                "IPv4"
                "IPv6"
              ]
            )
          );
        };
        "multiClusterService" = mkOption {
          description = "Enable multiClusterService to export the Services between peer clusters";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecNetworkMultiClusterService"));
        };
        "provider" = mkOption {
          description = "Provider is what provides network connectivity to the cluster e.g. \"host\" or \"multus\".\nIf the Provider is updated from being empty to \"host\" on a running cluster, then the operator will automatically fail over all the mons to apply the \"host\" network settings.";
          type = (
            types.nullOr (
              types.enum [
                ""
                "host"
                "multus"
              ]
            )
          );
        };
        "selectors" = mkOption {
          description = "Selectors define NetworkAttachmentDefinitions to be used for Ceph public and/or cluster\nnetworks when the \"multus\" network provider is used. This config section is not used for\nother network providers.\n\nValid keys are \"public\" and \"cluster\". Refer to Ceph networking documentation for more:\nhttps://docs.ceph.com/en/latest/rados/configuration/network-config-ref/\n\nRefer to Multus network annotation documentation for help selecting values:\nhttps://github.com/k8snetworkplumbingwg/multus-cni/blob/master/docs/how-to-use.md#run-pod-with-network-annotation\n\nRook will make a best-effort attempt to automatically detect CIDR address ranges for given\nnetwork attachment definitions. Rook's methods are robust but may be imprecise for\nsufficiently complicated networks. Rook's auto-detection process obtains a new IP address\nlease for each CephCluster reconcile. If Rook fails to detect, incorrectly detects, only\npartially detects, or if underlying networks do not support reusing old IP addresses, it is\nbest to use the 'addressRanges' config section to specify CIDR ranges for the Ceph cluster.\n\nAs a contrived example, one can use a theoretical Kubernetes-wide network for Ceph client\ntraffic and a theoretical Rook-only network for Ceph replication traffic as shown:\n  selectors:\n    public: \"default/cluster-fast-net\"\n    cluster: \"rook-ceph/ceph-backend-net\"";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "addressRanges" = mkOverride 1002 null;
        "connections" = mkOverride 1002 null;
        "dualStack" = mkOverride 1002 null;
        "hostNetwork" = mkOverride 1002 null;
        "ipFamily" = mkOverride 1002 null;
        "multiClusterService" = mkOverride 1002 null;
        "provider" = mkOverride 1002 null;
        "selectors" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecNetworkAddressRanges" = {

      options = {
        "cluster" = mkOption {
          description = "Cluster defines a list of CIDRs to use for Ceph cluster network communication.";
          type = (types.nullOr (types.listOf types.str));
        };
        "public" = mkOption {
          description = "Public defines a list of CIDRs to use for Ceph public network communication.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "cluster" = mkOverride 1002 null;
        "public" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecNetworkConnections" = {

      options = {
        "compression" = mkOption {
          description = "Compression settings for the network connections.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecNetworkConnectionsCompression"));
        };
        "encryption" = mkOption {
          description = "Encryption settings for the network connections.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecNetworkConnectionsEncryption"));
        };
        "requireMsgr2" = mkOption {
          description = "Whether to require msgr2 (port 3300) even if compression or encryption are not enabled.\nIf true, the msgr1 port (6789) will be disabled.\nRequires a kernel that supports msgr2 (kernel 5.11 or CentOS 8.4 or newer).";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "compression" = mkOverride 1002 null;
        "encryption" = mkOverride 1002 null;
        "requireMsgr2" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecNetworkConnectionsCompression" = {

      options = {
        "enabled" = mkOption {
          description = "Whether to compress the data in transit across the wire.\nThe default is not set.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecNetworkConnectionsEncryption" = {

      options = {
        "enabled" = mkOption {
          description = "Whether to encrypt the data in transit across the wire to prevent eavesdropping\nthe data on the network. The default is not set. Even if encryption is not enabled,\nclients still establish a strong initial authentication for the connection\nand data integrity is still validated with a crc check. When encryption is enabled,\nall communication between clients and Ceph daemons, or between Ceph daemons will\nbe encrypted.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecNetworkMultiClusterService" = {

      options = {
        "clusterID" = mkOption {
          description = "ClusterID uniquely identifies a cluster. It is used as a prefix to nslookup exported\nservices. For example: <clusterid>.<svc>.<ns>.svc.clusterset.local";
          type = (types.nullOr types.str);
        };
        "enabled" = mkOption {
          description = "Enable multiClusterService to export the mon and OSD services to peer cluster.\nEnsure that peer clusters are connected using an MCS API compatible application,\nlike Globalnet Submariner.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "clusterID" = mkOverride 1002 null;
        "enabled" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecSecurity" = {

      options = {
        "cephx" = mkOption {
          description = "CephX configures CephX key settings. More: https://docs.ceph.com/en/latest/dev/cephx/";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecSecurityCephx"));
        };
        "keyRotation" = mkOption {
          description = "KeyRotation defines options for rotation of OSD disk encryption keys.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecSecurityKeyRotation"));
        };
        "kms" = mkOption {
          description = "KeyManagementService is the main Key Management option";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecSecurityKms"));
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
        "keyRotation" = mkOverride 1002 null;
        "kms" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecSecurityCephx" = {

      options = {
        "allowedCiphers" = mkOption {
          description = "AllowedCiphers is an advanced configuration that can disrupt cluster availability!\nReview Rook documentation carefully before setting this in a production cluster!\n\nAllowedCiphers sets the Ceph config `auth_allowed_ciphers` to the list given.\nIf the list is empty, Rook will enable support for all ciphers.";
          type = (
            types.nullOr (
              types.listOf (
                types.enum [
                  "aes"
                  "aes256k"
                ]
              )
            )
          );
        };
        "csi" = mkOption {
          description = "CSI configures CephX key rotation settings for the Ceph-CSI daemons in the current Kubernetes cluster.\nCSI key rotation can affect existing PV connections, so take care when exercising this option.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecSecurityCephxCsi"));
        };
        "daemon" = mkOption {
          description = "Daemon configures CephX key settings for local Ceph daemons managed by Rook and part of the\nCeph cluster. Daemon CephX keys can be rotated without affecting client connections.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecSecurityCephxDaemon"));
        };
        "rbdMirrorPeer" = mkOption {
          description = "RBDMirrorPeer configures CephX key settings of the `rbd-mirror-peer` user that is used for creating\nbootstrap peer token used connect peer clusters. Rotating the `rbd-mirror-peer` user key will update\nthe mirror peer token.\nRotation will affect any existing peers connected to this cluster, so take care when exercising this option.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecSecurityCephxRbdMirrorPeer"));
        };
      };

      config = {
        "allowedCiphers" = mkOverride 1002 null;
        "csi" = mkOverride 1002 null;
        "daemon" = mkOverride 1002 null;
        "rbdMirrorPeer" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecSecurityCephxCsi" = {

      options = {
        "keepPriorKeyCountMax" = mkOption {
          description = "KeepPriorKeyCountMax tells Rook how many prior keys to keep active.\nGenerally, this would be set to 1 to allow for a migration period for applications.\nIf desired, set this to 0 to delete prior keys after migration.\nThis config only applies to prior keys that already exist.\nIf PriorKeyCount is set to 2 while only a single key currently exists, only a single prior key will be kept,\nand the reported status will only indicate the actual number of prior keys,\nnot necessarily a reflection of PriorKeyCount config here.";
          type = (types.nullOr (types.withMaximum 10 (types.withMinimum 0 types.int)));
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration specifies the desired CephX key generation. This is used when KeyRotationPolicy\nis KeyGeneration and ignored for other policies. If this is set to greater than the current\nkey generation, relevant keys will be rotated, and the generation value will be updated to\nthis new value (generation values are not necessarily incremental, though that is the\nintended use case). If this is set to less than or equal to the current key generation, keys\nare not rotated.";
          type = (types.nullOr (types.withMaximum 4294967295 (types.withMinimum 0 types.int)));
        };
        "keyRotationPolicy" = mkOption {
          description = "KeyRotationPolicy controls if and when CephX keys are rotated after initial creation.\nOne of Disabled, or KeyGeneration. Default Disabled.";
          type = (
            types.nullOr (
              types.enum [
                ""
                "Disabled"
                "KeyGeneration"
              ]
            )
          );
        };
        "keyType" = mkOption {
          description = "KeyType specifies the desired CephX key cipher type.\nIf unspecified, Ceph's default will be used.\nIf KeyRotationPolicy is Disabled or unspecified (default), modifying this value will never\ninitiate key rotation.\nIf KeyRotationPolicy is set to an enabled value (any other value), modifying this may\ninitiate key rotation.";
          type = (
            types.nullOr (
              types.enum [
                "aes"
                "aes256k"
              ]
            )
          );
        };
      };

      config = {
        "keepPriorKeyCountMax" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyRotationPolicy" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecSecurityCephxDaemon" = {

      options = {
        "keyGeneration" = mkOption {
          description = "KeyGeneration specifies the desired CephX key generation. This is used when KeyRotationPolicy\nis KeyGeneration and ignored for other policies. If this is set to greater than the current\nkey generation, relevant keys will be rotated, and the generation value will be updated to\nthis new value (generation values are not necessarily incremental, though that is the\nintended use case). If this is set to less than or equal to the current key generation, keys\nare not rotated.";
          type = (types.nullOr (types.withMaximum 4294967295 (types.withMinimum 0 types.int)));
        };
        "keyRotationPolicy" = mkOption {
          description = "KeyRotationPolicy controls if and when CephX keys are rotated after initial creation.\nOne of Disabled, or KeyGeneration. Default Disabled.";
          type = (
            types.nullOr (
              types.enum [
                ""
                "Disabled"
                "KeyGeneration"
              ]
            )
          );
        };
        "keyType" = mkOption {
          description = "KeyType specifies the desired CephX key cipher type.\nIf unspecified, Ceph's default will be used.\nIf KeyRotationPolicy is Disabled or unspecified (default), modifying this value will never\ninitiate key rotation.\nIf KeyRotationPolicy is set to an enabled value (any other value), modifying this may\ninitiate key rotation.";
          type = (
            types.nullOr (
              types.enum [
                "aes"
                "aes256k"
              ]
            )
          );
        };
      };

      config = {
        "keyGeneration" = mkOverride 1002 null;
        "keyRotationPolicy" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecSecurityCephxRbdMirrorPeer" = {

      options = {
        "keyGeneration" = mkOption {
          description = "KeyGeneration specifies the desired CephX key generation. This is used when KeyRotationPolicy\nis KeyGeneration and ignored for other policies. If this is set to greater than the current\nkey generation, relevant keys will be rotated, and the generation value will be updated to\nthis new value (generation values are not necessarily incremental, though that is the\nintended use case). If this is set to less than or equal to the current key generation, keys\nare not rotated.";
          type = (types.nullOr (types.withMaximum 4294967295 (types.withMinimum 0 types.int)));
        };
        "keyRotationPolicy" = mkOption {
          description = "KeyRotationPolicy controls if and when CephX keys are rotated after initial creation.\nOne of Disabled, or KeyGeneration. Default Disabled.";
          type = (
            types.nullOr (
              types.enum [
                ""
                "Disabled"
                "KeyGeneration"
              ]
            )
          );
        };
        "keyType" = mkOption {
          description = "KeyType specifies the desired CephX key cipher type.\nIf unspecified, Ceph's default will be used.\nIf KeyRotationPolicy is Disabled or unspecified (default), modifying this value will never\ninitiate key rotation.\nIf KeyRotationPolicy is set to an enabled value (any other value), modifying this may\ninitiate key rotation.";
          type = (
            types.nullOr (
              types.enum [
                "aes"
                "aes256k"
              ]
            )
          );
        };
      };

      config = {
        "keyGeneration" = mkOverride 1002 null;
        "keyRotationPolicy" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecSecurityKeyRotation" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled represents whether the key rotation is enabled.";
          type = (types.nullOr types.bool);
        };
        "schedule" = mkOption {
          description = "Schedule represents the cron schedule for key rotation.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "schedule" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecSecurityKms" = {

      options = {
        "connectionDetails" = mkOption {
          description = "ConnectionDetails contains the KMS connection details (address, port etc)";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "tokenSecretName" = mkOption {
          description = "TokenSecretName is the kubernetes secret containing the KMS token";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "connectionDetails" = mkOverride 1002 null;
        "tokenSecretName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorage" = {

      options = {
        "allowDeviceClassUpdate" = mkOption {
          description = "Whether to allow updating the device class after the OSD is initially provisioned";
          type = (types.nullOr types.bool);
        };
        "allowOsdCrushWeightUpdate" = mkOption {
          description = "Whether Rook will resize the OSD CRUSH weight when the OSD PVC size is increased.\nThis allows cluster data to be rebalanced to make most effective use of new OSD space.\nThe default is false since data rebalancing can cause temporary cluster slowdown.";
          type = (types.nullOr types.bool);
        };
        "backfillFullRatio" = mkOption {
          description = "BackfillFullRatio is the ratio at which the cluster is too full for backfill. Backfill will be disabled if above this threshold. Default is 0.90.";
          type = (
            types.nullOr (types.withMaximum 1 (types.withMinimum 0 (types.either types.int types.float)))
          );
        };
        "config" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "deviceFilter" = mkOption {
          description = "A regular expression to allow more fine-grained selection of devices on nodes across the cluster";
          type = (types.nullOr types.str);
        };
        "devicePathFilter" = mkOption {
          description = "A regular expression to allow more fine-grained selection of devices with path names";
          type = (types.nullOr types.str);
        };
        "devices" = mkOption {
          description = "List of devices to use as storage devices";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephClusterSpecStorageDevices" "name" [ ]
            )
          );
          apply = attrsToList;
        };
        "flappingRestartIntervalHours" = mkOption {
          description = "FlappingRestartIntervalHours defines the time for which the OSD pods, that failed with zero exit code, will sleep before restarting.\nThis is needed for OSD flapping where OSD daemons are marked down more than 5 times in 600 seconds by Ceph.\nPreventing the OSD pods to restart immediately in such scenarios will prevent Rook from marking OSD as `up` and thus\npeering of the PGs mapped to the OSD.\nUser needs to manually restart the OSD pod if they manage to fix the underlying OSD flapping issue before the restart interval.\nThe sleep will be disabled if this interval is set to 0.";
          type = (types.nullOr types.int);
        };
        "fullRatio" = mkOption {
          description = "FullRatio is the ratio at which the cluster is considered full and ceph will stop accepting writes. Default is 0.95.";
          type = (
            types.nullOr (types.withMaximum 1 (types.withMinimum 0 (types.either types.int types.float)))
          );
        };
        "migration" = mkOption {
          description = "Migration handles the OSD migration";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageMigration"));
        };
        "nearFullRatio" = mkOption {
          description = "NearFullRatio is the ratio at which the cluster is considered nearly full and will raise a ceph health warning. Default is 0.85.";
          type = (
            types.nullOr (types.withMaximum 1 (types.withMinimum 0 (types.either types.int types.float)))
          );
        };
        "nodes" = mkOption {
          description = "";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephClusterSpecStorageNodes" "name" [ ]
            )
          );
          apply = attrsToList;
        };
        "onlyApplyOSDPlacement" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "osdMaxUpdatesInParallel" = mkOption {
          description = "The maximum number of OSDs to update in parallel.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "scheduleAlways" = mkOption {
          description = "Whether to always schedule OSDs on a node even if the node is not currently scheduleable or ready";
          type = (types.nullOr types.bool);
        };
        "storageClassDeviceSets" = mkOption {
          description = "";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSets"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "store" = mkOption {
          description = "OSDStore is the backend storage type used for creating the OSDs";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStore"));
        };
        "useAllDevices" = mkOption {
          description = "Whether to consume all the storage devices found on a machine";
          type = (types.nullOr types.bool);
        };
        "useAllNodes" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "volumeClaimTemplates" = mkOption {
          description = "PersistentVolumeClaims to use as storage";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplates")
            )
          );
        };
      };

      config = {
        "allowDeviceClassUpdate" = mkOverride 1002 null;
        "allowOsdCrushWeightUpdate" = mkOverride 1002 null;
        "backfillFullRatio" = mkOverride 1002 null;
        "config" = mkOverride 1002 null;
        "deviceFilter" = mkOverride 1002 null;
        "devicePathFilter" = mkOverride 1002 null;
        "devices" = mkOverride 1002 null;
        "flappingRestartIntervalHours" = mkOverride 1002 null;
        "fullRatio" = mkOverride 1002 null;
        "migration" = mkOverride 1002 null;
        "nearFullRatio" = mkOverride 1002 null;
        "nodes" = mkOverride 1002 null;
        "onlyApplyOSDPlacement" = mkOverride 1002 null;
        "osdMaxUpdatesInParallel" = mkOverride 1002 null;
        "scheduleAlways" = mkOverride 1002 null;
        "storageClassDeviceSets" = mkOverride 1002 null;
        "store" = mkOverride 1002 null;
        "useAllDevices" = mkOverride 1002 null;
        "useAllNodes" = mkOverride 1002 null;
        "volumeClaimTemplates" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageDevices" = {

      options = {
        "config" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "fullpath" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "config" = mkOverride 1002 null;
        "fullpath" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageMigration" = {

      options = {
        "confirmation" = mkOption {
          description = "A user confirmation to migrate the OSDs. It destroys each OSD one at a time, cleans up the backing disk\nand prepares OSD with same ID on that disk";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "confirmation" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodes" = {

      options = {
        "config" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "deviceFilter" = mkOption {
          description = "A regular expression to allow more fine-grained selection of devices on nodes across the cluster";
          type = (types.nullOr types.str);
        };
        "devicePathFilter" = mkOption {
          description = "A regular expression to allow more fine-grained selection of devices with path names";
          type = (types.nullOr types.str);
        };
        "devices" = mkOption {
          description = "List of devices to use as storage devices";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephClusterSpecStorageNodesDevices" "name" [ ]
            )
          );
          apply = attrsToList;
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "resources" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageNodesResources"));
        };
        "useAllDevices" = mkOption {
          description = "Whether to consume all the storage devices found on a machine";
          type = (types.nullOr types.bool);
        };
        "volumeClaimTemplates" = mkOption {
          description = "PersistentVolumeClaims to use as storage";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplates")
            )
          );
        };
      };

      config = {
        "config" = mkOverride 1002 null;
        "deviceFilter" = mkOverride 1002 null;
        "devicePathFilter" = mkOverride 1002 null;
        "devices" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "useAllDevices" = mkOverride 1002 null;
        "volumeClaimTemplates" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesDevices" = {

      options = {
        "config" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "fullpath" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "config" = mkOverride 1002 null;
        "fullpath" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephClusterSpecStorageNodesResourcesClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplates" = {

      options = {
        "metadata" = mkOption {
          description = "Standard object's metadata.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesMetadata")
          );
        };
        "spec" = mkOption {
          description = "spec defines the desired characteristics of a volume requested by a pod author.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpec")
          );
        };
      };

      config = {
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesMetadata" = {

      options = {
        "annotations" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "finalizers" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "labels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "finalizers" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpec" = {

      options = {
        "accessModes" = mkOption {
          description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
          type = (types.nullOr (types.listOf types.str));
        };
        "dataSource" = mkOption {
          description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecDataSource"
            )
          );
        };
        "dataSourceRef" = mkOption {
          description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecDataSourceRef"
            )
          );
        };
        "resources" = mkOption {
          description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecResources"
            )
          );
        };
        "selector" = mkOption {
          description = "selector is a label query over volumes to consider for binding.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecSelector"
            )
          );
        };
        "storageClassName" = mkOption {
          description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
          type = (types.nullOr types.str);
        };
        "volumeAttributesClassName" = mkOption {
          description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
          type = (types.nullOr types.str);
        };
        "volumeMode" = mkOption {
          description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
          type = (types.nullOr types.str);
        };
        "volumeName" = mkOption {
          description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "accessModes" = mkOverride 1002 null;
        "dataSource" = mkOverride 1002 null;
        "dataSourceRef" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "selector" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
        "volumeAttributesClassName" = mkOverride 1002 null;
        "volumeMode" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecDataSource" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecDataSourceRef" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
        "namespace" = mkOption {
          description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecResources" = {

      options = {
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageNodesVolumeClaimTemplatesSpecSelectorMatchExpressions" = {

      options = {
        "key" = mkOption {
          description = "key is the label key that the selector applies to.";
          type = types.str;
        };
        "operator" = mkOption {
          description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
          type = types.str;
        };
        "values" = mkOption {
          description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "values" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSets" = {

      options = {
        "config" = mkOption {
          description = "Provider-specific device configuration";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "count" = mkOption {
          description = "Count is the number of devices in this set";
          type = (types.withMinimum 1 types.int);
        };
        "encrypted" = mkOption {
          description = "Whether to encrypt the deviceSet";
          type = (types.nullOr types.bool);
        };
        "name" = mkOption {
          description = "Name is a unique identifier for the set";
          type = types.str;
        };
        "placement" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacement")
          );
        };
        "portable" = mkOption {
          description = "Portable represents OSD portability across the hosts";
          type = (types.nullOr types.bool);
        };
        "preparePlacement" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacement"
            )
          );
        };
        "resources" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsResources")
          );
        };
        "schedulerName" = mkOption {
          description = "Scheduler name for OSD pod placement";
          type = (types.nullOr types.str);
        };
        "tuneDeviceClass" = mkOption {
          description = "TuneSlowDeviceClass Tune the OSD when running on a slow Device Class";
          type = (types.nullOr types.bool);
        };
        "tuneFastDeviceClass" = mkOption {
          description = "TuneFastDeviceClass Tune the OSD when running on a fast Device Class";
          type = (types.nullOr types.bool);
        };
        "volumeClaimTemplates" = mkOption {
          description = "VolumeClaimTemplates is a list of PVC templates for the underlying storage devices";
          type = (
            types.listOf (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplates"
            )
          );
        };
      };

      config = {
        "config" = mkOverride 1002 null;
        "encrypted" = mkOverride 1002 null;
        "placement" = mkOverride 1002 null;
        "portable" = mkOverride 1002 null;
        "preparePlacement" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "schedulerName" = mkOverride 1002 null;
        "tuneDeviceClass" = mkOverride 1002 null;
        "tuneFastDeviceClass" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacement" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinity"
            )
          );
        };
        "podAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinity"
            )
          );
        };
        "podAntiAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinity"
            )
          );
        };
        "tolerations" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementTolerations"
              )
            )
          );
        };
        "topologySpreadConstraints" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementTopologySpreadConstraints"
              )
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topologySpreadConstraints" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "";
            type = (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementTolerations" = {

      options = {
        "effect" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementTopologySpreadConstraints" = {

      options = {
        "labelSelector" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementTopologySpreadConstraintsLabelSelector"
            )
          );
        };
        "matchLabelKeys" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "maxSkew" = mkOption {
          description = "";
          type = types.int;
        };
        "minDomains" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "nodeAffinityPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "nodeTaintsPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "topologyKey" = mkOption {
          description = "";
          type = types.str;
        };
        "whenUnsatisfiable" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "matchLabelKeys" = mkOverride 1002 null;
        "minDomains" = mkOverride 1002 null;
        "nodeAffinityPolicy" = mkOverride 1002 null;
        "nodeTaintsPolicy" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementTopologySpreadConstraintsLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacement" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinity"
            )
          );
        };
        "podAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinity"
            )
          );
        };
        "podAntiAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinity"
            )
          );
        };
        "tolerations" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementTolerations"
              )
            )
          );
        };
        "topologySpreadConstraints" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementTopologySpreadConstraints"
              )
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topologySpreadConstraints" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "";
            type = (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementTolerations" = {

      options = {
        "effect" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementTopologySpreadConstraints" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementTopologySpreadConstraintsLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "maxSkew" = mkOption {
            description = "";
            type = types.int;
          };
          "minDomains" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "nodeAffinityPolicy" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "nodeTaintsPolicy" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
          "whenUnsatisfiable" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "minDomains" = mkOverride 1002 null;
          "nodeAffinityPolicy" = mkOverride 1002 null;
          "nodeTaintsPolicy" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementTopologySpreadConstraintsLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementTopologySpreadConstraintsLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsPreparePlacementTopologySpreadConstraintsLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsResourcesClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplates" = {

      options = {
        "metadata" = mkOption {
          description = "Standard object's metadata.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesMetadata"
            )
          );
        };
        "spec" = mkOption {
          description = "spec defines the desired characteristics of a volume requested by a pod author.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpec"
            )
          );
        };
      };

      config = {
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesMetadata" = {

      options = {
        "annotations" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "finalizers" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "labels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "finalizers" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpec" = {

      options = {
        "accessModes" = mkOption {
          description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
          type = (types.nullOr (types.listOf types.str));
        };
        "dataSource" = mkOption {
          description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecDataSource"
            )
          );
        };
        "dataSourceRef" = mkOption {
          description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecDataSourceRef"
            )
          );
        };
        "resources" = mkOption {
          description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecResources"
            )
          );
        };
        "selector" = mkOption {
          description = "selector is a label query over volumes to consider for binding.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecSelector"
            )
          );
        };
        "storageClassName" = mkOption {
          description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
          type = (types.nullOr types.str);
        };
        "volumeAttributesClassName" = mkOption {
          description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
          type = (types.nullOr types.str);
        };
        "volumeMode" = mkOption {
          description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
          type = (types.nullOr types.str);
        };
        "volumeName" = mkOption {
          description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "accessModes" = mkOverride 1002 null;
        "dataSource" = mkOverride 1002 null;
        "dataSourceRef" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "selector" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
        "volumeAttributesClassName" = mkOverride 1002 null;
        "volumeMode" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecDataSource" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecDataSourceRef" =
      {

        options = {
          "apiGroup" = mkOption {
            description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
            type = (types.nullOr types.str);
          };
          "kind" = mkOption {
            description = "Kind is the type of resource being referenced";
            type = types.str;
          };
          "name" = mkOption {
            description = "Name is the name of resource being referenced";
            type = types.str;
          };
          "namespace" = mkOption {
            description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "apiGroup" = mkOverride 1002 null;
          "namespace" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecResources" = {

      options = {
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageStorageClassDeviceSetsVolumeClaimTemplatesSpecSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephClusterSpecStorageStore" = {

      options = {
        "type" = mkOption {
          description = "Type of backend storage to be used while creating OSDs. If empty, then bluestore will be used";
          type = (
            types.nullOr (
              types.enum [
                "bluestore"
                "bluestore-rdr"
              ]
            )
          );
        };
        "updateStore" = mkOption {
          description = "UpdateStore updates the backend store for existing OSDs. It destroys each OSD one at a time, cleans up the backing disk\nand prepares same OSD on that disk";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
        "updateStore" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplates" = {

      options = {
        "metadata" = mkOption {
          description = "Standard object's metadata.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesMetadata")
          );
        };
        "spec" = mkOption {
          description = "spec defines the desired characteristics of a volume requested by a pod author.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpec")
          );
        };
      };

      config = {
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesMetadata" = {

      options = {
        "annotations" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "finalizers" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "labels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "finalizers" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpec" = {

      options = {
        "accessModes" = mkOption {
          description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
          type = (types.nullOr (types.listOf types.str));
        };
        "dataSource" = mkOption {
          description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecDataSource"
            )
          );
        };
        "dataSourceRef" = mkOption {
          description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecDataSourceRef"
            )
          );
        };
        "resources" = mkOption {
          description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecResources")
          );
        };
        "selector" = mkOption {
          description = "selector is a label query over volumes to consider for binding.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecSelector")
          );
        };
        "storageClassName" = mkOption {
          description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
          type = (types.nullOr types.str);
        };
        "volumeAttributesClassName" = mkOption {
          description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
          type = (types.nullOr types.str);
        };
        "volumeMode" = mkOption {
          description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
          type = (types.nullOr types.str);
        };
        "volumeName" = mkOption {
          description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "accessModes" = mkOverride 1002 null;
        "dataSource" = mkOverride 1002 null;
        "dataSourceRef" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "selector" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
        "volumeAttributesClassName" = mkOverride 1002 null;
        "volumeMode" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecDataSource" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecDataSourceRef" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
        "namespace" = mkOption {
          description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecResources" = {

      options = {
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterSpecStorageVolumeClaimTemplatesSpecSelectorMatchExpressions" = {

      options = {
        "key" = mkOption {
          description = "key is the label key that the selector applies to.";
          type = types.str;
        };
        "operator" = mkOption {
          description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
          type = types.str;
        };
        "values" = mkOption {
          description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "values" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatus" = {

      options = {
        "ceph" = mkOption {
          description = "CephStatus is the details health of a Ceph Cluster";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCeph"));
        };
        "cephx" = mkOption {
          description = "ClusterCephxStatus defines the cephx key rotation status of various daemons on the cephCluster resource";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephx"));
        };
        "conditions" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephClusterStatusConditions")));
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
        "state" = mkOption {
          description = "ClusterState represents the state of a Ceph Cluster";
          type = (types.nullOr types.str);
        };
        "storage" = mkOption {
          description = "CephStorage represents flavors of Ceph Cluster Storage";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusStorage"));
        };
        "version" = mkOption {
          description = "ClusterVersion represents the version of a Ceph Cluster";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusVersion"));
        };
      };

      config = {
        "ceph" = mkOverride 1002 null;
        "cephx" = mkOverride 1002 null;
        "conditions" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
        "state" = mkOverride 1002 null;
        "storage" = mkOverride 1002 null;
        "version" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCeph" = {

      options = {
        "capacity" = mkOption {
          description = "Capacity is the capacity information of a Ceph Cluster";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephCapacity"));
        };
        "details" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "fsid" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "health" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastChanged" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastChecked" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "previousHealth" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "versions" = mkOption {
          description = "CephDaemonsVersions show the current ceph version for different ceph daemons";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephVersions"));
        };
      };

      config = {
        "capacity" = mkOverride 1002 null;
        "details" = mkOverride 1002 null;
        "fsid" = mkOverride 1002 null;
        "health" = mkOverride 1002 null;
        "lastChanged" = mkOverride 1002 null;
        "lastChecked" = mkOverride 1002 null;
        "previousHealth" = mkOverride 1002 null;
        "versions" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephCapacity" = {

      options = {
        "bytesAvailable" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "bytesTotal" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "bytesUsed" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "lastUpdated" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "bytesAvailable" = mkOverride 1002 null;
        "bytesTotal" = mkOverride 1002 null;
        "bytesUsed" = mkOverride 1002 null;
        "lastUpdated" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephVersions" = {

      options = {
        "cephfs-mirror" = mkOption {
          description = "CephFSMirror shows CephFSMirror Ceph version";
          type = (types.nullOr (types.attrsOf types.int));
        };
        "mds" = mkOption {
          description = "Mds shows Mds Ceph version";
          type = (types.nullOr (types.attrsOf types.int));
        };
        "mgr" = mkOption {
          description = "Mgr shows Mgr Ceph version";
          type = (types.nullOr (types.attrsOf types.int));
        };
        "mon" = mkOption {
          description = "Mon shows Mon Ceph version";
          type = (types.nullOr (types.attrsOf types.int));
        };
        "osd" = mkOption {
          description = "Osd shows Osd Ceph version";
          type = (types.nullOr (types.attrsOf types.int));
        };
        "overall" = mkOption {
          description = "Overall shows overall Ceph version";
          type = (types.nullOr (types.attrsOf types.int));
        };
        "rbd-mirror" = mkOption {
          description = "RbdMirror shows RbdMirror Ceph version";
          type = (types.nullOr (types.attrsOf types.int));
        };
        "rgw" = mkOption {
          description = "Rgw shows Rgw Ceph version";
          type = (types.nullOr (types.attrsOf types.int));
        };
      };

      config = {
        "cephfs-mirror" = mkOverride 1002 null;
        "mds" = mkOverride 1002 null;
        "mgr" = mkOverride 1002 null;
        "mon" = mkOverride 1002 null;
        "osd" = mkOverride 1002 null;
        "overall" = mkOverride 1002 null;
        "rbd-mirror" = mkOverride 1002 null;
        "rgw" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephx" = {

      options = {
        "admin" = mkOption {
          description = "Admin shows the CephX key status for the client.admin key";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephxAdmin"));
        };
        "cephExporter" = mkOption {
          description = "Ceph Exporter represents the cephx key rotation status of the ceph exporter daemon";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephxCephExporter"));
        };
        "crashCollector" = mkOption {
          description = "Crash Collector represents the cephx key rotation status of the crash collector daemon";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephxCrashCollector"));
        };
        "csi" = mkOption {
          description = "CSI shows the CephX key status for Ceph-CSI components.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephxCsi"));
        };
        "mgr" = mkOption {
          description = "Mgr represents the cephx key rotation status of the ceph manager daemon";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephxMgr"));
        };
        "mon" = mkOption {
          description = "Mon represents the CephX key status of the Monitor daemons";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephxMon"));
        };
        "osd" = mkOption {
          description = "OSD shows the CephX key status of OSDs";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephxOsd"));
        };
        "rbdMirrorPeer" = mkOption {
          description = "RBDMirrorPeer represents the cephx key rotation status of the `rbd-mirror-peer` user";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusCephxRbdMirrorPeer"));
        };
      };

      config = {
        "admin" = mkOverride 1002 null;
        "cephExporter" = mkOverride 1002 null;
        "crashCollector" = mkOverride 1002 null;
        "csi" = mkOverride 1002 null;
        "mgr" = mkOverride 1002 null;
        "mon" = mkOverride 1002 null;
        "osd" = mkOverride 1002 null;
        "rbdMirrorPeer" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephxAdmin" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephxCephExporter" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephxCrashCollector" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephxCsi" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
        "priorKeyCount" = mkOption {
          description = "PriorKeyCount reports the number of prior-generation CephX keys that remain active for the related component";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
        "priorKeyCount" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephxMgr" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephxMon" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephxOsd" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusCephxRbdMirrorPeer" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusStorage" = {

      options = {
        "deprecatedOSDs" = mkOption {
          description = "";
          type = (types.nullOr (types.loaOf types.int));
        };
        "deviceClasses" = mkOption {
          description = "";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephClusterStatusStorageDeviceClasses" "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "osd" = mkOption {
          description = "OSDStatus represents OSD status of the ceph Cluster";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusStorageOsd"));
        };
      };

      config = {
        "deprecatedOSDs" = mkOverride 1002 null;
        "deviceClasses" = mkOverride 1002 null;
        "osd" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusStorageDeviceClasses" = {

      options = {
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusStorageOsd" = {

      options = {
        "migrationStatus" = mkOption {
          description = "MigrationStatus status represents the current status of any OSD migration.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephClusterStatusStorageOsdMigrationStatus"));
        };
        "storeType" = mkOption {
          description = "StoreType is a mapping between the OSD backend stores and number of OSDs using these stores";
          type = (types.nullOr (types.attrsOf types.int));
        };
      };

      config = {
        "migrationStatus" = mkOverride 1002 null;
        "storeType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusStorageOsdMigrationStatus" = {

      options = {
        "pending" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "pending" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephClusterStatusVersion" = {

      options = {
        "image" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "version" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "image" = mkOverride 1002 null;
        "version" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystem" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "FilesystemSpec represents the spec of a file system";
          type = (submoduleOf "ceph.rook.io.v1.CephFilesystemSpec");
        };
        "status" = mkOption {
          description = "CephFilesystemStatus represents the status of a Ceph Filesystem";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirror" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "FilesystemMirroringSpec is the filesystem mirroring specification";
          type = (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpec");
        };
        "status" = mkOption {
          description = "FileMirrorStatus represents the status of the FileSystem mirror resource";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpec" = {

      options = {
        "annotations" = mkOption {
          description = "The annotations-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "labels" = mkOption {
          description = "The labels-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "placement" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacement"));
        };
        "priorityClassName" = mkOption {
          description = "PriorityClassName sets priority class on the cephfs-mirror pods";
          type = (types.nullOr types.str);
        };
        "resources" = mkOption {
          description = "The resource requirements for the cephfs-mirror pods";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecResources"));
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "placement" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacement" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinity"));
        };
        "podAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinity"));
        };
        "podAntiAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinity")
          );
        };
        "tolerations" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementTolerations")
            )
          );
        };
        "topologySpreadConstraints" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementTopologySpreadConstraints"
              )
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topologySpreadConstraints" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "";
            type = (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementTolerations" = {

      options = {
        "effect" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementTopologySpreadConstraints" = {

      options = {
        "labelSelector" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementTopologySpreadConstraintsLabelSelector"
            )
          );
        };
        "matchLabelKeys" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "maxSkew" = mkOption {
          description = "";
          type = types.int;
        };
        "minDomains" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "nodeAffinityPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "nodeTaintsPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "topologyKey" = mkOption {
          description = "";
          type = types.str;
        };
        "whenUnsatisfiable" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "matchLabelKeys" = mkOverride 1002 null;
        "minDomains" = mkOverride 1002 null;
        "nodeAffinityPolicy" = mkOverride 1002 null;
        "nodeTaintsPolicy" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementTopologySpreadConstraintsLabelSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephFilesystemMirrorSpecResourcesClaims" "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorSpecResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorStatus" = {

      options = {
        "cephx" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorStatusCephx"));
        };
        "conditions" = mkOption {
          description = "";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorStatusConditions"))
          );
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
        "conditions" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorStatusCephx" = {

      options = {
        "daemon" = mkOption {
          description = "Daemon shows the CephX key status for local Ceph daemons associated with this resources.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemMirrorStatusCephxDaemon"));
        };
      };

      config = {
        "daemon" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorStatusCephxDaemon" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemMirrorStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpec" = {

      options = {
        "dataPools" = mkOption {
          description = "The data pool settings, with optional predefined pool name.";
          type = (
            coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephFilesystemSpecDataPools" "name" [ ]
          );
          apply = attrsToList;
        };
        "metadataPool" = mkOption {
          description = "The metadata pool settings";
          type = (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPool");
        };
        "metadataServer" = mkOption {
          description = "The mds pod info";
          type = (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServer");
        };
        "mirroring" = mkOption {
          description = "The mirroring settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMirroring"));
        };
        "preserveFilesystemOnDelete" = mkOption {
          description = "Preserve the fs in the cluster on CephFilesystem CR deletion. Setting this to true automatically implies PreservePoolsOnDelete is true.";
          type = (types.nullOr types.bool);
        };
        "preservePoolNames" = mkOption {
          description = "Preserve pool names as specified";
          type = (types.nullOr types.bool);
        };
        "preservePoolsOnDelete" = mkOption {
          description = "Preserve pools on filesystem deletion";
          type = (types.nullOr types.bool);
        };
        "statusCheck" = mkOption {
          description = "The mirroring statusCheck";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecStatusCheck"));
        };
      };

      config = {
        "mirroring" = mkOverride 1002 null;
        "preserveFilesystemOnDelete" = mkOverride 1002 null;
        "preservePoolNames" = mkOverride 1002 null;
        "preservePoolsOnDelete" = mkOverride 1002 null;
        "statusCheck" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPools" = {

      options = {
        "application" = mkOption {
          description = "The application name to set on the pool. Only expected to be set for rgw pools.";
          type = (types.nullOr types.str);
        };
        "compressionMode" = mkOption {
          description = "DEPRECATED: use Parameters instead, e.g., Parameters[\"compression_mode\"] = \"force\"\nThe inline compression mode in Bluestore OSD to set to (options are: none, passive, aggressive, force)\nDo NOT set a default value for kubebuilder as this will override the Parameters";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "passive"
                "aggressive"
                "force"
                ""
              ]
            )
          );
        };
        "crushRoot" = mkOption {
          description = "The root of the crush hierarchy utilized by the pool";
          type = (types.nullOr types.str);
        };
        "deviceClass" = mkOption {
          description = "The device class the OSD should set to for use in the pool";
          type = (types.nullOr types.str);
        };
        "enableCrushUpdates" = mkOption {
          description = "Allow rook operator to change the pool CRUSH tunables once the pool is created";
          type = (types.nullOr types.bool);
        };
        "enableRBDStats" = mkOption {
          description = "EnableRBDStats is used to enable gathering of statistics for all RBD images in the pool";
          type = (types.nullOr types.bool);
        };
        "erasureCoded" = mkOption {
          description = "The erasure code settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecDataPoolsErasureCoded"));
        };
        "failureDomain" = mkOption {
          description = "The failure domain: osd/host/(region or zone if available) - technically also any type in the crush map";
          type = (types.nullOr types.str);
        };
        "mirroring" = mkOption {
          description = "The mirroring settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecDataPoolsMirroring"));
        };
        "name" = mkOption {
          description = "Name of the pool";
          type = (types.nullOr types.str);
        };
        "parameters" = mkOption {
          description = "Parameters is a list of properties to enable on a given pool";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "quotas" = mkOption {
          description = "The quota settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecDataPoolsQuotas"));
        };
        "replicated" = mkOption {
          description = "The replication settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecDataPoolsReplicated"));
        };
        "statusCheck" = mkOption {
          description = "The mirroring statusCheck";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecDataPoolsStatusCheck"));
        };
      };

      config = {
        "application" = mkOverride 1002 null;
        "compressionMode" = mkOverride 1002 null;
        "crushRoot" = mkOverride 1002 null;
        "deviceClass" = mkOverride 1002 null;
        "enableCrushUpdates" = mkOverride 1002 null;
        "enableRBDStats" = mkOverride 1002 null;
        "erasureCoded" = mkOverride 1002 null;
        "failureDomain" = mkOverride 1002 null;
        "mirroring" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "parameters" = mkOverride 1002 null;
        "quotas" = mkOverride 1002 null;
        "replicated" = mkOverride 1002 null;
        "statusCheck" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPoolsErasureCoded" = {

      options = {
        "algorithm" = mkOption {
          description = "The algorithm for erasure coding.\nIf absent, defaults to the plugin specified in osd_pool_default_erasure_code_profile.";
          type = (
            types.nullOr (
              types.enum [
                "isa"
                "jerasure"
              ]
            )
          );
        };
        "codingChunks" = mkOption {
          description = "Number of coding chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThis is the number of OSDs that can be lost simultaneously before data cannot be recovered.";
          type = (types.withMinimum 0 types.int);
        };
        "crushNumFailureDomains" = mkOption {
          description = "Number of failure domains to use for erasure coded chunk placement.\nWhen specified along with crushOSDsPerFailureDomain, a CRUSH MSR rule will be created\nthat distributes chunks across this many failure domains.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "crushOSDsPerFailureDomain" = mkOption {
          description = "Number of OSDs allowed per failure domain for erasure coded chunk placement.\nWhen specified along with crushNumFailureDomains, a CRUSH MSR rule will be created\nthat allows up to this many chunks on OSDs within each failure domain.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "dataChunks" = mkOption {
          description = "Number of data chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThe number of chunks required to recover an object when any single OSD is lost is the same\nas dataChunks so be aware that the larger the number of data chunks, the higher the cost of recovery.";
          type = (types.withMinimum 0 types.int);
        };
        "stripeUnit" = mkOption {
          description = "Erasure code stripe size in bytes. Ceph default is 4096 bytes (4 KiB).\nValue must be a multiple of 4096 (4Ki).";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "algorithm" = mkOverride 1002 null;
        "crushNumFailureDomains" = mkOverride 1002 null;
        "crushOSDsPerFailureDomain" = mkOverride 1002 null;
        "stripeUnit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPoolsMirroring" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled whether this pool is mirrored or not";
          type = (types.nullOr types.bool);
        };
        "mode" = mkOption {
          description = "Mode is the mirroring mode: pool, image or init-only.";
          type = (
            types.nullOr (
              types.enum [
                "pool"
                "image"
                "init-only"
              ]
            )
          );
        };
        "peers" = mkOption {
          description = "Peers represents the peers spec";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecDataPoolsMirroringPeers"));
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the scheduling of snapshot for mirrored images/pools";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecDataPoolsMirroringSnapshotSchedules")
            )
          );
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPoolsMirroringPeers" = {

      options = {
        "secretNames" = mkOption {
          description = "SecretNames represents the Kubernetes Secret names to add rbd-mirror or cephfs-mirror peers";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "secretNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPoolsMirroringSnapshotSchedules" = {

      options = {
        "interval" = mkOption {
          description = "Interval represent the periodicity of the snapshot.";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot, only valid for CephFS";
          type = (types.nullOr types.str);
        };
        "startTime" = mkOption {
          description = "StartTime indicates when to start the snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "startTime" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPoolsQuotas" = {

      options = {
        "maxBytes" = mkOption {
          description = "MaxBytes represents the quota in bytes\nDeprecated in favor of MaxSize";
          type = (types.nullOr types.int);
        };
        "maxObjects" = mkOption {
          description = "MaxObjects represents the quota in objects";
          type = (types.nullOr types.int);
        };
        "maxSize" = mkOption {
          description = "MaxSize represents the quota in bytes as a string";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "maxBytes" = mkOverride 1002 null;
        "maxObjects" = mkOverride 1002 null;
        "maxSize" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPoolsReplicated" = {

      options = {
        "hybridStorage" = mkOption {
          description = "HybridStorage represents hybrid storage tier settings";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecDataPoolsReplicatedHybridStorage")
          );
        };
        "replicasPerFailureDomain" = mkOption {
          description = "ReplicasPerFailureDomain the number of replica in the specified failure domain";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "requireSafeReplicaSize" = mkOption {
          description = "RequireSafeReplicaSize if false allows you to set replica 1";
          type = (types.nullOr types.bool);
        };
        "size" = mkOption {
          description = "Size - Number of copies per object in a replicated storage pool, including the object itself (required for replicated pool type)";
          type = (types.withMinimum 0 types.int);
        };
        "subFailureDomain" = mkOption {
          description = "SubFailureDomain the name of the sub-failure domain";
          type = (types.nullOr types.str);
        };
        "targetSizeRatio" = mkOption {
          description = "TargetSizeRatio gives a hint (%) to Ceph in terms of expected consumption of the total cluster capacity";
          type = (types.nullOr (types.withMinimum 0 (types.either types.int types.float)));
        };
      };

      config = {
        "hybridStorage" = mkOverride 1002 null;
        "replicasPerFailureDomain" = mkOverride 1002 null;
        "requireSafeReplicaSize" = mkOverride 1002 null;
        "subFailureDomain" = mkOverride 1002 null;
        "targetSizeRatio" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPoolsReplicatedHybridStorage" = {

      options = {
        "primaryDeviceClass" = mkOption {
          description = "PrimaryDeviceClass represents high performance tier (for example SSD or NVME) for Primary OSD";
          type = (types.withMinLength 1 types.str);
        };
        "secondaryDeviceClass" = mkOption {
          description = "SecondaryDeviceClass represents low performance tier (for example HDDs) for remaining OSDs";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPoolsStatusCheck" = {

      options = {
        "mirror" = mkOption {
          description = "HealthCheckSpec represents the health check of an object store bucket";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecDataPoolsStatusCheckMirror"));
        };
      };

      config = {
        "mirror" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecDataPoolsStatusCheckMirror" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPool" = {

      options = {
        "application" = mkOption {
          description = "The application name to set on the pool. Only expected to be set for rgw pools.";
          type = (types.nullOr types.str);
        };
        "compressionMode" = mkOption {
          description = "DEPRECATED: use Parameters instead, e.g., Parameters[\"compression_mode\"] = \"force\"\nThe inline compression mode in Bluestore OSD to set to (options are: none, passive, aggressive, force)\nDo NOT set a default value for kubebuilder as this will override the Parameters";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "passive"
                "aggressive"
                "force"
                ""
              ]
            )
          );
        };
        "crushRoot" = mkOption {
          description = "The root of the crush hierarchy utilized by the pool";
          type = (types.nullOr types.str);
        };
        "deviceClass" = mkOption {
          description = "The device class the OSD should set to for use in the pool";
          type = (types.nullOr types.str);
        };
        "enableCrushUpdates" = mkOption {
          description = "Allow rook operator to change the pool CRUSH tunables once the pool is created";
          type = (types.nullOr types.bool);
        };
        "enableRBDStats" = mkOption {
          description = "EnableRBDStats is used to enable gathering of statistics for all RBD images in the pool";
          type = (types.nullOr types.bool);
        };
        "erasureCoded" = mkOption {
          description = "The erasure code settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolErasureCoded"));
        };
        "failureDomain" = mkOption {
          description = "The failure domain: osd/host/(region or zone if available) - technically also any type in the crush map";
          type = (types.nullOr types.str);
        };
        "mirroring" = mkOption {
          description = "The mirroring settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolMirroring"));
        };
        "name" = mkOption {
          description = "Name of the pool";
          type = (types.nullOr types.str);
        };
        "parameters" = mkOption {
          description = "Parameters is a list of properties to enable on a given pool";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "quotas" = mkOption {
          description = "The quota settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolQuotas"));
        };
        "replicated" = mkOption {
          description = "The replication settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolReplicated"));
        };
        "statusCheck" = mkOption {
          description = "The mirroring statusCheck";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolStatusCheck"));
        };
      };

      config = {
        "application" = mkOverride 1002 null;
        "compressionMode" = mkOverride 1002 null;
        "crushRoot" = mkOverride 1002 null;
        "deviceClass" = mkOverride 1002 null;
        "enableCrushUpdates" = mkOverride 1002 null;
        "enableRBDStats" = mkOverride 1002 null;
        "erasureCoded" = mkOverride 1002 null;
        "failureDomain" = mkOverride 1002 null;
        "mirroring" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "parameters" = mkOverride 1002 null;
        "quotas" = mkOverride 1002 null;
        "replicated" = mkOverride 1002 null;
        "statusCheck" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolErasureCoded" = {

      options = {
        "algorithm" = mkOption {
          description = "The algorithm for erasure coding.\nIf absent, defaults to the plugin specified in osd_pool_default_erasure_code_profile.";
          type = (
            types.nullOr (
              types.enum [
                "isa"
                "jerasure"
              ]
            )
          );
        };
        "codingChunks" = mkOption {
          description = "Number of coding chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThis is the number of OSDs that can be lost simultaneously before data cannot be recovered.";
          type = (types.withMinimum 0 types.int);
        };
        "crushNumFailureDomains" = mkOption {
          description = "Number of failure domains to use for erasure coded chunk placement.\nWhen specified along with crushOSDsPerFailureDomain, a CRUSH MSR rule will be created\nthat distributes chunks across this many failure domains.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "crushOSDsPerFailureDomain" = mkOption {
          description = "Number of OSDs allowed per failure domain for erasure coded chunk placement.\nWhen specified along with crushNumFailureDomains, a CRUSH MSR rule will be created\nthat allows up to this many chunks on OSDs within each failure domain.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "dataChunks" = mkOption {
          description = "Number of data chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThe number of chunks required to recover an object when any single OSD is lost is the same\nas dataChunks so be aware that the larger the number of data chunks, the higher the cost of recovery.";
          type = (types.withMinimum 0 types.int);
        };
        "stripeUnit" = mkOption {
          description = "Erasure code stripe size in bytes. Ceph default is 4096 bytes (4 KiB).\nValue must be a multiple of 4096 (4Ki).";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "algorithm" = mkOverride 1002 null;
        "crushNumFailureDomains" = mkOverride 1002 null;
        "crushOSDsPerFailureDomain" = mkOverride 1002 null;
        "stripeUnit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolMirroring" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled whether this pool is mirrored or not";
          type = (types.nullOr types.bool);
        };
        "mode" = mkOption {
          description = "Mode is the mirroring mode: pool, image or init-only.";
          type = (
            types.nullOr (
              types.enum [
                "pool"
                "image"
                "init-only"
              ]
            )
          );
        };
        "peers" = mkOption {
          description = "Peers represents the peers spec";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolMirroringPeers"));
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the scheduling of snapshot for mirrored images/pools";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolMirroringSnapshotSchedules"
              )
            )
          );
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolMirroringPeers" = {

      options = {
        "secretNames" = mkOption {
          description = "SecretNames represents the Kubernetes Secret names to add rbd-mirror or cephfs-mirror peers";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "secretNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolMirroringSnapshotSchedules" = {

      options = {
        "interval" = mkOption {
          description = "Interval represent the periodicity of the snapshot.";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot, only valid for CephFS";
          type = (types.nullOr types.str);
        };
        "startTime" = mkOption {
          description = "StartTime indicates when to start the snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "startTime" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolQuotas" = {

      options = {
        "maxBytes" = mkOption {
          description = "MaxBytes represents the quota in bytes\nDeprecated in favor of MaxSize";
          type = (types.nullOr types.int);
        };
        "maxObjects" = mkOption {
          description = "MaxObjects represents the quota in objects";
          type = (types.nullOr types.int);
        };
        "maxSize" = mkOption {
          description = "MaxSize represents the quota in bytes as a string";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "maxBytes" = mkOverride 1002 null;
        "maxObjects" = mkOverride 1002 null;
        "maxSize" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolReplicated" = {

      options = {
        "hybridStorage" = mkOption {
          description = "HybridStorage represents hybrid storage tier settings";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolReplicatedHybridStorage")
          );
        };
        "replicasPerFailureDomain" = mkOption {
          description = "ReplicasPerFailureDomain the number of replica in the specified failure domain";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "requireSafeReplicaSize" = mkOption {
          description = "RequireSafeReplicaSize if false allows you to set replica 1";
          type = (types.nullOr types.bool);
        };
        "size" = mkOption {
          description = "Size - Number of copies per object in a replicated storage pool, including the object itself (required for replicated pool type)";
          type = (types.withMinimum 0 types.int);
        };
        "subFailureDomain" = mkOption {
          description = "SubFailureDomain the name of the sub-failure domain";
          type = (types.nullOr types.str);
        };
        "targetSizeRatio" = mkOption {
          description = "TargetSizeRatio gives a hint (%) to Ceph in terms of expected consumption of the total cluster capacity";
          type = (types.nullOr (types.withMinimum 0 (types.either types.int types.float)));
        };
      };

      config = {
        "hybridStorage" = mkOverride 1002 null;
        "replicasPerFailureDomain" = mkOverride 1002 null;
        "requireSafeReplicaSize" = mkOverride 1002 null;
        "subFailureDomain" = mkOverride 1002 null;
        "targetSizeRatio" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolReplicatedHybridStorage" = {

      options = {
        "primaryDeviceClass" = mkOption {
          description = "PrimaryDeviceClass represents high performance tier (for example SSD or NVME) for Primary OSD";
          type = (types.withMinLength 1 types.str);
        };
        "secondaryDeviceClass" = mkOption {
          description = "SecondaryDeviceClass represents low performance tier (for example HDDs) for remaining OSDs";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolStatusCheck" = {

      options = {
        "mirror" = mkOption {
          description = "HealthCheckSpec represents the health check of an object store bucket";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolStatusCheckMirror")
          );
        };
      };

      config = {
        "mirror" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataPoolStatusCheckMirror" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServer" = {

      options = {
        "activeCount" = mkOption {
          description = "The number of metadata servers that are active. The remaining servers in the cluster will be in standby mode.";
          type = (types.withMaximum 50 (types.withMinimum 1 types.int));
        };
        "activeStandby" = mkOption {
          description = "Whether each active MDS instance will have an active standby with a warm metadata cache for faster failover.\nIf false, standbys will still be available, but will not have a warm metadata cache.";
          type = (types.nullOr types.bool);
        };
        "annotations" = mkOption {
          description = "The annotations-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "cacheMemoryLimitFactor" = mkOption {
          description = "CacheMemoryLimitFactor is the factor applied to the memory limit to determine the MDS cache memory limit.\nMDS cache memory limit should be set to 50-60% of RAM reserved for the MDS container.\nMDS uses approximately 125% of the value of mds_cache_memory_limit in RAM.\nThis factor is applied when resources.limits.memory is set.";
          type = (
            types.nullOr (types.withMaximum 1 (types.withMinimum 0 (types.either types.int types.float)))
          );
        };
        "cacheMemoryRequestFactor" = mkOption {
          description = "CacheMemoryRequestFactor is the factor applied to the memory request to determine the MDS cache memory limit.\nThis factor is applied when resources.requests.memory is set and resources.limits.memory is not set.";
          type = (
            types.nullOr (types.withMaximum 1 (types.withMinimum 0 (types.either types.int types.float)))
          );
        };
        "labels" = mkOption {
          description = "The labels-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "livenessProbe" = mkOption {
          description = "ProbeSpec is a wrapper around Probe so it can be enabled or disabled for a Ceph daemon";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbe"));
        };
        "placement" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacement"));
        };
        "priorityClassName" = mkOption {
          description = "PriorityClassName sets priority classes on components";
          type = (types.nullOr types.str);
        };
        "resources" = mkOption {
          description = "The resource requirements for the mds pods";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerResources"));
        };
        "startupProbe" = mkOption {
          description = "ProbeSpec is a wrapper around Probe so it can be enabled or disabled for a Ceph daemon";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbe"));
        };
      };

      config = {
        "activeStandby" = mkOverride 1002 null;
        "annotations" = mkOverride 1002 null;
        "cacheMemoryLimitFactor" = mkOverride 1002 null;
        "cacheMemoryRequestFactor" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "livenessProbe" = mkOverride 1002 null;
        "placement" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "startupProbe" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbe" = {

      options = {
        "disabled" = mkOption {
          description = "Disabled determines whether probe is disable or not";
          type = (types.nullOr types.bool);
        };
        "probe" = mkOption {
          description = "Probe describes a health check to be performed against a container to determine whether it is\nalive or ready to receive traffic.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbe")
          );
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "probe" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbe" = {

      options = {
        "exec" = mkOption {
          description = "Exec specifies a command to execute in the container.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeExec")
          );
        };
        "failureThreshold" = mkOption {
          description = "Minimum consecutive failures for the probe to be considered failed after having succeeded.\nDefaults to 3. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "grpc" = mkOption {
          description = "GRPC specifies a GRPC HealthCheckRequest.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeGrpc")
          );
        };
        "httpGet" = mkOption {
          description = "HTTPGet specifies an HTTP GET request to perform.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeHttpGet"
            )
          );
        };
        "initialDelaySeconds" = mkOption {
          description = "Number of seconds after the container has started before liveness probes are initiated.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
        "periodSeconds" = mkOption {
          description = "How often (in seconds) to perform the probe.\nDefault to 10 seconds. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "successThreshold" = mkOption {
          description = "Minimum consecutive successes for the probe to be considered successful after having failed.\nDefaults to 1. Must be 1 for liveness and startup. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "tcpSocket" = mkOption {
          description = "TCPSocket specifies a connection to a TCP port.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeTcpSocket"
            )
          );
        };
        "terminationGracePeriodSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "timeoutSeconds" = mkOption {
          description = "Number of seconds after which the probe times out.\nDefaults to 1 second. Minimum value is 1.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "exec" = mkOverride 1002 null;
        "failureThreshold" = mkOverride 1002 null;
        "grpc" = mkOverride 1002 null;
        "httpGet" = mkOverride 1002 null;
        "initialDelaySeconds" = mkOverride 1002 null;
        "periodSeconds" = mkOverride 1002 null;
        "successThreshold" = mkOverride 1002 null;
        "tcpSocket" = mkOverride 1002 null;
        "terminationGracePeriodSeconds" = mkOverride 1002 null;
        "timeoutSeconds" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeExec" = {

      options = {
        "command" = mkOption {
          description = "Command is the command line to execute inside the container, the working directory for the\ncommand  is root ('/') in the container's filesystem. The command is simply exec'd, it is\nnot run inside a shell, so traditional shell instructions ('|', etc) won't work. To use\na shell, you need to explicitly call out to that shell.\nExit status of 0 is treated as live/healthy and non-zero is unhealthy.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "command" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeGrpc" = {

      options = {
        "port" = mkOption {
          description = "Port number of the gRPC service. Number must be in the range 1 to 65535.";
          type = types.int;
        };
        "service" = mkOption {
          description = "Service is the name of the service to place in the gRPC HealthCheckRequest\n(see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).\n\nIf this is not specified, the default behavior is defined by gRPC.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "service" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeHttpGet" = {

      options = {
        "host" = mkOption {
          description = "Host name to connect to, defaults to the pod IP. You probably want to set\n\"Host\" in httpHeaders instead.";
          type = (types.nullOr types.str);
        };
        "httpHeaders" = mkOption {
          description = "Custom headers to set in the request. HTTP allows repeated headers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeHttpGetHttpHeaders"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "path" = mkOption {
          description = "Path to access on the HTTP server.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Name or number of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
        "scheme" = mkOption {
          description = "Scheme to use for connecting to the host.\nDefaults to HTTP.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
        "httpHeaders" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "scheme" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeHttpGetHttpHeaders" = {

      options = {
        "name" = mkOption {
          description = "The header field name.\nThis will be canonicalized upon output, so case-variant names will be understood as the same header.";
          type = types.str;
        };
        "value" = mkOption {
          description = "The header field value";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerLivenessProbeProbeTcpSocket" = {

      options = {
        "host" = mkOption {
          description = "Optional: Host name to connect to, defaults to the pod IP.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Number or name of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacement" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinity")
          );
        };
        "podAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinity")
          );
        };
        "podAntiAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinity"
            )
          );
        };
        "tolerations" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementTolerations")
            )
          );
        };
        "topologySpreadConstraints" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementTopologySpreadConstraints"
              )
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topologySpreadConstraints" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "";
            type = (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementTolerations" = {

      options = {
        "effect" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementTopologySpreadConstraints" = {

      options = {
        "labelSelector" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementTopologySpreadConstraintsLabelSelector"
            )
          );
        };
        "matchLabelKeys" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "maxSkew" = mkOption {
          description = "";
          type = types.int;
        };
        "minDomains" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "nodeAffinityPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "nodeTaintsPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "topologyKey" = mkOption {
          description = "";
          type = types.str;
        };
        "whenUnsatisfiable" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "matchLabelKeys" = mkOverride 1002 null;
        "minDomains" = mkOverride 1002 null;
        "nodeAffinityPolicy" = mkOverride 1002 null;
        "nodeTaintsPolicy" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementTopologySpreadConstraintsLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephFilesystemSpecMetadataServerResourcesClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbe" = {

      options = {
        "disabled" = mkOption {
          description = "Disabled determines whether probe is disable or not";
          type = (types.nullOr types.bool);
        };
        "probe" = mkOption {
          description = "Probe describes a health check to be performed against a container to determine whether it is\nalive or ready to receive traffic.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbe")
          );
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "probe" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbe" = {

      options = {
        "exec" = mkOption {
          description = "Exec specifies a command to execute in the container.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeExec")
          );
        };
        "failureThreshold" = mkOption {
          description = "Minimum consecutive failures for the probe to be considered failed after having succeeded.\nDefaults to 3. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "grpc" = mkOption {
          description = "GRPC specifies a GRPC HealthCheckRequest.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeGrpc")
          );
        };
        "httpGet" = mkOption {
          description = "HTTPGet specifies an HTTP GET request to perform.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeHttpGet"
            )
          );
        };
        "initialDelaySeconds" = mkOption {
          description = "Number of seconds after the container has started before liveness probes are initiated.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
        "periodSeconds" = mkOption {
          description = "How often (in seconds) to perform the probe.\nDefault to 10 seconds. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "successThreshold" = mkOption {
          description = "Minimum consecutive successes for the probe to be considered successful after having failed.\nDefaults to 1. Must be 1 for liveness and startup. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "tcpSocket" = mkOption {
          description = "TCPSocket specifies a connection to a TCP port.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeTcpSocket"
            )
          );
        };
        "terminationGracePeriodSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "timeoutSeconds" = mkOption {
          description = "Number of seconds after which the probe times out.\nDefaults to 1 second. Minimum value is 1.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "exec" = mkOverride 1002 null;
        "failureThreshold" = mkOverride 1002 null;
        "grpc" = mkOverride 1002 null;
        "httpGet" = mkOverride 1002 null;
        "initialDelaySeconds" = mkOverride 1002 null;
        "periodSeconds" = mkOverride 1002 null;
        "successThreshold" = mkOverride 1002 null;
        "tcpSocket" = mkOverride 1002 null;
        "terminationGracePeriodSeconds" = mkOverride 1002 null;
        "timeoutSeconds" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeExec" = {

      options = {
        "command" = mkOption {
          description = "Command is the command line to execute inside the container, the working directory for the\ncommand  is root ('/') in the container's filesystem. The command is simply exec'd, it is\nnot run inside a shell, so traditional shell instructions ('|', etc) won't work. To use\na shell, you need to explicitly call out to that shell.\nExit status of 0 is treated as live/healthy and non-zero is unhealthy.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "command" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeGrpc" = {

      options = {
        "port" = mkOption {
          description = "Port number of the gRPC service. Number must be in the range 1 to 65535.";
          type = types.int;
        };
        "service" = mkOption {
          description = "Service is the name of the service to place in the gRPC HealthCheckRequest\n(see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).\n\nIf this is not specified, the default behavior is defined by gRPC.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "service" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeHttpGet" = {

      options = {
        "host" = mkOption {
          description = "Host name to connect to, defaults to the pod IP. You probably want to set\n\"Host\" in httpHeaders instead.";
          type = (types.nullOr types.str);
        };
        "httpHeaders" = mkOption {
          description = "Custom headers to set in the request. HTTP allows repeated headers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeHttpGetHttpHeaders"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "path" = mkOption {
          description = "Path to access on the HTTP server.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Name or number of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
        "scheme" = mkOption {
          description = "Scheme to use for connecting to the host.\nDefaults to HTTP.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
        "httpHeaders" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "scheme" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeHttpGetHttpHeaders" = {

      options = {
        "name" = mkOption {
          description = "The header field name.\nThis will be canonicalized upon output, so case-variant names will be understood as the same header.";
          type = types.str;
        };
        "value" = mkOption {
          description = "The header field value";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMetadataServerStartupProbeProbeTcpSocket" = {

      options = {
        "host" = mkOption {
          description = "Optional: Host name to connect to, defaults to the pod IP.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Number or name of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMirroring" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled whether this filesystem is mirrored or not";
          type = (types.nullOr types.bool);
        };
        "peers" = mkOption {
          description = "Peers represents the peers spec";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMirroringPeers"));
        };
        "snapshotRetention" = mkOption {
          description = "Retention is the retention policy for a snapshot schedule\nOne path has exactly one retention policy.\nA policy can however contain multiple count-time period pairs in order to specify complex retention policies";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMirroringSnapshotRetention")
            )
          );
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the scheduling of snapshot for mirrored filesystems";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecMirroringSnapshotSchedules")
            )
          );
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "snapshotRetention" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMirroringPeers" = {

      options = {
        "secretNames" = mkOption {
          description = "SecretNames represents the Kubernetes Secret names to add rbd-mirror or cephfs-mirror peers";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "secretNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMirroringSnapshotRetention" = {

      options = {
        "duration" = mkOption {
          description = "Duration represents the retention duration for a snapshot";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "duration" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecMirroringSnapshotSchedules" = {

      options = {
        "interval" = mkOption {
          description = "Interval represent the periodicity of the snapshot.";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot, only valid for CephFS";
          type = (types.nullOr types.str);
        };
        "startTime" = mkOption {
          description = "StartTime indicates when to start the snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "startTime" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecStatusCheck" = {

      options = {
        "mirror" = mkOption {
          description = "HealthCheckSpec represents the health check of an object store bucket";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSpecStatusCheckMirror"));
        };
      };

      config = {
        "mirror" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSpecStatusCheckMirror" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatus" = {

      options = {
        "cephx" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemStatusCephx"));
        };
        "conditions" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephFilesystemStatusConditions")));
        };
        "info" = mkOption {
          description = "Use only info and put mirroringStatus in it?";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "mirroringStatus" = mkOption {
          description = "MirroringStatus is the filesystem mirroring status";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemStatusMirroringStatus"));
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
        "snapshotScheduleStatus" = mkOption {
          description = "FilesystemSnapshotScheduleStatusSpec is the status of the snapshot schedule";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemStatusSnapshotScheduleStatus"));
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
        "conditions" = mkOverride 1002 null;
        "info" = mkOverride 1002 null;
        "mirroringStatus" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
        "snapshotScheduleStatus" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusCephx" = {

      options = {
        "daemon" = mkOption {
          description = "Daemon shows the CephX key status for local Ceph daemons associated with this resources.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemStatusCephxDaemon"));
        };
      };

      config = {
        "daemon" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusCephxDaemon" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusMirroringStatus" = {

      options = {
        "daemonsStatus" = mkOption {
          description = "PoolMirroringStatus is the mirroring status of a filesystem";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatus")
            )
          );
        };
        "details" = mkOption {
          description = "Details contains potential status errors";
          type = (types.nullOr types.str);
        };
        "lastChanged" = mkOption {
          description = "LastChanged is the last time the status last changed";
          type = (types.nullOr types.str);
        };
        "lastChecked" = mkOption {
          description = "LastChecked is the last time the status was checked";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "daemonsStatus" = mkOverride 1002 null;
        "details" = mkOverride 1002 null;
        "lastChanged" = mkOverride 1002 null;
        "lastChecked" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatus" = {

      options = {
        "daemon_id" = mkOption {
          description = "DaemonID is the cephfs-mirror name";
          type = (types.nullOr types.int);
        };
        "filesystems" = mkOption {
          description = "Filesystems is the list of filesystems managed by a given cephfs-mirror daemon";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatusFilesystems"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
      };

      config = {
        "daemon_id" = mkOverride 1002 null;
        "filesystems" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatusFilesystems" = {

      options = {
        "directory_count" = mkOption {
          description = "DirectoryCount is the number of directories in the filesystem";
          type = (types.nullOr types.int);
        };
        "filesystem_id" = mkOption {
          description = "FilesystemID is the filesystem identifier";
          type = (types.nullOr types.int);
        };
        "name" = mkOption {
          description = "Name is name of the filesystem";
          type = (types.nullOr types.str);
        };
        "peers" = mkOption {
          description = "Peers represents the mirroring peers";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatusFilesystemsPeers"
              )
            )
          );
        };
      };

      config = {
        "directory_count" = mkOverride 1002 null;
        "filesystem_id" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatusFilesystemsPeers" = {

      options = {
        "remote" = mkOption {
          description = "Remote are the remote cluster information";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatusFilesystemsPeersRemote"
            )
          );
        };
        "stats" = mkOption {
          description = "Stats are the stat a peer mirror";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatusFilesystemsPeersStats"
            )
          );
        };
        "uuid" = mkOption {
          description = "UUID is the peer unique identifier";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "remote" = mkOverride 1002 null;
        "stats" = mkOverride 1002 null;
        "uuid" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatusFilesystemsPeersRemote" = {

      options = {
        "client_name" = mkOption {
          description = "ClientName is cephx name";
          type = (types.nullOr types.str);
        };
        "cluster_name" = mkOption {
          description = "ClusterName is the name of the cluster";
          type = (types.nullOr types.str);
        };
        "fs_name" = mkOption {
          description = "FsName is the filesystem name";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "client_name" = mkOverride 1002 null;
        "cluster_name" = mkOverride 1002 null;
        "fs_name" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusMirroringStatusDaemonsStatusFilesystemsPeersStats" = {

      options = {
        "failure_count" = mkOption {
          description = "FailureCount is the number of mirroring failure";
          type = (types.nullOr types.int);
        };
        "recovery_count" = mkOption {
          description = "RecoveryCount is the number of recovery attempted after failures";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "failure_count" = mkOverride 1002 null;
        "recovery_count" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusSnapshotScheduleStatus" = {

      options = {
        "details" = mkOption {
          description = "Details contains potential status errors";
          type = (types.nullOr types.str);
        };
        "lastChanged" = mkOption {
          description = "LastChanged is the last time the status last changed";
          type = (types.nullOr types.str);
        };
        "lastChecked" = mkOption {
          description = "LastChecked is the last time the status was checked";
          type = (types.nullOr types.str);
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the list of snapshots scheduled";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephFilesystemStatusSnapshotScheduleStatusSnapshotSchedules"
              )
            )
          );
        };
      };

      config = {
        "details" = mkOverride 1002 null;
        "lastChanged" = mkOverride 1002 null;
        "lastChecked" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusSnapshotScheduleStatusSnapshotSchedules" = {

      options = {
        "fs" = mkOption {
          description = "Fs is the name of the Ceph Filesystem";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path on the filesystem";
          type = (types.nullOr types.str);
        };
        "rel_path" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "retention" = mkOption {
          description = "FilesystemSnapshotScheduleStatusRetention is the retention specification for a filesystem snapshot schedule";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephFilesystemStatusSnapshotScheduleStatusSnapshotSchedulesRetention"
            )
          );
        };
        "schedule" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "subvol" = mkOption {
          description = "Subvol is the name of the sub volume";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fs" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "rel_path" = mkOverride 1002 null;
        "retention" = mkOverride 1002 null;
        "schedule" = mkOverride 1002 null;
        "subvol" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemStatusSnapshotScheduleStatusSnapshotSchedulesRetention" = {

      options = {
        "active" = mkOption {
          description = "Active is whether the scheduled is active or not";
          type = (types.nullOr types.bool);
        };
        "created" = mkOption {
          description = "Created is when the snapshot schedule was created";
          type = (types.nullOr types.str);
        };
        "created_count" = mkOption {
          description = "CreatedCount is total amount of snapshots";
          type = (types.nullOr types.int);
        };
        "first" = mkOption {
          description = "First is when the first snapshot schedule was taken";
          type = (types.nullOr types.str);
        };
        "last" = mkOption {
          description = "Last is when the last snapshot schedule was taken";
          type = (types.nullOr types.str);
        };
        "last_pruned" = mkOption {
          description = "LastPruned is when the last snapshot schedule was pruned";
          type = (types.nullOr types.str);
        };
        "pruned_count" = mkOption {
          description = "PrunedCount is total amount of pruned snapshots";
          type = (types.nullOr types.int);
        };
        "start" = mkOption {
          description = "Start is when the snapshot schedule starts";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "active" = mkOverride 1002 null;
        "created" = mkOverride 1002 null;
        "created_count" = mkOverride 1002 null;
        "first" = mkOverride 1002 null;
        "last" = mkOverride 1002 null;
        "last_pruned" = mkOverride 1002 null;
        "pruned_count" = mkOverride 1002 null;
        "start" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSubVolumeGroup" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "Spec represents the specification of a Ceph Filesystem SubVolumeGroup";
          type = (submoduleOf "ceph.rook.io.v1.CephFilesystemSubVolumeGroupSpec");
        };
        "status" = mkOption {
          description = "Status represents the status of a CephFilesystem SubvolumeGroup";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSubVolumeGroupStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSubVolumeGroupSpec" = {

      options = {
        "clusterID" = mkOption {
          description = "ClusterID to be used for this subvolume group in the CSI configuration.\nIt must be unique among all Ceph clusters managed by Rook.\nIf not specified, the clusterID will be generated and can be found in the CR status.";
          type = (types.nullOr (types.withMaxLength 36 (types.withMinLength 1 types.str)));
        };
        "csiMetadataRadosNamespace" = mkOption {
          description = "The RADOS namespace ceph-csi uses for additional metadata it stores in the metadata pool of the CephFS.\nIf not specified the default of the ceph-csi driver is used.";
          type = (types.nullOr (types.withMaxLength 1024 (types.withMinLength 1 types.str)));
        };
        "dataPoolName" = mkOption {
          description = "The data pool name for the Ceph Filesystem subvolume group layout, if the default CephFS pool is not desired.";
          type = (types.nullOr types.str);
        };
        "filesystemName" = mkOption {
          description = "FilesystemName is the name of Ceph Filesystem SubVolumeGroup volume name. Typically it's the name of\nthe CephFilesystem CR. If not coming from the CephFilesystem CR, it can be retrieved from the\nlist of Ceph Filesystem volumes with `ceph fs volume ls`. To learn more about Ceph Filesystem\nabstractions see https://docs.ceph.com/en/latest/cephfs/fs-volumes/#fs-volumes-and-subvolumes";
          type = types.str;
        };
        "name" = mkOption {
          description = "The name of the subvolume group. If not set, the default is the name of the subvolumeGroup CR.";
          type = (types.nullOr types.str);
        };
        "pinning" = mkOption {
          description = "Pinning configuration of CephFilesystemSubVolumeGroup,\nreference https://docs.ceph.com/en/latest/cephfs/fs-volumes/#pinning-subvolumes-and-subvolume-groups\nonly one out of (export, distributed, random) can be set at a time";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephFilesystemSubVolumeGroupSpecPinning"));
        };
        "quota" = mkOption {
          description = "Quota size of the Ceph Filesystem subvolume group.";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "clusterID" = mkOverride 1002 null;
        "csiMetadataRadosNamespace" = mkOverride 1002 null;
        "dataPoolName" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "pinning" = mkOverride 1002 null;
        "quota" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSubVolumeGroupSpecPinning" = {

      options = {
        "distributed" = mkOption {
          description = "";
          type = (types.nullOr (types.withMaximum 1 (types.withMinimum 0 types.int)));
        };
        "export" = mkOption {
          description = "";
          type = (types.nullOr (types.withMaximum 256 (types.withMinimum (-1) types.int)));
        };
        "random" = mkOption {
          description = "";
          type = (
            types.nullOr (types.withMaximum 1 (types.withMinimum 0 (types.either types.int types.float)))
          );
        };
      };

      config = {
        "distributed" = mkOverride 1002 null;
        "export" = mkOverride 1002 null;
        "random" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephFilesystemSubVolumeGroupStatus" = {

      options = {
        "info" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "info" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFS" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "NFSGaneshaSpec represents the spec of an nfs ganesha server";
          type = (submoduleOf "ceph.rook.io.v1.CephNFSSpec");
        };
        "status" = mkOption {
          description = "NFSStatus represents the status of Ceph NFS";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpec" = {

      options = {
        "rados" = mkOption {
          description = "RADOS is the Ganesha RADOS specification";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecRados"));
        };
        "security" = mkOption {
          description = "Security allows specifying security configurations for the NFS cluster";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurity"));
        };
        "server" = mkOption {
          description = "Server is the Ganesha Server specification";
          type = (submoduleOf "ceph.rook.io.v1.CephNFSSpecServer");
        };
      };

      config = {
        "rados" = mkOverride 1002 null;
        "security" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecRados" = {

      options = {
        "namespace" = mkOption {
          description = "The namespace inside the Ceph pool (set by 'pool') where shared NFS-Ganesha config is stored.\nThis setting is deprecated as it is internally set to the name of the CephNFS.";
          type = (types.nullOr types.str);
        };
        "pool" = mkOption {
          description = "The Ceph pool used store the shared configuration for NFS-Ganesha daemons.\nThis setting is deprecated, as it is internally required to be \".nfs\".";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "namespace" = mkOverride 1002 null;
        "pool" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurity" = {

      options = {
        "kerberos" = mkOption {
          description = "Kerberos configures NFS-Ganesha to secure NFS client connections with Kerberos.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberos"));
        };
        "sssd" = mkOption {
          description = "SSSD enables integration with System Security Services Daemon (SSSD). SSSD can be used to\nprovide user ID mapping from a number of sources. See https://sssd.io for more information\nabout the SSSD project.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssd"));
        };
      };

      config = {
        "kerberos" = mkOverride 1002 null;
        "sssd" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberos" = {

      options = {
        "configFiles" = mkOption {
          description = "ConfigFiles defines where the Kerberos configuration should be sourced from. Config files\nwill be placed into the `/etc/krb5.conf.rook/` directory.\n\nIf this is left empty, Rook will not add any files. This allows you to manage the files\nyourself however you wish. For example, you may build them into your custom Ceph container\nimage or use the Vault agent injector to securely add the files via annotations on the\nCephNFS spec (passed to the NFS server pods).\n\nRook configures Kerberos to log to stderr. We suggest removing logging sections from config\nfiles to avoid consuming unnecessary disk space from logging to files.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFiles"));
        };
        "domainName" = mkOption {
          description = "DomainName should be set to the Kerberos Realm.";
          type = (types.nullOr types.str);
        };
        "keytabFile" = mkOption {
          description = "KeytabFile defines where the Kerberos keytab should be sourced from. The keytab file will be\nplaced into `/etc/krb5.keytab`. If this is left empty, Rook will not add the file.\nThis allows you to manage the `krb5.keytab` file yourself however you wish. For example, you\nmay build it into your custom Ceph container image or use the Vault agent injector to\nsecurely add the file via annotations on the CephNFS spec (passed to the NFS server pods).";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFile"));
        };
        "principalName" = mkOption {
          description = "PrincipalName corresponds directly to NFS-Ganesha's NFS_KRB5:PrincipalName config. In\npractice, this is the service prefix of the principal name. The default is \"nfs\".\nThis value is combined with (a) the namespace and name of the CephNFS (with a hyphen between)\nand (b) the Realm configured in the user-provided krb5.conf to determine the full principal\nname: <principalName>/<namespace>-<name>@<realm>. e.g., nfs/rook-ceph-my-nfs@example.net.\nSee https://github.com/nfs-ganesha/nfs-ganesha/wiki/RPCSEC_GSS for more detail.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "configFiles" = mkOverride 1002 null;
        "domainName" = mkOverride 1002 null;
        "keytabFile" = mkOverride 1002 null;
        "principalName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFiles" = {

      options = {
        "volumeSource" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSource")
          );
        };
      };

      config = {
        "volumeSource" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSource" = {

      options = {
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceConfigMap"
            )
          );
        };
        "emptyDir" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceEmptyDir"
            )
          );
        };
        "hostPath" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceHostPath"
            )
          );
        };
        "persistentVolumeClaim" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourcePersistentVolumeClaim"
            )
          );
        };
        "projected" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjected"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceSecret"
            )
          );
        };
      };

      config = {
        "configMap" = mkOverride 1002 null;
        "emptyDir" = mkOverride 1002 null;
        "hostPath" = mkOverride 1002 null;
        "persistentVolumeClaim" = mkOverride 1002 null;
        "projected" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceConfigMap" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceEmptyDir" = {

      options = {
        "medium" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "sizeLimit" = mkOption {
          description = "";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "medium" = mkOverride 1002 null;
        "sizeLimit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceHostPath" = {

      options = {
        "path" = mkOption {
          description = "";
          type = types.str;
        };
        "type" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourcePersistentVolumeClaim" = {

      options = {
        "claimName" = mkOption {
          description = "";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjected" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "sources" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSources"
              )
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "sources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSources" = {

      options = {
        "clusterTrustBundle" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesClusterTrustBundle"
            )
          );
        };
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesConfigMap"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesDownwardAPI"
            )
          );
        };
        "podCertificate" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesPodCertificate"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesSecret"
            )
          );
        };
        "serviceAccountToken" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesServiceAccountToken"
            )
          );
        };
      };

      config = {
        "clusterTrustBundle" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "podCertificate" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "serviceAccountToken" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesClusterTrustBundle" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector"
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "signerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
          "signerName" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesConfigMap" = {

      options = {
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesConfigMapItems" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesDownwardAPI" = {

      options = {
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesDownwardAPIItems"
              )
            )
          );
        };
      };

      config = {
        "items" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesDownwardAPIItems" =
      {

        options = {
          "fieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef"
              )
            );
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "resourceFieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef"
              )
            );
          };
        };

        config = {
          "fieldRef" = mkOverride 1002 null;
          "mode" = mkOverride 1002 null;
          "resourceFieldRef" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef" =
      {

        options = {
          "apiVersion" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "fieldPath" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "apiVersion" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesPodCertificate" =
      {

        options = {
          "certificateChainPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "credentialBundlePath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyType" = mkOption {
            description = "";
            type = types.str;
          };
          "maxExpirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "signerName" = mkOption {
            description = "";
            type = types.str;
          };
          "userAnnotations" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "certificateChainPath" = mkOverride 1002 null;
          "credentialBundlePath" = mkOverride 1002 null;
          "keyPath" = mkOverride 1002 null;
          "maxExpirationSeconds" = mkOverride 1002 null;
          "userAnnotations" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesSecret" = {

      options = {
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesSecretItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesSecretItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceProjectedSourcesServiceAccountToken" =
      {

        options = {
          "audience" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "expirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "audience" = mkOverride 1002 null;
          "expirationSeconds" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceSecret" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceSecretItems"
              )
            )
          );
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosConfigFilesVolumeSourceSecretItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFile" = {

      options = {
        "volumeSource" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSource")
          );
        };
      };

      config = {
        "volumeSource" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSource" = {

      options = {
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceConfigMap"
            )
          );
        };
        "emptyDir" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceEmptyDir"
            )
          );
        };
        "hostPath" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceHostPath"
            )
          );
        };
        "persistentVolumeClaim" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourcePersistentVolumeClaim"
            )
          );
        };
        "projected" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjected"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceSecret")
          );
        };
      };

      config = {
        "configMap" = mkOverride 1002 null;
        "emptyDir" = mkOverride 1002 null;
        "hostPath" = mkOverride 1002 null;
        "persistentVolumeClaim" = mkOverride 1002 null;
        "projected" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceConfigMap" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceEmptyDir" = {

      options = {
        "medium" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "sizeLimit" = mkOption {
          description = "";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "medium" = mkOverride 1002 null;
        "sizeLimit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceHostPath" = {

      options = {
        "path" = mkOption {
          description = "";
          type = types.str;
        };
        "type" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourcePersistentVolumeClaim" = {

      options = {
        "claimName" = mkOption {
          description = "";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjected" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "sources" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSources"
              )
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "sources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSources" = {

      options = {
        "clusterTrustBundle" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesClusterTrustBundle"
            )
          );
        };
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesConfigMap"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesDownwardAPI"
            )
          );
        };
        "podCertificate" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesPodCertificate"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesSecret"
            )
          );
        };
        "serviceAccountToken" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesServiceAccountToken"
            )
          );
        };
      };

      config = {
        "clusterTrustBundle" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "podCertificate" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "serviceAccountToken" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesClusterTrustBundle" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector"
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "signerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
          "signerName" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesConfigMap" = {

      options = {
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesConfigMapItems" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesDownwardAPI" = {

      options = {
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesDownwardAPIItems"
              )
            )
          );
        };
      };

      config = {
        "items" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesDownwardAPIItems" =
      {

        options = {
          "fieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef"
              )
            );
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "resourceFieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef"
              )
            );
          };
        };

        config = {
          "fieldRef" = mkOverride 1002 null;
          "mode" = mkOverride 1002 null;
          "resourceFieldRef" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef" =
      {

        options = {
          "apiVersion" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "fieldPath" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "apiVersion" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesPodCertificate" =
      {

        options = {
          "certificateChainPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "credentialBundlePath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyType" = mkOption {
            description = "";
            type = types.str;
          };
          "maxExpirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "signerName" = mkOption {
            description = "";
            type = types.str;
          };
          "userAnnotations" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "certificateChainPath" = mkOverride 1002 null;
          "credentialBundlePath" = mkOverride 1002 null;
          "keyPath" = mkOverride 1002 null;
          "maxExpirationSeconds" = mkOverride 1002 null;
          "userAnnotations" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesSecret" = {

      options = {
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesSecretItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesSecretItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceProjectedSourcesServiceAccountToken" =
      {

        options = {
          "audience" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "expirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "audience" = mkOverride 1002 null;
          "expirationSeconds" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceSecret" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceSecretItems"
              )
            )
          );
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecurityKerberosKeytabFileVolumeSourceSecretItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssd" = {

      options = {
        "sidecar" = mkOption {
          description = "Sidecar tells Rook to run SSSD in a sidecar alongside the NFS-Ganesha server in each NFS pod.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecar"));
        };
      };

      config = {
        "sidecar" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecar" = {

      options = {
        "additionalFiles" = mkOption {
          description = "AdditionalFiles defines any number of additional files that should be mounted into the SSSD\nsidecar with a directory root of `/etc/sssd/rook-additional/`.\nThese files may be referenced by the sssd.conf config file.";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFiles")
            )
          );
        };
        "debugLevel" = mkOption {
          description = "DebugLevel sets the debug level for SSSD. If unset or set to 0, Rook does nothing. Otherwise,\nthis may be a value between 1 and 10. See SSSD docs for more info:\nhttps://sssd.io/troubleshooting/basics.html#sssd-debug-logs";
          type = (types.nullOr (types.withMaximum 10 (types.withMinimum 0 types.int)));
        };
        "image" = mkOption {
          description = "Image defines the container image that should be used for the SSSD sidecar.";
          type = (types.withMinLength 1 types.str);
        };
        "resources" = mkOption {
          description = "Resources allow specifying resource requests/limits on the SSSD sidecar container.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarResources"));
        };
        "sssdConfigFile" = mkOption {
          description = "SSSDConfigFile defines where the SSSD configuration should be sourced from. The config file\nwill be placed into `/etc/sssd/sssd.conf`. If this is left empty, Rook will not add the file.\nThis allows you to manage the `sssd.conf` file yourself however you wish. For example, you\nmay build it into your custom Ceph container image or use the Vault agent injector to\nsecurely add the file via annotations on the CephNFS spec (passed to the NFS server pods).";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFile"));
        };
      };

      config = {
        "additionalFiles" = mkOverride 1002 null;
        "debugLevel" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "sssdConfigFile" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFiles" = {

      options = {
        "subPath" = mkOption {
          description = "SubPath defines the sub-path (subdirectory) of the directory root where the volumeSource will\nbe mounted. All files/keys in the volume source's volume will be mounted to the subdirectory.\nThis is not the same as the Kubernetes `subPath` volume mount option.\nEach subPath definition must be unique and must not contain ':'.";
          type = (types.withMinLength 1 types.str);
        };
        "volumeSource" = mkOption {
          description = "";
          type = (submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSource");
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSource" = {

      options = {
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceConfigMap"
            )
          );
        };
        "emptyDir" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceEmptyDir"
            )
          );
        };
        "hostPath" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceHostPath"
            )
          );
        };
        "persistentVolumeClaim" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourcePersistentVolumeClaim"
            )
          );
        };
        "projected" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjected"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceSecret"
            )
          );
        };
      };

      config = {
        "configMap" = mkOverride 1002 null;
        "emptyDir" = mkOverride 1002 null;
        "hostPath" = mkOverride 1002 null;
        "persistentVolumeClaim" = mkOverride 1002 null;
        "projected" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceConfigMap" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceEmptyDir" = {

      options = {
        "medium" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "sizeLimit" = mkOption {
          description = "";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "medium" = mkOverride 1002 null;
        "sizeLimit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceHostPath" = {

      options = {
        "path" = mkOption {
          description = "";
          type = types.str;
        };
        "type" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourcePersistentVolumeClaim" = {

      options = {
        "claimName" = mkOption {
          description = "";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjected" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "sources" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSources"
              )
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "sources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSources" = {

      options = {
        "clusterTrustBundle" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesClusterTrustBundle"
            )
          );
        };
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesConfigMap"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesDownwardAPI"
            )
          );
        };
        "podCertificate" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesPodCertificate"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesSecret"
            )
          );
        };
        "serviceAccountToken" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesServiceAccountToken"
            )
          );
        };
      };

      config = {
        "clusterTrustBundle" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "podCertificate" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "serviceAccountToken" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesClusterTrustBundle" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector"
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "signerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
          "signerName" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesConfigMap" =
      {

        options = {
          "items" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesConfigMapItems"
                )
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "items" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesConfigMapItems" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesDownwardAPI" =
      {

        options = {
          "items" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesDownwardAPIItems"
                )
              )
            );
          };
        };

        config = {
          "items" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesDownwardAPIItems" =
      {

        options = {
          "fieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef"
              )
            );
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "resourceFieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef"
              )
            );
          };
        };

        config = {
          "fieldRef" = mkOverride 1002 null;
          "mode" = mkOverride 1002 null;
          "resourceFieldRef" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef" =
      {

        options = {
          "apiVersion" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "fieldPath" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "apiVersion" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesPodCertificate" =
      {

        options = {
          "certificateChainPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "credentialBundlePath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyType" = mkOption {
            description = "";
            type = types.str;
          };
          "maxExpirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "signerName" = mkOption {
            description = "";
            type = types.str;
          };
          "userAnnotations" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "certificateChainPath" = mkOverride 1002 null;
          "credentialBundlePath" = mkOverride 1002 null;
          "keyPath" = mkOverride 1002 null;
          "maxExpirationSeconds" = mkOverride 1002 null;
          "userAnnotations" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesSecret" =
      {

        options = {
          "items" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesSecretItems"
                )
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "items" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesSecretItems" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceProjectedSourcesServiceAccountToken" =
      {

        options = {
          "audience" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "expirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "audience" = mkOverride 1002 null;
          "expirationSeconds" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceSecret" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceSecretItems"
              )
            )
          );
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarAdditionalFilesVolumeSourceSecretItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarResourcesClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFile" = {

      options = {
        "volumeSource" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSource"
            )
          );
        };
      };

      config = {
        "volumeSource" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSource" = {

      options = {
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceConfigMap"
            )
          );
        };
        "emptyDir" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceEmptyDir"
            )
          );
        };
        "hostPath" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceHostPath"
            )
          );
        };
        "persistentVolumeClaim" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourcePersistentVolumeClaim"
            )
          );
        };
        "projected" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjected"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceSecret"
            )
          );
        };
      };

      config = {
        "configMap" = mkOverride 1002 null;
        "emptyDir" = mkOverride 1002 null;
        "hostPath" = mkOverride 1002 null;
        "persistentVolumeClaim" = mkOverride 1002 null;
        "projected" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceConfigMap" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceEmptyDir" = {

      options = {
        "medium" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "sizeLimit" = mkOption {
          description = "";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "medium" = mkOverride 1002 null;
        "sizeLimit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceHostPath" = {

      options = {
        "path" = mkOption {
          description = "";
          type = types.str;
        };
        "type" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourcePersistentVolumeClaim" = {

      options = {
        "claimName" = mkOption {
          description = "";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjected" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "sources" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSources"
              )
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "sources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSources" = {

      options = {
        "clusterTrustBundle" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesClusterTrustBundle"
            )
          );
        };
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesConfigMap"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesDownwardAPI"
            )
          );
        };
        "podCertificate" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesPodCertificate"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesSecret"
            )
          );
        };
        "serviceAccountToken" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesServiceAccountToken"
            )
          );
        };
      };

      config = {
        "clusterTrustBundle" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "podCertificate" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "serviceAccountToken" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesClusterTrustBundle" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector"
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "signerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
          "signerName" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesConfigMap" =
      {

        options = {
          "items" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesConfigMapItems"
                )
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "items" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesConfigMapItems" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesDownwardAPI" =
      {

        options = {
          "items" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesDownwardAPIItems"
                )
              )
            );
          };
        };

        config = {
          "items" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesDownwardAPIItems" =
      {

        options = {
          "fieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef"
              )
            );
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "resourceFieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef"
              )
            );
          };
        };

        config = {
          "fieldRef" = mkOverride 1002 null;
          "mode" = mkOverride 1002 null;
          "resourceFieldRef" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef" =
      {

        options = {
          "apiVersion" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "fieldPath" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "apiVersion" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesPodCertificate" =
      {

        options = {
          "certificateChainPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "credentialBundlePath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyType" = mkOption {
            description = "";
            type = types.str;
          };
          "maxExpirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "signerName" = mkOption {
            description = "";
            type = types.str;
          };
          "userAnnotations" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "certificateChainPath" = mkOverride 1002 null;
          "credentialBundlePath" = mkOverride 1002 null;
          "keyPath" = mkOverride 1002 null;
          "maxExpirationSeconds" = mkOverride 1002 null;
          "userAnnotations" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesSecret" = {

      options = {
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesSecretItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesSecretItems" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceProjectedSourcesServiceAccountToken" =
      {

        options = {
          "audience" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "expirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "audience" = mkOverride 1002 null;
          "expirationSeconds" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceSecret" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceSecretItems"
              )
            )
          );
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecSecuritySssdSidecarSssdConfigFileVolumeSourceSecretItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServer" = {

      options = {
        "active" = mkOption {
          description = "The number of active Ganesha servers";
          type = types.int;
        };
        "annotations" = mkOption {
          description = "The annotations-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "hostNetwork" = mkOption {
          description = "Whether host networking is enabled for the Ganesha server. If not set, the network settings from the cluster CR will be applied.";
          type = (types.nullOr types.bool);
        };
        "image" = mkOption {
          description = "Image is the container image used to launch the Ceph NFS (Ganesha) daemon(s).\nThe image must include the NFS Ganesha binaries, such as are included with the official Ceph releases. E.g.: quay.io/ceph/ceph:<tag>\nIf not specified, the Ceph image defined in the CephCluster is used.\nOverriding the CephCluster defined image is not normally necessary when using the official Ceph images.\nThe image must contain the NFS Ganesha and dbus packages.\nIf the SSSD sidecar is enabled, the image must also contain the sssd-client package.";
          type = (types.nullOr (types.withMaxLength 1572864 (types.withMinLength 1 types.str)));
        };
        "imagePullPolicy" = mkOption {
          description = "ImagePullPolicy describes a policy for if/when to pull a container image\nOne of Always, Never, IfNotPresent.\nThis field only has effect if an image is specified.";
          type = (
            types.nullOr (
              types.enum [
                "IfNotPresent"
                "Always"
                "Never"
                ""
              ]
            )
          );
        };
        "labels" = mkOption {
          description = "The labels-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "livenessProbe" = mkOption {
          description = "A liveness-probe to verify that Ganesha server has valid run-time state.\nIf LivenessProbe.Disabled is false and LivenessProbe.Probe is nil uses default probe.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerLivenessProbe"));
        };
        "logLevel" = mkOption {
          description = "LogLevel set logging level";
          type = (types.nullOr types.str);
        };
        "placement" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacement"));
        };
        "port" = mkOption {
          description = "The port the NFS server (NFS-Ganesha) will listen on for NFS clients. Defaults to 2049.\nUseful when host networking is enabled and the default NFS port is already in use.";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 1 types.int)));
        };
        "priorityClassName" = mkOption {
          description = "PriorityClassName sets the priority class on the pods";
          type = (types.nullOr types.str);
        };
        "resources" = mkOption {
          description = "Resources set resource requests and limits";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerResources"));
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "hostNetwork" = mkOverride 1002 null;
        "image" = mkOverride 1002 null;
        "imagePullPolicy" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "livenessProbe" = mkOverride 1002 null;
        "logLevel" = mkOverride 1002 null;
        "placement" = mkOverride 1002 null;
        "port" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerLivenessProbe" = {

      options = {
        "disabled" = mkOption {
          description = "Disabled determines whether probe is disable or not";
          type = (types.nullOr types.bool);
        };
        "probe" = mkOption {
          description = "Probe describes a health check to be performed against a container to determine whether it is\nalive or ready to receive traffic.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbe"));
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "probe" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbe" = {

      options = {
        "exec" = mkOption {
          description = "Exec specifies a command to execute in the container.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeExec"));
        };
        "failureThreshold" = mkOption {
          description = "Minimum consecutive failures for the probe to be considered failed after having succeeded.\nDefaults to 3. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "grpc" = mkOption {
          description = "GRPC specifies a GRPC HealthCheckRequest.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeGrpc"));
        };
        "httpGet" = mkOption {
          description = "HTTPGet specifies an HTTP GET request to perform.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeHttpGet"));
        };
        "initialDelaySeconds" = mkOption {
          description = "Number of seconds after the container has started before liveness probes are initiated.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
        "periodSeconds" = mkOption {
          description = "How often (in seconds) to perform the probe.\nDefault to 10 seconds. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "successThreshold" = mkOption {
          description = "Minimum consecutive successes for the probe to be considered successful after having failed.\nDefaults to 1. Must be 1 for liveness and startup. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "tcpSocket" = mkOption {
          description = "TCPSocket specifies a connection to a TCP port.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeTcpSocket"));
        };
        "terminationGracePeriodSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "timeoutSeconds" = mkOption {
          description = "Number of seconds after which the probe times out.\nDefaults to 1 second. Minimum value is 1.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "exec" = mkOverride 1002 null;
        "failureThreshold" = mkOverride 1002 null;
        "grpc" = mkOverride 1002 null;
        "httpGet" = mkOverride 1002 null;
        "initialDelaySeconds" = mkOverride 1002 null;
        "periodSeconds" = mkOverride 1002 null;
        "successThreshold" = mkOverride 1002 null;
        "tcpSocket" = mkOverride 1002 null;
        "terminationGracePeriodSeconds" = mkOverride 1002 null;
        "timeoutSeconds" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeExec" = {

      options = {
        "command" = mkOption {
          description = "Command is the command line to execute inside the container, the working directory for the\ncommand  is root ('/') in the container's filesystem. The command is simply exec'd, it is\nnot run inside a shell, so traditional shell instructions ('|', etc) won't work. To use\na shell, you need to explicitly call out to that shell.\nExit status of 0 is treated as live/healthy and non-zero is unhealthy.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "command" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeGrpc" = {

      options = {
        "port" = mkOption {
          description = "Port number of the gRPC service. Number must be in the range 1 to 65535.";
          type = types.int;
        };
        "service" = mkOption {
          description = "Service is the name of the service to place in the gRPC HealthCheckRequest\n(see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).\n\nIf this is not specified, the default behavior is defined by gRPC.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "service" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeHttpGet" = {

      options = {
        "host" = mkOption {
          description = "Host name to connect to, defaults to the pod IP. You probably want to set\n\"Host\" in httpHeaders instead.";
          type = (types.nullOr types.str);
        };
        "httpHeaders" = mkOption {
          description = "Custom headers to set in the request. HTTP allows repeated headers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeHttpGetHttpHeaders"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "path" = mkOption {
          description = "Path to access on the HTTP server.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Name or number of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
        "scheme" = mkOption {
          description = "Scheme to use for connecting to the host.\nDefaults to HTTP.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
        "httpHeaders" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "scheme" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeHttpGetHttpHeaders" = {

      options = {
        "name" = mkOption {
          description = "The header field name.\nThis will be canonicalized upon output, so case-variant names will be understood as the same header.";
          type = types.str;
        };
        "value" = mkOption {
          description = "The header field value";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephNFSSpecServerLivenessProbeProbeTcpSocket" = {

      options = {
        "host" = mkOption {
          description = "Optional: Host name to connect to, defaults to the pod IP.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Number or name of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerPlacement" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinity"));
        };
        "podAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinity"));
        };
        "podAntiAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinity"));
        };
        "tolerations" = mkOption {
          description = "";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementTolerations"))
          );
        };
        "topologySpreadConstraints" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementTopologySpreadConstraints")
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topologySpreadConstraints" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "";
            type = (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementTolerations" = {

      options = {
        "effect" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementTopologySpreadConstraints" = {

      options = {
        "labelSelector" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementTopologySpreadConstraintsLabelSelector"
            )
          );
        };
        "matchLabelKeys" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "maxSkew" = mkOption {
          description = "";
          type = types.int;
        };
        "minDomains" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "nodeAffinityPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "nodeTaintsPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "topologyKey" = mkOption {
          description = "";
          type = types.str;
        };
        "whenUnsatisfiable" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "matchLabelKeys" = mkOverride 1002 null;
        "minDomains" = mkOverride 1002 null;
        "nodeAffinityPolicy" = mkOverride 1002 null;
        "nodeTaintsPolicy" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementTopologySpreadConstraintsLabelSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNFSSpecServerPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNFSSpecServerResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephNFSSpecServerResourcesClaims" "name" [
                "name"
              ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSSpecServerResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSStatus" = {

      options = {
        "cephx" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSStatusCephx"));
        };
        "conditions" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephNFSStatusConditions")));
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
        "conditions" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSStatusCephx" = {

      options = {
        "daemon" = mkOption {
          description = "Daemon shows the CephX key status for local Ceph daemons associated with this resources.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNFSStatusCephxDaemon"));
        };
      };

      config = {
        "daemon" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSStatusCephxDaemon" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNFSStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGateway" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "NVMeOFGatewaySpec represents the spec of an NVMe-oF gateway";
          type = (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpec");
        };
        "status" = mkOption {
          description = "NVMeOFGatewayStatus represents the status of Ceph NVMe-oF Gateway";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewayStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpec" = {

      options = {
        "annotations" = mkOption {
          description = "The annotations-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "configMapRef" = mkOption {
          description = "ConfigMapRef is the name of the ConfigMap containing nvmeof.conf configuration\nIf not specified, a default configuration will be generated";
          type = (types.nullOr (types.withMinLength 1 types.str));
        };
        "group" = mkOption {
          description = "Group is the gateway group name for high availability (ANA group)";
          type = (types.withMinLength 1 types.str);
        };
        "hostNetwork" = mkOption {
          description = "Whether host networking is enabled for the gateway. If not set, the network settings from the cluster CR will be applied.";
          type = (types.nullOr types.bool);
        };
        "image" = mkOption {
          description = "Image is the container image to use for the NVMe-oF gateway daemon.\nFor example, quay.io/ceph/nvmeof:1.5\nIf not specified, the image is fetched from the Ceph mon config store\n(mgr/cephadm/container_image_nvmeof).";
          type = (types.nullOr (types.withMaxLength 1024 (types.withMinLength 1 types.str)));
        };
        "instances" = mkOption {
          description = "The number of active gateway instances";
          type = (types.withMinimum 1 types.int);
        };
        "labels" = mkOption {
          description = "The labels-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "livenessProbe" = mkOption {
          description = "A liveness-probe to verify that gateway has valid run-time state.\nIf LivenessProbe.Disabled is false and LivenessProbe.Probe is nil uses default probe.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbe"));
        };
        "nvmeofConfig" = mkOption {
          description = "NVMeOFConfig is a map of section names to key-value pairs for nvmeof.conf configuration\nThis allows users to override or add configuration options without needing to manage a ConfigMap";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "placement" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacement"));
        };
        "pool" = mkOption {
          description = "Pool is the RADOS pool where NVMe-oF configuration is stored";
          type = (types.withMinLength 1 types.str);
        };
        "ports" = mkOption {
          description = "Ports configuration for the NVMe-oF gateway";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPorts"));
        };
        "priorityClassName" = mkOption {
          description = "PriorityClassName sets the priority class on the pods";
          type = (types.nullOr types.str);
        };
        "resources" = mkOption {
          description = "Resources set resource requests and limits";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecResources"));
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "configMapRef" = mkOverride 1002 null;
        "hostNetwork" = mkOverride 1002 null;
        "image" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "livenessProbe" = mkOverride 1002 null;
        "nvmeofConfig" = mkOverride 1002 null;
        "placement" = mkOverride 1002 null;
        "ports" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbe" = {

      options = {
        "disabled" = mkOption {
          description = "Disabled determines whether probe is disable or not";
          type = (types.nullOr types.bool);
        };
        "probe" = mkOption {
          description = "Probe describes a health check to be performed against a container to determine whether it is\nalive or ready to receive traffic.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbe"));
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "probe" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbe" = {

      options = {
        "exec" = mkOption {
          description = "Exec specifies a command to execute in the container.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeExec"));
        };
        "failureThreshold" = mkOption {
          description = "Minimum consecutive failures for the probe to be considered failed after having succeeded.\nDefaults to 3. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "grpc" = mkOption {
          description = "GRPC specifies a GRPC HealthCheckRequest.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeGrpc"));
        };
        "httpGet" = mkOption {
          description = "HTTPGet specifies an HTTP GET request to perform.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeHttpGet")
          );
        };
        "initialDelaySeconds" = mkOption {
          description = "Number of seconds after the container has started before liveness probes are initiated.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
        "periodSeconds" = mkOption {
          description = "How often (in seconds) to perform the probe.\nDefault to 10 seconds. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "successThreshold" = mkOption {
          description = "Minimum consecutive successes for the probe to be considered successful after having failed.\nDefaults to 1. Must be 1 for liveness and startup. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "tcpSocket" = mkOption {
          description = "TCPSocket specifies a connection to a TCP port.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeTcpSocket")
          );
        };
        "terminationGracePeriodSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "timeoutSeconds" = mkOption {
          description = "Number of seconds after which the probe times out.\nDefaults to 1 second. Minimum value is 1.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "exec" = mkOverride 1002 null;
        "failureThreshold" = mkOverride 1002 null;
        "grpc" = mkOverride 1002 null;
        "httpGet" = mkOverride 1002 null;
        "initialDelaySeconds" = mkOverride 1002 null;
        "periodSeconds" = mkOverride 1002 null;
        "successThreshold" = mkOverride 1002 null;
        "tcpSocket" = mkOverride 1002 null;
        "terminationGracePeriodSeconds" = mkOverride 1002 null;
        "timeoutSeconds" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeExec" = {

      options = {
        "command" = mkOption {
          description = "Command is the command line to execute inside the container, the working directory for the\ncommand  is root ('/') in the container's filesystem. The command is simply exec'd, it is\nnot run inside a shell, so traditional shell instructions ('|', etc) won't work. To use\na shell, you need to explicitly call out to that shell.\nExit status of 0 is treated as live/healthy and non-zero is unhealthy.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "command" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeGrpc" = {

      options = {
        "port" = mkOption {
          description = "Port number of the gRPC service. Number must be in the range 1 to 65535.";
          type = types.int;
        };
        "service" = mkOption {
          description = "Service is the name of the service to place in the gRPC HealthCheckRequest\n(see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).\n\nIf this is not specified, the default behavior is defined by gRPC.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "service" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeHttpGet" = {

      options = {
        "host" = mkOption {
          description = "Host name to connect to, defaults to the pod IP. You probably want to set\n\"Host\" in httpHeaders instead.";
          type = (types.nullOr types.str);
        };
        "httpHeaders" = mkOption {
          description = "Custom headers to set in the request. HTTP allows repeated headers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeHttpGetHttpHeaders"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "path" = mkOption {
          description = "Path to access on the HTTP server.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Name or number of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
        "scheme" = mkOption {
          description = "Scheme to use for connecting to the host.\nDefaults to HTTP.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
        "httpHeaders" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "scheme" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeHttpGetHttpHeaders" = {

      options = {
        "name" = mkOption {
          description = "The header field name.\nThis will be canonicalized upon output, so case-variant names will be understood as the same header.";
          type = types.str;
        };
        "value" = mkOption {
          description = "The header field value";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecLivenessProbeProbeTcpSocket" = {

      options = {
        "host" = mkOption {
          description = "Optional: Host name to connect to, defaults to the pod IP.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Number or name of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacement" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinity"));
        };
        "podAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinity"));
        };
        "podAntiAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinity"));
        };
        "tolerations" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementTolerations")
            )
          );
        };
        "topologySpreadConstraints" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementTopologySpreadConstraints")
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topologySpreadConstraints" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "";
            type = (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementTolerations" = {

      options = {
        "effect" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementTopologySpreadConstraints" = {

      options = {
        "labelSelector" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementTopologySpreadConstraintsLabelSelector"
            )
          );
        };
        "matchLabelKeys" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "maxSkew" = mkOption {
          description = "";
          type = types.int;
        };
        "minDomains" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "nodeAffinityPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "nodeTaintsPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "topologyKey" = mkOption {
          description = "";
          type = types.str;
        };
        "whenUnsatisfiable" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "matchLabelKeys" = mkOverride 1002 null;
        "minDomains" = mkOverride 1002 null;
        "nodeAffinityPolicy" = mkOverride 1002 null;
        "nodeTaintsPolicy" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementTopologySpreadConstraintsLabelSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecPorts" = {

      options = {
        "discoveryPort" = mkOption {
          description = "DiscoveryPort is the port for discovery service (default: 8009)";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 1 types.int)));
        };
        "gatewayPort" = mkOption {
          description = "GatewayPort is the port for the gateway service (default: 5500)";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 1 types.int)));
        };
        "ioPort" = mkOption {
          description = "IOPort is the port for NVMe-oF IO traffic (default: 4420)";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 1 types.int)));
        };
        "monitorPort" = mkOption {
          description = "MonitorPort is the port for the monitor service (default: 5499)";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 1 types.int)));
        };
      };

      config = {
        "discoveryPort" = mkOverride 1002 null;
        "gatewayPort" = mkOverride 1002 null;
        "ioPort" = mkOverride 1002 null;
        "monitorPort" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephNVMeOFGatewaySpecResourcesClaims" "name" [
                "name"
              ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewaySpecResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewayStatus" = {

      options = {
        "cephx" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewayStatusCephx"));
        };
        "conditions" = mkOption {
          description = "";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewayStatusConditions"))
          );
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
        "conditions" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewayStatusCephx" = {

      options = {
        "daemon" = mkOption {
          description = "Daemon shows the CephX key status for local Ceph daemons associated with this resources.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephNVMeOFGatewayStatusCephxDaemon"));
        };
      };

      config = {
        "daemon" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewayStatusCephxDaemon" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephNVMeOFGatewayStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectRealm" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "ObjectRealmSpec represent the spec of an ObjectRealm";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectRealmSpec"));
        };
        "status" = mkOption {
          description = "Status represents the status of an object";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectRealmStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectRealmSpec" = {

      options = {
        "defaultRealm" = mkOption {
          description = "Set this realm as the default in Ceph. Only one realm should be default.";
          type = (types.nullOr types.bool);
        };
        "pull" = mkOption {
          description = "PullSpec represents the pulling specification of a Ceph Object Storage Gateway Realm";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectRealmSpecPull"));
        };
      };

      config = {
        "defaultRealm" = mkOverride 1002 null;
        "pull" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectRealmSpecPull" = {

      options = {
        "endpoint" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "endpoint" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectRealmStatus" = {

      options = {
        "conditions" = mkOption {
          description = "";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectRealmStatusConditions"))
          );
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "conditions" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectRealmStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStore" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "ObjectStoreSpec represent the spec of a pool";
          type = (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpec");
        };
        "status" = mkOption {
          description = "ObjectStoreStatus represents the status of a Ceph Object Store resource";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreAccount" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "ObjectStoreAccountSpec represent the spec of a RGW Account";
          type = (submoduleOf "ceph.rook.io.v1.CephObjectStoreAccountSpec");
        };
        "status" = mkOption {
          description = "ObjectStoreAccountStatus represents the status of a CephObjectStoreAccount resource";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreAccountStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreAccountSpec" = {

      options = {
        "accountID" = mkOption {
          description = "AccountID uniquely identifies an account and resource ownership. Format should be RGW followed by 17 digits\n(e.g., RGW00889737169837717). If not specified, the ID will be automatically generated.";
          type = (types.nullOr (types.withMaxLength 20 (types.withMinLength 20 types.str)));
        };
        "name" = mkOption {
          description = "Name is the desired display name of the RGW account if different from the CephObjectStoreAccount CR name.";
          type = (types.nullOr (types.withMaxLength 2048 (types.withMinLength 1 types.str)));
        };
        "rootUser" = mkOption {
          description = "RootUser configures the root user for the account. The root user is created by default\nand has default permissions across all account resources.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreAccountSpecRootUser"));
        };
        "store" = mkOption {
          description = "Store is the CephObjectStore the account will be associated with";
          type = (types.withMaxLength 253 (types.withMinLength 1 types.str));
        };
      };

      config = {
        "accountID" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "rootUser" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreAccountSpecRootUser" = {

      options = {
        "displayName" = mkOption {
          description = "DisplayName for the root user";
          type = (types.nullOr (types.withMaxLength 64 (types.withMinLength 1 types.str)));
        };
        "skipCreate" = mkOption {
          description = "SkipCreate when set to true, the root user will not be created for this account.\nThis can be useful if the user wants to manually manage the root user outside of Rook.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "displayName" = mkOverride 1002 null;
        "skipCreate" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreAccountStatus" = {

      options = {
        "accountID" = mkOption {
          description = "AccountID associated with the RGW user account";
          type = (types.nullOr (types.withMaxLength 20 (types.withMinLength 20 types.str)));
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "rootAccountSecretName" = mkOption {
          description = "RootAccountSecretName is the name of the Kubernetes secret containing the root user's access credentials";
          type = (types.nullOr (types.withMaxLength 253 (types.withMinLength 1 types.str)));
        };
      };

      config = {
        "accountID" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
        "rootAccountSecretName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpec" = {

      options = {
        "allowUsersInNamespaces" = mkOption {
          description = "The list of allowed namespaces in addition to the object store namespace\nwhere ceph object store users may be created. Specify \"*\" to allow all\nnamespaces, otherwise list individual namespaces that are to be allowed.\nThis is useful for applications that need object store credentials\nto be created in their own namespace, where neither OBCs nor COSI\nis being used to create buckets. The default is empty.";
          type = (types.nullOr (types.listOf types.str));
        };
        "auth" = mkOption {
          description = "The authentication configuration";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecAuth"));
        };
        "dataPool" = mkOption {
          description = "The data pool settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPool"));
        };
        "defaultRealm" = mkOption {
          description = "Set this realm as the default in Ceph. Only one realm should be default.\nDo not set this true on more than one CephObjectStore.\nThis may not be set when zone is also specified; in this case, the realm\nreferenced by the zone's zonegroup should configure defaulting behavior.";
          type = (types.nullOr types.bool);
        };
        "gateway" = mkOption {
          description = "The rgw pod info";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGateway"));
        };
        "healthCheck" = mkOption {
          description = "The RGW health probes";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheck"));
        };
        "hosting" = mkOption {
          description = "Hosting settings for the object store.\nA common use case for hosting configuration is to inform Rook of endpoints that support DNS\nwildcards, which in turn allows virtual host-style bucket addressing.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHosting"));
        };
        "metadataPool" = mkOption {
          description = "The metadata pool settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPool"));
        };
        "preservePoolsOnDelete" = mkOption {
          description = "Preserve pools on object store deletion";
          type = (types.nullOr types.bool);
        };
        "protocols" = mkOption {
          description = "The protocol specification";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecProtocols"));
        };
        "security" = mkOption {
          description = "Security represents security settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecSecurity"));
        };
        "sharedPools" = mkOption {
          description = "The pool information when configuring RADOS namespaces in existing pools.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecSharedPools"));
        };
        "zone" = mkOption {
          description = "The multisite info";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecZone"));
        };
      };

      config = {
        "allowUsersInNamespaces" = mkOverride 1002 null;
        "auth" = mkOverride 1002 null;
        "dataPool" = mkOverride 1002 null;
        "defaultRealm" = mkOverride 1002 null;
        "gateway" = mkOverride 1002 null;
        "healthCheck" = mkOverride 1002 null;
        "hosting" = mkOverride 1002 null;
        "metadataPool" = mkOverride 1002 null;
        "preservePoolsOnDelete" = mkOverride 1002 null;
        "protocols" = mkOverride 1002 null;
        "security" = mkOverride 1002 null;
        "sharedPools" = mkOverride 1002 null;
        "zone" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecAuth" = {

      options = {
        "keystone" = mkOption {
          description = "The spec for Keystone";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecAuthKeystone"));
        };
      };

      config = {
        "keystone" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecAuthKeystone" = {

      options = {
        "acceptedRoles" = mkOption {
          description = "The roles requires to serve requests.";
          type = (types.listOf types.str);
        };
        "implicitTenants" = mkOption {
          description = "Create new users in their own tenants of the same name. Possible values are true, false, swift and s3. The latter have the effect of splitting the identity space such that only the indicated protocol will use implicit tenants.";
          type = (types.nullOr types.str);
        };
        "revocationInterval" = mkOption {
          description = "The number of seconds between token revocation checks.";
          type = (types.nullOr types.int);
        };
        "serviceUserSecretName" = mkOption {
          description = "The name of the secret containing the credentials for the service user account used by RGW. It has to be in the same namespace as the object store resource.";
          type = types.str;
        };
        "tokenCacheSize" = mkOption {
          description = "The maximum number of entries in each Keystone token cache.";
          type = (types.nullOr types.int);
        };
        "url" = mkOption {
          description = "The URL for the Keystone server.";
          type = types.str;
        };
      };

      config = {
        "implicitTenants" = mkOverride 1002 null;
        "revocationInterval" = mkOverride 1002 null;
        "tokenCacheSize" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPool" = {

      options = {
        "application" = mkOption {
          description = "The application name to set on the pool. Only expected to be set for rgw pools.";
          type = (types.nullOr types.str);
        };
        "compressionMode" = mkOption {
          description = "DEPRECATED: use Parameters instead, e.g., Parameters[\"compression_mode\"] = \"force\"\nThe inline compression mode in Bluestore OSD to set to (options are: none, passive, aggressive, force)\nDo NOT set a default value for kubebuilder as this will override the Parameters";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "passive"
                "aggressive"
                "force"
                ""
              ]
            )
          );
        };
        "crushRoot" = mkOption {
          description = "The root of the crush hierarchy utilized by the pool";
          type = (types.nullOr types.str);
        };
        "deviceClass" = mkOption {
          description = "The device class the OSD should set to for use in the pool";
          type = (types.nullOr types.str);
        };
        "enableCrushUpdates" = mkOption {
          description = "Allow rook operator to change the pool CRUSH tunables once the pool is created";
          type = (types.nullOr types.bool);
        };
        "enableRBDStats" = mkOption {
          description = "EnableRBDStats is used to enable gathering of statistics for all RBD images in the pool";
          type = (types.nullOr types.bool);
        };
        "erasureCoded" = mkOption {
          description = "The erasure code settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPoolErasureCoded"));
        };
        "failureDomain" = mkOption {
          description = "The failure domain: osd/host/(region or zone if available) - technically also any type in the crush map";
          type = (types.nullOr types.str);
        };
        "mirroring" = mkOption {
          description = "The mirroring settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPoolMirroring"));
        };
        "parameters" = mkOption {
          description = "Parameters is a list of properties to enable on a given pool";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "quotas" = mkOption {
          description = "The quota settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPoolQuotas"));
        };
        "replicated" = mkOption {
          description = "The replication settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPoolReplicated"));
        };
        "statusCheck" = mkOption {
          description = "The mirroring statusCheck";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPoolStatusCheck"));
        };
      };

      config = {
        "application" = mkOverride 1002 null;
        "compressionMode" = mkOverride 1002 null;
        "crushRoot" = mkOverride 1002 null;
        "deviceClass" = mkOverride 1002 null;
        "enableCrushUpdates" = mkOverride 1002 null;
        "enableRBDStats" = mkOverride 1002 null;
        "erasureCoded" = mkOverride 1002 null;
        "failureDomain" = mkOverride 1002 null;
        "mirroring" = mkOverride 1002 null;
        "parameters" = mkOverride 1002 null;
        "quotas" = mkOverride 1002 null;
        "replicated" = mkOverride 1002 null;
        "statusCheck" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPoolErasureCoded" = {

      options = {
        "algorithm" = mkOption {
          description = "The algorithm for erasure coding.\nIf absent, defaults to the plugin specified in osd_pool_default_erasure_code_profile.";
          type = (
            types.nullOr (
              types.enum [
                "isa"
                "jerasure"
              ]
            )
          );
        };
        "codingChunks" = mkOption {
          description = "Number of coding chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThis is the number of OSDs that can be lost simultaneously before data cannot be recovered.";
          type = (types.withMinimum 0 types.int);
        };
        "crushNumFailureDomains" = mkOption {
          description = "Number of failure domains to use for erasure coded chunk placement.\nWhen specified along with crushOSDsPerFailureDomain, a CRUSH MSR rule will be created\nthat distributes chunks across this many failure domains.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "crushOSDsPerFailureDomain" = mkOption {
          description = "Number of OSDs allowed per failure domain for erasure coded chunk placement.\nWhen specified along with crushNumFailureDomains, a CRUSH MSR rule will be created\nthat allows up to this many chunks on OSDs within each failure domain.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "dataChunks" = mkOption {
          description = "Number of data chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThe number of chunks required to recover an object when any single OSD is lost is the same\nas dataChunks so be aware that the larger the number of data chunks, the higher the cost of recovery.";
          type = (types.withMinimum 0 types.int);
        };
        "stripeUnit" = mkOption {
          description = "Erasure code stripe size in bytes. Ceph default is 4096 bytes (4 KiB).\nValue must be a multiple of 4096 (4Ki).";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "algorithm" = mkOverride 1002 null;
        "crushNumFailureDomains" = mkOverride 1002 null;
        "crushOSDsPerFailureDomain" = mkOverride 1002 null;
        "stripeUnit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPoolMirroring" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled whether this pool is mirrored or not";
          type = (types.nullOr types.bool);
        };
        "mode" = mkOption {
          description = "Mode is the mirroring mode: pool, image or init-only.";
          type = (
            types.nullOr (
              types.enum [
                "pool"
                "image"
                "init-only"
              ]
            )
          );
        };
        "peers" = mkOption {
          description = "Peers represents the peers spec";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPoolMirroringPeers"));
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the scheduling of snapshot for mirrored images/pools";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPoolMirroringSnapshotSchedules")
            )
          );
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPoolMirroringPeers" = {

      options = {
        "secretNames" = mkOption {
          description = "SecretNames represents the Kubernetes Secret names to add rbd-mirror or cephfs-mirror peers";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "secretNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPoolMirroringSnapshotSchedules" = {

      options = {
        "interval" = mkOption {
          description = "Interval represent the periodicity of the snapshot.";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot, only valid for CephFS";
          type = (types.nullOr types.str);
        };
        "startTime" = mkOption {
          description = "StartTime indicates when to start the snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "startTime" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPoolQuotas" = {

      options = {
        "maxBytes" = mkOption {
          description = "MaxBytes represents the quota in bytes\nDeprecated in favor of MaxSize";
          type = (types.nullOr types.int);
        };
        "maxObjects" = mkOption {
          description = "MaxObjects represents the quota in objects";
          type = (types.nullOr types.int);
        };
        "maxSize" = mkOption {
          description = "MaxSize represents the quota in bytes as a string";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "maxBytes" = mkOverride 1002 null;
        "maxObjects" = mkOverride 1002 null;
        "maxSize" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPoolReplicated" = {

      options = {
        "hybridStorage" = mkOption {
          description = "HybridStorage represents hybrid storage tier settings";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPoolReplicatedHybridStorage")
          );
        };
        "replicasPerFailureDomain" = mkOption {
          description = "ReplicasPerFailureDomain the number of replica in the specified failure domain";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "requireSafeReplicaSize" = mkOption {
          description = "RequireSafeReplicaSize if false allows you to set replica 1";
          type = (types.nullOr types.bool);
        };
        "size" = mkOption {
          description = "Size - Number of copies per object in a replicated storage pool, including the object itself (required for replicated pool type)";
          type = (types.withMinimum 0 types.int);
        };
        "subFailureDomain" = mkOption {
          description = "SubFailureDomain the name of the sub-failure domain";
          type = (types.nullOr types.str);
        };
        "targetSizeRatio" = mkOption {
          description = "TargetSizeRatio gives a hint (%) to Ceph in terms of expected consumption of the total cluster capacity";
          type = (types.nullOr (types.withMinimum 0 (types.either types.int types.float)));
        };
      };

      config = {
        "hybridStorage" = mkOverride 1002 null;
        "replicasPerFailureDomain" = mkOverride 1002 null;
        "requireSafeReplicaSize" = mkOverride 1002 null;
        "subFailureDomain" = mkOverride 1002 null;
        "targetSizeRatio" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPoolReplicatedHybridStorage" = {

      options = {
        "primaryDeviceClass" = mkOption {
          description = "PrimaryDeviceClass represents high performance tier (for example SSD or NVME) for Primary OSD";
          type = (types.withMinLength 1 types.str);
        };
        "secondaryDeviceClass" = mkOption {
          description = "SecondaryDeviceClass represents low performance tier (for example HDDs) for remaining OSDs";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPoolStatusCheck" = {

      options = {
        "mirror" = mkOption {
          description = "HealthCheckSpec represents the health check of an object store bucket";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecDataPoolStatusCheckMirror"));
        };
      };

      config = {
        "mirror" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecDataPoolStatusCheckMirror" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGateway" = {

      options = {
        "additionalVolumeMounts" = mkOption {
          description = "AdditionalVolumeMounts allows additional volumes to be mounted to the RGW pod.\nThe root directory for each additional volume mount is `/var/rgw`.\nExample: for an additional mount at subPath `ldap`, mounted from a secret that has key\n`bindpass.secret`, the file would reside at `/var/rgw/ldap/bindpass.secret`.";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMounts")
            )
          );
        };
        "annotations" = mkOption {
          description = "The annotations-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "caBundleRef" = mkOption {
          description = "The name of the secret that stores custom ca-bundle with root and intermediate certificates.";
          type = (types.nullOr types.str);
        };
        "dashboardEnabled" = mkOption {
          description = "Whether rgw dashboard is enabled for the rgw daemon. If not set, the rgw dashboard will be enabled.";
          type = (types.nullOr types.bool);
        };
        "disableMultisiteSyncTraffic" = mkOption {
          description = "DisableMultisiteSyncTraffic, when true, prevents this object store's gateways from\ntransmitting multisite replication data. Note that this value does not affect whether\ngateways receive multisite replication traffic: see ObjectZone.spec.customEndpoints for that.\nIf false or unset, this object store's gateways will be able to transmit multisite\nreplication data.";
          type = (types.nullOr types.bool);
        };
        "externalRgwEndpoints" = mkOption {
          description = "ExternalRgwEndpoints points to external RGW endpoint(s). Multiple endpoints can be given, but\nfor stability of ObjectBucketClaims, we highly recommend that users give only a single\nexternal RGW endpoint that is a load balancer that sends requests to the multiple RGWs.";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayExternalRgwEndpoints")
            )
          );
        };
        "hostNetwork" = mkOption {
          description = "Whether host networking is enabled for the rgw daemon. If not set, the network settings from the cluster CR will be applied.";
          type = (types.nullOr types.bool);
        };
        "instances" = mkOption {
          description = "The number of pods in the rgw replicaset.";
          type = (types.nullOr types.int);
        };
        "labels" = mkOption {
          description = "The labels-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "opsLogSidecar" = mkOption {
          description = "Enable enhanced operation Logs for S3 in a sidecar named ops-log";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayOpsLogSidecar"));
        };
        "placement" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacement"));
        };
        "port" = mkOption {
          description = "The port the rgw service will be listening on (http)";
          type = (types.nullOr types.int);
        };
        "priorityClassName" = mkOption {
          description = "PriorityClassName sets priority classes on the rgw pods";
          type = (types.nullOr types.str);
        };
        "readAffinity" = mkOption {
          description = "ReadAffinity defines the RGW read affinity policy to optimize the read requests for the RGW clients\nNote: Only supported from Ceph Tentacle (v20)";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayReadAffinity"));
        };
        "resources" = mkOption {
          description = "The resource requirements for the rgw pods";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayResources"));
        };
        "rgwCommandFlags" = mkOption {
          description = "RgwCommandFlags sets Ceph RGW config values for the gateway clients that serve this object\nstore. Values are modified at RGW startup, resulting in RGW pod restarts.\nThis feature is intended for advanced users. It allows breaking configurations to be easily\napplied. Use with caution.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "rgwConfig" = mkOption {
          description = "RgwConfig sets Ceph RGW config values for the gateway clients that serve this object store.\nValues are modified at runtime without RGW restart.\nThis feature is intended for advanced users. It allows breaking configurations to be easily\napplied. Use with caution.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "rgwConfigFromSecret" = mkOption {
          description = "RgwConfigFromSecret works exactly like RgwConfig but takes config value from Secret Key reference.\nValues are modified at runtime without RGW restart.\nThis feature is intended for advanced users. It allows breaking configurations to be easily\napplied. Use with caution.";
          type = (types.nullOr (types.attrsOf types.attrs));
        };
        "securePort" = mkOption {
          description = "The port the rgw service will be listening on (https)";
          type = (types.nullOr (types.withMaximum 65535 (types.withMinimum 0 types.int)));
        };
        "service" = mkOption {
          description = "The configuration related to add/set on each rgw service.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayService"));
        };
        "sslCertificateRef" = mkOption {
          description = "The name of the secret that stores the ssl certificate for secure rgw connections";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "additionalVolumeMounts" = mkOverride 1002 null;
        "annotations" = mkOverride 1002 null;
        "caBundleRef" = mkOverride 1002 null;
        "dashboardEnabled" = mkOverride 1002 null;
        "disableMultisiteSyncTraffic" = mkOverride 1002 null;
        "externalRgwEndpoints" = mkOverride 1002 null;
        "hostNetwork" = mkOverride 1002 null;
        "instances" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "opsLogSidecar" = mkOverride 1002 null;
        "placement" = mkOverride 1002 null;
        "port" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "readAffinity" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "rgwCommandFlags" = mkOverride 1002 null;
        "rgwConfig" = mkOverride 1002 null;
        "rgwConfigFromSecret" = mkOverride 1002 null;
        "securePort" = mkOverride 1002 null;
        "service" = mkOverride 1002 null;
        "sslCertificateRef" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMounts" = {

      options = {
        "subPath" = mkOption {
          description = "SubPath defines the sub-path (subdirectory) of the directory root where the volumeSource will\nbe mounted. All files/keys in the volume source's volume will be mounted to the subdirectory.\nThis is not the same as the Kubernetes `subPath` volume mount option.\nEach subPath definition must be unique and must not contain ':'.";
          type = (types.withMinLength 1 types.str);
        };
        "volumeSource" = mkOption {
          description = "";
          type = (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSource");
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSource" = {

      options = {
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceConfigMap"
            )
          );
        };
        "emptyDir" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceEmptyDir"
            )
          );
        };
        "hostPath" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceHostPath"
            )
          );
        };
        "persistentVolumeClaim" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourcePersistentVolumeClaim"
            )
          );
        };
        "projected" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjected"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceSecret"
            )
          );
        };
      };

      config = {
        "configMap" = mkOverride 1002 null;
        "emptyDir" = mkOverride 1002 null;
        "hostPath" = mkOverride 1002 null;
        "persistentVolumeClaim" = mkOverride 1002 null;
        "projected" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceConfigMap" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceEmptyDir" = {

      options = {
        "medium" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "sizeLimit" = mkOption {
          description = "";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "medium" = mkOverride 1002 null;
        "sizeLimit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceHostPath" = {

      options = {
        "path" = mkOption {
          description = "";
          type = types.str;
        };
        "type" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourcePersistentVolumeClaim" =
      {

        options = {
          "claimName" = mkOption {
            description = "";
            type = types.str;
          };
          "readOnly" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "readOnly" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjected" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "sources" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSources"
              )
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "sources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSources" = {

      options = {
        "clusterTrustBundle" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesClusterTrustBundle"
            )
          );
        };
        "configMap" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesConfigMap"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesDownwardAPI"
            )
          );
        };
        "podCertificate" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesPodCertificate"
            )
          );
        };
        "secret" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesSecret"
            )
          );
        };
        "serviceAccountToken" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesServiceAccountToken"
            )
          );
        };
      };

      config = {
        "clusterTrustBundle" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "podCertificate" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "serviceAccountToken" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesClusterTrustBundle" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector"
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "signerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
          "signerName" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesClusterTrustBundleLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesConfigMap" =
      {

        options = {
          "items" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesConfigMapItems"
                )
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "items" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesConfigMapItems" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesDownwardAPI" =
      {

        options = {
          "items" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesDownwardAPIItems"
                )
              )
            );
          };
        };

        config = {
          "items" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesDownwardAPIItems" =
      {

        options = {
          "fieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef"
              )
            );
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
          "resourceFieldRef" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef"
              )
            );
          };
        };

        config = {
          "fieldRef" = mkOverride 1002 null;
          "mode" = mkOverride 1002 null;
          "resourceFieldRef" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesDownwardAPIItemsFieldRef" =
      {

        options = {
          "apiVersion" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "fieldPath" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "apiVersion" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesPodCertificate" =
      {

        options = {
          "certificateChainPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "credentialBundlePath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyPath" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "keyType" = mkOption {
            description = "";
            type = types.str;
          };
          "maxExpirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "signerName" = mkOption {
            description = "";
            type = types.str;
          };
          "userAnnotations" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "certificateChainPath" = mkOverride 1002 null;
          "credentialBundlePath" = mkOverride 1002 null;
          "keyPath" = mkOverride 1002 null;
          "maxExpirationSeconds" = mkOverride 1002 null;
          "userAnnotations" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesSecret" =
      {

        options = {
          "items" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesSecretItems"
                )
              )
            );
          };
          "name" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "items" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesSecretItems" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "mode" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceProjectedSourcesServiceAccountToken" =
      {

        options = {
          "audience" = mkOption {
            description = "";
            type = (types.nullOr types.str);
          };
          "expirationSeconds" = mkOption {
            description = "";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "audience" = mkOverride 1002 null;
          "expirationSeconds" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceSecret" = {

      options = {
        "defaultMode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceSecretItems"
              )
            )
          );
        };
        "optional" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayAdditionalVolumeMountsVolumeSourceSecretItems" = {

      options = {
        "key" = mkOption {
          description = "";
          type = types.str;
        };
        "mode" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayExternalRgwEndpoints" = {

      options = {
        "hostname" = mkOption {
          description = "The DNS-addressable Hostname of this endpoint. This field will be preferred over IP if both are given.";
          type = (types.nullOr types.str);
        };
        "ip" = mkOption {
          description = "The IP of this endpoint. As a legacy behavior, this supports being given a DNS-addressable hostname as well.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "hostname" = mkOverride 1002 null;
        "ip" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayOpsLogSidecar" = {

      options = {
        "resources" = mkOption {
          description = "Resources represents the way to specify resource requirements for the ops-log sidecar";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayOpsLogSidecarResources")
          );
        };
      };

      config = {
        "resources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayOpsLogSidecarResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephObjectStoreSpecGatewayOpsLogSidecarResourcesClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayOpsLogSidecarResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacement" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinity")
          );
        };
        "podAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinity")
          );
        };
        "podAntiAffinity" = mkOption {
          description = "";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinity")
          );
        };
        "tolerations" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementTolerations")
            )
          );
        };
        "topologySpreadConstraints" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementTopologySpreadConstraints"
              )
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topologySpreadConstraints" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "";
            type = (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementTolerations" = {

      options = {
        "effect" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementTopologySpreadConstraints" = {

      options = {
        "labelSelector" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementTopologySpreadConstraintsLabelSelector"
            )
          );
        };
        "matchLabelKeys" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "maxSkew" = mkOption {
          description = "";
          type = types.int;
        };
        "minDomains" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "nodeAffinityPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "nodeTaintsPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "topologyKey" = mkOption {
          description = "";
          type = types.str;
        };
        "whenUnsatisfiable" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "matchLabelKeys" = mkOverride 1002 null;
        "minDomains" = mkOverride 1002 null;
        "nodeAffinityPolicy" = mkOverride 1002 null;
        "nodeTaintsPolicy" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementTopologySpreadConstraintsLabelSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayReadAffinity" = {

      options = {
        "type" = mkOption {
          description = "Type defines the RGW ReadAffinity type\nlocalize: read from the nearest OSD based on crush location of the RGW client\nbalance: picks a random OSD from the PG's active set\ndefault: read from the primary OSD";
          type = (
            types.enum [
              "localize"
              "balance"
              "default"
            ]
          );
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephObjectStoreSpecGatewayResourcesClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecGatewayService" = {

      options = {
        "annotations" = mkOption {
          description = "The annotations-related configuration to add/set on each rgw service.\nnullable\noptional";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "labels" = mkOption {
          description = "The labels-related configuration to add/set on each rgw service.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheck" = {

      options = {
        "readinessProbe" = mkOption {
          description = "ProbeSpec is a wrapper around Probe so it can be enabled or disabled for a Ceph daemon";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbe"));
        };
        "startupProbe" = mkOption {
          description = "ProbeSpec is a wrapper around Probe so it can be enabled or disabled for a Ceph daemon";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbe"));
        };
      };

      config = {
        "readinessProbe" = mkOverride 1002 null;
        "startupProbe" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbe" = {

      options = {
        "disabled" = mkOption {
          description = "Disabled determines whether probe is disable or not";
          type = (types.nullOr types.bool);
        };
        "probe" = mkOption {
          description = "Probe describes a health check to be performed against a container to determine whether it is\nalive or ready to receive traffic.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbe")
          );
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "probe" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbe" = {

      options = {
        "exec" = mkOption {
          description = "Exec specifies a command to execute in the container.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeExec")
          );
        };
        "failureThreshold" = mkOption {
          description = "Minimum consecutive failures for the probe to be considered failed after having succeeded.\nDefaults to 3. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "grpc" = mkOption {
          description = "GRPC specifies a GRPC HealthCheckRequest.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeGrpc")
          );
        };
        "httpGet" = mkOption {
          description = "HTTPGet specifies an HTTP GET request to perform.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeHttpGet"
            )
          );
        };
        "initialDelaySeconds" = mkOption {
          description = "Number of seconds after the container has started before liveness probes are initiated.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
        "periodSeconds" = mkOption {
          description = "How often (in seconds) to perform the probe.\nDefault to 10 seconds. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "successThreshold" = mkOption {
          description = "Minimum consecutive successes for the probe to be considered successful after having failed.\nDefaults to 1. Must be 1 for liveness and startup. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "tcpSocket" = mkOption {
          description = "TCPSocket specifies a connection to a TCP port.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeTcpSocket"
            )
          );
        };
        "terminationGracePeriodSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "timeoutSeconds" = mkOption {
          description = "Number of seconds after which the probe times out.\nDefaults to 1 second. Minimum value is 1.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "exec" = mkOverride 1002 null;
        "failureThreshold" = mkOverride 1002 null;
        "grpc" = mkOverride 1002 null;
        "httpGet" = mkOverride 1002 null;
        "initialDelaySeconds" = mkOverride 1002 null;
        "periodSeconds" = mkOverride 1002 null;
        "successThreshold" = mkOverride 1002 null;
        "tcpSocket" = mkOverride 1002 null;
        "terminationGracePeriodSeconds" = mkOverride 1002 null;
        "timeoutSeconds" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeExec" = {

      options = {
        "command" = mkOption {
          description = "Command is the command line to execute inside the container, the working directory for the\ncommand  is root ('/') in the container's filesystem. The command is simply exec'd, it is\nnot run inside a shell, so traditional shell instructions ('|', etc) won't work. To use\na shell, you need to explicitly call out to that shell.\nExit status of 0 is treated as live/healthy and non-zero is unhealthy.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "command" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeGrpc" = {

      options = {
        "port" = mkOption {
          description = "Port number of the gRPC service. Number must be in the range 1 to 65535.";
          type = types.int;
        };
        "service" = mkOption {
          description = "Service is the name of the service to place in the gRPC HealthCheckRequest\n(see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).\n\nIf this is not specified, the default behavior is defined by gRPC.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "service" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeHttpGet" = {

      options = {
        "host" = mkOption {
          description = "Host name to connect to, defaults to the pod IP. You probably want to set\n\"Host\" in httpHeaders instead.";
          type = (types.nullOr types.str);
        };
        "httpHeaders" = mkOption {
          description = "Custom headers to set in the request. HTTP allows repeated headers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeHttpGetHttpHeaders"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "path" = mkOption {
          description = "Path to access on the HTTP server.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Name or number of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
        "scheme" = mkOption {
          description = "Scheme to use for connecting to the host.\nDefaults to HTTP.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
        "httpHeaders" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "scheme" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeHttpGetHttpHeaders" = {

      options = {
        "name" = mkOption {
          description = "The header field name.\nThis will be canonicalized upon output, so case-variant names will be understood as the same header.";
          type = types.str;
        };
        "value" = mkOption {
          description = "The header field value";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckReadinessProbeProbeTcpSocket" = {

      options = {
        "host" = mkOption {
          description = "Optional: Host name to connect to, defaults to the pod IP.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Number or name of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbe" = {

      options = {
        "disabled" = mkOption {
          description = "Disabled determines whether probe is disable or not";
          type = (types.nullOr types.bool);
        };
        "probe" = mkOption {
          description = "Probe describes a health check to be performed against a container to determine whether it is\nalive or ready to receive traffic.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbe")
          );
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "probe" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbe" = {

      options = {
        "exec" = mkOption {
          description = "Exec specifies a command to execute in the container.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeExec")
          );
        };
        "failureThreshold" = mkOption {
          description = "Minimum consecutive failures for the probe to be considered failed after having succeeded.\nDefaults to 3. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "grpc" = mkOption {
          description = "GRPC specifies a GRPC HealthCheckRequest.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeGrpc")
          );
        };
        "httpGet" = mkOption {
          description = "HTTPGet specifies an HTTP GET request to perform.";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeHttpGet")
          );
        };
        "initialDelaySeconds" = mkOption {
          description = "Number of seconds after the container has started before liveness probes are initiated.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
        "periodSeconds" = mkOption {
          description = "How often (in seconds) to perform the probe.\nDefault to 10 seconds. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "successThreshold" = mkOption {
          description = "Minimum consecutive successes for the probe to be considered successful after having failed.\nDefaults to 1. Must be 1 for liveness and startup. Minimum value is 1.";
          type = (types.nullOr types.int);
        };
        "tcpSocket" = mkOption {
          description = "TCPSocket specifies a connection to a TCP port.";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeTcpSocket"
            )
          );
        };
        "terminationGracePeriodSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "timeoutSeconds" = mkOption {
          description = "Number of seconds after which the probe times out.\nDefaults to 1 second. Minimum value is 1.\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes";
          type = (types.nullOr types.int);
        };
      };

      config = {
        "exec" = mkOverride 1002 null;
        "failureThreshold" = mkOverride 1002 null;
        "grpc" = mkOverride 1002 null;
        "httpGet" = mkOverride 1002 null;
        "initialDelaySeconds" = mkOverride 1002 null;
        "periodSeconds" = mkOverride 1002 null;
        "successThreshold" = mkOverride 1002 null;
        "tcpSocket" = mkOverride 1002 null;
        "terminationGracePeriodSeconds" = mkOverride 1002 null;
        "timeoutSeconds" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeExec" = {

      options = {
        "command" = mkOption {
          description = "Command is the command line to execute inside the container, the working directory for the\ncommand  is root ('/') in the container's filesystem. The command is simply exec'd, it is\nnot run inside a shell, so traditional shell instructions ('|', etc) won't work. To use\na shell, you need to explicitly call out to that shell.\nExit status of 0 is treated as live/healthy and non-zero is unhealthy.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "command" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeGrpc" = {

      options = {
        "port" = mkOption {
          description = "Port number of the gRPC service. Number must be in the range 1 to 65535.";
          type = types.int;
        };
        "service" = mkOption {
          description = "Service is the name of the service to place in the gRPC HealthCheckRequest\n(see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).\n\nIf this is not specified, the default behavior is defined by gRPC.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "service" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeHttpGet" = {

      options = {
        "host" = mkOption {
          description = "Host name to connect to, defaults to the pod IP. You probably want to set\n\"Host\" in httpHeaders instead.";
          type = (types.nullOr types.str);
        };
        "httpHeaders" = mkOption {
          description = "Custom headers to set in the request. HTTP allows repeated headers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeHttpGetHttpHeaders"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "path" = mkOption {
          description = "Path to access on the HTTP server.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Name or number of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
        "scheme" = mkOption {
          description = "Scheme to use for connecting to the host.\nDefaults to HTTP.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
        "httpHeaders" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "scheme" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeHttpGetHttpHeaders" = {

      options = {
        "name" = mkOption {
          description = "The header field name.\nThis will be canonicalized upon output, so case-variant names will be understood as the same header.";
          type = types.str;
        };
        "value" = mkOption {
          description = "The header field value";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHealthCheckStartupProbeProbeTcpSocket" = {

      options = {
        "host" = mkOption {
          description = "Optional: Host name to connect to, defaults to the pod IP.";
          type = (types.nullOr types.str);
        };
        "port" = mkOption {
          description = "Number or name of the port to access on the container.\nNumber must be in the range 1 to 65535.\nName must be an IANA_SVC_NAME.";
          type = (types.either types.int types.str);
        };
      };

      config = {
        "host" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHosting" = {

      options = {
        "advertiseEndpoint" = mkOption {
          description = "AdvertiseEndpoint is the default endpoint Rook will return for resources dependent on this\nobject store. This endpoint will be returned to CephObjectStoreUsers, Object Bucket Claims,\nand COSI Buckets/Accesses.\nBy default, Rook returns the endpoint for the object store's Kubernetes service using HTTPS\nwith `gateway.securePort` if it is defined (otherwise, HTTP with `gateway.port`).";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecHostingAdvertiseEndpoint"));
        };
        "dnsNames" = mkOption {
          description = "A list of DNS host names on which object store gateways will accept client S3 connections.\nWhen specified, object store gateways will reject client S3 connections to hostnames that are\nnot present in this list, so include all endpoints.\nThe object store's advertiseEndpoint and Kubernetes service endpoint, plus CephObjectZone\n`customEndpoints` are automatically added to the list but may be set here again if desired.\nEach DNS name must be valid according RFC-1123.\nIf the DNS name corresponds to an endpoint with DNS wildcard support, do not include the\nwildcard itself in the list of hostnames.\nE.g., use \"mystore.example.com\" instead of \"*.mystore.example.com\".";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "advertiseEndpoint" = mkOverride 1002 null;
        "dnsNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecHostingAdvertiseEndpoint" = {

      options = {
        "dnsName" = mkOption {
          description = "DnsName is the DNS name (in RFC-1123 format) of the endpoint.\nIf the DNS name corresponds to an endpoint with DNS wildcard support, do not include the\nwildcard itself in the list of hostnames.\nE.g., use \"mystore.example.com\" instead of \"*.mystore.example.com\".";
          type = (types.withMinLength 1 types.str);
        };
        "port" = mkOption {
          description = "Port is the port on which S3 connections can be made for this endpoint.";
          type = (types.withMaximum 65535 (types.withMinimum 1 types.int));
        };
        "useTls" = mkOption {
          description = "UseTls defines whether the endpoint uses TLS (HTTPS) or not (HTTP).";
          type = types.bool;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPool" = {

      options = {
        "application" = mkOption {
          description = "The application name to set on the pool. Only expected to be set for rgw pools.";
          type = (types.nullOr types.str);
        };
        "compressionMode" = mkOption {
          description = "DEPRECATED: use Parameters instead, e.g., Parameters[\"compression_mode\"] = \"force\"\nThe inline compression mode in Bluestore OSD to set to (options are: none, passive, aggressive, force)\nDo NOT set a default value for kubebuilder as this will override the Parameters";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "passive"
                "aggressive"
                "force"
                ""
              ]
            )
          );
        };
        "crushRoot" = mkOption {
          description = "The root of the crush hierarchy utilized by the pool";
          type = (types.nullOr types.str);
        };
        "deviceClass" = mkOption {
          description = "The device class the OSD should set to for use in the pool";
          type = (types.nullOr types.str);
        };
        "enableCrushUpdates" = mkOption {
          description = "Allow rook operator to change the pool CRUSH tunables once the pool is created";
          type = (types.nullOr types.bool);
        };
        "enableRBDStats" = mkOption {
          description = "EnableRBDStats is used to enable gathering of statistics for all RBD images in the pool";
          type = (types.nullOr types.bool);
        };
        "erasureCoded" = mkOption {
          description = "The erasure code settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolErasureCoded"));
        };
        "failureDomain" = mkOption {
          description = "The failure domain: osd/host/(region or zone if available) - technically also any type in the crush map";
          type = (types.nullOr types.str);
        };
        "mirroring" = mkOption {
          description = "The mirroring settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolMirroring"));
        };
        "parameters" = mkOption {
          description = "Parameters is a list of properties to enable on a given pool";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "quotas" = mkOption {
          description = "The quota settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolQuotas"));
        };
        "replicated" = mkOption {
          description = "The replication settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolReplicated"));
        };
        "statusCheck" = mkOption {
          description = "The mirroring statusCheck";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolStatusCheck"));
        };
      };

      config = {
        "application" = mkOverride 1002 null;
        "compressionMode" = mkOverride 1002 null;
        "crushRoot" = mkOverride 1002 null;
        "deviceClass" = mkOverride 1002 null;
        "enableCrushUpdates" = mkOverride 1002 null;
        "enableRBDStats" = mkOverride 1002 null;
        "erasureCoded" = mkOverride 1002 null;
        "failureDomain" = mkOverride 1002 null;
        "mirroring" = mkOverride 1002 null;
        "parameters" = mkOverride 1002 null;
        "quotas" = mkOverride 1002 null;
        "replicated" = mkOverride 1002 null;
        "statusCheck" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolErasureCoded" = {

      options = {
        "algorithm" = mkOption {
          description = "The algorithm for erasure coding.\nIf absent, defaults to the plugin specified in osd_pool_default_erasure_code_profile.";
          type = (
            types.nullOr (
              types.enum [
                "isa"
                "jerasure"
              ]
            )
          );
        };
        "codingChunks" = mkOption {
          description = "Number of coding chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThis is the number of OSDs that can be lost simultaneously before data cannot be recovered.";
          type = (types.withMinimum 0 types.int);
        };
        "crushNumFailureDomains" = mkOption {
          description = "Number of failure domains to use for erasure coded chunk placement.\nWhen specified along with crushOSDsPerFailureDomain, a CRUSH MSR rule will be created\nthat distributes chunks across this many failure domains.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "crushOSDsPerFailureDomain" = mkOption {
          description = "Number of OSDs allowed per failure domain for erasure coded chunk placement.\nWhen specified along with crushNumFailureDomains, a CRUSH MSR rule will be created\nthat allows up to this many chunks on OSDs within each failure domain.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "dataChunks" = mkOption {
          description = "Number of data chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThe number of chunks required to recover an object when any single OSD is lost is the same\nas dataChunks so be aware that the larger the number of data chunks, the higher the cost of recovery.";
          type = (types.withMinimum 0 types.int);
        };
        "stripeUnit" = mkOption {
          description = "Erasure code stripe size in bytes. Ceph default is 4096 bytes (4 KiB).\nValue must be a multiple of 4096 (4Ki).";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "algorithm" = mkOverride 1002 null;
        "crushNumFailureDomains" = mkOverride 1002 null;
        "crushOSDsPerFailureDomain" = mkOverride 1002 null;
        "stripeUnit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolMirroring" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled whether this pool is mirrored or not";
          type = (types.nullOr types.bool);
        };
        "mode" = mkOption {
          description = "Mode is the mirroring mode: pool, image or init-only.";
          type = (
            types.nullOr (
              types.enum [
                "pool"
                "image"
                "init-only"
              ]
            )
          );
        };
        "peers" = mkOption {
          description = "Peers represents the peers spec";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolMirroringPeers"));
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the scheduling of snapshot for mirrored images/pools";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolMirroringSnapshotSchedules"
              )
            )
          );
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolMirroringPeers" = {

      options = {
        "secretNames" = mkOption {
          description = "SecretNames represents the Kubernetes Secret names to add rbd-mirror or cephfs-mirror peers";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "secretNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolMirroringSnapshotSchedules" = {

      options = {
        "interval" = mkOption {
          description = "Interval represent the periodicity of the snapshot.";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot, only valid for CephFS";
          type = (types.nullOr types.str);
        };
        "startTime" = mkOption {
          description = "StartTime indicates when to start the snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "startTime" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolQuotas" = {

      options = {
        "maxBytes" = mkOption {
          description = "MaxBytes represents the quota in bytes\nDeprecated in favor of MaxSize";
          type = (types.nullOr types.int);
        };
        "maxObjects" = mkOption {
          description = "MaxObjects represents the quota in objects";
          type = (types.nullOr types.int);
        };
        "maxSize" = mkOption {
          description = "MaxSize represents the quota in bytes as a string";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "maxBytes" = mkOverride 1002 null;
        "maxObjects" = mkOverride 1002 null;
        "maxSize" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolReplicated" = {

      options = {
        "hybridStorage" = mkOption {
          description = "HybridStorage represents hybrid storage tier settings";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolReplicatedHybridStorage")
          );
        };
        "replicasPerFailureDomain" = mkOption {
          description = "ReplicasPerFailureDomain the number of replica in the specified failure domain";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "requireSafeReplicaSize" = mkOption {
          description = "RequireSafeReplicaSize if false allows you to set replica 1";
          type = (types.nullOr types.bool);
        };
        "size" = mkOption {
          description = "Size - Number of copies per object in a replicated storage pool, including the object itself (required for replicated pool type)";
          type = (types.withMinimum 0 types.int);
        };
        "subFailureDomain" = mkOption {
          description = "SubFailureDomain the name of the sub-failure domain";
          type = (types.nullOr types.str);
        };
        "targetSizeRatio" = mkOption {
          description = "TargetSizeRatio gives a hint (%) to Ceph in terms of expected consumption of the total cluster capacity";
          type = (types.nullOr (types.withMinimum 0 (types.either types.int types.float)));
        };
      };

      config = {
        "hybridStorage" = mkOverride 1002 null;
        "replicasPerFailureDomain" = mkOverride 1002 null;
        "requireSafeReplicaSize" = mkOverride 1002 null;
        "subFailureDomain" = mkOverride 1002 null;
        "targetSizeRatio" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolReplicatedHybridStorage" = {

      options = {
        "primaryDeviceClass" = mkOption {
          description = "PrimaryDeviceClass represents high performance tier (for example SSD or NVME) for Primary OSD";
          type = (types.withMinLength 1 types.str);
        };
        "secondaryDeviceClass" = mkOption {
          description = "SecondaryDeviceClass represents low performance tier (for example HDDs) for remaining OSDs";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolStatusCheck" = {

      options = {
        "mirror" = mkOption {
          description = "HealthCheckSpec represents the health check of an object store bucket";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolStatusCheckMirror")
          );
        };
      };

      config = {
        "mirror" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecMetadataPoolStatusCheckMirror" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecProtocols" = {

      options = {
        "enableAPIs" = mkOption {
          description = "Represents RGW 'rgw_enable_apis' config option. See: https://docs.ceph.com/en/reef/radosgw/config-ref/#confval-rgw_enable_apis\nIf no value provided then all APIs will be enabled: s3, s3website, swift, swift_auth, admin, sts, iam, notifications\nIf enabled APIs are set, all remaining APIs will be disabled.\nThis option overrides S3.Enabled value.";
          type = (
            types.nullOr (
              types.listOf (
                types.enum [
                  "s3"
                  "s3website"
                  "swift"
                  "swift_auth"
                  "admin"
                  "sts"
                  "iam"
                  "notifications"
                ]
              )
            )
          );
        };
        "s3" = mkOption {
          description = "The spec for S3";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecProtocolsS3"));
        };
        "swift" = mkOption {
          description = "The spec for Swift";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecProtocolsSwift"));
        };
      };

      config = {
        "enableAPIs" = mkOverride 1002 null;
        "s3" = mkOverride 1002 null;
        "swift" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecProtocolsS3" = {

      options = {
        "authUseKeystone" = mkOption {
          description = "Whether to use Keystone for authentication. This option maps directly to the rgw_s3_auth_use_keystone option. Enabling it allows generating S3 credentials via an OpenStack API call, see the docs. If not given, the defaults of the corresponding RGW option apply.";
          type = (types.nullOr types.bool);
        };
        "enabled" = mkOption {
          description = "Deprecated: use protocol.enableAPIs instead.\nWhether to enable S3. This defaults to true (even if protocols.s3 is not present in the CRD). This maintains backwards compatibility – by default S3 is enabled.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "authUseKeystone" = mkOverride 1002 null;
        "enabled" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecProtocolsSwift" = {

      options = {
        "accountInUrl" = mkOption {
          description = "Whether or not the Swift account name should be included in the Swift API URL. If set to false (the default), then the Swift API will listen on a URL formed like http://host:port/<rgw_swift_url_prefix>/v1. If set to true, the Swift API URL will be http://host:port/<rgw_swift_url_prefix>/v1/AUTH_<account_name>. You must set this option to true (and update the Keystone service catalog) if you want radosgw to support publicly-readable containers and temporary URLs.";
          type = (types.nullOr types.bool);
        };
        "urlPrefix" = mkOption {
          description = "The URL prefix for the Swift API, to distinguish it from the S3 API endpoint. The default is swift, which makes the Swift API available at the URL http://host:port/swift/v1 (or http://host:port/swift/v1/AUTH_%(tenant_id)s if rgw swift account in url is enabled).";
          type = (types.nullOr types.str);
        };
        "versioningEnabled" = mkOption {
          description = "Enables the Object Versioning of OpenStack Object Storage API. This allows clients to put the X-Versions-Location attribute on containers that should be versioned.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "accountInUrl" = mkOverride 1002 null;
        "urlPrefix" = mkOverride 1002 null;
        "versioningEnabled" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecSecurity" = {

      options = {
        "cipherSuites" = mkOption {
          description = "CipherSuites specifies the cipher suites used during the TLS handshake for TLS v1.3.\nEach value must be a valid OpenSSL cipher suite name.\nSee https://docs.openssl.org/master/man1/openssl-ciphers/";
          type = (types.nullOr (types.listOf types.str));
        };
        "ciphers" = mkOption {
          description = "Ciphers specifies the cipher suites used during the TLS handshake for TLS v1.2 and below.\nEach value must be a valid OpenSSL cipher suite name.\nSee https://docs.openssl.org/master/man1/openssl-ciphers/";
          type = (types.nullOr (types.listOf types.str));
        };
        "keyRotation" = mkOption {
          description = "KeyRotation defines options for Key Rotation.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecSecurityKeyRotation"));
        };
        "kms" = mkOption {
          description = "KeyManagementService is the main Key Management option";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecSecurityKms"));
        };
        "s3" = mkOption {
          description = "The settings for supporting AWS-SSE:S3 with RGW";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecSecurityS3"));
        };
        "sslOptions" = mkOption {
          description = "SslOptions sets the SSL context options for the Ceph RGW beast frontend.\nSee https://docs.ceph.com/en/latest/radosgw/frontends/#options";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreSpecSecuritySslOptions"));
        };
        "tlsGroups" = mkOption {
          description = "TlsGroups specifies one or more TLS Group strings separated by colons.\nMultiple suites are supported with ':' colon-separated.\nThe pseudo group name DEFAULT can be used to select the OpenSSL built-in default list of groups.\nOther valid group names will depend on OpenSSL version\nSee https://docs.openssl.org/master/man3/SSL_CTX_set1_curves/#description";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "cipherSuites" = mkOverride 1002 null;
        "ciphers" = mkOverride 1002 null;
        "keyRotation" = mkOverride 1002 null;
        "kms" = mkOverride 1002 null;
        "s3" = mkOverride 1002 null;
        "sslOptions" = mkOverride 1002 null;
        "tlsGroups" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecSecurityKeyRotation" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled represents whether the key rotation is enabled.";
          type = (types.nullOr types.bool);
        };
        "schedule" = mkOption {
          description = "Schedule represents the cron schedule for key rotation.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "schedule" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecSecurityKms" = {

      options = {
        "connectionDetails" = mkOption {
          description = "ConnectionDetails contains the KMS connection details (address, port etc)";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "tokenSecretName" = mkOption {
          description = "TokenSecretName is the kubernetes secret containing the KMS token";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "connectionDetails" = mkOverride 1002 null;
        "tokenSecretName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecSecurityS3" = {

      options = {
        "connectionDetails" = mkOption {
          description = "ConnectionDetails contains the KMS connection details (address, port etc)";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "tokenSecretName" = mkOption {
          description = "TokenSecretName is the kubernetes secret containing the KMS token";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "connectionDetails" = mkOverride 1002 null;
        "tokenSecretName" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecSecuritySslOptions" = {

      options = {
        "defaultWorkarounds" = mkOption {
          description = "defaultWorkarounds implements various bug workaround when true, or disables it when false. Default disabled.";
          type = (types.nullOr types.bool);
        };
        "noCompression" = mkOption {
          description = "noCompression disables compression when true, or disables it when false. Default disabled.";
          type = (types.nullOr types.bool);
        };
        "singleDiffieHellmanUse" = mkOption {
          description = "SingleDiffieHellmanUse always creates a new key when using tmp_dh parameters when true, or disables it when false. Default disabled.";
          type = (types.nullOr types.bool);
        };
        "sslv2" = mkOption {
          description = "sslv2 enables SSL v2 when true, or disables it when false. Default disabled.";
          type = (types.nullOr types.bool);
        };
        "sslv3" = mkOption {
          description = "sslv3 enables SSL v3 when true, or disables it when false. Default disabled.";
          type = (types.nullOr types.bool);
        };
        "tlsv1_0" = mkOption {
          description = "tlsv1_0 enables TLS v1.0 when true, or disables it when false. Default disabled.";
          type = (types.nullOr types.bool);
        };
        "tlsv1_1" = mkOption {
          description = "tlsv1_1 enables TLS v1.1 when true, or disables it when false. Default disabled.";
          type = (types.nullOr types.bool);
        };
        "tlsv1_2" = mkOption {
          description = "tlsv1_2 enables TLS v1.2 when true, or disables it when false. Default enabled.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultWorkarounds" = mkOverride 1002 null;
        "noCompression" = mkOverride 1002 null;
        "singleDiffieHellmanUse" = mkOverride 1002 null;
        "sslv2" = mkOverride 1002 null;
        "sslv3" = mkOverride 1002 null;
        "tlsv1_0" = mkOverride 1002 null;
        "tlsv1_1" = mkOverride 1002 null;
        "tlsv1_2" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecSharedPools" = {

      options = {
        "dataPoolName" = mkOption {
          description = "The data pool used for creating RADOS namespaces in the object store";
          type = (types.nullOr types.str);
        };
        "metadataPoolName" = mkOption {
          description = "The metadata pool used for creating RADOS namespaces in the object store";
          type = (types.nullOr types.str);
        };
        "poolPlacements" = mkOption {
          description = "PoolPlacements control which Pools are associated with a particular RGW bucket.\nOnce PoolPlacements are defined, RGW client will be able to associate pool\nwith ObjectStore bucket by providing \"<LocationConstraint>\" during s3 bucket creation\nor \"X-Storage-Policy\" header during swift container creation.\nSee: https://docs.ceph.com/en/latest/radosgw/placement/#placement-targets\nPoolPlacement with name: \"default\" will be used as a default pool if no option\nis provided during bucket creation.\nIf default placement is not provided, spec.sharedPools.dataPoolName and spec.sharedPools.MetadataPoolName will be used as default pools.\nIf spec.sharedPools are also empty, then RGW pools (spec.dataPool and spec.metadataPool) will be used as defaults.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephObjectStoreSpecSharedPoolsPoolPlacements"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "preserveRadosNamespaceDataOnDelete" = mkOption {
          description = "Whether the RADOS namespaces should be preserved on deletion of the object store";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "dataPoolName" = mkOverride 1002 null;
        "metadataPoolName" = mkOverride 1002 null;
        "poolPlacements" = mkOverride 1002 null;
        "preserveRadosNamespaceDataOnDelete" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecSharedPoolsPoolPlacements" = {

      options = {
        "dataNonECPoolName" = mkOption {
          description = "The data pool used to store ObjectStore data that cannot use erasure coding (ex: multi-part uploads).\nIf dataPoolName is not erasure coded, then there is no need for dataNonECPoolName.\nWARNING: Do not change this field after creation. Pool names are used in RADOS namespaces and renaming leads to data loss.";
          type = (types.nullOr types.str);
        };
        "dataPoolName" = mkOption {
          description = "The data pool used to store ObjectStore objects data.\nWARNING: Do not change this field after creation. Pool names are used in RADOS namespaces and renaming leads to data loss.";
          type = (types.withMinLength 1 types.str);
        };
        "default" = mkOption {
          description = "Sets given placement as default. Only one placement in the list can be marked as default.\nDefault is false.";
          type = (types.nullOr types.bool);
        };
        "metadataPoolName" = mkOption {
          description = "The metadata pool used to store ObjectStore bucket index.\nWARNING: Do not change this field after creation. Pool names are used in RADOS namespaces and renaming leads to data loss.";
          type = (types.withMinLength 1 types.str);
        };
        "name" = mkOption {
          description = "Pool placement name. Name can be arbitrary. Placement with name \"default\" will be used as default.";
          type = (types.withMinLength 1 types.str);
        };
        "storageClasses" = mkOption {
          description = "StorageClasses can be selected by user to override dataPoolName during object creation.\nEach placement has default STANDARD StorageClass pointing to dataPoolName.\nThis list allows defining additional StorageClasses on top of default STANDARD storage class.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephObjectStoreSpecSharedPoolsPoolPlacementsStorageClasses"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
      };

      config = {
        "dataNonECPoolName" = mkOverride 1002 null;
        "default" = mkOverride 1002 null;
        "storageClasses" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecSharedPoolsPoolPlacementsStorageClasses" = {

      options = {
        "dataPoolName" = mkOption {
          description = "DataPoolName is the data pool used to store ObjectStore objects data.\nWARNING: Do not change this field after creation. Pool names are used in RADOS namespaces and renaming leads to data loss.";
          type = (types.withMinLength 1 types.str);
        };
        "name" = mkOption {
          description = "Name is the StorageClass name. Ceph allows arbitrary name for StorageClasses,\nhowever most clients/libs insist on AWS names so it is recommended to use\none of the valid x-amz-storage-class values for better compatibility:\nREDUCED_REDUNDANCY | STANDARD_IA | ONEZONE_IA | INTELLIGENT_TIERING | GLACIER | DEEP_ARCHIVE | OUTPOSTS | GLACIER_IR | SNOW | EXPRESS_ONEZONE\nSee AWS docs: https://aws.amazon.com/de/s3/storage-classes/";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreSpecZone" = {

      options = {
        "name" = mkOption {
          description = "CephObjectStoreZone name this CephObjectStore is part of";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreStatus" = {

      options = {
        "cephx" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreStatusCephx"));
        };
        "conditions" = mkOption {
          description = "";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectStoreStatusConditions"))
          );
        };
        "endpoints" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreStatusEndpoints"));
        };
        "info" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
        "replicas" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "selector" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
        "conditions" = mkOverride 1002 null;
        "endpoints" = mkOverride 1002 null;
        "info" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
        "replicas" = mkOverride 1002 null;
        "selector" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreStatusCephx" = {

      options = {
        "daemon" = mkOption {
          description = "Daemon shows the CephX key status for local Ceph daemons associated with this resources.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreStatusCephxDaemon"));
        };
      };

      config = {
        "daemon" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreStatusCephxDaemon" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreStatusEndpoints" = {

      options = {
        "insecure" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "secure" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "insecure" = mkOverride 1002 null;
        "secure" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreUser" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "ObjectStoreUserSpec represent the spec of an Objectstoreuser";
          type = (submoduleOf "ceph.rook.io.v1.CephObjectStoreUserSpec");
        };
        "status" = mkOption {
          description = "ObjectStoreUserStatus represents the status Ceph Object Store Gateway User";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreUserStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreUserSpec" = {

      options = {
        "accountRef" = mkOption {
          description = "AccountRef is a reference to a CephObjectStoreAccount to associate this user with.\nThe referenced account must be in the same namespace as the user.\nWhen set, the user is created as an account user with no default permissions,\nand resources created by this user are owned by the account.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreUserSpecAccountRef"));
        };
        "capabilities" = mkOption {
          description = "Additional admin-level capabilities for the Ceph object store user";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreUserSpecCapabilities"));
        };
        "clusterNamespace" = mkOption {
          description = "The namespace where the parent CephCluster and CephObjectStore are found";
          type = (types.nullOr types.str);
        };
        "displayName" = mkOption {
          description = "The display name for the ceph user.";
          type = (types.nullOr types.str);
        };
        "keys" = mkOption {
          description = "Allows specifying credentials for the user. If not provided, the operator\nwill generate them.";
          type = (types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectStoreUserSpecKeys")));
        };
        "opMask" = mkOption {
          description = "The op-mask of the user.";
          type = (
            types.nullOr (
              types.listOf (
                types.enum [
                  "read"
                  "write"
                  "delete"
                ]
              )
            )
          );
        };
        "quotas" = mkOption {
          description = "ObjectUserQuotaSpec can be used to set quotas for the object store user to limit their usage. See the [Ceph docs](https://docs.ceph.com/en/latest/radosgw/admin/?#quota-management) for more";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreUserSpecQuotas"));
        };
        "store" = mkOption {
          description = "The store the user will be created in";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "accountRef" = mkOverride 1002 null;
        "capabilities" = mkOverride 1002 null;
        "clusterNamespace" = mkOverride 1002 null;
        "displayName" = mkOverride 1002 null;
        "keys" = mkOverride 1002 null;
        "opMask" = mkOverride 1002 null;
        "quotas" = mkOverride 1002 null;
        "store" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreUserSpecAccountRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the CephObjectStoreAccount CR";
          type = (types.withMaxLength 2048 (types.withMinLength 1 types.str));
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectStoreUserSpecCapabilities" = {

      options = {
        "amz-cache" = mkOption {
          description = "Add capabilities for user to send request to RGW Cache API header. Documented in https://docs.ceph.com/en/latest/radosgw/rgw-cache/#cache-api";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "bilog" = mkOption {
          description = "Add capabilities for user to change bucket index logging. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "bucket" = mkOption {
          description = "Admin capabilities to read/write Ceph object store buckets. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "buckets" = mkOption {
          description = "Admin capabilities to read/write Ceph object store buckets. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "datalog" = mkOption {
          description = "Add capabilities for user to change data logging. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "info" = mkOption {
          description = "Admin capabilities to read/write information about the user. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "mdlog" = mkOption {
          description = "Add capabilities for user to change metadata logging. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "metadata" = mkOption {
          description = "Admin capabilities to read/write Ceph object store metadata. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "oidc-provider" = mkOption {
          description = "Add capabilities for user to change oidc provider. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "ratelimit" = mkOption {
          description = "Add capabilities for user to set rate limiter for user and bucket. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "roles" = mkOption {
          description = "Admin capabilities to read/write roles for user. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "usage" = mkOption {
          description = "Admin capabilities to read/write Ceph object store usage. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "user" = mkOption {
          description = "Admin capabilities to read/write Ceph object store users. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "user-policy" = mkOption {
          description = "Add capabilities for user to change user policies. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "users" = mkOption {
          description = "Admin capabilities to read/write Ceph object store users. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
        "zone" = mkOption {
          description = "Admin capabilities to read/write Ceph object store zones. Documented in https://docs.ceph.com/en/latest/radosgw/admin/?#add-remove-admin-capabilities";
          type = (
            types.nullOr (
              types.enum [
                "*"
                "read"
                "write"
                "read, write"
              ]
            )
          );
        };
      };

      config = {
        "amz-cache" = mkOverride 1002 null;
        "bilog" = mkOverride 1002 null;
        "bucket" = mkOverride 1002 null;
        "buckets" = mkOverride 1002 null;
        "datalog" = mkOverride 1002 null;
        "info" = mkOverride 1002 null;
        "mdlog" = mkOverride 1002 null;
        "metadata" = mkOverride 1002 null;
        "oidc-provider" = mkOverride 1002 null;
        "ratelimit" = mkOverride 1002 null;
        "roles" = mkOverride 1002 null;
        "usage" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
        "user-policy" = mkOverride 1002 null;
        "users" = mkOverride 1002 null;
        "zone" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreUserSpecKeys" = {

      options = {
        "accessKeyRef" = mkOption {
          description = "Secret key selector for the access_key (commonly referred to as AWS_ACCESS_KEY_ID).";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreUserSpecKeysAccessKeyRef"));
        };
        "secretKeyRef" = mkOption {
          description = "Secret key selector for the secret_key (commonly referred to as AWS_SECRET_ACCESS_KEY).";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectStoreUserSpecKeysSecretKeyRef"));
        };
      };

      config = {
        "accessKeyRef" = mkOverride 1002 null;
        "secretKeyRef" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreUserSpecKeysAccessKeyRef" = {

      options = {
        "key" = mkOption {
          description = "The key of the secret to select from.  Must be a valid secret key.";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "Specify whether the Secret or its key must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreUserSpecKeysSecretKeyRef" = {

      options = {
        "key" = mkOption {
          description = "The key of the secret to select from.  Must be a valid secret key.";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "Specify whether the Secret or its key must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreUserSpecQuotas" = {

      options = {
        "maxBuckets" = mkOption {
          description = "Maximum bucket limit for the ceph user";
          type = (types.nullOr types.int);
        };
        "maxObjects" = mkOption {
          description = "Maximum number of objects across all the user's buckets";
          type = (types.nullOr types.int);
        };
        "maxSize" = mkOption {
          description = "Maximum size limit of all objects across all the user's buckets\nSee https://pkg.go.dev/k8s.io/apimachinery/pkg/api/resource#Quantity for more info.";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "maxBuckets" = mkOverride 1002 null;
        "maxObjects" = mkOverride 1002 null;
        "maxSize" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreUserStatus" = {

      options = {
        "info" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "keys" = mkOption {
          description = "";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephObjectStoreUserStatusKeys" "name" [ ]
            )
          );
          apply = attrsToList;
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "info" = mkOverride 1002 null;
        "keys" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectStoreUserStatusKeys" = {

      options = {
        "name" = mkOption {
          description = "name is unique within a namespace to reference a secret resource.";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "namespace defines the space within which the secret name must be unique.";
          type = (types.nullOr types.str);
        };
        "resourceVersion" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "uid" = mkOption {
          description = "UID is a type that holds unique ID values, including UUIDs.  Because we\ndon't ONLY use UUIDs, this is an alias to string.  Being a type captures\nintent and helps make sure that UIDs and names do not get conflated.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
        "resourceVersion" = mkOverride 1002 null;
        "uid" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZone" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "ObjectZoneSpec represent the spec of an ObjectZone";
          type = (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpec");
        };
        "status" = mkOption {
          description = "Status represents the status of an object";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneGroup" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "ObjectZoneGroupSpec represent the spec of an ObjectZoneGroup";
          type = (submoduleOf "ceph.rook.io.v1.CephObjectZoneGroupSpec");
        };
        "status" = mkOption {
          description = "Status represents the status of an object";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneGroupStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneGroupSpec" = {

      options = {
        "realm" = mkOption {
          description = "The name of the realm the zone group is a member of.";
          type = types.str;
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectZoneGroupStatus" = {

      options = {
        "conditions" = mkOption {
          description = "";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectZoneGroupStatusConditions"))
          );
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "conditions" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneGroupStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpec" = {

      options = {
        "customEndpoints" = mkOption {
          description = "If this zone cannot be accessed from other peer Ceph clusters via the ClusterIP Service\nendpoint created by Rook, you must set this to the externally reachable endpoint(s). You may\ninclude the port in the definition. For example: \"https://my-object-store.my-domain.net:443\".\nIn many cases, you should set this to the endpoint of the ingress resource that makes the\nCephObjectStore associated with this CephObjectStoreZone reachable to peer clusters.\nThe list can have one or more endpoints pointing to different RGW servers in the zone.\n\nIf a CephObjectStore endpoint is omitted from this list, that object store's gateways will\nnot receive multisite replication data\n(see CephObjectStore.spec.gateway.disableMultisiteSyncTraffic).";
          type = (types.nullOr (types.listOf types.str));
        };
        "dataPool" = mkOption {
          description = "The data pool settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPool"));
        };
        "metadataPool" = mkOption {
          description = "The metadata pool settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPool"));
        };
        "preservePoolsOnDelete" = mkOption {
          description = "Preserve pools on object zone deletion";
          type = (types.nullOr types.bool);
        };
        "sharedPools" = mkOption {
          description = "The pool information when configuring RADOS namespaces in existing pools.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecSharedPools"));
        };
        "zoneGroup" = mkOption {
          description = "The name of the zone group the zone is a member of.";
          type = types.str;
        };
      };

      config = {
        "customEndpoints" = mkOverride 1002 null;
        "dataPool" = mkOverride 1002 null;
        "metadataPool" = mkOverride 1002 null;
        "preservePoolsOnDelete" = mkOverride 1002 null;
        "sharedPools" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPool" = {

      options = {
        "application" = mkOption {
          description = "The application name to set on the pool. Only expected to be set for rgw pools.";
          type = (types.nullOr types.str);
        };
        "compressionMode" = mkOption {
          description = "DEPRECATED: use Parameters instead, e.g., Parameters[\"compression_mode\"] = \"force\"\nThe inline compression mode in Bluestore OSD to set to (options are: none, passive, aggressive, force)\nDo NOT set a default value for kubebuilder as this will override the Parameters";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "passive"
                "aggressive"
                "force"
                ""
              ]
            )
          );
        };
        "crushRoot" = mkOption {
          description = "The root of the crush hierarchy utilized by the pool";
          type = (types.nullOr types.str);
        };
        "deviceClass" = mkOption {
          description = "The device class the OSD should set to for use in the pool";
          type = (types.nullOr types.str);
        };
        "enableCrushUpdates" = mkOption {
          description = "Allow rook operator to change the pool CRUSH tunables once the pool is created";
          type = (types.nullOr types.bool);
        };
        "enableRBDStats" = mkOption {
          description = "EnableRBDStats is used to enable gathering of statistics for all RBD images in the pool";
          type = (types.nullOr types.bool);
        };
        "erasureCoded" = mkOption {
          description = "The erasure code settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPoolErasureCoded"));
        };
        "failureDomain" = mkOption {
          description = "The failure domain: osd/host/(region or zone if available) - technically also any type in the crush map";
          type = (types.nullOr types.str);
        };
        "mirroring" = mkOption {
          description = "The mirroring settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPoolMirroring"));
        };
        "parameters" = mkOption {
          description = "Parameters is a list of properties to enable on a given pool";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "quotas" = mkOption {
          description = "The quota settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPoolQuotas"));
        };
        "replicated" = mkOption {
          description = "The replication settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPoolReplicated"));
        };
        "statusCheck" = mkOption {
          description = "The mirroring statusCheck";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPoolStatusCheck"));
        };
      };

      config = {
        "application" = mkOverride 1002 null;
        "compressionMode" = mkOverride 1002 null;
        "crushRoot" = mkOverride 1002 null;
        "deviceClass" = mkOverride 1002 null;
        "enableCrushUpdates" = mkOverride 1002 null;
        "enableRBDStats" = mkOverride 1002 null;
        "erasureCoded" = mkOverride 1002 null;
        "failureDomain" = mkOverride 1002 null;
        "mirroring" = mkOverride 1002 null;
        "parameters" = mkOverride 1002 null;
        "quotas" = mkOverride 1002 null;
        "replicated" = mkOverride 1002 null;
        "statusCheck" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPoolErasureCoded" = {

      options = {
        "algorithm" = mkOption {
          description = "The algorithm for erasure coding.\nIf absent, defaults to the plugin specified in osd_pool_default_erasure_code_profile.";
          type = (
            types.nullOr (
              types.enum [
                "isa"
                "jerasure"
              ]
            )
          );
        };
        "codingChunks" = mkOption {
          description = "Number of coding chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThis is the number of OSDs that can be lost simultaneously before data cannot be recovered.";
          type = (types.withMinimum 0 types.int);
        };
        "crushNumFailureDomains" = mkOption {
          description = "Number of failure domains to use for erasure coded chunk placement.\nWhen specified along with crushOSDsPerFailureDomain, a CRUSH MSR rule will be created\nthat distributes chunks across this many failure domains.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "crushOSDsPerFailureDomain" = mkOption {
          description = "Number of OSDs allowed per failure domain for erasure coded chunk placement.\nWhen specified along with crushNumFailureDomains, a CRUSH MSR rule will be created\nthat allows up to this many chunks on OSDs within each failure domain.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "dataChunks" = mkOption {
          description = "Number of data chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThe number of chunks required to recover an object when any single OSD is lost is the same\nas dataChunks so be aware that the larger the number of data chunks, the higher the cost of recovery.";
          type = (types.withMinimum 0 types.int);
        };
        "stripeUnit" = mkOption {
          description = "Erasure code stripe size in bytes. Ceph default is 4096 bytes (4 KiB).\nValue must be a multiple of 4096 (4Ki).";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "algorithm" = mkOverride 1002 null;
        "crushNumFailureDomains" = mkOverride 1002 null;
        "crushOSDsPerFailureDomain" = mkOverride 1002 null;
        "stripeUnit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPoolMirroring" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled whether this pool is mirrored or not";
          type = (types.nullOr types.bool);
        };
        "mode" = mkOption {
          description = "Mode is the mirroring mode: pool, image or init-only.";
          type = (
            types.nullOr (
              types.enum [
                "pool"
                "image"
                "init-only"
              ]
            )
          );
        };
        "peers" = mkOption {
          description = "Peers represents the peers spec";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPoolMirroringPeers"));
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the scheduling of snapshot for mirrored images/pools";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPoolMirroringSnapshotSchedules")
            )
          );
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPoolMirroringPeers" = {

      options = {
        "secretNames" = mkOption {
          description = "SecretNames represents the Kubernetes Secret names to add rbd-mirror or cephfs-mirror peers";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "secretNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPoolMirroringSnapshotSchedules" = {

      options = {
        "interval" = mkOption {
          description = "Interval represent the periodicity of the snapshot.";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot, only valid for CephFS";
          type = (types.nullOr types.str);
        };
        "startTime" = mkOption {
          description = "StartTime indicates when to start the snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "startTime" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPoolQuotas" = {

      options = {
        "maxBytes" = mkOption {
          description = "MaxBytes represents the quota in bytes\nDeprecated in favor of MaxSize";
          type = (types.nullOr types.int);
        };
        "maxObjects" = mkOption {
          description = "MaxObjects represents the quota in objects";
          type = (types.nullOr types.int);
        };
        "maxSize" = mkOption {
          description = "MaxSize represents the quota in bytes as a string";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "maxBytes" = mkOverride 1002 null;
        "maxObjects" = mkOverride 1002 null;
        "maxSize" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPoolReplicated" = {

      options = {
        "hybridStorage" = mkOption {
          description = "HybridStorage represents hybrid storage tier settings";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPoolReplicatedHybridStorage")
          );
        };
        "replicasPerFailureDomain" = mkOption {
          description = "ReplicasPerFailureDomain the number of replica in the specified failure domain";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "requireSafeReplicaSize" = mkOption {
          description = "RequireSafeReplicaSize if false allows you to set replica 1";
          type = (types.nullOr types.bool);
        };
        "size" = mkOption {
          description = "Size - Number of copies per object in a replicated storage pool, including the object itself (required for replicated pool type)";
          type = (types.withMinimum 0 types.int);
        };
        "subFailureDomain" = mkOption {
          description = "SubFailureDomain the name of the sub-failure domain";
          type = (types.nullOr types.str);
        };
        "targetSizeRatio" = mkOption {
          description = "TargetSizeRatio gives a hint (%) to Ceph in terms of expected consumption of the total cluster capacity";
          type = (types.nullOr (types.withMinimum 0 (types.either types.int types.float)));
        };
      };

      config = {
        "hybridStorage" = mkOverride 1002 null;
        "replicasPerFailureDomain" = mkOverride 1002 null;
        "requireSafeReplicaSize" = mkOverride 1002 null;
        "subFailureDomain" = mkOverride 1002 null;
        "targetSizeRatio" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPoolReplicatedHybridStorage" = {

      options = {
        "primaryDeviceClass" = mkOption {
          description = "PrimaryDeviceClass represents high performance tier (for example SSD or NVME) for Primary OSD";
          type = (types.withMinLength 1 types.str);
        };
        "secondaryDeviceClass" = mkOption {
          description = "SecondaryDeviceClass represents low performance tier (for example HDDs) for remaining OSDs";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPoolStatusCheck" = {

      options = {
        "mirror" = mkOption {
          description = "HealthCheckSpec represents the health check of an object store bucket";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecDataPoolStatusCheckMirror"));
        };
      };

      config = {
        "mirror" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecDataPoolStatusCheckMirror" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPool" = {

      options = {
        "application" = mkOption {
          description = "The application name to set on the pool. Only expected to be set for rgw pools.";
          type = (types.nullOr types.str);
        };
        "compressionMode" = mkOption {
          description = "DEPRECATED: use Parameters instead, e.g., Parameters[\"compression_mode\"] = \"force\"\nThe inline compression mode in Bluestore OSD to set to (options are: none, passive, aggressive, force)\nDo NOT set a default value for kubebuilder as this will override the Parameters";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "passive"
                "aggressive"
                "force"
                ""
              ]
            )
          );
        };
        "crushRoot" = mkOption {
          description = "The root of the crush hierarchy utilized by the pool";
          type = (types.nullOr types.str);
        };
        "deviceClass" = mkOption {
          description = "The device class the OSD should set to for use in the pool";
          type = (types.nullOr types.str);
        };
        "enableCrushUpdates" = mkOption {
          description = "Allow rook operator to change the pool CRUSH tunables once the pool is created";
          type = (types.nullOr types.bool);
        };
        "enableRBDStats" = mkOption {
          description = "EnableRBDStats is used to enable gathering of statistics for all RBD images in the pool";
          type = (types.nullOr types.bool);
        };
        "erasureCoded" = mkOption {
          description = "The erasure code settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolErasureCoded"));
        };
        "failureDomain" = mkOption {
          description = "The failure domain: osd/host/(region or zone if available) - technically also any type in the crush map";
          type = (types.nullOr types.str);
        };
        "mirroring" = mkOption {
          description = "The mirroring settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolMirroring"));
        };
        "parameters" = mkOption {
          description = "Parameters is a list of properties to enable on a given pool";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "quotas" = mkOption {
          description = "The quota settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolQuotas"));
        };
        "replicated" = mkOption {
          description = "The replication settings";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolReplicated"));
        };
        "statusCheck" = mkOption {
          description = "The mirroring statusCheck";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolStatusCheck"));
        };
      };

      config = {
        "application" = mkOverride 1002 null;
        "compressionMode" = mkOverride 1002 null;
        "crushRoot" = mkOverride 1002 null;
        "deviceClass" = mkOverride 1002 null;
        "enableCrushUpdates" = mkOverride 1002 null;
        "enableRBDStats" = mkOverride 1002 null;
        "erasureCoded" = mkOverride 1002 null;
        "failureDomain" = mkOverride 1002 null;
        "mirroring" = mkOverride 1002 null;
        "parameters" = mkOverride 1002 null;
        "quotas" = mkOverride 1002 null;
        "replicated" = mkOverride 1002 null;
        "statusCheck" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolErasureCoded" = {

      options = {
        "algorithm" = mkOption {
          description = "The algorithm for erasure coding.\nIf absent, defaults to the plugin specified in osd_pool_default_erasure_code_profile.";
          type = (
            types.nullOr (
              types.enum [
                "isa"
                "jerasure"
              ]
            )
          );
        };
        "codingChunks" = mkOption {
          description = "Number of coding chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThis is the number of OSDs that can be lost simultaneously before data cannot be recovered.";
          type = (types.withMinimum 0 types.int);
        };
        "crushNumFailureDomains" = mkOption {
          description = "Number of failure domains to use for erasure coded chunk placement.\nWhen specified along with crushOSDsPerFailureDomain, a CRUSH MSR rule will be created\nthat distributes chunks across this many failure domains.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "crushOSDsPerFailureDomain" = mkOption {
          description = "Number of OSDs allowed per failure domain for erasure coded chunk placement.\nWhen specified along with crushNumFailureDomains, a CRUSH MSR rule will be created\nthat allows up to this many chunks on OSDs within each failure domain.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "dataChunks" = mkOption {
          description = "Number of data chunks per object in an erasure coded storage pool (required for erasure-coded pool type).\nThe number of chunks required to recover an object when any single OSD is lost is the same\nas dataChunks so be aware that the larger the number of data chunks, the higher the cost of recovery.";
          type = (types.withMinimum 0 types.int);
        };
        "stripeUnit" = mkOption {
          description = "Erasure code stripe size in bytes. Ceph default is 4096 bytes (4 KiB).\nValue must be a multiple of 4096 (4Ki).";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "algorithm" = mkOverride 1002 null;
        "crushNumFailureDomains" = mkOverride 1002 null;
        "crushOSDsPerFailureDomain" = mkOverride 1002 null;
        "stripeUnit" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolMirroring" = {

      options = {
        "enabled" = mkOption {
          description = "Enabled whether this pool is mirrored or not";
          type = (types.nullOr types.bool);
        };
        "mode" = mkOption {
          description = "Mode is the mirroring mode: pool, image or init-only.";
          type = (
            types.nullOr (
              types.enum [
                "pool"
                "image"
                "init-only"
              ]
            )
          );
        };
        "peers" = mkOption {
          description = "Peers represents the peers spec";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolMirroringPeers"));
        };
        "snapshotSchedules" = mkOption {
          description = "SnapshotSchedules is the scheduling of snapshot for mirrored images/pools";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolMirroringSnapshotSchedules"
              )
            )
          );
        };
      };

      config = {
        "enabled" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "snapshotSchedules" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolMirroringPeers" = {

      options = {
        "secretNames" = mkOption {
          description = "SecretNames represents the Kubernetes Secret names to add rbd-mirror or cephfs-mirror peers";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "secretNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolMirroringSnapshotSchedules" = {

      options = {
        "interval" = mkOption {
          description = "Interval represent the periodicity of the snapshot.";
          type = (types.nullOr types.str);
        };
        "path" = mkOption {
          description = "Path is the path to snapshot, only valid for CephFS";
          type = (types.nullOr types.str);
        };
        "startTime" = mkOption {
          description = "StartTime indicates when to start the snapshot";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "interval" = mkOverride 1002 null;
        "path" = mkOverride 1002 null;
        "startTime" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolQuotas" = {

      options = {
        "maxBytes" = mkOption {
          description = "MaxBytes represents the quota in bytes\nDeprecated in favor of MaxSize";
          type = (types.nullOr types.int);
        };
        "maxObjects" = mkOption {
          description = "MaxObjects represents the quota in objects";
          type = (types.nullOr types.int);
        };
        "maxSize" = mkOption {
          description = "MaxSize represents the quota in bytes as a string";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "maxBytes" = mkOverride 1002 null;
        "maxObjects" = mkOverride 1002 null;
        "maxSize" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolReplicated" = {

      options = {
        "hybridStorage" = mkOption {
          description = "HybridStorage represents hybrid storage tier settings";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolReplicatedHybridStorage")
          );
        };
        "replicasPerFailureDomain" = mkOption {
          description = "ReplicasPerFailureDomain the number of replica in the specified failure domain";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "requireSafeReplicaSize" = mkOption {
          description = "RequireSafeReplicaSize if false allows you to set replica 1";
          type = (types.nullOr types.bool);
        };
        "size" = mkOption {
          description = "Size - Number of copies per object in a replicated storage pool, including the object itself (required for replicated pool type)";
          type = (types.withMinimum 0 types.int);
        };
        "subFailureDomain" = mkOption {
          description = "SubFailureDomain the name of the sub-failure domain";
          type = (types.nullOr types.str);
        };
        "targetSizeRatio" = mkOption {
          description = "TargetSizeRatio gives a hint (%) to Ceph in terms of expected consumption of the total cluster capacity";
          type = (types.nullOr (types.withMinimum 0 (types.either types.int types.float)));
        };
      };

      config = {
        "hybridStorage" = mkOverride 1002 null;
        "replicasPerFailureDomain" = mkOverride 1002 null;
        "requireSafeReplicaSize" = mkOverride 1002 null;
        "subFailureDomain" = mkOverride 1002 null;
        "targetSizeRatio" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolReplicatedHybridStorage" = {

      options = {
        "primaryDeviceClass" = mkOption {
          description = "PrimaryDeviceClass represents high performance tier (for example SSD or NVME) for Primary OSD";
          type = (types.withMinLength 1 types.str);
        };
        "secondaryDeviceClass" = mkOption {
          description = "SecondaryDeviceClass represents low performance tier (for example HDDs) for remaining OSDs";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolStatusCheck" = {

      options = {
        "mirror" = mkOption {
          description = "HealthCheckSpec represents the health check of an object store bucket";
          type = (
            types.nullOr (submoduleOf "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolStatusCheckMirror")
          );
        };
      };

      config = {
        "mirror" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecMetadataPoolStatusCheckMirror" = {

      options = {
        "disabled" = mkOption {
          description = "";
          type = (types.nullOr types.bool);
        };
        "interval" = mkOption {
          description = "Interval is the internal in second or minute for the health check to run like 60s for 60 seconds";
          type = (types.nullOr types.str);
        };
        "timeout" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "disabled" = mkOverride 1002 null;
        "interval" = mkOverride 1002 null;
        "timeout" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecSharedPools" = {

      options = {
        "dataPoolName" = mkOption {
          description = "The data pool used for creating RADOS namespaces in the object store";
          type = (types.nullOr types.str);
        };
        "metadataPoolName" = mkOption {
          description = "The metadata pool used for creating RADOS namespaces in the object store";
          type = (types.nullOr types.str);
        };
        "poolPlacements" = mkOption {
          description = "PoolPlacements control which Pools are associated with a particular RGW bucket.\nOnce PoolPlacements are defined, RGW client will be able to associate pool\nwith ObjectStore bucket by providing \"<LocationConstraint>\" during s3 bucket creation\nor \"X-Storage-Policy\" header during swift container creation.\nSee: https://docs.ceph.com/en/latest/radosgw/placement/#placement-targets\nPoolPlacement with name: \"default\" will be used as a default pool if no option\nis provided during bucket creation.\nIf default placement is not provided, spec.sharedPools.dataPoolName and spec.sharedPools.MetadataPoolName will be used as default pools.\nIf spec.sharedPools are also empty, then RGW pools (spec.dataPool and spec.metadataPool) will be used as defaults.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephObjectZoneSpecSharedPoolsPoolPlacements"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
        "preserveRadosNamespaceDataOnDelete" = mkOption {
          description = "Whether the RADOS namespaces should be preserved on deletion of the object store";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "dataPoolName" = mkOverride 1002 null;
        "metadataPoolName" = mkOverride 1002 null;
        "poolPlacements" = mkOverride 1002 null;
        "preserveRadosNamespaceDataOnDelete" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecSharedPoolsPoolPlacements" = {

      options = {
        "dataNonECPoolName" = mkOption {
          description = "The data pool used to store ObjectStore data that cannot use erasure coding (ex: multi-part uploads).\nIf dataPoolName is not erasure coded, then there is no need for dataNonECPoolName.\nWARNING: Do not change this field after creation. Pool names are used in RADOS namespaces and renaming leads to data loss.";
          type = (types.nullOr types.str);
        };
        "dataPoolName" = mkOption {
          description = "The data pool used to store ObjectStore objects data.\nWARNING: Do not change this field after creation. Pool names are used in RADOS namespaces and renaming leads to data loss.";
          type = (types.withMinLength 1 types.str);
        };
        "default" = mkOption {
          description = "Sets given placement as default. Only one placement in the list can be marked as default.\nDefault is false.";
          type = (types.nullOr types.bool);
        };
        "metadataPoolName" = mkOption {
          description = "The metadata pool used to store ObjectStore bucket index.\nWARNING: Do not change this field after creation. Pool names are used in RADOS namespaces and renaming leads to data loss.";
          type = (types.withMinLength 1 types.str);
        };
        "name" = mkOption {
          description = "Pool placement name. Name can be arbitrary. Placement with name \"default\" will be used as default.";
          type = (types.withMinLength 1 types.str);
        };
        "storageClasses" = mkOption {
          description = "StorageClasses can be selected by user to override dataPoolName during object creation.\nEach placement has default STANDARD StorageClass pointing to dataPoolName.\nThis list allows defining additional StorageClasses on top of default STANDARD storage class.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "ceph.rook.io.v1.CephObjectZoneSpecSharedPoolsPoolPlacementsStorageClasses"
                "name"
                [ ]
            )
          );
          apply = attrsToList;
        };
      };

      config = {
        "dataNonECPoolName" = mkOverride 1002 null;
        "default" = mkOverride 1002 null;
        "storageClasses" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneSpecSharedPoolsPoolPlacementsStorageClasses" = {

      options = {
        "dataPoolName" = mkOption {
          description = "DataPoolName is the data pool used to store ObjectStore objects data.\nWARNING: Do not change this field after creation. Pool names are used in RADOS namespaces and renaming leads to data loss.";
          type = (types.withMinLength 1 types.str);
        };
        "name" = mkOption {
          description = "Name is the StorageClass name. Ceph allows arbitrary name for StorageClasses,\nhowever most clients/libs insist on AWS names so it is recommended to use\none of the valid x-amz-storage-class values for better compatibility:\nREDUCED_REDUNDANCY | STANDARD_IA | ONEZONE_IA | INTELLIGENT_TIERING | GLACIER | DEEP_ARCHIVE | OUTPOSTS | GLACIER_IR | SNOW | EXPRESS_ONEZONE\nSee AWS docs: https://aws.amazon.com/de/s3/storage-classes/";
          type = (types.withMinLength 1 types.str);
        };
      };

      config = { };

    };
    "ceph.rook.io.v1.CephObjectZoneStatus" = {

      options = {
        "conditions" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephObjectZoneStatusConditions")));
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "conditions" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephObjectZoneStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirror" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta");
        };
        "spec" = mkOption {
          description = "RBDMirroringSpec represents the specification of an RBD mirror daemon";
          type = (submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpec");
        };
        "status" = mkOption {
          description = "RBDMirrorStatus represents the status of the RBD mirror resource";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephRBDMirrorStatus"));
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpec" = {

      options = {
        "annotations" = mkOption {
          description = "The annotations-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "count" = mkOption {
          description = "Count represents the number of rbd mirror instance to run";
          type = (types.withMinimum 1 types.int);
        };
        "labels" = mkOption {
          description = "The labels-related configuration to add/set on each Pod related object.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "peers" = mkOption {
          description = "Peers represents the peers spec";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPeers"));
        };
        "placement" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacement"));
        };
        "priorityClassName" = mkOption {
          description = "PriorityClassName sets priority class on the rbd mirror pods";
          type = (types.nullOr types.str);
        };
        "resources" = mkOption {
          description = "The resource requirements for the rbd mirror pods";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecResources"));
        };
      };

      config = {
        "annotations" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "peers" = mkOverride 1002 null;
        "placement" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecPeers" = {

      options = {
        "secretNames" = mkOption {
          description = "SecretNames represents the Kubernetes Secret names to add rbd-mirror or cephfs-mirror peers";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "secretNames" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacement" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinity"));
        };
        "podAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinity"));
        };
        "podAntiAffinity" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinity"));
        };
        "tolerations" = mkOption {
          description = "";
          type = (
            types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementTolerations"))
          );
        };
        "topologySpreadConstraints" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementTopologySpreadConstraints")
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topologySpreadConstraints" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "";
            type = (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "";
            type = (
              submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "";
            type = types.int;
          };
        };

        config = { };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "";
            type = (
              types.nullOr (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementTolerations" = {

      options = {
        "effect" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementTopologySpreadConstraints" = {

      options = {
        "labelSelector" = mkOption {
          description = "";
          type = (
            types.nullOr (
              submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementTopologySpreadConstraintsLabelSelector"
            )
          );
        };
        "matchLabelKeys" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf types.str));
        };
        "maxSkew" = mkOption {
          description = "";
          type = types.int;
        };
        "minDomains" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "nodeAffinityPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "nodeTaintsPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "topologyKey" = mkOption {
          description = "";
          type = types.str;
        };
        "whenUnsatisfiable" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "matchLabelKeys" = mkOverride 1002 null;
        "minDomains" = mkOverride 1002 null;
        "nodeAffinityPolicy" = mkOverride 1002 null;
        "nodeTaintsPolicy" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementTopologySpreadConstraintsLabelSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "ceph.rook.io.v1.CephRBDMirrorSpecPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecPlacementTopologySpreadConstraintsLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "";
            type = types.str;
          };
          "operator" = mkOption {
            description = "";
            type = types.str;
          };
          "values" = mkOption {
            description = "";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "ceph.rook.io.v1.CephRBDMirrorSpecResources" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "ceph.rook.io.v1.CephRBDMirrorSpecResourcesClaims" "name" [
                "name"
              ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorSpecResourcesClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorStatus" = {

      options = {
        "cephx" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephRBDMirrorStatusCephx"));
        };
        "conditions" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf (submoduleOf "ceph.rook.io.v1.CephRBDMirrorStatusConditions")));
        };
        "observedGeneration" = mkOption {
          description = "ObservedGeneration is the latest generation observed by the controller.";
          type = (types.nullOr types.int);
        };
        "phase" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "cephx" = mkOverride 1002 null;
        "conditions" = mkOverride 1002 null;
        "observedGeneration" = mkOverride 1002 null;
        "phase" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorStatusCephx" = {

      options = {
        "daemon" = mkOption {
          description = "Daemon shows the CephX key status for local Ceph daemons associated with this resources.";
          type = (types.nullOr (submoduleOf "ceph.rook.io.v1.CephRBDMirrorStatusCephxDaemon"));
        };
      };

      config = {
        "daemon" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorStatusCephxDaemon" = {

      options = {
        "keyCephVersion" = mkOption {
          description = "KeyCephVersion reports the Ceph version that created the current generation's keys. This is\nsame string format as reported by `CephCluster.status.version.version` to allow them to be\ncompared. E.g., `20.2.0-0`.\nFor all newly-created resources, this field set to the version of Ceph that created the key.\nThe special value \"Uninitialized\" indicates that keys are being created for the first time.\nAn empty string indicates that the version is unknown, as expected in brownfield deployments.";
          type = (types.nullOr types.str);
        };
        "keyGeneration" = mkOption {
          description = "KeyGeneration represents the CephX key generation for the last successful reconcile.\nFor all newly-created resources, this field is set to `1`.\nWhen keys are rotated due to any rotation policy, the generation is incremented or updated to\nthe configured policy generation.\nGeneration `0` indicates that keys existed prior to the implementation of key tracking.";
          type = (types.nullOr types.int);
        };
        "keyType" = mkOption {
          description = "KeyType identifies the CephX key type for the current generation's keys, if known.\nIf unknown, the value will be empty.";
          type = (types.nullOr (types.withMaxLength 7 (types.withMinLength 3 types.str)));
        };
      };

      config = {
        "keyCephVersion" = mkOverride 1002 null;
        "keyGeneration" = mkOverride 1002 null;
        "keyType" = mkOverride 1002 null;
      };

    };
    "ceph.rook.io.v1.CephRBDMirrorStatusConditions" = {

      options = {
        "lastHeartbeatTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "lastTransitionTime" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "message" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "reason" = mkOption {
          description = "ConditionReason is a reason for a condition";
          type = (types.nullOr types.str);
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "type" = mkOption {
          description = "ConditionType represent a resource's status";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "lastHeartbeatTime" = mkOverride 1002 null;
        "lastTransitionTime" = mkOverride 1002 null;
        "message" = mkOverride 1002 null;
        "reason" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.CephConnection" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (types.nullOr (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta"));
        };
        "spec" = mkOption {
          description = "CephConnectionSpec defines the desired state of CephConnection";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.CephConnectionSpec"));
        };
        "status" = mkOption {
          description = "CephConnectionStatus defines the observed state of CephConnection";
          type = (types.nullOr types.attrs);
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.CephConnectionSpec" = {

      options = {
        "monitors" = mkOption {
          description = "";
          type = (types.listOf types.str);
        };
        "rbdMirrorDaemonCount" = mkOption {
          description = "";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "readAffinity" = mkOption {
          description = "ReadAffinitySpec capture Ceph CSI read affinity settings";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.CephConnectionSpecReadAffinity"));
        };
      };

      config = {
        "rbdMirrorDaemonCount" = mkOverride 1002 null;
        "readAffinity" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.CephConnectionSpecReadAffinity" = {

      options = {
        "crushLocationLabels" = mkOption {
          description = "";
          type = (types.listOf types.str);
        };
      };

      config = { };

    };
    "csi.ceph.io.v1.ClientProfile" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (types.nullOr (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta"));
        };
        "spec" = mkOption {
          description = "ClientProfileSpec defines the desired state of Ceph CSI\nconfiguration for volumes and snapshots configured to use\nthis profile";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.ClientProfileSpec"));
        };
        "status" = mkOption {
          description = "ClientProfileStatus defines the observed state of Ceph CSI\nconfiguration for volumes and snapshots configured to use\nthis profile";
          type = (types.nullOr types.attrs);
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileMapping" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (types.nullOr (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta"));
        };
        "spec" = mkOption {
          description = "ClientProfileMappingSpec defines the desired state of ClientProfileMapping";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.ClientProfileMappingSpec"));
        };
        "status" = mkOption {
          description = "ClientProfileMappingStatus defines the observed state of ClientProfileMapping";
          type = (types.nullOr types.attrs);
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileMappingSpec" = {

      options = {
        "mappings" = mkOption {
          description = "";
          type = (types.listOf (submoduleOf "csi.ceph.io.v1.ClientProfileMappingSpecMappings"));
        };
      };

      config = { };

    };
    "csi.ceph.io.v1.ClientProfileMappingSpecMappings" = {

      options = {
        "blockPoolIdMapping" = mkOption {
          description = "";
          type = (types.nullOr (types.listOf (types.listOf types.str)));
        };
        "localClientProfile" = mkOption {
          description = "";
          type = types.str;
        };
        "remoteClientProfile" = mkOption {
          description = "";
          type = types.str;
        };
      };

      config = {
        "blockPoolIdMapping" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpec" = {

      options = {
        "cephConnectionRef" = mkOption {
          description = "LocalObjectReference contains enough information to let you locate the\nreferenced object inside the same namespace.";
          type = (submoduleOf "csi.ceph.io.v1.ClientProfileSpecCephConnectionRef");
        };
        "cephFs" = mkOption {
          description = "CephFsConfigSpec defines the desired CephFs configuration";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.ClientProfileSpecCephFs"));
        };
        "nfs" = mkOption {
          description = "NfsConfigSpec defines the desired NFS configuration";
          type = (types.nullOr types.attrs);
        };
        "nvmeof" = mkOption {
          description = "NvmeofConfigSpec defines the desired NVMe-oF configuration";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.ClientProfileSpecNvmeof"));
        };
        "rbd" = mkOption {
          description = "RbdConfigSpec defines the desired RBD configuration";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.ClientProfileSpecRbd"));
        };
      };

      config = {
        "cephFs" = mkOverride 1002 null;
        "nfs" = mkOverride 1002 null;
        "nvmeof" = mkOverride 1002 null;
        "rbd" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecCephConnectionRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecCephFs" = {

      options = {
        "cephCsiSecrets" = mkOption {
          description = "CephCsiSecretsSpec defines the secrets used by the client profile\nto access the Ceph cluster and perform operations\non volumes.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.ClientProfileSpecCephFsCephCsiSecrets"));
        };
        "fuseMountOptions" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "kernelMountOptions" = mkOption {
          description = "";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "radosNamespace" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "subVolumeGroup" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "cephCsiSecrets" = mkOverride 1002 null;
        "fuseMountOptions" = mkOverride 1002 null;
        "kernelMountOptions" = mkOverride 1002 null;
        "radosNamespace" = mkOverride 1002 null;
        "subVolumeGroup" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecCephFsCephCsiSecrets" = {

      options = {
        "controllerPublishSecret" = mkOption {
          description = "SecretReference represents a Secret Reference. It has enough information to retrieve secret\nin any namespace";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.ClientProfileSpecCephFsCephCsiSecretsControllerPublishSecret"
            )
          );
        };
      };

      config = {
        "controllerPublishSecret" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecCephFsCephCsiSecretsControllerPublishSecret" = {

      options = {
        "name" = mkOption {
          description = "name is unique within a namespace to reference a secret resource.";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "namespace defines the space within which the secret name must be unique.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecNvmeof" = {

      options = {
        "cephCsiSecrets" = mkOption {
          description = "CephCsiSecretsSpec defines the secrets used by the client profile\nto access the Ceph cluster and perform operations\non volumes.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.ClientProfileSpecNvmeofCephCsiSecrets"));
        };
        "radosNamespace" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "cephCsiSecrets" = mkOverride 1002 null;
        "radosNamespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecNvmeofCephCsiSecrets" = {

      options = {
        "controllerPublishSecret" = mkOption {
          description = "SecretReference represents a Secret Reference. It has enough information to retrieve secret\nin any namespace";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.ClientProfileSpecNvmeofCephCsiSecretsControllerPublishSecret"
            )
          );
        };
      };

      config = {
        "controllerPublishSecret" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecNvmeofCephCsiSecretsControllerPublishSecret" = {

      options = {
        "name" = mkOption {
          description = "name is unique within a namespace to reference a secret resource.";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "namespace defines the space within which the secret name must be unique.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecRbd" = {

      options = {
        "cephCsiSecrets" = mkOption {
          description = "CephCsiSecretsSpec defines the secrets used by the client profile\nto access the Ceph cluster and perform operations\non volumes.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.ClientProfileSpecRbdCephCsiSecrets"));
        };
        "radosNamespace" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "cephCsiSecrets" = mkOverride 1002 null;
        "radosNamespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecRbdCephCsiSecrets" = {

      options = {
        "controllerPublishSecret" = mkOption {
          description = "SecretReference represents a Secret Reference. It has enough information to retrieve secret\nin any namespace";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.ClientProfileSpecRbdCephCsiSecretsControllerPublishSecret"
            )
          );
        };
      };

      config = {
        "controllerPublishSecret" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.ClientProfileSpecRbdCephCsiSecretsControllerPublishSecret" = {

      options = {
        "name" = mkOption {
          description = "name is unique within a namespace to reference a secret resource.";
          type = (types.nullOr types.str);
        };
        "namespace" = mkOption {
          description = "namespace defines the space within which the secret name must be unique.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.Driver" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (types.nullOr (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta"));
        };
        "spec" = mkOption {
          description = "DriverSpec defines the desired state of Driver";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpec"));
        };
        "status" = mkOption {
          description = "DriverStatus defines the observed state of Driver";
          type = (types.nullOr types.attrs);
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpec" = {

      options = {
        "attachRequired" = mkOption {
          description = "Whether to skip any attach operation altogether for CephCsi PVCs.\nSee more details [here](https://kubernetes-csi.github.io/docs/skip-attach.html#skip-attach-with-csi-driver-object).\nIf set to false it skips the volume attachments and makes the creation of pods using the CephCsi PVC fast.\n**WARNING** It's highly discouraged to use this for RWO volumes. for RBD PVC it can cause data corruption,\ncsi-addons operations like Reclaimspace and PVC Keyrotation will also not be supported if set to false\nsince we'll have no VolumeAttachments to determine which node the PVC is mounted on.\nRefer to this [issue](https://github.com/kubernetes/kubernetes/issues/103305) for more details.";
          type = (types.nullOr types.bool);
        };
        "cephFsClientType" = mkOption {
          description = "Select between between cephfs kernel driver and ceph-fuse\nIf you select a non-kernel client, your application may be disrupted during upgrade.\nSee the upgrade guide: https://rook.io/docs/rook/latest/ceph-upgrade.html\nNOTE! cephfs quota is not supported in kernel version < 4.17";
          type = (
            types.nullOr (
              types.enum [
                "autodetect"
                "kernel"
              ]
            )
          );
        };
        "clusterName" = mkOption {
          description = "Cluster name identifier to set as metadata on the CephFS subvolume and RBD images. This will be useful in cases\nwhen two container orchestrator clusters (Kubernetes/OCP) are using a single ceph cluster.";
          type = (types.nullOr types.str);
        };
        "controllerPlugin" = mkOption {
          description = "Driver's controller plugin configuration";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPlugin"));
        };
        "deployCsiAddons" = mkOption {
          description = "a list of additional sidecars?";
          type = (types.nullOr types.bool);
        };
        "enableFencing" = mkOption {
          description = "Set to true to enable fencing for the driver.\nFencing is a feature that allows the driver to fence a node when it is tainted with node.kubernetes.io/out-of-service.";
          type = (types.nullOr types.bool);
        };
        "enableMetadata" = mkOption {
          description = "Deprecated: This field is no longer used by the ceph-csi driver and will be ignored.";
          type = (types.nullOr types.bool);
        };
        "encryption" = mkOption {
          description = "Driver's encryption settings";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecEncryption"));
        };
        "fsGroupPolicy" = mkOption {
          description = "Policy for modifying a volume's ownership or permissions when the PVC is being mounted.\nsupported values are documented at https://kubernetes-csi.github.io/docs/support-fsgroup.html";
          type = (types.nullOr types.str);
        };
        "fuseMountOptions" = mkOption {
          description = "Set mount options to use when using the Fuse client";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "generateOMapInfo" = mkOption {
          description = "OMAP generator will generate the omap mapping between the PV name and the RBD image.\nNeed to be enabled when we are using rbd mirroring feature.\nBy default OMAP generator sidecar is not deployed with Csi controller plugin pod, to enable\nit set it to true.";
          type = (types.nullOr types.bool);
        };
        "grpcTimeout" = mkOption {
          description = "Set the gRPC timeout for gRPC call issued by the driver components";
          type = (types.nullOr (types.withMinimum 0 types.int));
        };
        "imageSet" = mkOption {
          description = "A reference to a ConfigMap resource holding image overwrite for deployed\ncontainers";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecImageSet"));
        };
        "kernelMountOptions" = mkOption {
          description = "Set mount options to use https://docs.ceph.com/en/latest/man/8/mount.ceph/#options\nSet to \"ms_mode=secure\" when connections.encrypted is enabled in Ceph";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "leaderElection" = mkOption {
          description = "Leader election setting";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecLeaderElection"));
        };
        "liveness" = mkOption {
          description = "Liveness metrics configuration.\ndisabled by default.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecLiveness"));
        };
        "log" = mkOption {
          description = "Logging configuration for driver's pods";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecLog"));
        };
        "nodePlugin" = mkOption {
          description = "Driver's plugin configuration";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePlugin"));
        };
        "snapshotPolicy" = mkOption {
          description = "Select a policy for snapshot behavior: none, autodetect, snapshot, sanpshotGroup";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "volumeGroupSnapshot"
                "volumeSnapshot"
              ]
            )
          );
        };
      };

      config = {
        "attachRequired" = mkOverride 1002 null;
        "cephFsClientType" = mkOverride 1002 null;
        "clusterName" = mkOverride 1002 null;
        "controllerPlugin" = mkOverride 1002 null;
        "deployCsiAddons" = mkOverride 1002 null;
        "enableFencing" = mkOverride 1002 null;
        "enableMetadata" = mkOverride 1002 null;
        "encryption" = mkOverride 1002 null;
        "fsGroupPolicy" = mkOverride 1002 null;
        "fuseMountOptions" = mkOverride 1002 null;
        "generateOMapInfo" = mkOverride 1002 null;
        "grpcTimeout" = mkOverride 1002 null;
        "imageSet" = mkOverride 1002 null;
        "kernelMountOptions" = mkOverride 1002 null;
        "leaderElection" = mkOverride 1002 null;
        "liveness" = mkOverride 1002 null;
        "log" = mkOverride 1002 null;
        "nodePlugin" = mkOverride 1002 null;
        "snapshotPolicy" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPlugin" = {

      options = {
        "affinity" = mkOption {
          description = "Pod's affinity settings";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinity"));
        };
        "annotations" = mkOption {
          description = "Pod's annotations";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "containerExtraArgs" = mkOption {
          description = "Extra arguments for containers in the controller plugin deployment.\nThe key is the container name, the value is a list of CLI arguments.\nThe operator will not override the default CLI argument values it's setting.\nThis is more for customizing what comes as default with cephcsi.\nExamples of container names: csi-rbdplugin, csi-provisioner, csi-attacher,\ncsi-resizer, csi-snapshotter, csi-omap-generator, liveness-prometheus, etc.";
          type = (types.nullOr (types.loaOf types.str));
        };
        "deploymentStrategy" = mkOption {
          description = "DeploymentStrategy describes how to replace existing pods with new ones\nDefault value is Recreate";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginDeploymentStrategy"));
        };
        "hostNetwork" = mkOption {
          description = "hostNetwork setting to be propagated to CSI controller plugin pods";
          type = (types.nullOr types.bool);
        };
        "imagePullPolicy" = mkOption {
          description = "To indicate the image pull policy to be applied to all the containers in the csi driver pods.";
          type = (types.nullOr types.str);
        };
        "labels" = mkOption {
          description = "Pod's labels";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "priorityClassName" = mkOption {
          description = "Pod's user defined priority class name";
          type = (types.nullOr types.str);
        };
        "privileged" = mkOption {
          description = "To enable logrotation for csi pods,\nSome platforms require controller plugin to run privileged,\nFor example, OpenShift with SELinux restrictions requires the pod to be privileged to write to hostPath.";
          type = (types.nullOr types.bool);
        };
        "replicas" = mkOption {
          description = "Set replicas for controller plugin's deployment.\nIf not set, defaults to 2 or to the number of available nodes\non single-node clusters. When OperatorConfig specifies a replica\ncount, it is used as the default for all drivers.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "resources" = mkOption {
          description = "Resource requirements for controller plugin's containers";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResources"));
        };
        "serviceAccountName" = mkOption {
          description = "Service account name to be used for driver's pods";
          type = (types.nullOr types.str);
        };
        "tolerations" = mkOption {
          description = "Pod's tolerations list";
          type = (
            types.nullOr (types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginTolerations"))
          );
        };
        "volumes" = mkOption {
          description = "Volume and volume mount definitions to attach to the pod";
          type = (
            types.nullOr (types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumes"))
          );
        };
      };

      config = {
        "affinity" = mkOverride 1002 null;
        "annotations" = mkOverride 1002 null;
        "containerExtraArgs" = mkOverride 1002 null;
        "deploymentStrategy" = mkOverride 1002 null;
        "hostNetwork" = mkOverride 1002 null;
        "imagePullPolicy" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "privileged" = mkOverride 1002 null;
        "replicas" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "serviceAccountName" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "volumes" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinity" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "Describes node affinity scheduling rules for the pod.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinity"));
        };
        "podAffinity" = mkOption {
          description = "Describes pod affinity scheduling rules (e.g. co-locate this pod in the same node, zone, etc. as some other pod(s)).";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinity"));
        };
        "podAntiAffinity" = mkOption {
          description = "Describes pod anti-affinity scheduling rules (e.g. avoid putting this pod in the same node, zone, etc. as some other pod(s)).";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinity")
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and adding\n\"weight\" to the sum if the node matches the corresponding matchExpressions; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to an update), the system\nmay or may not try to eventually evict the pod from its node.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "A node selector term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "Weight associated with matching the corresponding nodeSelectorTerm, in the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "A list of node selector requirements by node's labels.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "A list of node selector requirements by node's fields.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "Required. A list of node selector terms. The terms are ORed.";
            type = (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "A list of node selector requirements by node's labels.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "A list of node selector requirements by node's fields.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and adding\n\"weight\" to the sum if the node has pods which matches the corresponding podAffinityTerm; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to a pod label update), the\nsystem may or may not try to eventually evict the pod from its node.\nWhen there are multiple elements, the lists of nodes corresponding to each\npodAffinityTerm are intersected, i.e. all terms must be satisfied.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "Required. A pod affinity term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "weight associated with matching the corresponding podAffinityTerm,\nin the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe anti-affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling anti-affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and subtracting\n\"weight\" from the sum if the node has pods which matches the corresponding podAffinityTerm; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the anti-affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the anti-affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to a pod label update), the\nsystem may or may not try to eventually evict the pod from its node.\nWhen there are multiple elements, the lists of nodes corresponding to each\npodAffinityTerm are intersected, i.e. all terms must be satisfied.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "Required. A pod affinity term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "weight associated with matching the corresponding podAffinityTerm,\nin the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginDeploymentStrategy" = {

      options = {
        "rollingUpdate" = mkOption {
          description = "Rolling update config params. Present only if DeploymentStrategyType =\nRollingUpdate.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginDeploymentStrategyRollingUpdate"
            )
          );
        };
        "type" = mkOption {
          description = "Type of deployment. Can be \"Recreate\" or \"RollingUpdate\". Default is RollingUpdate.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "rollingUpdate" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginDeploymentStrategyRollingUpdate" = {

      options = {
        "maxSurge" = mkOption {
          description = "The maximum number of pods that can be scheduled above the desired number of\npods.\nValue can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%).\nThis can not be 0 if MaxUnavailable is 0.\nAbsolute number is calculated from percentage by rounding up.\nDefaults to 25%.\nExample: when this is set to 30%, the new ReplicaSet can be scaled up immediately when\nthe rolling update starts, such that the total number of old and new pods do not exceed\n130% of desired pods. Once old pods have been killed,\nnew ReplicaSet can be scaled up further, ensuring that total number of pods running\nat any time during the update is at most 130% of desired pods.";
          type = (types.nullOr (types.either types.int types.str));
        };
        "maxUnavailable" = mkOption {
          description = "The maximum number of pods that can be unavailable during the update.\nValue can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%).\nAbsolute number is calculated from percentage by rounding down.\nThis can not be 0 if MaxSurge is 0.\nDefaults to 25%.\nExample: when this is set to 30%, the old ReplicaSet can be scaled down to 70% of desired pods\nimmediately when the rolling update starts. Once new pods are ready, old ReplicaSet\ncan be scaled down further, followed by scaling up the new ReplicaSet, ensuring\nthat the total number of pods available at all times during the update is at\nleast 70% of desired pods.";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "maxSurge" = mkOverride 1002 null;
        "maxUnavailable" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResources" = {

      options = {
        "addons" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResourcesAddons"));
        };
        "attacher" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResourcesAttacher"));
        };
        "liveness" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResourcesLiveness"));
        };
        "logRotator" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResourcesLogRotator"));
        };
        "omapGenerator" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResourcesOmapGenerator")
          );
        };
        "plugin" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResourcesPlugin"));
        };
        "provisioner" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResourcesProvisioner"));
        };
        "resizer" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResourcesResizer"));
        };
        "snapshotter" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginResourcesSnapshotter"));
        };
      };

      config = {
        "addons" = mkOverride 1002 null;
        "attacher" = mkOverride 1002 null;
        "liveness" = mkOverride 1002 null;
        "logRotator" = mkOverride 1002 null;
        "omapGenerator" = mkOverride 1002 null;
        "plugin" = mkOverride 1002 null;
        "provisioner" = mkOverride 1002 null;
        "resizer" = mkOverride 1002 null;
        "snapshotter" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesAddons" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "csi.ceph.io.v1.DriverSpecControllerPluginResourcesAddonsClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesAddonsClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesAttacher" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.DriverSpecControllerPluginResourcesAttacherClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesAttacherClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesLiveness" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.DriverSpecControllerPluginResourcesLivenessClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesLivenessClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesLogRotator" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.DriverSpecControllerPluginResourcesLogRotatorClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesLogRotatorClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesOmapGenerator" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.DriverSpecControllerPluginResourcesOmapGeneratorClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesOmapGeneratorClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesPlugin" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "csi.ceph.io.v1.DriverSpecControllerPluginResourcesPluginClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesPluginClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesProvisioner" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.DriverSpecControllerPluginResourcesProvisionerClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesProvisionerClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesResizer" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "csi.ceph.io.v1.DriverSpecControllerPluginResourcesResizerClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesResizerClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesSnapshotter" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.DriverSpecControllerPluginResourcesSnapshotterClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginResourcesSnapshotterClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginTolerations" = {

      options = {
        "effect" = mkOption {
          description = "Effect indicates the taint effect to match. Empty means match all taint effects.\nWhen specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "Key is the taint key that the toleration applies to. Empty means match all taint keys.\nIf the key is empty, operator must be Exists; this combination means to match all values and all keys.";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "Operator represents a key's relationship to the value.\nValid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.\nExists is equivalent to wildcard for value, so that a pod can\ntolerate all taints of a particular category.\nLt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "TolerationSeconds represents the period of time the toleration (which must be\nof effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,\nit is not set, which means tolerate the taint forever (do not evict). Zero and\nnegative values will be treated as 0 (evict immediately) by the system.";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "Value is the taint value the toleration matches to.\nIf the operator is Exists, the value should be empty, otherwise just a regular string.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumes" = {

      options = {
        "mount" = mkOption {
          description = "VolumeMount describes a mounting of a Volume within a container.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesMount"));
        };
        "volume" = mkOption {
          description = "Volume represents a named volume in a pod that may be accessed by any container in the pod.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolume"));
        };
      };

      config = {
        "mount" = mkOverride 1002 null;
        "volume" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesMount" = {

      options = {
        "mountPath" = mkOption {
          description = "Path within the container at which the volume should be mounted.  Must\nnot contain ':'.";
          type = types.str;
        };
        "mountPropagation" = mkOption {
          description = "mountPropagation determines how mounts are propagated from the host\nto container and the other way around.\nWhen not set, MountPropagationNone is used.\nThis field is beta in 1.10.\nWhen RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified\n(which defaults to None).";
          type = (types.nullOr types.str);
        };
        "name" = mkOption {
          description = "This must match the Name of a Volume.";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "Mounted read-only if true, read-write otherwise (false or unspecified).\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "recursiveReadOnly" = mkOption {
          description = "RecursiveReadOnly specifies whether read-only mounts should be handled\nrecursively.\n\nIf ReadOnly is false, this field has no meaning and must be unspecified.\n\nIf ReadOnly is true, and this field is set to Disabled, the mount is not made\nrecursively read-only.  If this field is set to IfPossible, the mount is made\nrecursively read-only, if it is supported by the container runtime.  If this\nfield is set to Enabled, the mount is made recursively read-only if it is\nsupported by the container runtime, otherwise the pod will not be started and\nan error will be generated to indicate the reason.\n\nIf this field is set to IfPossible or Enabled, MountPropagation must be set to\nNone (or be unspecified, which defaults to None).\n\nIf this field is not specified, it is treated as an equivalent of Disabled.";
          type = (types.nullOr types.str);
        };
        "subPath" = mkOption {
          description = "Path within the volume from which the container's volume should be mounted.\nDefaults to \"\" (volume's root).";
          type = (types.nullOr types.str);
        };
        "subPathExpr" = mkOption {
          description = "Expanded path within the volume from which the container's volume should be mounted.\nBehaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.\nDefaults to \"\" (volume's root).\nSubPathExpr and SubPath are mutually exclusive.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "mountPropagation" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "recursiveReadOnly" = mkOverride 1002 null;
        "subPath" = mkOverride 1002 null;
        "subPathExpr" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolume" = {

      options = {
        "awsElasticBlockStore" = mkOption {
          description = "awsElasticBlockStore represents an AWS Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nDeprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree\nawsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeAwsElasticBlockStore"
            )
          );
        };
        "azureDisk" = mkOption {
          description = "azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod.\nDeprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type\nare redirected to the disk.csi.azure.com CSI driver.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeAzureDisk")
          );
        };
        "azureFile" = mkOption {
          description = "azureFile represents an Azure File Service mount on the host and bind mount to the pod.\nDeprecated: AzureFile is deprecated. All operations for the in-tree azureFile type\nare redirected to the file.csi.azure.com CSI driver.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeAzureFile")
          );
        };
        "cephfs" = mkOption {
          description = "cephFS represents a Ceph FS mount on the host that shares a pod's lifetime.\nDeprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCephfs"));
        };
        "cinder" = mkOption {
          description = "cinder represents a cinder volume attached and mounted on kubelets host machine.\nDeprecated: Cinder is deprecated. All operations for the in-tree cinder type\nare redirected to the cinder.csi.openstack.org CSI driver.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCinder"));
        };
        "configMap" = mkOption {
          description = "configMap represents a configMap that should populate this volume";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeConfigMap")
          );
        };
        "csi" = mkOption {
          description = "csi (Container Storage Interface) represents ephemeral storage that is handled by certain external CSI drivers.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCsi"));
        };
        "downwardAPI" = mkOption {
          description = "downwardAPI represents downward API about the pod that should populate this volume";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeDownwardAPI")
          );
        };
        "emptyDir" = mkOption {
          description = "emptyDir represents a temporary directory that shares a pod's lifetime.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEmptyDir")
          );
        };
        "ephemeral" = mkOption {
          description = "ephemeral represents a volume that is handled by a cluster storage driver.\nThe volume's lifecycle is tied to the pod that defines it - it will be created before the pod starts,\nand deleted when the pod is removed.\n\nUse this if:\na) the volume is only needed while the pod runs,\nb) features of normal volumes like restoring from snapshot or capacity\n   tracking are needed,\nc) the storage driver is specified through a storage class, and\nd) the storage driver supports dynamic volume provisioning through\n   a PersistentVolumeClaim (see EphemeralVolumeSource for more\n   information on the connection between this volume type\n   and PersistentVolumeClaim).\n\nUse PersistentVolumeClaim or one of the vendor-specific\nAPIs for volumes that persist for longer than the lifecycle\nof an individual pod.\n\nUse CSI for light-weight local ephemeral volumes if the CSI driver is meant to\nbe used that way - see the documentation of the driver for\nmore information.\n\nA pod can use both types of ephemeral volumes and\npersistent volumes at the same time.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeral")
          );
        };
        "fc" = mkOption {
          description = "fc represents a Fibre Channel resource that is attached to a kubelet's host machine and then exposed to the pod.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeFc"));
        };
        "flexVolume" = mkOption {
          description = "flexVolume represents a generic volume resource that is\nprovisioned/attached using an exec based plugin.\nDeprecated: FlexVolume is deprecated. Consider using a CSIDriver instead.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeFlexVolume")
          );
        };
        "flocker" = mkOption {
          description = "flocker represents a Flocker volume attached to a kubelet's host machine. This depends on the Flocker control service being running.\nDeprecated: Flocker is deprecated and the in-tree flocker type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeFlocker"));
        };
        "gcePersistentDisk" = mkOption {
          description = "gcePersistentDisk represents a GCE Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nDeprecated: GCEPersistentDisk is deprecated. All operations for the in-tree\ngcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeGcePersistentDisk")
          );
        };
        "gitRepo" = mkOption {
          description = "gitRepo represents a git repository at a particular revision.\nDeprecated: GitRepo is deprecated. To provision a container with a git repo, mount an\nEmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir\ninto the Pod's container.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeGitRepo"));
        };
        "glusterfs" = mkOption {
          description = "glusterfs represents a Glusterfs mount on the host that shares a pod's lifetime.\nDeprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeGlusterfs")
          );
        };
        "hostPath" = mkOption {
          description = "hostPath represents a pre-existing file or directory on the host\nmachine that is directly exposed to the container. This is generally\nused for system agents or other privileged things that are allowed\nto see the host machine. Most containers will NOT need this.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeHostPath")
          );
        };
        "image" = mkOption {
          description = "image represents an OCI object (a container image or artifact) pulled and mounted on the kubelet's host machine.\nThe volume is resolved at pod startup depending on which PullPolicy value is provided:\n\n- Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.\n- Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.\n- IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.\n\nThe volume gets re-resolved if the pod gets deleted and recreated, which means that new remote content will become available on pod recreation.\nA failure to resolve or pull the image during pod startup will block containers from starting and may add significant latency. Failures will be retried using normal volume backoff and will be reported on the pod reason and message.\nThe types of objects that may be mounted by this volume are defined by the container runtime implementation on a host machine and at minimum must include all valid types supported by the container image field.\nThe OCI object gets mounted in a single directory (spec.containers[*].volumeMounts.mountPath) by merging the manifest layers in the same way as for container images.\nThe volume will be mounted read-only (ro) and non-executable files (noexec).\nSub path mounts for containers are not supported (spec.containers[*].volumeMounts.subpath) before 1.33.\nThe field spec.securityContext.fsGroupChangePolicy has no effect on this volume type.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeImage"));
        };
        "iscsi" = mkOption {
          description = "iscsi represents an ISCSI Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes/#iscsi";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeIscsi"));
        };
        "name" = mkOption {
          description = "name of the volume.\nMust be a DNS_LABEL and unique within the pod.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = types.str;
        };
        "nfs" = mkOption {
          description = "nfs represents an NFS mount on the host that shares a pod's lifetime\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeNfs"));
        };
        "persistentVolumeClaim" = mkOption {
          description = "persistentVolumeClaimVolumeSource represents a reference to a\nPersistentVolumeClaim in the same namespace.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumePersistentVolumeClaim"
            )
          );
        };
        "photonPersistentDisk" = mkOption {
          description = "photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine.\nDeprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumePhotonPersistentDisk"
            )
          );
        };
        "portworxVolume" = mkOption {
          description = "portworxVolume represents a portworx volume attached and mounted on kubelets host machine.\nDeprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type\nare redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate\nis on.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumePortworxVolume")
          );
        };
        "projected" = mkOption {
          description = "projected items for all in one resources secrets, configmaps, and downward API";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjected")
          );
        };
        "quobyte" = mkOption {
          description = "quobyte represents a Quobyte mount on the host that shares a pod's lifetime.\nDeprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeQuobyte"));
        };
        "rbd" = mkOption {
          description = "rbd represents a Rados Block Device mount on the host that shares a pod's lifetime.\nDeprecated: RBD is deprecated and the in-tree rbd type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeRbd"));
        };
        "scaleIO" = mkOption {
          description = "scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes.\nDeprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeScaleIO"));
        };
        "secret" = mkOption {
          description = "secret represents a secret that should populate this volume.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#secret";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeSecret"));
        };
        "storageos" = mkOption {
          description = "storageOS represents a StorageOS volume attached and mounted on Kubernetes nodes.\nDeprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeStorageos")
          );
        };
        "vsphereVolume" = mkOption {
          description = "vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine.\nDeprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type\nare redirected to the csi.vsphere.vmware.com CSI driver.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeVsphereVolume")
          );
        };
      };

      config = {
        "awsElasticBlockStore" = mkOverride 1002 null;
        "azureDisk" = mkOverride 1002 null;
        "azureFile" = mkOverride 1002 null;
        "cephfs" = mkOverride 1002 null;
        "cinder" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "csi" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "emptyDir" = mkOverride 1002 null;
        "ephemeral" = mkOverride 1002 null;
        "fc" = mkOverride 1002 null;
        "flexVolume" = mkOverride 1002 null;
        "flocker" = mkOverride 1002 null;
        "gcePersistentDisk" = mkOverride 1002 null;
        "gitRepo" = mkOverride 1002 null;
        "glusterfs" = mkOverride 1002 null;
        "hostPath" = mkOverride 1002 null;
        "image" = mkOverride 1002 null;
        "iscsi" = mkOverride 1002 null;
        "nfs" = mkOverride 1002 null;
        "persistentVolumeClaim" = mkOverride 1002 null;
        "photonPersistentDisk" = mkOverride 1002 null;
        "portworxVolume" = mkOverride 1002 null;
        "projected" = mkOverride 1002 null;
        "quobyte" = mkOverride 1002 null;
        "rbd" = mkOverride 1002 null;
        "scaleIO" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "storageos" = mkOverride 1002 null;
        "vsphereVolume" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeAwsElasticBlockStore" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (types.nullOr types.str);
        };
        "partition" = mkOption {
          description = "partition is the partition in the volume that you want to mount.\nIf omitted, the default is to mount by volume name.\nExamples: For volume /dev/sda1, you specify the partition as \"1\".\nSimilarly, the volume partition for /dev/sda is \"0\" (or you can leave the property empty).";
          type = (types.nullOr types.int);
        };
        "readOnly" = mkOption {
          description = "readOnly value true will force the readOnly setting in VolumeMounts.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (types.nullOr types.bool);
        };
        "volumeID" = mkOption {
          description = "volumeID is unique ID of the persistent disk resource in AWS (Amazon EBS volume).\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "partition" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeAzureDisk" = {

      options = {
        "cachingMode" = mkOption {
          description = "cachingMode is the Host Caching mode: None, Read Only, Read Write.";
          type = (types.nullOr types.str);
        };
        "diskName" = mkOption {
          description = "diskName is the Name of the data disk in the blob storage";
          type = types.str;
        };
        "diskURI" = mkOption {
          description = "diskURI is the URI of data disk in the blob storage";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType is Filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "kind expected values are Shared: multiple blob disks per storage account  Dedicated: single blob disk per storage account  Managed: azure managed data disk (only in managed availability set). defaults to shared";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "cachingMode" = mkOverride 1002 null;
        "fsType" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeAzureFile" = {

      options = {
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "secretName is the  name of secret that contains Azure Storage Account Name and Key";
          type = types.str;
        };
        "shareName" = mkOption {
          description = "shareName is the azure share Name";
          type = types.str;
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCephfs" = {

      options = {
        "monitors" = mkOption {
          description = "monitors is Required: Monitors is a collection of Ceph monitors\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.listOf types.str);
        };
        "path" = mkOption {
          description = "path is Optional: Used as the mounted root, rather than the full Ceph tree, default is /";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.bool);
        };
        "secretFile" = mkOption {
          description = "secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "secretRef" = mkOption {
          description = "secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty.\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCephfsSecretRef")
          );
        };
        "user" = mkOption {
          description = "user is optional: User is the rados user name, default is admin\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "path" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretFile" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCephfsSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCinder" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is optional: points to a secret object containing parameters used to connect\nto OpenStack.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCinderSecretRef")
          );
        };
        "volumeID" = mkOption {
          description = "volumeID used to identify the volume in cinder.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCinderSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeConfigMap" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode is optional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDefaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "items if unspecified, each key-value pair in the Data field of the referenced\nConfigMap will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the ConfigMap,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeConfigMapItems")
            )
          );
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "optional specify whether the ConfigMap or its keys must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCsi" = {

      options = {
        "driver" = mkOption {
          description = "driver is the name of the CSI driver that handles this volume.\nConsult with your admin for the correct name as registered in the cluster.";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType to mount. Ex. \"ext4\", \"xfs\", \"ntfs\".\nIf not provided, the empty value is passed to the associated CSI driver\nwhich will determine the default filesystem to apply.";
          type = (types.nullOr types.str);
        };
        "nodePublishSecretRef" = mkOption {
          description = "nodePublishSecretRef is a reference to the secret object containing\nsensitive information to pass to the CSI driver to complete the CSI\nNodePublishVolume and NodeUnpublishVolume calls.\nThis field is optional, and  may be empty if no secret is required. If the\nsecret object contains more than one secret, all secret references are passed.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCsiNodePublishSecretRef"
            )
          );
        };
        "readOnly" = mkOption {
          description = "readOnly specifies a read-only configuration for the volume.\nDefaults to false (read/write).";
          type = (types.nullOr types.bool);
        };
        "volumeAttributes" = mkOption {
          description = "volumeAttributes stores driver-specific properties that are passed to the CSI\ndriver. Consult your driver's documentation for supported values.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "nodePublishSecretRef" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "volumeAttributes" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeCsiNodePublishSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeDownwardAPI" = {

      options = {
        "defaultMode" = mkOption {
          description = "Optional: mode bits to use on created files by default. Must be a\nOptional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDefaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "Items is a list of downward API volume file";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeDownwardAPIItems")
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeDownwardAPIItems" = {

      options = {
        "fieldRef" = mkOption {
          description = "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeDownwardAPIItemsFieldRef"
            )
          );
        };
        "mode" = mkOption {
          description = "Optional: mode bits used to set permissions on this file, must be an octal value\nbetween 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the '..' path. Must be utf-8 encoded. The first item of the relative path must not start with '..'";
          type = types.str;
        };
        "resourceFieldRef" = mkOption {
          description = "Selects a resource of the container: only resources limits and requests\n(limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeDownwardAPIItemsResourceFieldRef"
            )
          );
        };
      };

      config = {
        "fieldRef" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "resourceFieldRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeDownwardAPIItemsFieldRef" = {

      options = {
        "apiVersion" = mkOption {
          description = "Version of the schema the FieldPath is written in terms of, defaults to \"v1\".";
          type = (types.nullOr types.str);
        };
        "fieldPath" = mkOption {
          description = "Path of the field to select in the specified API version.";
          type = types.str;
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeDownwardAPIItemsResourceFieldRef" = {

      options = {
        "containerName" = mkOption {
          description = "Container name: required for volumes, optional for env vars";
          type = (types.nullOr types.str);
        };
        "divisor" = mkOption {
          description = "Specifies the output format of the exposed resources, defaults to \"1\"";
          type = (types.nullOr (types.either types.int types.str));
        };
        "resource" = mkOption {
          description = "Required: resource to select";
          type = types.str;
        };
      };

      config = {
        "containerName" = mkOverride 1002 null;
        "divisor" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEmptyDir" = {

      options = {
        "medium" = mkOption {
          description = "medium represents what type of storage medium should back this directory.\nThe default is \"\" which means to use the node's default medium.\nMust be an empty string (default) or Memory.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (types.nullOr types.str);
        };
        "sizeLimit" = mkOption {
          description = "sizeLimit is the total amount of local storage required for this EmptyDir volume.\nThe size limit is also applicable for memory medium.\nThe maximum usage on memory medium EmptyDir would be the minimum value between\nthe SizeLimit specified here and the sum of memory limits of all containers in a pod.\nThe default is nil which means that the limit is undefined.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "medium" = mkOverride 1002 null;
        "sizeLimit" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeral" = {

      options = {
        "volumeClaimTemplate" = mkOption {
          description = "Will be used to create a stand-alone PVC to provision the volume.\nThe pod in which this EphemeralVolumeSource is embedded will be the\nowner of the PVC, i.e. the PVC will be deleted together with the\npod.  The name of the PVC will be `<pod name>-<volume name>` where\n`<volume name>` is the name from the `PodSpec.Volumes` array\nentry. Pod validation will reject the pod if the concatenated name\nis not valid for a PVC (for example, too long).\n\nAn existing PVC with that name that is not owned by the pod\nwill *not* be used for the pod to avoid using an unrelated\nvolume by mistake. Starting the pod is then blocked until\nthe unrelated PVC is removed. If such a pre-created PVC is\nmeant to be used by the pod, the PVC has to updated with an\nowner reference to the pod once the pod exists. Normally\nthis should not be necessary, but it may be useful when\nmanually reconstructing a broken cluster.\n\nThis field is read-only and no changes will be made by Kubernetes\nto the PVC after it has been created.\n\nRequired, must not be nil.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplate"
            )
          );
        };
      };

      config = {
        "volumeClaimTemplate" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplate" = {

      options = {
        "metadata" = mkOption {
          description = "May contain labels and annotations that will be copied into the PVC\nwhen creating it. No other fields are allowed and will be rejected during\nvalidation.";
          type = (types.nullOr types.attrs);
        };
        "spec" = mkOption {
          description = "The specification for the PersistentVolumeClaim. The entire content is\ncopied unchanged into the PVC that gets created from this\ntemplate. The same fields as in a PersistentVolumeClaim\nare also valid here.";
          type = (
            submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpec"
          );
        };
      };

      config = {
        "metadata" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpec" = {

      options = {
        "accessModes" = mkOption {
          description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
          type = (types.nullOr (types.listOf types.str));
        };
        "dataSource" = mkOption {
          description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSource"
            )
          );
        };
        "dataSourceRef" = mkOption {
          description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSourceRef"
            )
          );
        };
        "resources" = mkOption {
          description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecResources"
            )
          );
        };
        "selector" = mkOption {
          description = "selector is a label query over volumes to consider for binding.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelector"
            )
          );
        };
        "storageClassName" = mkOption {
          description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
          type = (types.nullOr types.str);
        };
        "volumeAttributesClassName" = mkOption {
          description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
          type = (types.nullOr types.str);
        };
        "volumeMode" = mkOption {
          description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
          type = (types.nullOr types.str);
        };
        "volumeName" = mkOption {
          description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "accessModes" = mkOverride 1002 null;
        "dataSource" = mkOverride 1002 null;
        "dataSourceRef" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "selector" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
        "volumeAttributesClassName" = mkOverride 1002 null;
        "volumeMode" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSource" =
      {

        options = {
          "apiGroup" = mkOption {
            description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
            type = (types.nullOr types.str);
          };
          "kind" = mkOption {
            description = "Kind is the type of resource being referenced";
            type = types.str;
          };
          "name" = mkOption {
            description = "Name is the name of resource being referenced";
            type = types.str;
          };
        };

        config = {
          "apiGroup" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSourceRef" =
      {

        options = {
          "apiGroup" = mkOption {
            description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
            type = (types.nullOr types.str);
          };
          "kind" = mkOption {
            description = "Kind is the type of resource being referenced";
            type = types.str;
          };
          "name" = mkOption {
            description = "Name is the name of resource being referenced";
            type = types.str;
          };
          "namespace" = mkOption {
            description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "apiGroup" = mkOverride 1002 null;
          "namespace" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecResources" =
      {

        options = {
          "limits" = mkOption {
            description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
            type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
          };
          "requests" = mkOption {
            description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
            type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
          };
        };

        config = {
          "limits" = mkOverride 1002 null;
          "requests" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeFc" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "lun" = mkOption {
          description = "lun is Optional: FC target lun number";
          type = (types.nullOr types.int);
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "targetWWNs" = mkOption {
          description = "targetWWNs is Optional: FC target worldwide names (WWNs)";
          type = (types.nullOr (types.listOf types.str));
        };
        "wwids" = mkOption {
          description = "wwids Optional: FC volume world wide identifiers (wwids)\nEither wwids or combination of targetWWNs and lun must be set, but not both simultaneously.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "lun" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "targetWWNs" = mkOverride 1002 null;
        "wwids" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeFlexVolume" = {

      options = {
        "driver" = mkOption {
          description = "driver is the name of the driver to use for this volume.";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". The default filesystem depends on FlexVolume script.";
          type = (types.nullOr types.str);
        };
        "options" = mkOption {
          description = "options is Optional: this field holds extra command options if any.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is Optional: secretRef is reference to the secret object containing\nsensitive information to pass to the plugin scripts. This may be\nempty if no secret object is specified. If the secret object\ncontains more than one secret, all secrets are passed to the plugin\nscripts.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeFlexVolumeSecretRef"
            )
          );
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "options" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeFlexVolumeSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeFlocker" = {

      options = {
        "datasetName" = mkOption {
          description = "datasetName is Name of the dataset stored as metadata -> name on the dataset for Flocker\nshould be considered as deprecated";
          type = (types.nullOr types.str);
        };
        "datasetUUID" = mkOption {
          description = "datasetUUID is the UUID of the dataset. This is unique identifier of a Flocker dataset";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "datasetName" = mkOverride 1002 null;
        "datasetUUID" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeGcePersistentDisk" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (types.nullOr types.str);
        };
        "partition" = mkOption {
          description = "partition is the partition in the volume that you want to mount.\nIf omitted, the default is to mount by volume name.\nExamples: For volume /dev/sda1, you specify the partition as \"1\".\nSimilarly, the volume partition for /dev/sda is \"0\" (or you can leave the property empty).\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (types.nullOr types.int);
        };
        "pdName" = mkOption {
          description = "pdName is unique name of the PD resource in GCE. Used to identify the disk in GCE.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "partition" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeGitRepo" = {

      options = {
        "directory" = mkOption {
          description = "directory is the target directory name.\nMust not contain or start with '..'.  If '.' is supplied, the volume directory will be the\ngit repository.  Otherwise, if specified, the volume will contain the git repository in\nthe subdirectory with the given name.";
          type = (types.nullOr types.str);
        };
        "repository" = mkOption {
          description = "repository is the URL";
          type = types.str;
        };
        "revision" = mkOption {
          description = "revision is the commit hash for the specified revision.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "directory" = mkOverride 1002 null;
        "revision" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeGlusterfs" = {

      options = {
        "endpoints" = mkOption {
          description = "endpoints is the endpoint name that details Glusterfs topology.";
          type = types.str;
        };
        "path" = mkOption {
          description = "path is the Glusterfs volume path.\nMore info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the Glusterfs volume to be mounted with read-only permissions.\nDefaults to false.\nMore info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeHostPath" = {

      options = {
        "path" = mkOption {
          description = "path of the directory on the host.\nIf the path is a symlink, it will follow the link to the real path.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = types.str;
        };
        "type" = mkOption {
          description = "type for HostPath Volume\nDefaults to \"\"\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeImage" = {

      options = {
        "pullPolicy" = mkOption {
          description = "Policy for pulling OCI objects. Possible values are:\nAlways: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.\nNever: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.\nIfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.\nDefaults to Always if :latest tag is specified, or IfNotPresent otherwise.";
          type = (types.nullOr types.str);
        };
        "reference" = mkOption {
          description = "Required: Image or artifact reference to be used.\nBehaves in the same way as pod.spec.containers[*].image.\nPull secrets will be assembled in the same way as for the container image by looking up node credentials, SA image pull secrets, and pod spec image pull secrets.\nMore info: https://kubernetes.io/docs/concepts/containers/images\nThis field is optional to allow higher level config management to default or override\ncontainer images in workload controllers like Deployments and StatefulSets.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "pullPolicy" = mkOverride 1002 null;
        "reference" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeIscsi" = {

      options = {
        "chapAuthDiscovery" = mkOption {
          description = "chapAuthDiscovery defines whether support iSCSI Discovery CHAP authentication";
          type = (types.nullOr types.bool);
        };
        "chapAuthSession" = mkOption {
          description = "chapAuthSession defines whether support iSCSI Session CHAP authentication";
          type = (types.nullOr types.bool);
        };
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi";
          type = (types.nullOr types.str);
        };
        "initiatorName" = mkOption {
          description = "initiatorName is the custom iSCSI Initiator Name.\nIf initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface\n<target portal>:<volume name> will be created for the connection.";
          type = (types.nullOr types.str);
        };
        "iqn" = mkOption {
          description = "iqn is the target iSCSI Qualified Name.";
          type = types.str;
        };
        "iscsiInterface" = mkOption {
          description = "iscsiInterface is the interface Name that uses an iSCSI transport.\nDefaults to 'default' (tcp).";
          type = (types.nullOr types.str);
        };
        "lun" = mkOption {
          description = "lun represents iSCSI Target Lun number.";
          type = types.int;
        };
        "portals" = mkOption {
          description = "portals is the iSCSI Target Portal List. The portal is either an IP or ip_addr:port if the port\nis other than default (typically TCP ports 860 and 3260).";
          type = (types.nullOr (types.listOf types.str));
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is the CHAP Secret for iSCSI target and initiator authentication";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeIscsiSecretRef")
          );
        };
        "targetPortal" = mkOption {
          description = "targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port\nis other than default (typically TCP ports 860 and 3260).";
          type = types.str;
        };
      };

      config = {
        "chapAuthDiscovery" = mkOverride 1002 null;
        "chapAuthSession" = mkOverride 1002 null;
        "fsType" = mkOverride 1002 null;
        "initiatorName" = mkOverride 1002 null;
        "iscsiInterface" = mkOverride 1002 null;
        "portals" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeIscsiSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeNfs" = {

      options = {
        "path" = mkOption {
          description = "path that is exported by the NFS server.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the NFS export to be mounted with read-only permissions.\nDefaults to false.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = (types.nullOr types.bool);
        };
        "server" = mkOption {
          description = "server is the hostname or IP address of the NFS server.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = types.str;
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumePersistentVolumeClaim" = {

      options = {
        "claimName" = mkOption {
          description = "claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly Will force the ReadOnly setting in VolumeMounts.\nDefault false.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumePhotonPersistentDisk" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "pdID" = mkOption {
          description = "pdID is the ID that identifies Photon Controller persistent disk";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumePortworxVolume" = {

      options = {
        "fsType" = mkOption {
          description = "fSType represents the filesystem type to mount\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "volumeID" = mkOption {
          description = "volumeID uniquely identifies a Portworx volume";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjected" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode are the mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "sources" = mkOption {
          description = "sources is the list of volume projections. Each entry in this list\nhandles one source.";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSources")
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "sources" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSources" = {

      options = {
        "clusterTrustBundle" = mkOption {
          description = "ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field\nof ClusterTrustBundle objects in an auto-updating file.\n\nAlpha, gated by the ClusterTrustBundleProjection feature gate.\n\nClusterTrustBundle objects can either be selected by name, or by the\ncombination of signer name and a label selector.\n\nKubelet performs aggressive normalization of the PEM contents written\ninto the pod filesystem.  Esoteric PEM features such as inter-block\ncomments and block headers are stripped.  Certificates are deduplicated.\nThe ordering of certificates within the file is arbitrary, and Kubelet\nmay change the order over time.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundle"
            )
          );
        };
        "configMap" = mkOption {
          description = "configMap information about the configMap data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesConfigMap"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "downwardAPI information about the downwardAPI data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesDownwardAPI"
            )
          );
        };
        "podCertificate" = mkOption {
          description = "Projects an auto-rotating credential bundle (private key and certificate\nchain) that the pod can use either as a TLS client or server.\n\nKubelet generates a private key and uses it to send a\nPodCertificateRequest to the named signer.  Once the signer approves the\nrequest and issues a certificate chain, Kubelet writes the key and\ncertificate chain to the pod filesystem.  The pod does not start until\ncertificates have been issued for each podCertificate projected volume\nsource in its spec.\n\nKubelet will begin trying to rotate the certificate at the time indicated\nby the signer using the PodCertificateRequest.Status.BeginRefreshAt\ntimestamp.\n\nKubelet can write a single file, indicated by the credentialBundlePath\nfield, or separate files, indicated by the keyPath and\ncertificateChainPath fields.\n\nThe credential bundle is a single file in PEM format.  The first PEM\nentry is the private key (in PKCS#8 format), and the remaining PEM\nentries are the certificate chain issued by the signer (typically,\nsigners will return their certificate chain in leaf-to-root order).\n\nPrefer using the credential bundle format, since your application code\ncan read it atomically.  If you use keyPath and certificateChainPath,\nyour application must make two separate file reads. If these coincide\nwith a certificate rotation, it is possible that the private key and leaf\ncertificate you read may not correspond to each other.  Your application\nwill need to check for this condition, and re-read until they are\nconsistent.\n\nThe named signer controls chooses the format of the certificate it\nissues; consult the signer implementation's documentation to learn how to\nuse the certificates it issues.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesPodCertificate"
            )
          );
        };
        "secret" = mkOption {
          description = "secret information about the secret data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesSecret"
            )
          );
        };
        "serviceAccountToken" = mkOption {
          description = "serviceAccountToken is information about the serviceAccountToken data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesServiceAccountToken"
            )
          );
        };
      };

      config = {
        "clusterTrustBundle" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "podCertificate" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "serviceAccountToken" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundle" = {

      options = {
        "labelSelector" = mkOption {
          description = "Select all ClusterTrustBundles that match this label selector.  Only has\neffect if signerName is set.  Mutually-exclusive with name.  If unset,\ninterpreted as \"match nothing\".  If set but empty, interpreted as \"match\neverything\".";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelector"
            )
          );
        };
        "name" = mkOption {
          description = "Select a single ClusterTrustBundle by object name.  Mutually-exclusive\nwith signerName and labelSelector.";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "If true, don't block pod startup if the referenced ClusterTrustBundle(s)\naren't available.  If using name, then the named ClusterTrustBundle is\nallowed not to exist.  If using signerName, then the combination of\nsignerName and labelSelector is allowed to match zero\nClusterTrustBundles.";
          type = (types.nullOr types.bool);
        };
        "path" = mkOption {
          description = "Relative path from the volume root to write the bundle.";
          type = types.str;
        };
        "signerName" = mkOption {
          description = "Select all ClusterTrustBundles that match this signer name.\nMutually-exclusive with name.  The contents of all selected\nClusterTrustBundles will be unified and deduplicated.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "signerName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesConfigMap" = {

      options = {
        "items" = mkOption {
          description = "items if unspecified, each key-value pair in the Data field of the referenced\nConfigMap will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the ConfigMap,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "optional specify whether the ConfigMap or its keys must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesDownwardAPI" = {

      options = {
        "items" = mkOption {
          description = "Items is a list of DownwardAPIVolume file";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItems"
              )
            )
          );
        };
      };

      config = {
        "items" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItems" = {

      options = {
        "fieldRef" = mkOption {
          description = "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItemsFieldRef"
            )
          );
        };
        "mode" = mkOption {
          description = "Optional: mode bits used to set permissions on this file, must be an octal value\nbetween 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the '..' path. Must be utf-8 encoded. The first item of the relative path must not start with '..'";
          type = types.str;
        };
        "resourceFieldRef" = mkOption {
          description = "Selects a resource of the container: only resources limits and requests\n(limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItemsResourceFieldRef"
            )
          );
        };
      };

      config = {
        "fieldRef" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "resourceFieldRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItemsFieldRef" = {

      options = {
        "apiVersion" = mkOption {
          description = "Version of the schema the FieldPath is written in terms of, defaults to \"v1\".";
          type = (types.nullOr types.str);
        };
        "fieldPath" = mkOption {
          description = "Path of the field to select in the specified API version.";
          type = types.str;
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "Container name: required for volumes, optional for env vars";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "Specifies the output format of the exposed resources, defaults to \"1\"";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "Required: resource to select";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesPodCertificate" = {

      options = {
        "certificateChainPath" = mkOption {
          description = "Write the certificate chain at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath\nand certificateChainPath, your application needs to check that the key\nand leaf certificate are consistent, because it is possible to read the\nfiles mid-rotation.";
          type = (types.nullOr types.str);
        };
        "credentialBundlePath" = mkOption {
          description = "Write the credential bundle at this path in the projected volume.\n\nThe credential bundle is a single file that contains multiple PEM blocks.\nThe first PEM block is a PRIVATE KEY block, containing a PKCS#8 private\nkey.\n\nThe remaining blocks are CERTIFICATE blocks, containing the issued\ncertificate chain from the signer (leaf and any intermediates).\n\nUsing credentialBundlePath lets your Pod's application code make a single\natomic read that retrieves a consistent key and certificate chain.  If you\nproject them to separate files, your application code will need to\nadditionally check that the leaf certificate was issued to the key.";
          type = (types.nullOr types.str);
        };
        "keyPath" = mkOption {
          description = "Write the key at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath\nand certificateChainPath, your application needs to check that the key\nand leaf certificate are consistent, because it is possible to read the\nfiles mid-rotation.";
          type = (types.nullOr types.str);
        };
        "keyType" = mkOption {
          description = "The type of keypair Kubelet will generate for the pod.\n\nValid values are \"RSA3072\", \"RSA4096\", \"ECDSAP256\", \"ECDSAP384\",\n\"ECDSAP521\", and \"ED25519\".";
          type = types.str;
        };
        "maxExpirationSeconds" = mkOption {
          description = "maxExpirationSeconds is the maximum lifetime permitted for the\ncertificate.\n\nKubelet copies this value verbatim into the PodCertificateRequests it\ngenerates for this projection.\n\nIf omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver\nwill reject values shorter than 3600 (1 hour).  The maximum allowable\nvalue is 7862400 (91 days).\n\nThe signer implementation is then free to issue a certificate with any\nlifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600\nseconds (1 hour).  This constraint is enforced by kube-apiserver.\n`kubernetes.io` signers will never issue certificates with a lifetime\nlonger than 24 hours.";
          type = (types.nullOr types.int);
        };
        "signerName" = mkOption {
          description = "Kubelet's generated CSRs will be addressed to this signer.";
          type = types.str;
        };
        "userAnnotations" = mkOption {
          description = "userAnnotations allow pod authors to pass additional information to\nthe signer implementation.  Kubernetes does not restrict or validate this\nmetadata in any way.\n\nThese values are copied verbatim into the `spec.unverifiedUserAnnotations` field of\nthe PodCertificateRequest objects that Kubelet creates.\n\nEntries are subject to the same validation as object metadata annotations,\nwith the addition that all keys must be domain-prefixed. No restrictions\nare placed on values, except an overall size limitation on the entire field.\n\nSigners should document the keys and values they support. Signers should\ndeny requests that contain keys they do not recognize.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "certificateChainPath" = mkOverride 1002 null;
        "credentialBundlePath" = mkOverride 1002 null;
        "keyPath" = mkOverride 1002 null;
        "maxExpirationSeconds" = mkOverride 1002 null;
        "userAnnotations" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesSecret" = {

      options = {
        "items" = mkOption {
          description = "items if unspecified, each key-value pair in the Data field of the referenced\nSecret will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the Secret,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesSecretItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "optional field specify whether the Secret or its key must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesSecretItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeProjectedSourcesServiceAccountToken" = {

      options = {
        "audience" = mkOption {
          description = "audience is the intended audience of the token. A recipient of a token\nmust identify itself with an identifier specified in the audience of the\ntoken, and otherwise should reject the token. The audience defaults to the\nidentifier of the apiserver.";
          type = (types.nullOr types.str);
        };
        "expirationSeconds" = mkOption {
          description = "expirationSeconds is the requested duration of validity of the service\naccount token. As the token approaches expiration, the kubelet volume\nplugin will proactively rotate the service account token. The kubelet will\nstart trying to rotate the token if the token is older than 80 percent of\nits time to live or if the token is older than 24 hours.Defaults to 1 hour\nand must be at least 10 minutes.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the path relative to the mount point of the file to project the\ntoken into.";
          type = types.str;
        };
      };

      config = {
        "audience" = mkOverride 1002 null;
        "expirationSeconds" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeQuobyte" = {

      options = {
        "group" = mkOption {
          description = "group to map volume access to\nDefault is no group";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the Quobyte volume to be mounted with read-only permissions.\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "registry" = mkOption {
          description = "registry represents a single or multiple Quobyte Registry services\nspecified as a string as host:port pair (multiple entries are separated with commas)\nwhich acts as the central registry for volumes";
          type = types.str;
        };
        "tenant" = mkOption {
          description = "tenant owning the given Quobyte volume in the Backend\nUsed with dynamically provisioned Quobyte volumes, value is set by the plugin";
          type = (types.nullOr types.str);
        };
        "user" = mkOption {
          description = "user to map volume access to\nDefaults to serivceaccount user";
          type = (types.nullOr types.str);
        };
        "volume" = mkOption {
          description = "volume is a string that references an already created Quobyte volume by name.";
          type = types.str;
        };
      };

      config = {
        "group" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "tenant" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeRbd" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#rbd";
          type = (types.nullOr types.str);
        };
        "image" = mkOption {
          description = "image is the rados image name.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = types.str;
        };
        "keyring" = mkOption {
          description = "keyring is the path to key ring for RBDUser.\nDefault is /etc/ceph/keyring.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "monitors" = mkOption {
          description = "monitors is a collection of Ceph monitors.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.listOf types.str);
        };
        "pool" = mkOption {
          description = "pool is the rados pool name.\nDefault is rbd.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is name of the authentication secret for RBDUser. If provided\noverrides keyring.\nDefault is nil.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeRbdSecretRef")
          );
        };
        "user" = mkOption {
          description = "user is the rados user name.\nDefault is admin.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "keyring" = mkOverride 1002 null;
        "pool" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeRbdSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeScaleIO" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\".\nDefault is \"xfs\".";
          type = (types.nullOr types.str);
        };
        "gateway" = mkOption {
          description = "gateway is the host address of the ScaleIO API Gateway.";
          type = types.str;
        };
        "protectionDomain" = mkOption {
          description = "protectionDomain is the name of the ScaleIO Protection Domain for the configured storage.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef references to the secret for ScaleIO user and other\nsensitive information. If this is not provided, Login operation will fail.";
          type = (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeScaleIOSecretRef");
        };
        "sslEnabled" = mkOption {
          description = "sslEnabled Flag enable/disable SSL communication with Gateway, default false";
          type = (types.nullOr types.bool);
        };
        "storageMode" = mkOption {
          description = "storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned.\nDefault is ThinProvisioned.";
          type = (types.nullOr types.str);
        };
        "storagePool" = mkOption {
          description = "storagePool is the ScaleIO Storage Pool associated with the protection domain.";
          type = (types.nullOr types.str);
        };
        "system" = mkOption {
          description = "system is the name of the storage system as configured in ScaleIO.";
          type = types.str;
        };
        "volumeName" = mkOption {
          description = "volumeName is the name of a volume already created in the ScaleIO system\nthat is associated with this volume source.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "protectionDomain" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "sslEnabled" = mkOverride 1002 null;
        "storageMode" = mkOverride 1002 null;
        "storagePool" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeScaleIOSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeSecret" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode is Optional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values\nfor mode bits. Defaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "items If unspecified, each key-value pair in the Data field of the referenced\nSecret will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the Secret,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeSecretItems")
            )
          );
        };
        "optional" = mkOption {
          description = "optional field specify whether the Secret or its keys must be defined";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "secretName is the name of the secret in the pod's namespace to use.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#secret";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeSecretItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeStorageos" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef specifies the secret to use for obtaining the StorageOS API\ncredentials.  If not specified, default values will be attempted.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeStorageosSecretRef"
            )
          );
        };
        "volumeName" = mkOption {
          description = "volumeName is the human-readable name of the StorageOS volume.  Volume\nnames are only unique within a namespace.";
          type = (types.nullOr types.str);
        };
        "volumeNamespace" = mkOption {
          description = "volumeNamespace specifies the scope of the volume within StorageOS.  If no\nnamespace is specified then the Pod's namespace will be used.  This allows the\nKubernetes name scoping to be mirrored within StorageOS for tighter integration.\nSet VolumeName to any name to override the default behaviour.\nSet to \"default\" if you are not using namespaces within StorageOS.\nNamespaces that do not pre-exist within StorageOS will be created.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
        "volumeNamespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeStorageosSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecControllerPluginVolumesVolumeVsphereVolume" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "storagePolicyID" = mkOption {
          description = "storagePolicyID is the storage Policy Based Management (SPBM) profile ID associated with the StoragePolicyName.";
          type = (types.nullOr types.str);
        };
        "storagePolicyName" = mkOption {
          description = "storagePolicyName is the storage Policy Based Management (SPBM) profile name.";
          type = (types.nullOr types.str);
        };
        "volumePath" = mkOption {
          description = "volumePath is the path that identifies vSphere volume vmdk";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "storagePolicyID" = mkOverride 1002 null;
        "storagePolicyName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecEncryption" = {

      options = {
        "configMapName" = mkOption {
          description = "LocalObjectReference contains enough information to let you locate the\nreferenced object inside the same namespace.";
          type = (submoduleOf "csi.ceph.io.v1.DriverSpecEncryptionConfigMapName");
        };
      };

      config = { };

    };
    "csi.ceph.io.v1.DriverSpecEncryptionConfigMapName" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecImageSet" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecLeaderElection" = {

      options = {
        "leaseDuration" = mkOption {
          description = "Duration in seconds that non-leader candidates will wait to force acquire leadership.\nDefault to 137 seconds.";
          type = (types.nullOr (types.withMinimum 0 types.int));
        };
        "renewDeadline" = mkOption {
          description = "Deadline in seconds that the acting leader will retry refreshing leadership before giving up.\nDefaults to 107 seconds.";
          type = (types.nullOr (types.withMinimum 0 types.int));
        };
        "retryPeriod" = mkOption {
          description = "Retry Period in seconds the LeaderElector clients should wait between tries of actions.\nDefaults to 26 seconds.";
          type = (types.nullOr (types.withMinimum 0 types.int));
        };
      };

      config = {
        "leaseDuration" = mkOverride 1002 null;
        "renewDeadline" = mkOverride 1002 null;
        "retryPeriod" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecLiveness" = {

      options = {
        "metricsPort" = mkOption {
          description = "Port to expose liveness metrics";
          type = (types.withMaximum 65535 (types.withMinimum 1024 types.int));
        };
      };

      config = { };

    };
    "csi.ceph.io.v1.DriverSpecLog" = {

      options = {
        "rotation" = mkOption {
          description = "log rotation for csi pods";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecLogRotation"));
        };
        "verbosity" = mkOption {
          description = "Log verbosity level for driver pods,\nSupported values from 0 to 5. 0 for general useful logs (the default), 5 for trace level verbosity.\nDefault to 0";
          type = (types.nullOr (types.withMaximum 5 (types.withMinimum 0 types.int)));
        };
      };

      config = {
        "rotation" = mkOverride 1002 null;
        "verbosity" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecLogRotation" = {

      options = {
        "logHostPath" = mkOption {
          description = "LogHostPath is the prefix directory path for the csi log files\nDefault to /var/lib/cephcsi";
          type = (types.nullOr types.str);
        };
        "maxFiles" = mkOption {
          description = "MaxFiles is the number of logrtoate files\nDefault to 7";
          type = (types.nullOr types.int);
        };
        "maxLogSize" = mkOption {
          description = "MaxLogSize is the maximum size of the log file per csi pods";
          type = (types.nullOr (types.either types.int types.str));
        };
        "periodicity" = mkOption {
          description = "Periodicity is the periodicity of the log rotation.";
          type = (
            types.nullOr (
              types.enum [
                "hourly"
                "daily"
                "weekly"
                "monthly"
              ]
            )
          );
        };
      };

      config = {
        "logHostPath" = mkOverride 1002 null;
        "maxFiles" = mkOverride 1002 null;
        "maxLogSize" = mkOverride 1002 null;
        "periodicity" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePlugin" = {

      options = {
        "affinity" = mkOption {
          description = "Pod's affinity settings";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinity"));
        };
        "annotations" = mkOption {
          description = "Pod's annotations";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "containerExtraArgs" = mkOption {
          description = "Extra arguments for containers in the node plugin daemonset.\nThe key is the container name, the value is a list of CLI arguments.\nThe operator will not override the default CLI argument values it's setting.\nThis is more for customizing what comes as default with cephcsi.\nExamples of container names: csi-rbdplugin, driver-registrar, csi-addons,\nliveness-prometheus, etc.";
          type = (types.nullOr (types.loaOf types.str));
        };
        "enableSeLinuxHostMount" = mkOption {
          description = "Control the host mount of /etc/selinux for csi plugin pods. Defaults to false";
          type = (types.nullOr types.bool);
        };
        "imagePullPolicy" = mkOption {
          description = "To indicate the image pull policy to be applied to all the containers in the csi driver pods.";
          type = (types.nullOr types.str);
        };
        "kubeletDirPath" = mkOption {
          description = "kubelet directory path, if kubelet configured to use other than /var/lib/kubelet path.";
          type = (types.nullOr types.str);
        };
        "labels" = mkOption {
          description = "Pod's labels";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "priorityClassName" = mkOption {
          description = "Pod's user defined priority class name";
          type = (types.nullOr types.str);
        };
        "resources" = mkOption {
          description = "Resource requirements for plugin's containers";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginResources"));
        };
        "serviceAccountName" = mkOption {
          description = "Service account name to be used for driver's pods";
          type = (types.nullOr types.str);
        };
        "tolerations" = mkOption {
          description = "Pod's tolerations list";
          type = (types.nullOr (types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginTolerations")));
        };
        "topology" = mkOption {
          description = "Topology settings for the plugin pods";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginTopology"));
        };
        "updateStrategy" = mkOption {
          description = "Driver's plugin daemonset update strategy, supported values are OnDelete and RollingUpdate.\nDefault value is RollingUpdate with MaxAvailabile set to 1";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginUpdateStrategy"));
        };
        "volumes" = mkOption {
          description = "Volume and volume mount definitions to attach to the pod";
          type = (types.nullOr (types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumes")));
        };
      };

      config = {
        "affinity" = mkOverride 1002 null;
        "annotations" = mkOverride 1002 null;
        "containerExtraArgs" = mkOverride 1002 null;
        "enableSeLinuxHostMount" = mkOverride 1002 null;
        "imagePullPolicy" = mkOverride 1002 null;
        "kubeletDirPath" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "serviceAccountName" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topology" = mkOverride 1002 null;
        "updateStrategy" = mkOverride 1002 null;
        "volumes" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinity" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "Describes node affinity scheduling rules for the pod.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinity"));
        };
        "podAffinity" = mkOption {
          description = "Describes pod affinity scheduling rules (e.g. co-locate this pod in the same node, zone, etc. as some other pod(s)).";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinity"));
        };
        "podAntiAffinity" = mkOption {
          description = "Describes pod anti-affinity scheduling rules (e.g. avoid putting this pod in the same node, zone, etc. as some other pod(s)).";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinity"));
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and adding\n\"weight\" to the sum if the node matches the corresponding matchExpressions; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to an update), the system\nmay or may not try to eventually evict the pod from its node.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "A node selector term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "Weight associated with matching the corresponding nodeSelectorTerm, in the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "A list of node selector requirements by node's labels.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "A list of node selector requirements by node's fields.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "Required. A list of node selector terms. The terms are ORed.";
            type = (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "A list of node selector requirements by node's labels.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "A list of node selector requirements by node's fields.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and adding\n\"weight\" to the sum if the node has pods which matches the corresponding podAffinityTerm; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to a pod label update), the\nsystem may or may not try to eventually evict the pod from its node.\nWhen there are multiple elements, the lists of nodes corresponding to each\npodAffinityTerm are intersected, i.e. all terms must be satisfied.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "Required. A pod affinity term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "weight associated with matching the corresponding podAffinityTerm,\nin the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe anti-affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling anti-affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and subtracting\n\"weight\" from the sum if the node has pods which matches the corresponding podAffinityTerm; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the anti-affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the anti-affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to a pod label update), the\nsystem may or may not try to eventually evict the pod from its node.\nWhen there are multiple elements, the lists of nodes corresponding to each\npodAffinityTerm are intersected, i.e. all terms must be satisfied.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "Required. A pod affinity term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "weight associated with matching the corresponding podAffinityTerm,\nin the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginResources" = {

      options = {
        "addons" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginResourcesAddons"));
        };
        "liveness" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginResourcesLiveness"));
        };
        "logRotator" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginResourcesLogRotator"));
        };
        "plugin" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginResourcesPlugin"));
        };
        "registrar" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginResourcesRegistrar"));
        };
      };

      config = {
        "addons" = mkOverride 1002 null;
        "liveness" = mkOverride 1002 null;
        "logRotator" = mkOverride 1002 null;
        "plugin" = mkOverride 1002 null;
        "registrar" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesAddons" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "csi.ceph.io.v1.DriverSpecNodePluginResourcesAddonsClaims" "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesAddonsClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesLiveness" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "csi.ceph.io.v1.DriverSpecNodePluginResourcesLivenessClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesLivenessClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesLogRotator" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "csi.ceph.io.v1.DriverSpecNodePluginResourcesLogRotatorClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesLogRotatorClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesPlugin" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "csi.ceph.io.v1.DriverSpecNodePluginResourcesPluginClaims" "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesPluginClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesRegistrar" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey "csi.ceph.io.v1.DriverSpecNodePluginResourcesRegistrarClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginResourcesRegistrarClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginTolerations" = {

      options = {
        "effect" = mkOption {
          description = "Effect indicates the taint effect to match. Empty means match all taint effects.\nWhen specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "Key is the taint key that the toleration applies to. Empty means match all taint keys.\nIf the key is empty, operator must be Exists; this combination means to match all values and all keys.";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "Operator represents a key's relationship to the value.\nValid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.\nExists is equivalent to wildcard for value, so that a pod can\ntolerate all taints of a particular category.\nLt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "TolerationSeconds represents the period of time the toleration (which must be\nof effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,\nit is not set, which means tolerate the taint forever (do not evict). Zero and\nnegative values will be treated as 0 (evict immediately) by the system.";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "Value is the taint value the toleration matches to.\nIf the operator is Exists, the value should be empty, otherwise just a regular string.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginTopology" = {

      options = {
        "domainLabels" = mkOption {
          description = "Domain labels define which node labels to use as domains for CSI nodeplugins to advertise their domains";
          type = (types.listOf types.str);
        };
      };

      config = { };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginUpdateStrategy" = {

      options = {
        "rollingUpdate" = mkOption {
          description = "Rolling update config params. Present only if type = \"RollingUpdate\".";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginUpdateStrategyRollingUpdate")
          );
        };
        "type" = mkOption {
          description = "Type of daemon set update. Can be \"RollingUpdate\" or \"OnDelete\". Default is RollingUpdate.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "rollingUpdate" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginUpdateStrategyRollingUpdate" = {

      options = {
        "maxSurge" = mkOption {
          description = "The maximum number of nodes with an existing available DaemonSet pod that\ncan have an updated DaemonSet pod during during an update.\nValue can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%).\nThis can not be 0 if MaxUnavailable is 0.\nAbsolute number is calculated from percentage by rounding up to a minimum of 1.\nDefault value is 0.\nExample: when this is set to 30%, at most 30% of the total number of nodes\nthat should be running the daemon pod (i.e. status.desiredNumberScheduled)\ncan have their a new pod created before the old pod is marked as deleted.\nThe update starts by launching new pods on 30% of nodes. Once an updated\npod is available (Ready for at least minReadySeconds) the old DaemonSet pod\non that node is marked deleted. If the old pod becomes unavailable for any\nreason (Ready transitions to false, is evicted, or is drained) an updated\npod is immediately created on that node without considering surge limits.\nAllowing surge implies the possibility that the resources consumed by the\ndaemonset on any given node can double if the readiness check fails, and\nso resource intensive daemonsets should take into account that they may\ncause evictions during disruption.";
          type = (types.nullOr (types.either types.int types.str));
        };
        "maxUnavailable" = mkOption {
          description = "The maximum number of DaemonSet pods that can be unavailable during the\nupdate. Value can be an absolute number (ex: 5) or a percentage of total\nnumber of DaemonSet pods at the start of the update (ex: 10%). Absolute\nnumber is calculated from percentage by rounding up.\nThis cannot be 0 if MaxSurge is 0\nDefault value is 1.\nExample: when this is set to 30%, at most 30% of the total number of nodes\nthat should be running the daemon pod (i.e. status.desiredNumberScheduled)\ncan have their pods stopped for an update at any given time. The update\nstarts by stopping at most 30% of those DaemonSet pods and then brings\nup new DaemonSet pods in their place. Once the new pods are available,\nit then proceeds onto other DaemonSet pods, thus ensuring that at least\n70% of original number of DaemonSet pods are available at all times during\nthe update.";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "maxSurge" = mkOverride 1002 null;
        "maxUnavailable" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumes" = {

      options = {
        "mount" = mkOption {
          description = "VolumeMount describes a mounting of a Volume within a container.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesMount"));
        };
        "volume" = mkOption {
          description = "Volume represents a named volume in a pod that may be accessed by any container in the pod.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolume"));
        };
      };

      config = {
        "mount" = mkOverride 1002 null;
        "volume" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesMount" = {

      options = {
        "mountPath" = mkOption {
          description = "Path within the container at which the volume should be mounted.  Must\nnot contain ':'.";
          type = types.str;
        };
        "mountPropagation" = mkOption {
          description = "mountPropagation determines how mounts are propagated from the host\nto container and the other way around.\nWhen not set, MountPropagationNone is used.\nThis field is beta in 1.10.\nWhen RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified\n(which defaults to None).";
          type = (types.nullOr types.str);
        };
        "name" = mkOption {
          description = "This must match the Name of a Volume.";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "Mounted read-only if true, read-write otherwise (false or unspecified).\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "recursiveReadOnly" = mkOption {
          description = "RecursiveReadOnly specifies whether read-only mounts should be handled\nrecursively.\n\nIf ReadOnly is false, this field has no meaning and must be unspecified.\n\nIf ReadOnly is true, and this field is set to Disabled, the mount is not made\nrecursively read-only.  If this field is set to IfPossible, the mount is made\nrecursively read-only, if it is supported by the container runtime.  If this\nfield is set to Enabled, the mount is made recursively read-only if it is\nsupported by the container runtime, otherwise the pod will not be started and\nan error will be generated to indicate the reason.\n\nIf this field is set to IfPossible or Enabled, MountPropagation must be set to\nNone (or be unspecified, which defaults to None).\n\nIf this field is not specified, it is treated as an equivalent of Disabled.";
          type = (types.nullOr types.str);
        };
        "subPath" = mkOption {
          description = "Path within the volume from which the container's volume should be mounted.\nDefaults to \"\" (volume's root).";
          type = (types.nullOr types.str);
        };
        "subPathExpr" = mkOption {
          description = "Expanded path within the volume from which the container's volume should be mounted.\nBehaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.\nDefaults to \"\" (volume's root).\nSubPathExpr and SubPath are mutually exclusive.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "mountPropagation" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "recursiveReadOnly" = mkOverride 1002 null;
        "subPath" = mkOverride 1002 null;
        "subPathExpr" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolume" = {

      options = {
        "awsElasticBlockStore" = mkOption {
          description = "awsElasticBlockStore represents an AWS Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nDeprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree\nawsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeAwsElasticBlockStore")
          );
        };
        "azureDisk" = mkOption {
          description = "azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod.\nDeprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type\nare redirected to the disk.csi.azure.com CSI driver.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeAzureDisk"));
        };
        "azureFile" = mkOption {
          description = "azureFile represents an Azure File Service mount on the host and bind mount to the pod.\nDeprecated: AzureFile is deprecated. All operations for the in-tree azureFile type\nare redirected to the file.csi.azure.com CSI driver.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeAzureFile"));
        };
        "cephfs" = mkOption {
          description = "cephFS represents a Ceph FS mount on the host that shares a pod's lifetime.\nDeprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCephfs"));
        };
        "cinder" = mkOption {
          description = "cinder represents a cinder volume attached and mounted on kubelets host machine.\nDeprecated: Cinder is deprecated. All operations for the in-tree cinder type\nare redirected to the cinder.csi.openstack.org CSI driver.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCinder"));
        };
        "configMap" = mkOption {
          description = "configMap represents a configMap that should populate this volume";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeConfigMap"));
        };
        "csi" = mkOption {
          description = "csi (Container Storage Interface) represents ephemeral storage that is handled by certain external CSI drivers.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCsi"));
        };
        "downwardAPI" = mkOption {
          description = "downwardAPI represents downward API about the pod that should populate this volume";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeDownwardAPI"));
        };
        "emptyDir" = mkOption {
          description = "emptyDir represents a temporary directory that shares a pod's lifetime.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEmptyDir"));
        };
        "ephemeral" = mkOption {
          description = "ephemeral represents a volume that is handled by a cluster storage driver.\nThe volume's lifecycle is tied to the pod that defines it - it will be created before the pod starts,\nand deleted when the pod is removed.\n\nUse this if:\na) the volume is only needed while the pod runs,\nb) features of normal volumes like restoring from snapshot or capacity\n   tracking are needed,\nc) the storage driver is specified through a storage class, and\nd) the storage driver supports dynamic volume provisioning through\n   a PersistentVolumeClaim (see EphemeralVolumeSource for more\n   information on the connection between this volume type\n   and PersistentVolumeClaim).\n\nUse PersistentVolumeClaim or one of the vendor-specific\nAPIs for volumes that persist for longer than the lifecycle\nof an individual pod.\n\nUse CSI for light-weight local ephemeral volumes if the CSI driver is meant to\nbe used that way - see the documentation of the driver for\nmore information.\n\nA pod can use both types of ephemeral volumes and\npersistent volumes at the same time.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeral"));
        };
        "fc" = mkOption {
          description = "fc represents a Fibre Channel resource that is attached to a kubelet's host machine and then exposed to the pod.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeFc"));
        };
        "flexVolume" = mkOption {
          description = "flexVolume represents a generic volume resource that is\nprovisioned/attached using an exec based plugin.\nDeprecated: FlexVolume is deprecated. Consider using a CSIDriver instead.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeFlexVolume"));
        };
        "flocker" = mkOption {
          description = "flocker represents a Flocker volume attached to a kubelet's host machine. This depends on the Flocker control service being running.\nDeprecated: Flocker is deprecated and the in-tree flocker type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeFlocker"));
        };
        "gcePersistentDisk" = mkOption {
          description = "gcePersistentDisk represents a GCE Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nDeprecated: GCEPersistentDisk is deprecated. All operations for the in-tree\ngcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeGcePersistentDisk")
          );
        };
        "gitRepo" = mkOption {
          description = "gitRepo represents a git repository at a particular revision.\nDeprecated: GitRepo is deprecated. To provision a container with a git repo, mount an\nEmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir\ninto the Pod's container.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeGitRepo"));
        };
        "glusterfs" = mkOption {
          description = "glusterfs represents a Glusterfs mount on the host that shares a pod's lifetime.\nDeprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeGlusterfs"));
        };
        "hostPath" = mkOption {
          description = "hostPath represents a pre-existing file or directory on the host\nmachine that is directly exposed to the container. This is generally\nused for system agents or other privileged things that are allowed\nto see the host machine. Most containers will NOT need this.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeHostPath"));
        };
        "image" = mkOption {
          description = "image represents an OCI object (a container image or artifact) pulled and mounted on the kubelet's host machine.\nThe volume is resolved at pod startup depending on which PullPolicy value is provided:\n\n- Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.\n- Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.\n- IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.\n\nThe volume gets re-resolved if the pod gets deleted and recreated, which means that new remote content will become available on pod recreation.\nA failure to resolve or pull the image during pod startup will block containers from starting and may add significant latency. Failures will be retried using normal volume backoff and will be reported on the pod reason and message.\nThe types of objects that may be mounted by this volume are defined by the container runtime implementation on a host machine and at minimum must include all valid types supported by the container image field.\nThe OCI object gets mounted in a single directory (spec.containers[*].volumeMounts.mountPath) by merging the manifest layers in the same way as for container images.\nThe volume will be mounted read-only (ro) and non-executable files (noexec).\nSub path mounts for containers are not supported (spec.containers[*].volumeMounts.subpath) before 1.33.\nThe field spec.securityContext.fsGroupChangePolicy has no effect on this volume type.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeImage"));
        };
        "iscsi" = mkOption {
          description = "iscsi represents an ISCSI Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes/#iscsi";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeIscsi"));
        };
        "name" = mkOption {
          description = "name of the volume.\nMust be a DNS_LABEL and unique within the pod.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = types.str;
        };
        "nfs" = mkOption {
          description = "nfs represents an NFS mount on the host that shares a pod's lifetime\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeNfs"));
        };
        "persistentVolumeClaim" = mkOption {
          description = "persistentVolumeClaimVolumeSource represents a reference to a\nPersistentVolumeClaim in the same namespace.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumePersistentVolumeClaim")
          );
        };
        "photonPersistentDisk" = mkOption {
          description = "photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine.\nDeprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumePhotonPersistentDisk")
          );
        };
        "portworxVolume" = mkOption {
          description = "portworxVolume represents a portworx volume attached and mounted on kubelets host machine.\nDeprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type\nare redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate\nis on.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumePortworxVolume")
          );
        };
        "projected" = mkOption {
          description = "projected items for all in one resources secrets, configmaps, and downward API";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjected"));
        };
        "quobyte" = mkOption {
          description = "quobyte represents a Quobyte mount on the host that shares a pod's lifetime.\nDeprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeQuobyte"));
        };
        "rbd" = mkOption {
          description = "rbd represents a Rados Block Device mount on the host that shares a pod's lifetime.\nDeprecated: RBD is deprecated and the in-tree rbd type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeRbd"));
        };
        "scaleIO" = mkOption {
          description = "scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes.\nDeprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeScaleIO"));
        };
        "secret" = mkOption {
          description = "secret represents a secret that should populate this volume.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#secret";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeSecret"));
        };
        "storageos" = mkOption {
          description = "storageOS represents a StorageOS volume attached and mounted on Kubernetes nodes.\nDeprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeStorageos"));
        };
        "vsphereVolume" = mkOption {
          description = "vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine.\nDeprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type\nare redirected to the csi.vsphere.vmware.com CSI driver.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeVsphereVolume"));
        };
      };

      config = {
        "awsElasticBlockStore" = mkOverride 1002 null;
        "azureDisk" = mkOverride 1002 null;
        "azureFile" = mkOverride 1002 null;
        "cephfs" = mkOverride 1002 null;
        "cinder" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "csi" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "emptyDir" = mkOverride 1002 null;
        "ephemeral" = mkOverride 1002 null;
        "fc" = mkOverride 1002 null;
        "flexVolume" = mkOverride 1002 null;
        "flocker" = mkOverride 1002 null;
        "gcePersistentDisk" = mkOverride 1002 null;
        "gitRepo" = mkOverride 1002 null;
        "glusterfs" = mkOverride 1002 null;
        "hostPath" = mkOverride 1002 null;
        "image" = mkOverride 1002 null;
        "iscsi" = mkOverride 1002 null;
        "nfs" = mkOverride 1002 null;
        "persistentVolumeClaim" = mkOverride 1002 null;
        "photonPersistentDisk" = mkOverride 1002 null;
        "portworxVolume" = mkOverride 1002 null;
        "projected" = mkOverride 1002 null;
        "quobyte" = mkOverride 1002 null;
        "rbd" = mkOverride 1002 null;
        "scaleIO" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "storageos" = mkOverride 1002 null;
        "vsphereVolume" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeAwsElasticBlockStore" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (types.nullOr types.str);
        };
        "partition" = mkOption {
          description = "partition is the partition in the volume that you want to mount.\nIf omitted, the default is to mount by volume name.\nExamples: For volume /dev/sda1, you specify the partition as \"1\".\nSimilarly, the volume partition for /dev/sda is \"0\" (or you can leave the property empty).";
          type = (types.nullOr types.int);
        };
        "readOnly" = mkOption {
          description = "readOnly value true will force the readOnly setting in VolumeMounts.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (types.nullOr types.bool);
        };
        "volumeID" = mkOption {
          description = "volumeID is unique ID of the persistent disk resource in AWS (Amazon EBS volume).\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "partition" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeAzureDisk" = {

      options = {
        "cachingMode" = mkOption {
          description = "cachingMode is the Host Caching mode: None, Read Only, Read Write.";
          type = (types.nullOr types.str);
        };
        "diskName" = mkOption {
          description = "diskName is the Name of the data disk in the blob storage";
          type = types.str;
        };
        "diskURI" = mkOption {
          description = "diskURI is the URI of data disk in the blob storage";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType is Filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "kind expected values are Shared: multiple blob disks per storage account  Dedicated: single blob disk per storage account  Managed: azure managed data disk (only in managed availability set). defaults to shared";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "cachingMode" = mkOverride 1002 null;
        "fsType" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeAzureFile" = {

      options = {
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "secretName is the  name of secret that contains Azure Storage Account Name and Key";
          type = types.str;
        };
        "shareName" = mkOption {
          description = "shareName is the azure share Name";
          type = types.str;
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCephfs" = {

      options = {
        "monitors" = mkOption {
          description = "monitors is Required: Monitors is a collection of Ceph monitors\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.listOf types.str);
        };
        "path" = mkOption {
          description = "path is Optional: Used as the mounted root, rather than the full Ceph tree, default is /";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.bool);
        };
        "secretFile" = mkOption {
          description = "secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "secretRef" = mkOption {
          description = "secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty.\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCephfsSecretRef")
          );
        };
        "user" = mkOption {
          description = "user is optional: User is the rados user name, default is admin\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "path" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretFile" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCephfsSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCinder" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is optional: points to a secret object containing parameters used to connect\nto OpenStack.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCinderSecretRef")
          );
        };
        "volumeID" = mkOption {
          description = "volumeID used to identify the volume in cinder.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCinderSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeConfigMap" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode is optional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDefaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "items if unspecified, each key-value pair in the Data field of the referenced\nConfigMap will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the ConfigMap,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeConfigMapItems")
            )
          );
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "optional specify whether the ConfigMap or its keys must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCsi" = {

      options = {
        "driver" = mkOption {
          description = "driver is the name of the CSI driver that handles this volume.\nConsult with your admin for the correct name as registered in the cluster.";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType to mount. Ex. \"ext4\", \"xfs\", \"ntfs\".\nIf not provided, the empty value is passed to the associated CSI driver\nwhich will determine the default filesystem to apply.";
          type = (types.nullOr types.str);
        };
        "nodePublishSecretRef" = mkOption {
          description = "nodePublishSecretRef is a reference to the secret object containing\nsensitive information to pass to the CSI driver to complete the CSI\nNodePublishVolume and NodeUnpublishVolume calls.\nThis field is optional, and  may be empty if no secret is required. If the\nsecret object contains more than one secret, all secret references are passed.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCsiNodePublishSecretRef")
          );
        };
        "readOnly" = mkOption {
          description = "readOnly specifies a read-only configuration for the volume.\nDefaults to false (read/write).";
          type = (types.nullOr types.bool);
        };
        "volumeAttributes" = mkOption {
          description = "volumeAttributes stores driver-specific properties that are passed to the CSI\ndriver. Consult your driver's documentation for supported values.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "nodePublishSecretRef" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "volumeAttributes" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeCsiNodePublishSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeDownwardAPI" = {

      options = {
        "defaultMode" = mkOption {
          description = "Optional: mode bits to use on created files by default. Must be a\nOptional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDefaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "Items is a list of downward API volume file";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeDownwardAPIItems")
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeDownwardAPIItems" = {

      options = {
        "fieldRef" = mkOption {
          description = "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeDownwardAPIItemsFieldRef"
            )
          );
        };
        "mode" = mkOption {
          description = "Optional: mode bits used to set permissions on this file, must be an octal value\nbetween 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the '..' path. Must be utf-8 encoded. The first item of the relative path must not start with '..'";
          type = types.str;
        };
        "resourceFieldRef" = mkOption {
          description = "Selects a resource of the container: only resources limits and requests\n(limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeDownwardAPIItemsResourceFieldRef"
            )
          );
        };
      };

      config = {
        "fieldRef" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "resourceFieldRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeDownwardAPIItemsFieldRef" = {

      options = {
        "apiVersion" = mkOption {
          description = "Version of the schema the FieldPath is written in terms of, defaults to \"v1\".";
          type = (types.nullOr types.str);
        };
        "fieldPath" = mkOption {
          description = "Path of the field to select in the specified API version.";
          type = types.str;
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeDownwardAPIItemsResourceFieldRef" = {

      options = {
        "containerName" = mkOption {
          description = "Container name: required for volumes, optional for env vars";
          type = (types.nullOr types.str);
        };
        "divisor" = mkOption {
          description = "Specifies the output format of the exposed resources, defaults to \"1\"";
          type = (types.nullOr (types.either types.int types.str));
        };
        "resource" = mkOption {
          description = "Required: resource to select";
          type = types.str;
        };
      };

      config = {
        "containerName" = mkOverride 1002 null;
        "divisor" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEmptyDir" = {

      options = {
        "medium" = mkOption {
          description = "medium represents what type of storage medium should back this directory.\nThe default is \"\" which means to use the node's default medium.\nMust be an empty string (default) or Memory.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (types.nullOr types.str);
        };
        "sizeLimit" = mkOption {
          description = "sizeLimit is the total amount of local storage required for this EmptyDir volume.\nThe size limit is also applicable for memory medium.\nThe maximum usage on memory medium EmptyDir would be the minimum value between\nthe SizeLimit specified here and the sum of memory limits of all containers in a pod.\nThe default is nil which means that the limit is undefined.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "medium" = mkOverride 1002 null;
        "sizeLimit" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeral" = {

      options = {
        "volumeClaimTemplate" = mkOption {
          description = "Will be used to create a stand-alone PVC to provision the volume.\nThe pod in which this EphemeralVolumeSource is embedded will be the\nowner of the PVC, i.e. the PVC will be deleted together with the\npod.  The name of the PVC will be `<pod name>-<volume name>` where\n`<volume name>` is the name from the `PodSpec.Volumes` array\nentry. Pod validation will reject the pod if the concatenated name\nis not valid for a PVC (for example, too long).\n\nAn existing PVC with that name that is not owned by the pod\nwill *not* be used for the pod to avoid using an unrelated\nvolume by mistake. Starting the pod is then blocked until\nthe unrelated PVC is removed. If such a pre-created PVC is\nmeant to be used by the pod, the PVC has to updated with an\nowner reference to the pod once the pod exists. Normally\nthis should not be necessary, but it may be useful when\nmanually reconstructing a broken cluster.\n\nThis field is read-only and no changes will be made by Kubernetes\nto the PVC after it has been created.\n\nRequired, must not be nil.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplate"
            )
          );
        };
      };

      config = {
        "volumeClaimTemplate" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplate" = {

      options = {
        "metadata" = mkOption {
          description = "May contain labels and annotations that will be copied into the PVC\nwhen creating it. No other fields are allowed and will be rejected during\nvalidation.";
          type = (types.nullOr types.attrs);
        };
        "spec" = mkOption {
          description = "The specification for the PersistentVolumeClaim. The entire content is\ncopied unchanged into the PVC that gets created from this\ntemplate. The same fields as in a PersistentVolumeClaim\nare also valid here.";
          type = (
            submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpec"
          );
        };
      };

      config = {
        "metadata" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpec" = {

      options = {
        "accessModes" = mkOption {
          description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
          type = (types.nullOr (types.listOf types.str));
        };
        "dataSource" = mkOption {
          description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSource"
            )
          );
        };
        "dataSourceRef" = mkOption {
          description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSourceRef"
            )
          );
        };
        "resources" = mkOption {
          description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecResources"
            )
          );
        };
        "selector" = mkOption {
          description = "selector is a label query over volumes to consider for binding.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelector"
            )
          );
        };
        "storageClassName" = mkOption {
          description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
          type = (types.nullOr types.str);
        };
        "volumeAttributesClassName" = mkOption {
          description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
          type = (types.nullOr types.str);
        };
        "volumeMode" = mkOption {
          description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
          type = (types.nullOr types.str);
        };
        "volumeName" = mkOption {
          description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "accessModes" = mkOverride 1002 null;
        "dataSource" = mkOverride 1002 null;
        "dataSourceRef" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "selector" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
        "volumeAttributesClassName" = mkOverride 1002 null;
        "volumeMode" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSource" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSourceRef" = {

      options = {
        "apiGroup" = mkOption {
          description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is the type of resource being referenced";
          type = types.str;
        };
        "name" = mkOption {
          description = "Name is the name of resource being referenced";
          type = types.str;
        };
        "namespace" = mkOption {
          description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "apiGroup" = mkOverride 1002 null;
        "namespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecResources" = {

      options = {
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelector" = {

      options = {
        "matchExpressions" = mkOption {
          description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelectorMatchExpressions"
              )
            )
          );
        };
        "matchLabels" = mkOption {
          description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "matchExpressions" = mkOverride 1002 null;
        "matchLabels" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeFc" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "lun" = mkOption {
          description = "lun is Optional: FC target lun number";
          type = (types.nullOr types.int);
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "targetWWNs" = mkOption {
          description = "targetWWNs is Optional: FC target worldwide names (WWNs)";
          type = (types.nullOr (types.listOf types.str));
        };
        "wwids" = mkOption {
          description = "wwids Optional: FC volume world wide identifiers (wwids)\nEither wwids or combination of targetWWNs and lun must be set, but not both simultaneously.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "lun" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "targetWWNs" = mkOverride 1002 null;
        "wwids" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeFlexVolume" = {

      options = {
        "driver" = mkOption {
          description = "driver is the name of the driver to use for this volume.";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". The default filesystem depends on FlexVolume script.";
          type = (types.nullOr types.str);
        };
        "options" = mkOption {
          description = "options is Optional: this field holds extra command options if any.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is Optional: secretRef is reference to the secret object containing\nsensitive information to pass to the plugin scripts. This may be\nempty if no secret object is specified. If the secret object\ncontains more than one secret, all secrets are passed to the plugin\nscripts.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeFlexVolumeSecretRef")
          );
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "options" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeFlexVolumeSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeFlocker" = {

      options = {
        "datasetName" = mkOption {
          description = "datasetName is Name of the dataset stored as metadata -> name on the dataset for Flocker\nshould be considered as deprecated";
          type = (types.nullOr types.str);
        };
        "datasetUUID" = mkOption {
          description = "datasetUUID is the UUID of the dataset. This is unique identifier of a Flocker dataset";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "datasetName" = mkOverride 1002 null;
        "datasetUUID" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeGcePersistentDisk" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (types.nullOr types.str);
        };
        "partition" = mkOption {
          description = "partition is the partition in the volume that you want to mount.\nIf omitted, the default is to mount by volume name.\nExamples: For volume /dev/sda1, you specify the partition as \"1\".\nSimilarly, the volume partition for /dev/sda is \"0\" (or you can leave the property empty).\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (types.nullOr types.int);
        };
        "pdName" = mkOption {
          description = "pdName is unique name of the PD resource in GCE. Used to identify the disk in GCE.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "partition" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeGitRepo" = {

      options = {
        "directory" = mkOption {
          description = "directory is the target directory name.\nMust not contain or start with '..'.  If '.' is supplied, the volume directory will be the\ngit repository.  Otherwise, if specified, the volume will contain the git repository in\nthe subdirectory with the given name.";
          type = (types.nullOr types.str);
        };
        "repository" = mkOption {
          description = "repository is the URL";
          type = types.str;
        };
        "revision" = mkOption {
          description = "revision is the commit hash for the specified revision.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "directory" = mkOverride 1002 null;
        "revision" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeGlusterfs" = {

      options = {
        "endpoints" = mkOption {
          description = "endpoints is the endpoint name that details Glusterfs topology.";
          type = types.str;
        };
        "path" = mkOption {
          description = "path is the Glusterfs volume path.\nMore info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the Glusterfs volume to be mounted with read-only permissions.\nDefaults to false.\nMore info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeHostPath" = {

      options = {
        "path" = mkOption {
          description = "path of the directory on the host.\nIf the path is a symlink, it will follow the link to the real path.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = types.str;
        };
        "type" = mkOption {
          description = "type for HostPath Volume\nDefaults to \"\"\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeImage" = {

      options = {
        "pullPolicy" = mkOption {
          description = "Policy for pulling OCI objects. Possible values are:\nAlways: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.\nNever: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.\nIfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.\nDefaults to Always if :latest tag is specified, or IfNotPresent otherwise.";
          type = (types.nullOr types.str);
        };
        "reference" = mkOption {
          description = "Required: Image or artifact reference to be used.\nBehaves in the same way as pod.spec.containers[*].image.\nPull secrets will be assembled in the same way as for the container image by looking up node credentials, SA image pull secrets, and pod spec image pull secrets.\nMore info: https://kubernetes.io/docs/concepts/containers/images\nThis field is optional to allow higher level config management to default or override\ncontainer images in workload controllers like Deployments and StatefulSets.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "pullPolicy" = mkOverride 1002 null;
        "reference" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeIscsi" = {

      options = {
        "chapAuthDiscovery" = mkOption {
          description = "chapAuthDiscovery defines whether support iSCSI Discovery CHAP authentication";
          type = (types.nullOr types.bool);
        };
        "chapAuthSession" = mkOption {
          description = "chapAuthSession defines whether support iSCSI Session CHAP authentication";
          type = (types.nullOr types.bool);
        };
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi";
          type = (types.nullOr types.str);
        };
        "initiatorName" = mkOption {
          description = "initiatorName is the custom iSCSI Initiator Name.\nIf initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface\n<target portal>:<volume name> will be created for the connection.";
          type = (types.nullOr types.str);
        };
        "iqn" = mkOption {
          description = "iqn is the target iSCSI Qualified Name.";
          type = types.str;
        };
        "iscsiInterface" = mkOption {
          description = "iscsiInterface is the interface Name that uses an iSCSI transport.\nDefaults to 'default' (tcp).";
          type = (types.nullOr types.str);
        };
        "lun" = mkOption {
          description = "lun represents iSCSI Target Lun number.";
          type = types.int;
        };
        "portals" = mkOption {
          description = "portals is the iSCSI Target Portal List. The portal is either an IP or ip_addr:port if the port\nis other than default (typically TCP ports 860 and 3260).";
          type = (types.nullOr (types.listOf types.str));
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is the CHAP Secret for iSCSI target and initiator authentication";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeIscsiSecretRef")
          );
        };
        "targetPortal" = mkOption {
          description = "targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port\nis other than default (typically TCP ports 860 and 3260).";
          type = types.str;
        };
      };

      config = {
        "chapAuthDiscovery" = mkOverride 1002 null;
        "chapAuthSession" = mkOverride 1002 null;
        "fsType" = mkOverride 1002 null;
        "initiatorName" = mkOverride 1002 null;
        "iscsiInterface" = mkOverride 1002 null;
        "portals" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeIscsiSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeNfs" = {

      options = {
        "path" = mkOption {
          description = "path that is exported by the NFS server.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the NFS export to be mounted with read-only permissions.\nDefaults to false.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = (types.nullOr types.bool);
        };
        "server" = mkOption {
          description = "server is the hostname or IP address of the NFS server.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = types.str;
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumePersistentVolumeClaim" = {

      options = {
        "claimName" = mkOption {
          description = "claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly Will force the ReadOnly setting in VolumeMounts.\nDefault false.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumePhotonPersistentDisk" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "pdID" = mkOption {
          description = "pdID is the ID that identifies Photon Controller persistent disk";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumePortworxVolume" = {

      options = {
        "fsType" = mkOption {
          description = "fSType represents the filesystem type to mount\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "volumeID" = mkOption {
          description = "volumeID uniquely identifies a Portworx volume";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjected" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode are the mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "sources" = mkOption {
          description = "sources is the list of volume projections. Each entry in this list\nhandles one source.";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSources")
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "sources" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSources" = {

      options = {
        "clusterTrustBundle" = mkOption {
          description = "ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field\nof ClusterTrustBundle objects in an auto-updating file.\n\nAlpha, gated by the ClusterTrustBundleProjection feature gate.\n\nClusterTrustBundle objects can either be selected by name, or by the\ncombination of signer name and a label selector.\n\nKubelet performs aggressive normalization of the PEM contents written\ninto the pod filesystem.  Esoteric PEM features such as inter-block\ncomments and block headers are stripped.  Certificates are deduplicated.\nThe ordering of certificates within the file is arbitrary, and Kubelet\nmay change the order over time.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesClusterTrustBundle"
            )
          );
        };
        "configMap" = mkOption {
          description = "configMap information about the configMap data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesConfigMap"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "downwardAPI information about the downwardAPI data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesDownwardAPI"
            )
          );
        };
        "podCertificate" = mkOption {
          description = "Projects an auto-rotating credential bundle (private key and certificate\nchain) that the pod can use either as a TLS client or server.\n\nKubelet generates a private key and uses it to send a\nPodCertificateRequest to the named signer.  Once the signer approves the\nrequest and issues a certificate chain, Kubelet writes the key and\ncertificate chain to the pod filesystem.  The pod does not start until\ncertificates have been issued for each podCertificate projected volume\nsource in its spec.\n\nKubelet will begin trying to rotate the certificate at the time indicated\nby the signer using the PodCertificateRequest.Status.BeginRefreshAt\ntimestamp.\n\nKubelet can write a single file, indicated by the credentialBundlePath\nfield, or separate files, indicated by the keyPath and\ncertificateChainPath fields.\n\nThe credential bundle is a single file in PEM format.  The first PEM\nentry is the private key (in PKCS#8 format), and the remaining PEM\nentries are the certificate chain issued by the signer (typically,\nsigners will return their certificate chain in leaf-to-root order).\n\nPrefer using the credential bundle format, since your application code\ncan read it atomically.  If you use keyPath and certificateChainPath,\nyour application must make two separate file reads. If these coincide\nwith a certificate rotation, it is possible that the private key and leaf\ncertificate you read may not correspond to each other.  Your application\nwill need to check for this condition, and re-read until they are\nconsistent.\n\nThe named signer controls chooses the format of the certificate it\nissues; consult the signer implementation's documentation to learn how to\nuse the certificates it issues.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesPodCertificate"
            )
          );
        };
        "secret" = mkOption {
          description = "secret information about the secret data to project";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesSecret")
          );
        };
        "serviceAccountToken" = mkOption {
          description = "serviceAccountToken is information about the serviceAccountToken data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesServiceAccountToken"
            )
          );
        };
      };

      config = {
        "clusterTrustBundle" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "podCertificate" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "serviceAccountToken" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesClusterTrustBundle" = {

      options = {
        "labelSelector" = mkOption {
          description = "Select all ClusterTrustBundles that match this label selector.  Only has\neffect if signerName is set.  Mutually-exclusive with name.  If unset,\ninterpreted as \"match nothing\".  If set but empty, interpreted as \"match\neverything\".";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelector"
            )
          );
        };
        "name" = mkOption {
          description = "Select a single ClusterTrustBundle by object name.  Mutually-exclusive\nwith signerName and labelSelector.";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "If true, don't block pod startup if the referenced ClusterTrustBundle(s)\naren't available.  If using name, then the named ClusterTrustBundle is\nallowed not to exist.  If using signerName, then the combination of\nsignerName and labelSelector is allowed to match zero\nClusterTrustBundles.";
          type = (types.nullOr types.bool);
        };
        "path" = mkOption {
          description = "Relative path from the volume root to write the bundle.";
          type = types.str;
        };
        "signerName" = mkOption {
          description = "Select all ClusterTrustBundles that match this signer name.\nMutually-exclusive with name.  The contents of all selected\nClusterTrustBundles will be unified and deduplicated.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "labelSelector" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "signerName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesConfigMap" = {

      options = {
        "items" = mkOption {
          description = "items if unspecified, each key-value pair in the Data field of the referenced\nConfigMap will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the ConfigMap,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "optional specify whether the ConfigMap or its keys must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesDownwardAPI" = {

      options = {
        "items" = mkOption {
          description = "Items is a list of DownwardAPIVolume file";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesDownwardAPIItems"
              )
            )
          );
        };
      };

      config = {
        "items" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesDownwardAPIItems" = {

      options = {
        "fieldRef" = mkOption {
          description = "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesDownwardAPIItemsFieldRef"
            )
          );
        };
        "mode" = mkOption {
          description = "Optional: mode bits used to set permissions on this file, must be an octal value\nbetween 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the '..' path. Must be utf-8 encoded. The first item of the relative path must not start with '..'";
          type = types.str;
        };
        "resourceFieldRef" = mkOption {
          description = "Selects a resource of the container: only resources limits and requests\n(limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesDownwardAPIItemsResourceFieldRef"
            )
          );
        };
      };

      config = {
        "fieldRef" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "resourceFieldRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesDownwardAPIItemsFieldRef" = {

      options = {
        "apiVersion" = mkOption {
          description = "Version of the schema the FieldPath is written in terms of, defaults to \"v1\".";
          type = (types.nullOr types.str);
        };
        "fieldPath" = mkOption {
          description = "Path of the field to select in the specified API version.";
          type = types.str;
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "Container name: required for volumes, optional for env vars";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "Specifies the output format of the exposed resources, defaults to \"1\"";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "Required: resource to select";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesPodCertificate" = {

      options = {
        "certificateChainPath" = mkOption {
          description = "Write the certificate chain at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath\nand certificateChainPath, your application needs to check that the key\nand leaf certificate are consistent, because it is possible to read the\nfiles mid-rotation.";
          type = (types.nullOr types.str);
        };
        "credentialBundlePath" = mkOption {
          description = "Write the credential bundle at this path in the projected volume.\n\nThe credential bundle is a single file that contains multiple PEM blocks.\nThe first PEM block is a PRIVATE KEY block, containing a PKCS#8 private\nkey.\n\nThe remaining blocks are CERTIFICATE blocks, containing the issued\ncertificate chain from the signer (leaf and any intermediates).\n\nUsing credentialBundlePath lets your Pod's application code make a single\natomic read that retrieves a consistent key and certificate chain.  If you\nproject them to separate files, your application code will need to\nadditionally check that the leaf certificate was issued to the key.";
          type = (types.nullOr types.str);
        };
        "keyPath" = mkOption {
          description = "Write the key at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath\nand certificateChainPath, your application needs to check that the key\nand leaf certificate are consistent, because it is possible to read the\nfiles mid-rotation.";
          type = (types.nullOr types.str);
        };
        "keyType" = mkOption {
          description = "The type of keypair Kubelet will generate for the pod.\n\nValid values are \"RSA3072\", \"RSA4096\", \"ECDSAP256\", \"ECDSAP384\",\n\"ECDSAP521\", and \"ED25519\".";
          type = types.str;
        };
        "maxExpirationSeconds" = mkOption {
          description = "maxExpirationSeconds is the maximum lifetime permitted for the\ncertificate.\n\nKubelet copies this value verbatim into the PodCertificateRequests it\ngenerates for this projection.\n\nIf omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver\nwill reject values shorter than 3600 (1 hour).  The maximum allowable\nvalue is 7862400 (91 days).\n\nThe signer implementation is then free to issue a certificate with any\nlifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600\nseconds (1 hour).  This constraint is enforced by kube-apiserver.\n`kubernetes.io` signers will never issue certificates with a lifetime\nlonger than 24 hours.";
          type = (types.nullOr types.int);
        };
        "signerName" = mkOption {
          description = "Kubelet's generated CSRs will be addressed to this signer.";
          type = types.str;
        };
        "userAnnotations" = mkOption {
          description = "userAnnotations allow pod authors to pass additional information to\nthe signer implementation.  Kubernetes does not restrict or validate this\nmetadata in any way.\n\nThese values are copied verbatim into the `spec.unverifiedUserAnnotations` field of\nthe PodCertificateRequest objects that Kubelet creates.\n\nEntries are subject to the same validation as object metadata annotations,\nwith the addition that all keys must be domain-prefixed. No restrictions\nare placed on values, except an overall size limitation on the entire field.\n\nSigners should document the keys and values they support. Signers should\ndeny requests that contain keys they do not recognize.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "certificateChainPath" = mkOverride 1002 null;
        "credentialBundlePath" = mkOverride 1002 null;
        "keyPath" = mkOverride 1002 null;
        "maxExpirationSeconds" = mkOverride 1002 null;
        "userAnnotations" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesSecret" = {

      options = {
        "items" = mkOption {
          description = "items if unspecified, each key-value pair in the Data field of the referenced\nSecret will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the Secret,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesSecretItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "optional field specify whether the Secret or its key must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesSecretItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeProjectedSourcesServiceAccountToken" = {

      options = {
        "audience" = mkOption {
          description = "audience is the intended audience of the token. A recipient of a token\nmust identify itself with an identifier specified in the audience of the\ntoken, and otherwise should reject the token. The audience defaults to the\nidentifier of the apiserver.";
          type = (types.nullOr types.str);
        };
        "expirationSeconds" = mkOption {
          description = "expirationSeconds is the requested duration of validity of the service\naccount token. As the token approaches expiration, the kubelet volume\nplugin will proactively rotate the service account token. The kubelet will\nstart trying to rotate the token if the token is older than 80 percent of\nits time to live or if the token is older than 24 hours.Defaults to 1 hour\nand must be at least 10 minutes.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the path relative to the mount point of the file to project the\ntoken into.";
          type = types.str;
        };
      };

      config = {
        "audience" = mkOverride 1002 null;
        "expirationSeconds" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeQuobyte" = {

      options = {
        "group" = mkOption {
          description = "group to map volume access to\nDefault is no group";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the Quobyte volume to be mounted with read-only permissions.\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "registry" = mkOption {
          description = "registry represents a single or multiple Quobyte Registry services\nspecified as a string as host:port pair (multiple entries are separated with commas)\nwhich acts as the central registry for volumes";
          type = types.str;
        };
        "tenant" = mkOption {
          description = "tenant owning the given Quobyte volume in the Backend\nUsed with dynamically provisioned Quobyte volumes, value is set by the plugin";
          type = (types.nullOr types.str);
        };
        "user" = mkOption {
          description = "user to map volume access to\nDefaults to serivceaccount user";
          type = (types.nullOr types.str);
        };
        "volume" = mkOption {
          description = "volume is a string that references an already created Quobyte volume by name.";
          type = types.str;
        };
      };

      config = {
        "group" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "tenant" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeRbd" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#rbd";
          type = (types.nullOr types.str);
        };
        "image" = mkOption {
          description = "image is the rados image name.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = types.str;
        };
        "keyring" = mkOption {
          description = "keyring is the path to key ring for RBDUser.\nDefault is /etc/ceph/keyring.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "monitors" = mkOption {
          description = "monitors is a collection of Ceph monitors.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.listOf types.str);
        };
        "pool" = mkOption {
          description = "pool is the rados pool name.\nDefault is rbd.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is name of the authentication secret for RBDUser. If provided\noverrides keyring.\nDefault is nil.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeRbdSecretRef"));
        };
        "user" = mkOption {
          description = "user is the rados user name.\nDefault is admin.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "keyring" = mkOverride 1002 null;
        "pool" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeRbdSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeScaleIO" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\".\nDefault is \"xfs\".";
          type = (types.nullOr types.str);
        };
        "gateway" = mkOption {
          description = "gateway is the host address of the ScaleIO API Gateway.";
          type = types.str;
        };
        "protectionDomain" = mkOption {
          description = "protectionDomain is the name of the ScaleIO Protection Domain for the configured storage.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef references to the secret for ScaleIO user and other\nsensitive information. If this is not provided, Login operation will fail.";
          type = (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeScaleIOSecretRef");
        };
        "sslEnabled" = mkOption {
          description = "sslEnabled Flag enable/disable SSL communication with Gateway, default false";
          type = (types.nullOr types.bool);
        };
        "storageMode" = mkOption {
          description = "storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned.\nDefault is ThinProvisioned.";
          type = (types.nullOr types.str);
        };
        "storagePool" = mkOption {
          description = "storagePool is the ScaleIO Storage Pool associated with the protection domain.";
          type = (types.nullOr types.str);
        };
        "system" = mkOption {
          description = "system is the name of the storage system as configured in ScaleIO.";
          type = types.str;
        };
        "volumeName" = mkOption {
          description = "volumeName is the name of a volume already created in the ScaleIO system\nthat is associated with this volume source.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "protectionDomain" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "sslEnabled" = mkOverride 1002 null;
        "storageMode" = mkOverride 1002 null;
        "storagePool" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeScaleIOSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeSecret" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode is Optional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values\nfor mode bits. Defaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "items If unspecified, each key-value pair in the Data field of the referenced\nSecret will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the Secret,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeSecretItems")
            )
          );
        };
        "optional" = mkOption {
          description = "optional field specify whether the Secret or its keys must be defined";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "secretName is the name of the secret in the pod's namespace to use.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#secret";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeSecretItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeStorageos" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef specifies the secret to use for obtaining the StorageOS API\ncredentials.  If not specified, default values will be attempted.";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeStorageosSecretRef")
          );
        };
        "volumeName" = mkOption {
          description = "volumeName is the human-readable name of the StorageOS volume.  Volume\nnames are only unique within a namespace.";
          type = (types.nullOr types.str);
        };
        "volumeNamespace" = mkOption {
          description = "volumeNamespace specifies the scope of the volume within StorageOS.  If no\nnamespace is specified then the Pod's namespace will be used.  This allows the\nKubernetes name scoping to be mirrored within StorageOS for tighter integration.\nSet VolumeName to any name to override the default behaviour.\nSet to \"default\" if you are not using namespaces within StorageOS.\nNamespaces that do not pre-exist within StorageOS will be created.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
        "volumeNamespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeStorageosSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.DriverSpecNodePluginVolumesVolumeVsphereVolume" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "storagePolicyID" = mkOption {
          description = "storagePolicyID is the storage Policy Based Management (SPBM) profile ID associated with the StoragePolicyName.";
          type = (types.nullOr types.str);
        };
        "storagePolicyName" = mkOption {
          description = "storagePolicyName is the storage Policy Based Management (SPBM) profile name.";
          type = (types.nullOr types.str);
        };
        "volumePath" = mkOption {
          description = "volumePath is the path that identifies vSphere volume vmdk";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "storagePolicyID" = mkOverride 1002 null;
        "storagePolicyName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfig" = {

      options = {
        "apiVersion" = mkOption {
          description = "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (types.nullOr (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta"));
        };
        "spec" = mkOption {
          description = "OperatorConfigSpec defines the desired state of OperatorConfig";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpec"));
        };
        "status" = mkOption {
          description = "OperatorConfigStatus defines the observed state of OperatorConfig";
          type = (types.nullOr types.attrs);
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpec" = {

      options = {
        "driverSpecDefaults" = mkOption {
          description = "Allow overwrite of hardcoded defaults for any driver managed by this operator";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaults"));
        };
        "log" = mkOption {
          description = "OperatorLogSpec provide log related settings for the operator";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecLog"));
        };
      };

      config = {
        "driverSpecDefaults" = mkOverride 1002 null;
        "log" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaults" = {

      options = {
        "attachRequired" = mkOption {
          description = "Whether to skip any attach operation altogether for CephCsi PVCs.\nSee more details [here](https://kubernetes-csi.github.io/docs/skip-attach.html#skip-attach-with-csi-driver-object).\nIf set to false it skips the volume attachments and makes the creation of pods using the CephCsi PVC fast.\n**WARNING** It's highly discouraged to use this for RWO volumes. for RBD PVC it can cause data corruption,\ncsi-addons operations like Reclaimspace and PVC Keyrotation will also not be supported if set to false\nsince we'll have no VolumeAttachments to determine which node the PVC is mounted on.\nRefer to this [issue](https://github.com/kubernetes/kubernetes/issues/103305) for more details.";
          type = (types.nullOr types.bool);
        };
        "cephFsClientType" = mkOption {
          description = "Select between between cephfs kernel driver and ceph-fuse\nIf you select a non-kernel client, your application may be disrupted during upgrade.\nSee the upgrade guide: https://rook.io/docs/rook/latest/ceph-upgrade.html\nNOTE! cephfs quota is not supported in kernel version < 4.17";
          type = (
            types.nullOr (
              types.enum [
                "autodetect"
                "kernel"
              ]
            )
          );
        };
        "clusterName" = mkOption {
          description = "Cluster name identifier to set as metadata on the CephFS subvolume and RBD images. This will be useful in cases\nwhen two container orchestrator clusters (Kubernetes/OCP) are using a single ceph cluster.";
          type = (types.nullOr types.str);
        };
        "controllerPlugin" = mkOption {
          description = "Driver's controller plugin configuration";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPlugin")
          );
        };
        "deployCsiAddons" = mkOption {
          description = "a list of additional sidecars?";
          type = (types.nullOr types.bool);
        };
        "enableFencing" = mkOption {
          description = "Set to true to enable fencing for the driver.\nFencing is a feature that allows the driver to fence a node when it is tainted with node.kubernetes.io/out-of-service.";
          type = (types.nullOr types.bool);
        };
        "enableMetadata" = mkOption {
          description = "Deprecated: This field is no longer used by the ceph-csi driver and will be ignored.";
          type = (types.nullOr types.bool);
        };
        "encryption" = mkOption {
          description = "Driver's encryption settings";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsEncryption"));
        };
        "fsGroupPolicy" = mkOption {
          description = "Policy for modifying a volume's ownership or permissions when the PVC is being mounted.\nsupported values are documented at https://kubernetes-csi.github.io/docs/support-fsgroup.html";
          type = (types.nullOr types.str);
        };
        "fuseMountOptions" = mkOption {
          description = "Set mount options to use when using the Fuse client";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "generateOMapInfo" = mkOption {
          description = "OMAP generator will generate the omap mapping between the PV name and the RBD image.\nNeed to be enabled when we are using rbd mirroring feature.\nBy default OMAP generator sidecar is not deployed with Csi controller plugin pod, to enable\nit set it to true.";
          type = (types.nullOr types.bool);
        };
        "grpcTimeout" = mkOption {
          description = "Set the gRPC timeout for gRPC call issued by the driver components";
          type = (types.nullOr (types.withMinimum 0 types.int));
        };
        "imageSet" = mkOption {
          description = "A reference to a ConfigMap resource holding image overwrite for deployed\ncontainers";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsImageSet"));
        };
        "kernelMountOptions" = mkOption {
          description = "Set mount options to use https://docs.ceph.com/en/latest/man/8/mount.ceph/#options\nSet to \"ms_mode=secure\" when connections.encrypted is enabled in Ceph";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "leaderElection" = mkOption {
          description = "Leader election setting";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsLeaderElection")
          );
        };
        "liveness" = mkOption {
          description = "Liveness metrics configuration.\ndisabled by default.";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsLiveness"));
        };
        "log" = mkOption {
          description = "Logging configuration for driver's pods";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsLog"));
        };
        "nodePlugin" = mkOption {
          description = "Driver's plugin configuration";
          type = (types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePlugin"));
        };
        "snapshotPolicy" = mkOption {
          description = "Select a policy for snapshot behavior: none, autodetect, snapshot, sanpshotGroup";
          type = (
            types.nullOr (
              types.enum [
                "none"
                "volumeGroupSnapshot"
                "volumeSnapshot"
              ]
            )
          );
        };
      };

      config = {
        "attachRequired" = mkOverride 1002 null;
        "cephFsClientType" = mkOverride 1002 null;
        "clusterName" = mkOverride 1002 null;
        "controllerPlugin" = mkOverride 1002 null;
        "deployCsiAddons" = mkOverride 1002 null;
        "enableFencing" = mkOverride 1002 null;
        "enableMetadata" = mkOverride 1002 null;
        "encryption" = mkOverride 1002 null;
        "fsGroupPolicy" = mkOverride 1002 null;
        "fuseMountOptions" = mkOverride 1002 null;
        "generateOMapInfo" = mkOverride 1002 null;
        "grpcTimeout" = mkOverride 1002 null;
        "imageSet" = mkOverride 1002 null;
        "kernelMountOptions" = mkOverride 1002 null;
        "leaderElection" = mkOverride 1002 null;
        "liveness" = mkOverride 1002 null;
        "log" = mkOverride 1002 null;
        "nodePlugin" = mkOverride 1002 null;
        "snapshotPolicy" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPlugin" = {

      options = {
        "affinity" = mkOption {
          description = "Pod's affinity settings";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinity"
            )
          );
        };
        "annotations" = mkOption {
          description = "Pod's annotations";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "containerExtraArgs" = mkOption {
          description = "Extra arguments for containers in the controller plugin deployment.\nThe key is the container name, the value is a list of CLI arguments.\nThe operator will not override the default CLI argument values it's setting.\nThis is more for customizing what comes as default with cephcsi.\nExamples of container names: csi-rbdplugin, csi-provisioner, csi-attacher,\ncsi-resizer, csi-snapshotter, csi-omap-generator, liveness-prometheus, etc.";
          type = (types.nullOr (types.loaOf types.str));
        };
        "deploymentStrategy" = mkOption {
          description = "DeploymentStrategy describes how to replace existing pods with new ones\nDefault value is Recreate";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginDeploymentStrategy"
            )
          );
        };
        "hostNetwork" = mkOption {
          description = "hostNetwork setting to be propagated to CSI controller plugin pods";
          type = (types.nullOr types.bool);
        };
        "imagePullPolicy" = mkOption {
          description = "To indicate the image pull policy to be applied to all the containers in the csi driver pods.";
          type = (types.nullOr types.str);
        };
        "labels" = mkOption {
          description = "Pod's labels";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "priorityClassName" = mkOption {
          description = "Pod's user defined priority class name";
          type = (types.nullOr types.str);
        };
        "privileged" = mkOption {
          description = "To enable logrotation for csi pods,\nSome platforms require controller plugin to run privileged,\nFor example, OpenShift with SELinux restrictions requires the pod to be privileged to write to hostPath.";
          type = (types.nullOr types.bool);
        };
        "replicas" = mkOption {
          description = "Set replicas for controller plugin's deployment.\nIf not set, defaults to 2 or to the number of available nodes\non single-node clusters. When OperatorConfig specifies a replica\ncount, it is used as the default for all drivers.";
          type = (types.nullOr (types.withMinimum 1 types.int));
        };
        "resources" = mkOption {
          description = "Resource requirements for controller plugin's containers";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResources"
            )
          );
        };
        "serviceAccountName" = mkOption {
          description = "Service account name to be used for driver's pods";
          type = (types.nullOr types.str);
        };
        "tolerations" = mkOption {
          description = "Pod's tolerations list";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginTolerations"
              )
            )
          );
        };
        "volumes" = mkOption {
          description = "Volume and volume mount definitions to attach to the pod";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumes"
              )
            )
          );
        };
      };

      config = {
        "affinity" = mkOverride 1002 null;
        "annotations" = mkOverride 1002 null;
        "containerExtraArgs" = mkOverride 1002 null;
        "deploymentStrategy" = mkOverride 1002 null;
        "hostNetwork" = mkOverride 1002 null;
        "imagePullPolicy" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "privileged" = mkOverride 1002 null;
        "replicas" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "serviceAccountName" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "volumes" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinity" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "Describes node affinity scheduling rules for the pod.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinity"
            )
          );
        };
        "podAffinity" = mkOption {
          description = "Describes pod affinity scheduling rules (e.g. co-locate this pod in the same node, zone, etc. as some other pod(s)).";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinity"
            )
          );
        };
        "podAntiAffinity" = mkOption {
          description = "Describes pod anti-affinity scheduling rules (e.g. avoid putting this pod in the same node, zone, etc. as some other pod(s)).";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinity"
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and adding\n\"weight\" to the sum if the node matches the corresponding matchExpressions; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to an update), the system\nmay or may not try to eventually evict the pod from its node.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "A node selector term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "Weight associated with matching the corresponding nodeSelectorTerm, in the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "A list of node selector requirements by node's labels.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "A list of node selector requirements by node's fields.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "Required. A list of node selector terms. The terms are ORed.";
            type = (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "A list of node selector requirements by node's labels.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "A list of node selector requirements by node's fields.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and adding\n\"weight\" to the sum if the node has pods which matches the corresponding podAffinityTerm; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to a pod label update), the\nsystem may or may not try to eventually evict the pod from its node.\nWhen there are multiple elements, the lists of nodes corresponding to each\npodAffinityTerm are intersected, i.e. all terms must be satisfied.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "Required. A pod affinity term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "weight associated with matching the corresponding podAffinityTerm,\nin the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe anti-affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling anti-affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and subtracting\n\"weight\" from the sum if the node has pods which matches the corresponding podAffinityTerm; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the anti-affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the anti-affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to a pod label update), the\nsystem may or may not try to eventually evict the pod from its node.\nWhen there are multiple elements, the lists of nodes corresponding to each\npodAffinityTerm are intersected, i.e. all terms must be satisfied.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "Required. A pod affinity term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "weight associated with matching the corresponding podAffinityTerm,\nin the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginDeploymentStrategy" = {

      options = {
        "rollingUpdate" = mkOption {
          description = "Rolling update config params. Present only if DeploymentStrategyType =\nRollingUpdate.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginDeploymentStrategyRollingUpdate"
            )
          );
        };
        "type" = mkOption {
          description = "Type of deployment. Can be \"Recreate\" or \"RollingUpdate\". Default is RollingUpdate.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "rollingUpdate" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginDeploymentStrategyRollingUpdate" =
      {

        options = {
          "maxSurge" = mkOption {
            description = "The maximum number of pods that can be scheduled above the desired number of\npods.\nValue can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%).\nThis can not be 0 if MaxUnavailable is 0.\nAbsolute number is calculated from percentage by rounding up.\nDefaults to 25%.\nExample: when this is set to 30%, the new ReplicaSet can be scaled up immediately when\nthe rolling update starts, such that the total number of old and new pods do not exceed\n130% of desired pods. Once old pods have been killed,\nnew ReplicaSet can be scaled up further, ensuring that total number of pods running\nat any time during the update is at most 130% of desired pods.";
            type = (types.nullOr (types.either types.int types.str));
          };
          "maxUnavailable" = mkOption {
            description = "The maximum number of pods that can be unavailable during the update.\nValue can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%).\nAbsolute number is calculated from percentage by rounding down.\nThis can not be 0 if MaxSurge is 0.\nDefaults to 25%.\nExample: when this is set to 30%, the old ReplicaSet can be scaled down to 70% of desired pods\nimmediately when the rolling update starts. Once new pods are ready, old ReplicaSet\ncan be scaled down further, followed by scaling up the new ReplicaSet, ensuring\nthat the total number of pods available at all times during the update is at\nleast 70% of desired pods.";
            type = (types.nullOr (types.either types.int types.str));
          };
        };

        config = {
          "maxSurge" = mkOverride 1002 null;
          "maxUnavailable" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResources" = {

      options = {
        "addons" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesAddons"
            )
          );
        };
        "attacher" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesAttacher"
            )
          );
        };
        "liveness" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesLiveness"
            )
          );
        };
        "logRotator" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesLogRotator"
            )
          );
        };
        "omapGenerator" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesOmapGenerator"
            )
          );
        };
        "plugin" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesPlugin"
            )
          );
        };
        "provisioner" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesProvisioner"
            )
          );
        };
        "resizer" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesResizer"
            )
          );
        };
        "snapshotter" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesSnapshotter"
            )
          );
        };
      };

      config = {
        "addons" = mkOverride 1002 null;
        "attacher" = mkOverride 1002 null;
        "liveness" = mkOverride 1002 null;
        "logRotator" = mkOverride 1002 null;
        "omapGenerator" = mkOverride 1002 null;
        "plugin" = mkOverride 1002 null;
        "provisioner" = mkOverride 1002 null;
        "resizer" = mkOverride 1002 null;
        "snapshotter" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesAddons" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesAddonsClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesAddonsClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesAttacher" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesAttacherClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesAttacherClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesLiveness" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesLivenessClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesLivenessClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesLogRotator" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesLogRotatorClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesLogRotatorClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesOmapGenerator" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesOmapGeneratorClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesOmapGeneratorClaims" =
      {

        options = {
          "name" = mkOption {
            description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
            type = types.str;
          };
          "request" = mkOption {
            description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "request" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesPlugin" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesPluginClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesPluginClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesProvisioner" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesProvisionerClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesProvisionerClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesResizer" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesResizerClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesResizerClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesSnapshotter" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesSnapshotterClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginResourcesSnapshotterClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginTolerations" = {

      options = {
        "effect" = mkOption {
          description = "Effect indicates the taint effect to match. Empty means match all taint effects.\nWhen specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "Key is the taint key that the toleration applies to. Empty means match all taint keys.\nIf the key is empty, operator must be Exists; this combination means to match all values and all keys.";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "Operator represents a key's relationship to the value.\nValid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.\nExists is equivalent to wildcard for value, so that a pod can\ntolerate all taints of a particular category.\nLt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "TolerationSeconds represents the period of time the toleration (which must be\nof effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,\nit is not set, which means tolerate the taint forever (do not evict). Zero and\nnegative values will be treated as 0 (evict immediately) by the system.";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "Value is the taint value the toleration matches to.\nIf the operator is Exists, the value should be empty, otherwise just a regular string.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumes" = {

      options = {
        "mount" = mkOption {
          description = "VolumeMount describes a mounting of a Volume within a container.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesMount"
            )
          );
        };
        "volume" = mkOption {
          description = "Volume represents a named volume in a pod that may be accessed by any container in the pod.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolume"
            )
          );
        };
      };

      config = {
        "mount" = mkOverride 1002 null;
        "volume" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesMount" = {

      options = {
        "mountPath" = mkOption {
          description = "Path within the container at which the volume should be mounted.  Must\nnot contain ':'.";
          type = types.str;
        };
        "mountPropagation" = mkOption {
          description = "mountPropagation determines how mounts are propagated from the host\nto container and the other way around.\nWhen not set, MountPropagationNone is used.\nThis field is beta in 1.10.\nWhen RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified\n(which defaults to None).";
          type = (types.nullOr types.str);
        };
        "name" = mkOption {
          description = "This must match the Name of a Volume.";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "Mounted read-only if true, read-write otherwise (false or unspecified).\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "recursiveReadOnly" = mkOption {
          description = "RecursiveReadOnly specifies whether read-only mounts should be handled\nrecursively.\n\nIf ReadOnly is false, this field has no meaning and must be unspecified.\n\nIf ReadOnly is true, and this field is set to Disabled, the mount is not made\nrecursively read-only.  If this field is set to IfPossible, the mount is made\nrecursively read-only, if it is supported by the container runtime.  If this\nfield is set to Enabled, the mount is made recursively read-only if it is\nsupported by the container runtime, otherwise the pod will not be started and\nan error will be generated to indicate the reason.\n\nIf this field is set to IfPossible or Enabled, MountPropagation must be set to\nNone (or be unspecified, which defaults to None).\n\nIf this field is not specified, it is treated as an equivalent of Disabled.";
          type = (types.nullOr types.str);
        };
        "subPath" = mkOption {
          description = "Path within the volume from which the container's volume should be mounted.\nDefaults to \"\" (volume's root).";
          type = (types.nullOr types.str);
        };
        "subPathExpr" = mkOption {
          description = "Expanded path within the volume from which the container's volume should be mounted.\nBehaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.\nDefaults to \"\" (volume's root).\nSubPathExpr and SubPath are mutually exclusive.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "mountPropagation" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "recursiveReadOnly" = mkOverride 1002 null;
        "subPath" = mkOverride 1002 null;
        "subPathExpr" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolume" = {

      options = {
        "awsElasticBlockStore" = mkOption {
          description = "awsElasticBlockStore represents an AWS Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nDeprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree\nawsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeAwsElasticBlockStore"
            )
          );
        };
        "azureDisk" = mkOption {
          description = "azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod.\nDeprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type\nare redirected to the disk.csi.azure.com CSI driver.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeAzureDisk"
            )
          );
        };
        "azureFile" = mkOption {
          description = "azureFile represents an Azure File Service mount on the host and bind mount to the pod.\nDeprecated: AzureFile is deprecated. All operations for the in-tree azureFile type\nare redirected to the file.csi.azure.com CSI driver.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeAzureFile"
            )
          );
        };
        "cephfs" = mkOption {
          description = "cephFS represents a Ceph FS mount on the host that shares a pod's lifetime.\nDeprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCephfs"
            )
          );
        };
        "cinder" = mkOption {
          description = "cinder represents a cinder volume attached and mounted on kubelets host machine.\nDeprecated: Cinder is deprecated. All operations for the in-tree cinder type\nare redirected to the cinder.csi.openstack.org CSI driver.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCinder"
            )
          );
        };
        "configMap" = mkOption {
          description = "configMap represents a configMap that should populate this volume";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeConfigMap"
            )
          );
        };
        "csi" = mkOption {
          description = "csi (Container Storage Interface) represents ephemeral storage that is handled by certain external CSI drivers.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCsi"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "downwardAPI represents downward API about the pod that should populate this volume";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeDownwardAPI"
            )
          );
        };
        "emptyDir" = mkOption {
          description = "emptyDir represents a temporary directory that shares a pod's lifetime.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEmptyDir"
            )
          );
        };
        "ephemeral" = mkOption {
          description = "ephemeral represents a volume that is handled by a cluster storage driver.\nThe volume's lifecycle is tied to the pod that defines it - it will be created before the pod starts,\nand deleted when the pod is removed.\n\nUse this if:\na) the volume is only needed while the pod runs,\nb) features of normal volumes like restoring from snapshot or capacity\n   tracking are needed,\nc) the storage driver is specified through a storage class, and\nd) the storage driver supports dynamic volume provisioning through\n   a PersistentVolumeClaim (see EphemeralVolumeSource for more\n   information on the connection between this volume type\n   and PersistentVolumeClaim).\n\nUse PersistentVolumeClaim or one of the vendor-specific\nAPIs for volumes that persist for longer than the lifecycle\nof an individual pod.\n\nUse CSI for light-weight local ephemeral volumes if the CSI driver is meant to\nbe used that way - see the documentation of the driver for\nmore information.\n\nA pod can use both types of ephemeral volumes and\npersistent volumes at the same time.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeral"
            )
          );
        };
        "fc" = mkOption {
          description = "fc represents a Fibre Channel resource that is attached to a kubelet's host machine and then exposed to the pod.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeFc"
            )
          );
        };
        "flexVolume" = mkOption {
          description = "flexVolume represents a generic volume resource that is\nprovisioned/attached using an exec based plugin.\nDeprecated: FlexVolume is deprecated. Consider using a CSIDriver instead.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeFlexVolume"
            )
          );
        };
        "flocker" = mkOption {
          description = "flocker represents a Flocker volume attached to a kubelet's host machine. This depends on the Flocker control service being running.\nDeprecated: Flocker is deprecated and the in-tree flocker type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeFlocker"
            )
          );
        };
        "gcePersistentDisk" = mkOption {
          description = "gcePersistentDisk represents a GCE Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nDeprecated: GCEPersistentDisk is deprecated. All operations for the in-tree\ngcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeGcePersistentDisk"
            )
          );
        };
        "gitRepo" = mkOption {
          description = "gitRepo represents a git repository at a particular revision.\nDeprecated: GitRepo is deprecated. To provision a container with a git repo, mount an\nEmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir\ninto the Pod's container.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeGitRepo"
            )
          );
        };
        "glusterfs" = mkOption {
          description = "glusterfs represents a Glusterfs mount on the host that shares a pod's lifetime.\nDeprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeGlusterfs"
            )
          );
        };
        "hostPath" = mkOption {
          description = "hostPath represents a pre-existing file or directory on the host\nmachine that is directly exposed to the container. This is generally\nused for system agents or other privileged things that are allowed\nto see the host machine. Most containers will NOT need this.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeHostPath"
            )
          );
        };
        "image" = mkOption {
          description = "image represents an OCI object (a container image or artifact) pulled and mounted on the kubelet's host machine.\nThe volume is resolved at pod startup depending on which PullPolicy value is provided:\n\n- Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.\n- Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.\n- IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.\n\nThe volume gets re-resolved if the pod gets deleted and recreated, which means that new remote content will become available on pod recreation.\nA failure to resolve or pull the image during pod startup will block containers from starting and may add significant latency. Failures will be retried using normal volume backoff and will be reported on the pod reason and message.\nThe types of objects that may be mounted by this volume are defined by the container runtime implementation on a host machine and at minimum must include all valid types supported by the container image field.\nThe OCI object gets mounted in a single directory (spec.containers[*].volumeMounts.mountPath) by merging the manifest layers in the same way as for container images.\nThe volume will be mounted read-only (ro) and non-executable files (noexec).\nSub path mounts for containers are not supported (spec.containers[*].volumeMounts.subpath) before 1.33.\nThe field spec.securityContext.fsGroupChangePolicy has no effect on this volume type.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeImage"
            )
          );
        };
        "iscsi" = mkOption {
          description = "iscsi represents an ISCSI Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes/#iscsi";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeIscsi"
            )
          );
        };
        "name" = mkOption {
          description = "name of the volume.\nMust be a DNS_LABEL and unique within the pod.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = types.str;
        };
        "nfs" = mkOption {
          description = "nfs represents an NFS mount on the host that shares a pod's lifetime\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeNfs"
            )
          );
        };
        "persistentVolumeClaim" = mkOption {
          description = "persistentVolumeClaimVolumeSource represents a reference to a\nPersistentVolumeClaim in the same namespace.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumePersistentVolumeClaim"
            )
          );
        };
        "photonPersistentDisk" = mkOption {
          description = "photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine.\nDeprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumePhotonPersistentDisk"
            )
          );
        };
        "portworxVolume" = mkOption {
          description = "portworxVolume represents a portworx volume attached and mounted on kubelets host machine.\nDeprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type\nare redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate\nis on.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumePortworxVolume"
            )
          );
        };
        "projected" = mkOption {
          description = "projected items for all in one resources secrets, configmaps, and downward API";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjected"
            )
          );
        };
        "quobyte" = mkOption {
          description = "quobyte represents a Quobyte mount on the host that shares a pod's lifetime.\nDeprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeQuobyte"
            )
          );
        };
        "rbd" = mkOption {
          description = "rbd represents a Rados Block Device mount on the host that shares a pod's lifetime.\nDeprecated: RBD is deprecated and the in-tree rbd type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeRbd"
            )
          );
        };
        "scaleIO" = mkOption {
          description = "scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes.\nDeprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeScaleIO"
            )
          );
        };
        "secret" = mkOption {
          description = "secret represents a secret that should populate this volume.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#secret";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeSecret"
            )
          );
        };
        "storageos" = mkOption {
          description = "storageOS represents a StorageOS volume attached and mounted on Kubernetes nodes.\nDeprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeStorageos"
            )
          );
        };
        "vsphereVolume" = mkOption {
          description = "vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine.\nDeprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type\nare redirected to the csi.vsphere.vmware.com CSI driver.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeVsphereVolume"
            )
          );
        };
      };

      config = {
        "awsElasticBlockStore" = mkOverride 1002 null;
        "azureDisk" = mkOverride 1002 null;
        "azureFile" = mkOverride 1002 null;
        "cephfs" = mkOverride 1002 null;
        "cinder" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "csi" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "emptyDir" = mkOverride 1002 null;
        "ephemeral" = mkOverride 1002 null;
        "fc" = mkOverride 1002 null;
        "flexVolume" = mkOverride 1002 null;
        "flocker" = mkOverride 1002 null;
        "gcePersistentDisk" = mkOverride 1002 null;
        "gitRepo" = mkOverride 1002 null;
        "glusterfs" = mkOverride 1002 null;
        "hostPath" = mkOverride 1002 null;
        "image" = mkOverride 1002 null;
        "iscsi" = mkOverride 1002 null;
        "nfs" = mkOverride 1002 null;
        "persistentVolumeClaim" = mkOverride 1002 null;
        "photonPersistentDisk" = mkOverride 1002 null;
        "portworxVolume" = mkOverride 1002 null;
        "projected" = mkOverride 1002 null;
        "quobyte" = mkOverride 1002 null;
        "rbd" = mkOverride 1002 null;
        "scaleIO" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "storageos" = mkOverride 1002 null;
        "vsphereVolume" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeAwsElasticBlockStore" =
      {

        options = {
          "fsType" = mkOption {
            description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
            type = (types.nullOr types.str);
          };
          "partition" = mkOption {
            description = "partition is the partition in the volume that you want to mount.\nIf omitted, the default is to mount by volume name.\nExamples: For volume /dev/sda1, you specify the partition as \"1\".\nSimilarly, the volume partition for /dev/sda is \"0\" (or you can leave the property empty).";
            type = (types.nullOr types.int);
          };
          "readOnly" = mkOption {
            description = "readOnly value true will force the readOnly setting in VolumeMounts.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
            type = (types.nullOr types.bool);
          };
          "volumeID" = mkOption {
            description = "volumeID is unique ID of the persistent disk resource in AWS (Amazon EBS volume).\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
            type = types.str;
          };
        };

        config = {
          "fsType" = mkOverride 1002 null;
          "partition" = mkOverride 1002 null;
          "readOnly" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeAzureDisk" = {

      options = {
        "cachingMode" = mkOption {
          description = "cachingMode is the Host Caching mode: None, Read Only, Read Write.";
          type = (types.nullOr types.str);
        };
        "diskName" = mkOption {
          description = "diskName is the Name of the data disk in the blob storage";
          type = types.str;
        };
        "diskURI" = mkOption {
          description = "diskURI is the URI of data disk in the blob storage";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType is Filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "kind expected values are Shared: multiple blob disks per storage account  Dedicated: single blob disk per storage account  Managed: azure managed data disk (only in managed availability set). defaults to shared";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "cachingMode" = mkOverride 1002 null;
        "fsType" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeAzureFile" = {

      options = {
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "secretName is the  name of secret that contains Azure Storage Account Name and Key";
          type = types.str;
        };
        "shareName" = mkOption {
          description = "shareName is the azure share Name";
          type = types.str;
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCephfs" = {

      options = {
        "monitors" = mkOption {
          description = "monitors is Required: Monitors is a collection of Ceph monitors\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.listOf types.str);
        };
        "path" = mkOption {
          description = "path is Optional: Used as the mounted root, rather than the full Ceph tree, default is /";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.bool);
        };
        "secretFile" = mkOption {
          description = "secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "secretRef" = mkOption {
          description = "secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty.\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCephfsSecretRef"
            )
          );
        };
        "user" = mkOption {
          description = "user is optional: User is the rados user name, default is admin\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "path" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretFile" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCephfsSecretRef" =
      {

        options = {
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "name" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCinder" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is optional: points to a secret object containing parameters used to connect\nto OpenStack.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCinderSecretRef"
            )
          );
        };
        "volumeID" = mkOption {
          description = "volumeID used to identify the volume in cinder.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCinderSecretRef" =
      {

        options = {
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "name" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeConfigMap" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode is optional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDefaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "items if unspecified, each key-value pair in the Data field of the referenced\nConfigMap will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the ConfigMap,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "optional specify whether the ConfigMap or its keys must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCsi" = {

      options = {
        "driver" = mkOption {
          description = "driver is the name of the CSI driver that handles this volume.\nConsult with your admin for the correct name as registered in the cluster.";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType to mount. Ex. \"ext4\", \"xfs\", \"ntfs\".\nIf not provided, the empty value is passed to the associated CSI driver\nwhich will determine the default filesystem to apply.";
          type = (types.nullOr types.str);
        };
        "nodePublishSecretRef" = mkOption {
          description = "nodePublishSecretRef is a reference to the secret object containing\nsensitive information to pass to the CSI driver to complete the CSI\nNodePublishVolume and NodeUnpublishVolume calls.\nThis field is optional, and  may be empty if no secret is required. If the\nsecret object contains more than one secret, all secret references are passed.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCsiNodePublishSecretRef"
            )
          );
        };
        "readOnly" = mkOption {
          description = "readOnly specifies a read-only configuration for the volume.\nDefaults to false (read/write).";
          type = (types.nullOr types.bool);
        };
        "volumeAttributes" = mkOption {
          description = "volumeAttributes stores driver-specific properties that are passed to the CSI\ndriver. Consult your driver's documentation for supported values.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "nodePublishSecretRef" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "volumeAttributes" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeCsiNodePublishSecretRef" =
      {

        options = {
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "name" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeDownwardAPI" = {

      options = {
        "defaultMode" = mkOption {
          description = "Optional: mode bits to use on created files by default. Must be a\nOptional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDefaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "Items is a list of downward API volume file";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeDownwardAPIItems"
              )
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeDownwardAPIItems" =
      {

        options = {
          "fieldRef" = mkOption {
            description = "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeDownwardAPIItemsFieldRef"
              )
            );
          };
          "mode" = mkOption {
            description = "Optional: mode bits used to set permissions on this file, must be an octal value\nbetween 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the '..' path. Must be utf-8 encoded. The first item of the relative path must not start with '..'";
            type = types.str;
          };
          "resourceFieldRef" = mkOption {
            description = "Selects a resource of the container: only resources limits and requests\n(limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeDownwardAPIItemsResourceFieldRef"
              )
            );
          };
        };

        config = {
          "fieldRef" = mkOverride 1002 null;
          "mode" = mkOverride 1002 null;
          "resourceFieldRef" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeDownwardAPIItemsFieldRef" =
      {

        options = {
          "apiVersion" = mkOption {
            description = "Version of the schema the FieldPath is written in terms of, defaults to \"v1\".";
            type = (types.nullOr types.str);
          };
          "fieldPath" = mkOption {
            description = "Path of the field to select in the specified API version.";
            type = types.str;
          };
        };

        config = {
          "apiVersion" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "Container name: required for volumes, optional for env vars";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "Specifies the output format of the exposed resources, defaults to \"1\"";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "Required: resource to select";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEmptyDir" = {

      options = {
        "medium" = mkOption {
          description = "medium represents what type of storage medium should back this directory.\nThe default is \"\" which means to use the node's default medium.\nMust be an empty string (default) or Memory.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (types.nullOr types.str);
        };
        "sizeLimit" = mkOption {
          description = "sizeLimit is the total amount of local storage required for this EmptyDir volume.\nThe size limit is also applicable for memory medium.\nThe maximum usage on memory medium EmptyDir would be the minimum value between\nthe SizeLimit specified here and the sum of memory limits of all containers in a pod.\nThe default is nil which means that the limit is undefined.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "medium" = mkOverride 1002 null;
        "sizeLimit" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeral" = {

      options = {
        "volumeClaimTemplate" = mkOption {
          description = "Will be used to create a stand-alone PVC to provision the volume.\nThe pod in which this EphemeralVolumeSource is embedded will be the\nowner of the PVC, i.e. the PVC will be deleted together with the\npod.  The name of the PVC will be `<pod name>-<volume name>` where\n`<volume name>` is the name from the `PodSpec.Volumes` array\nentry. Pod validation will reject the pod if the concatenated name\nis not valid for a PVC (for example, too long).\n\nAn existing PVC with that name that is not owned by the pod\nwill *not* be used for the pod to avoid using an unrelated\nvolume by mistake. Starting the pod is then blocked until\nthe unrelated PVC is removed. If such a pre-created PVC is\nmeant to be used by the pod, the PVC has to updated with an\nowner reference to the pod once the pod exists. Normally\nthis should not be necessary, but it may be useful when\nmanually reconstructing a broken cluster.\n\nThis field is read-only and no changes will be made by Kubernetes\nto the PVC after it has been created.\n\nRequired, must not be nil.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplate"
            )
          );
        };
      };

      config = {
        "volumeClaimTemplate" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplate" =
      {

        options = {
          "metadata" = mkOption {
            description = "May contain labels and annotations that will be copied into the PVC\nwhen creating it. No other fields are allowed and will be rejected during\nvalidation.";
            type = (types.nullOr types.attrs);
          };
          "spec" = mkOption {
            description = "The specification for the PersistentVolumeClaim. The entire content is\ncopied unchanged into the PVC that gets created from this\ntemplate. The same fields as in a PersistentVolumeClaim\nare also valid here.";
            type = (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpec"
            );
          };
        };

        config = {
          "metadata" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpec" =
      {

        options = {
          "accessModes" = mkOption {
            description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
            type = (types.nullOr (types.listOf types.str));
          };
          "dataSource" = mkOption {
            description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSource"
              )
            );
          };
          "dataSourceRef" = mkOption {
            description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSourceRef"
              )
            );
          };
          "resources" = mkOption {
            description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecResources"
              )
            );
          };
          "selector" = mkOption {
            description = "selector is a label query over volumes to consider for binding.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelector"
              )
            );
          };
          "storageClassName" = mkOption {
            description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
            type = (types.nullOr types.str);
          };
          "volumeAttributesClassName" = mkOption {
            description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
            type = (types.nullOr types.str);
          };
          "volumeMode" = mkOption {
            description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
            type = (types.nullOr types.str);
          };
          "volumeName" = mkOption {
            description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "accessModes" = mkOverride 1002 null;
          "dataSource" = mkOverride 1002 null;
          "dataSourceRef" = mkOverride 1002 null;
          "resources" = mkOverride 1002 null;
          "selector" = mkOverride 1002 null;
          "storageClassName" = mkOverride 1002 null;
          "volumeAttributesClassName" = mkOverride 1002 null;
          "volumeMode" = mkOverride 1002 null;
          "volumeName" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSource" =
      {

        options = {
          "apiGroup" = mkOption {
            description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
            type = (types.nullOr types.str);
          };
          "kind" = mkOption {
            description = "Kind is the type of resource being referenced";
            type = types.str;
          };
          "name" = mkOption {
            description = "Name is the name of resource being referenced";
            type = types.str;
          };
        };

        config = {
          "apiGroup" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSourceRef" =
      {

        options = {
          "apiGroup" = mkOption {
            description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
            type = (types.nullOr types.str);
          };
          "kind" = mkOption {
            description = "Kind is the type of resource being referenced";
            type = types.str;
          };
          "name" = mkOption {
            description = "Name is the name of resource being referenced";
            type = types.str;
          };
          "namespace" = mkOption {
            description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "apiGroup" = mkOverride 1002 null;
          "namespace" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecResources" =
      {

        options = {
          "limits" = mkOption {
            description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
            type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
          };
          "requests" = mkOption {
            description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
            type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
          };
        };

        config = {
          "limits" = mkOverride 1002 null;
          "requests" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeFc" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "lun" = mkOption {
          description = "lun is Optional: FC target lun number";
          type = (types.nullOr types.int);
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "targetWWNs" = mkOption {
          description = "targetWWNs is Optional: FC target worldwide names (WWNs)";
          type = (types.nullOr (types.listOf types.str));
        };
        "wwids" = mkOption {
          description = "wwids Optional: FC volume world wide identifiers (wwids)\nEither wwids or combination of targetWWNs and lun must be set, but not both simultaneously.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "lun" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "targetWWNs" = mkOverride 1002 null;
        "wwids" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeFlexVolume" = {

      options = {
        "driver" = mkOption {
          description = "driver is the name of the driver to use for this volume.";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". The default filesystem depends on FlexVolume script.";
          type = (types.nullOr types.str);
        };
        "options" = mkOption {
          description = "options is Optional: this field holds extra command options if any.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is Optional: secretRef is reference to the secret object containing\nsensitive information to pass to the plugin scripts. This may be\nempty if no secret object is specified. If the secret object\ncontains more than one secret, all secrets are passed to the plugin\nscripts.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeFlexVolumeSecretRef"
            )
          );
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "options" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeFlexVolumeSecretRef" =
      {

        options = {
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "name" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeFlocker" = {

      options = {
        "datasetName" = mkOption {
          description = "datasetName is Name of the dataset stored as metadata -> name on the dataset for Flocker\nshould be considered as deprecated";
          type = (types.nullOr types.str);
        };
        "datasetUUID" = mkOption {
          description = "datasetUUID is the UUID of the dataset. This is unique identifier of a Flocker dataset";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "datasetName" = mkOverride 1002 null;
        "datasetUUID" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeGcePersistentDisk" =
      {

        options = {
          "fsType" = mkOption {
            description = "fsType is filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
            type = (types.nullOr types.str);
          };
          "partition" = mkOption {
            description = "partition is the partition in the volume that you want to mount.\nIf omitted, the default is to mount by volume name.\nExamples: For volume /dev/sda1, you specify the partition as \"1\".\nSimilarly, the volume partition for /dev/sda is \"0\" (or you can leave the property empty).\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
            type = (types.nullOr types.int);
          };
          "pdName" = mkOption {
            description = "pdName is unique name of the PD resource in GCE. Used to identify the disk in GCE.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
            type = types.str;
          };
          "readOnly" = mkOption {
            description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "fsType" = mkOverride 1002 null;
          "partition" = mkOverride 1002 null;
          "readOnly" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeGitRepo" = {

      options = {
        "directory" = mkOption {
          description = "directory is the target directory name.\nMust not contain or start with '..'.  If '.' is supplied, the volume directory will be the\ngit repository.  Otherwise, if specified, the volume will contain the git repository in\nthe subdirectory with the given name.";
          type = (types.nullOr types.str);
        };
        "repository" = mkOption {
          description = "repository is the URL";
          type = types.str;
        };
        "revision" = mkOption {
          description = "revision is the commit hash for the specified revision.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "directory" = mkOverride 1002 null;
        "revision" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeGlusterfs" = {

      options = {
        "endpoints" = mkOption {
          description = "endpoints is the endpoint name that details Glusterfs topology.";
          type = types.str;
        };
        "path" = mkOption {
          description = "path is the Glusterfs volume path.\nMore info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the Glusterfs volume to be mounted with read-only permissions.\nDefaults to false.\nMore info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeHostPath" = {

      options = {
        "path" = mkOption {
          description = "path of the directory on the host.\nIf the path is a symlink, it will follow the link to the real path.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = types.str;
        };
        "type" = mkOption {
          description = "type for HostPath Volume\nDefaults to \"\"\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeImage" = {

      options = {
        "pullPolicy" = mkOption {
          description = "Policy for pulling OCI objects. Possible values are:\nAlways: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.\nNever: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.\nIfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.\nDefaults to Always if :latest tag is specified, or IfNotPresent otherwise.";
          type = (types.nullOr types.str);
        };
        "reference" = mkOption {
          description = "Required: Image or artifact reference to be used.\nBehaves in the same way as pod.spec.containers[*].image.\nPull secrets will be assembled in the same way as for the container image by looking up node credentials, SA image pull secrets, and pod spec image pull secrets.\nMore info: https://kubernetes.io/docs/concepts/containers/images\nThis field is optional to allow higher level config management to default or override\ncontainer images in workload controllers like Deployments and StatefulSets.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "pullPolicy" = mkOverride 1002 null;
        "reference" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeIscsi" = {

      options = {
        "chapAuthDiscovery" = mkOption {
          description = "chapAuthDiscovery defines whether support iSCSI Discovery CHAP authentication";
          type = (types.nullOr types.bool);
        };
        "chapAuthSession" = mkOption {
          description = "chapAuthSession defines whether support iSCSI Session CHAP authentication";
          type = (types.nullOr types.bool);
        };
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi";
          type = (types.nullOr types.str);
        };
        "initiatorName" = mkOption {
          description = "initiatorName is the custom iSCSI Initiator Name.\nIf initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface\n<target portal>:<volume name> will be created for the connection.";
          type = (types.nullOr types.str);
        };
        "iqn" = mkOption {
          description = "iqn is the target iSCSI Qualified Name.";
          type = types.str;
        };
        "iscsiInterface" = mkOption {
          description = "iscsiInterface is the interface Name that uses an iSCSI transport.\nDefaults to 'default' (tcp).";
          type = (types.nullOr types.str);
        };
        "lun" = mkOption {
          description = "lun represents iSCSI Target Lun number.";
          type = types.int;
        };
        "portals" = mkOption {
          description = "portals is the iSCSI Target Portal List. The portal is either an IP or ip_addr:port if the port\nis other than default (typically TCP ports 860 and 3260).";
          type = (types.nullOr (types.listOf types.str));
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is the CHAP Secret for iSCSI target and initiator authentication";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeIscsiSecretRef"
            )
          );
        };
        "targetPortal" = mkOption {
          description = "targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port\nis other than default (typically TCP ports 860 and 3260).";
          type = types.str;
        };
      };

      config = {
        "chapAuthDiscovery" = mkOverride 1002 null;
        "chapAuthSession" = mkOverride 1002 null;
        "fsType" = mkOverride 1002 null;
        "initiatorName" = mkOverride 1002 null;
        "iscsiInterface" = mkOverride 1002 null;
        "portals" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeIscsiSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeNfs" = {

      options = {
        "path" = mkOption {
          description = "path that is exported by the NFS server.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the NFS export to be mounted with read-only permissions.\nDefaults to false.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = (types.nullOr types.bool);
        };
        "server" = mkOption {
          description = "server is the hostname or IP address of the NFS server.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = types.str;
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumePersistentVolumeClaim" =
      {

        options = {
          "claimName" = mkOption {
            description = "claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
            type = types.str;
          };
          "readOnly" = mkOption {
            description = "readOnly Will force the ReadOnly setting in VolumeMounts.\nDefault false.";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "readOnly" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumePhotonPersistentDisk" =
      {

        options = {
          "fsType" = mkOption {
            description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
            type = (types.nullOr types.str);
          };
          "pdID" = mkOption {
            description = "pdID is the ID that identifies Photon Controller persistent disk";
            type = types.str;
          };
        };

        config = {
          "fsType" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumePortworxVolume" = {

      options = {
        "fsType" = mkOption {
          description = "fSType represents the filesystem type to mount\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "volumeID" = mkOption {
          description = "volumeID uniquely identifies a Portworx volume";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjected" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode are the mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "sources" = mkOption {
          description = "sources is the list of volume projections. Each entry in this list\nhandles one source.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSources"
              )
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "sources" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSources" =
      {

        options = {
          "clusterTrustBundle" = mkOption {
            description = "ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field\nof ClusterTrustBundle objects in an auto-updating file.\n\nAlpha, gated by the ClusterTrustBundleProjection feature gate.\n\nClusterTrustBundle objects can either be selected by name, or by the\ncombination of signer name and a label selector.\n\nKubelet performs aggressive normalization of the PEM contents written\ninto the pod filesystem.  Esoteric PEM features such as inter-block\ncomments and block headers are stripped.  Certificates are deduplicated.\nThe ordering of certificates within the file is arbitrary, and Kubelet\nmay change the order over time.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundle"
              )
            );
          };
          "configMap" = mkOption {
            description = "configMap information about the configMap data to project";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesConfigMap"
              )
            );
          };
          "downwardAPI" = mkOption {
            description = "downwardAPI information about the downwardAPI data to project";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesDownwardAPI"
              )
            );
          };
          "podCertificate" = mkOption {
            description = "Projects an auto-rotating credential bundle (private key and certificate\nchain) that the pod can use either as a TLS client or server.\n\nKubelet generates a private key and uses it to send a\nPodCertificateRequest to the named signer.  Once the signer approves the\nrequest and issues a certificate chain, Kubelet writes the key and\ncertificate chain to the pod filesystem.  The pod does not start until\ncertificates have been issued for each podCertificate projected volume\nsource in its spec.\n\nKubelet will begin trying to rotate the certificate at the time indicated\nby the signer using the PodCertificateRequest.Status.BeginRefreshAt\ntimestamp.\n\nKubelet can write a single file, indicated by the credentialBundlePath\nfield, or separate files, indicated by the keyPath and\ncertificateChainPath fields.\n\nThe credential bundle is a single file in PEM format.  The first PEM\nentry is the private key (in PKCS#8 format), and the remaining PEM\nentries are the certificate chain issued by the signer (typically,\nsigners will return their certificate chain in leaf-to-root order).\n\nPrefer using the credential bundle format, since your application code\ncan read it atomically.  If you use keyPath and certificateChainPath,\nyour application must make two separate file reads. If these coincide\nwith a certificate rotation, it is possible that the private key and leaf\ncertificate you read may not correspond to each other.  Your application\nwill need to check for this condition, and re-read until they are\nconsistent.\n\nThe named signer controls chooses the format of the certificate it\nissues; consult the signer implementation's documentation to learn how to\nuse the certificates it issues.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesPodCertificate"
              )
            );
          };
          "secret" = mkOption {
            description = "secret information about the secret data to project";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesSecret"
              )
            );
          };
          "serviceAccountToken" = mkOption {
            description = "serviceAccountToken is information about the serviceAccountToken data to project";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesServiceAccountToken"
              )
            );
          };
        };

        config = {
          "clusterTrustBundle" = mkOverride 1002 null;
          "configMap" = mkOverride 1002 null;
          "downwardAPI" = mkOverride 1002 null;
          "podCertificate" = mkOverride 1002 null;
          "secret" = mkOverride 1002 null;
          "serviceAccountToken" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundle" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "Select all ClusterTrustBundles that match this label selector.  Only has\neffect if signerName is set.  Mutually-exclusive with name.  If unset,\ninterpreted as \"match nothing\".  If set but empty, interpreted as \"match\neverything\".";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelector"
              )
            );
          };
          "name" = mkOption {
            description = "Select a single ClusterTrustBundle by object name.  Mutually-exclusive\nwith signerName and labelSelector.";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "If true, don't block pod startup if the referenced ClusterTrustBundle(s)\naren't available.  If using name, then the named ClusterTrustBundle is\nallowed not to exist.  If using signerName, then the combination of\nsignerName and labelSelector is allowed to match zero\nClusterTrustBundles.";
            type = (types.nullOr types.bool);
          };
          "path" = mkOption {
            description = "Relative path from the volume root to write the bundle.";
            type = types.str;
          };
          "signerName" = mkOption {
            description = "Select all ClusterTrustBundles that match this signer name.\nMutually-exclusive with name.  The contents of all selected\nClusterTrustBundles will be unified and deduplicated.";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
          "signerName" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesConfigMap" =
      {

        options = {
          "items" = mkOption {
            description = "items if unspecified, each key-value pair in the Data field of the referenced\nConfigMap will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the ConfigMap,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesConfigMapItems"
                )
              )
            );
          };
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "optional specify whether the ConfigMap or its keys must be defined";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "items" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesConfigMapItems" =
      {

        options = {
          "key" = mkOption {
            description = "key is the key to project.";
            type = types.str;
          };
          "mode" = mkOption {
            description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesDownwardAPI" =
      {

        options = {
          "items" = mkOption {
            description = "Items is a list of DownwardAPIVolume file";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItems"
                )
              )
            );
          };
        };

        config = {
          "items" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItems" =
      {

        options = {
          "fieldRef" = mkOption {
            description = "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItemsFieldRef"
              )
            );
          };
          "mode" = mkOption {
            description = "Optional: mode bits used to set permissions on this file, must be an octal value\nbetween 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the '..' path. Must be utf-8 encoded. The first item of the relative path must not start with '..'";
            type = types.str;
          };
          "resourceFieldRef" = mkOption {
            description = "Selects a resource of the container: only resources limits and requests\n(limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItemsResourceFieldRef"
              )
            );
          };
        };

        config = {
          "fieldRef" = mkOverride 1002 null;
          "mode" = mkOverride 1002 null;
          "resourceFieldRef" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItemsFieldRef" =
      {

        options = {
          "apiVersion" = mkOption {
            description = "Version of the schema the FieldPath is written in terms of, defaults to \"v1\".";
            type = (types.nullOr types.str);
          };
          "fieldPath" = mkOption {
            description = "Path of the field to select in the specified API version.";
            type = types.str;
          };
        };

        config = {
          "apiVersion" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "Container name: required for volumes, optional for env vars";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "Specifies the output format of the exposed resources, defaults to \"1\"";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "Required: resource to select";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesPodCertificate" =
      {

        options = {
          "certificateChainPath" = mkOption {
            description = "Write the certificate chain at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath\nand certificateChainPath, your application needs to check that the key\nand leaf certificate are consistent, because it is possible to read the\nfiles mid-rotation.";
            type = (types.nullOr types.str);
          };
          "credentialBundlePath" = mkOption {
            description = "Write the credential bundle at this path in the projected volume.\n\nThe credential bundle is a single file that contains multiple PEM blocks.\nThe first PEM block is a PRIVATE KEY block, containing a PKCS#8 private\nkey.\n\nThe remaining blocks are CERTIFICATE blocks, containing the issued\ncertificate chain from the signer (leaf and any intermediates).\n\nUsing credentialBundlePath lets your Pod's application code make a single\natomic read that retrieves a consistent key and certificate chain.  If you\nproject them to separate files, your application code will need to\nadditionally check that the leaf certificate was issued to the key.";
            type = (types.nullOr types.str);
          };
          "keyPath" = mkOption {
            description = "Write the key at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath\nand certificateChainPath, your application needs to check that the key\nand leaf certificate are consistent, because it is possible to read the\nfiles mid-rotation.";
            type = (types.nullOr types.str);
          };
          "keyType" = mkOption {
            description = "The type of keypair Kubelet will generate for the pod.\n\nValid values are \"RSA3072\", \"RSA4096\", \"ECDSAP256\", \"ECDSAP384\",\n\"ECDSAP521\", and \"ED25519\".";
            type = types.str;
          };
          "maxExpirationSeconds" = mkOption {
            description = "maxExpirationSeconds is the maximum lifetime permitted for the\ncertificate.\n\nKubelet copies this value verbatim into the PodCertificateRequests it\ngenerates for this projection.\n\nIf omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver\nwill reject values shorter than 3600 (1 hour).  The maximum allowable\nvalue is 7862400 (91 days).\n\nThe signer implementation is then free to issue a certificate with any\nlifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600\nseconds (1 hour).  This constraint is enforced by kube-apiserver.\n`kubernetes.io` signers will never issue certificates with a lifetime\nlonger than 24 hours.";
            type = (types.nullOr types.int);
          };
          "signerName" = mkOption {
            description = "Kubelet's generated CSRs will be addressed to this signer.";
            type = types.str;
          };
          "userAnnotations" = mkOption {
            description = "userAnnotations allow pod authors to pass additional information to\nthe signer implementation.  Kubernetes does not restrict or validate this\nmetadata in any way.\n\nThese values are copied verbatim into the `spec.unverifiedUserAnnotations` field of\nthe PodCertificateRequest objects that Kubelet creates.\n\nEntries are subject to the same validation as object metadata annotations,\nwith the addition that all keys must be domain-prefixed. No restrictions\nare placed on values, except an overall size limitation on the entire field.\n\nSigners should document the keys and values they support. Signers should\ndeny requests that contain keys they do not recognize.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "certificateChainPath" = mkOverride 1002 null;
          "credentialBundlePath" = mkOverride 1002 null;
          "keyPath" = mkOverride 1002 null;
          "maxExpirationSeconds" = mkOverride 1002 null;
          "userAnnotations" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesSecret" =
      {

        options = {
          "items" = mkOption {
            description = "items if unspecified, each key-value pair in the Data field of the referenced\nSecret will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the Secret,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesSecretItems"
                )
              )
            );
          };
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "optional field specify whether the Secret or its key must be defined";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "items" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesSecretItems" =
      {

        options = {
          "key" = mkOption {
            description = "key is the key to project.";
            type = types.str;
          };
          "mode" = mkOption {
            description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeProjectedSourcesServiceAccountToken" =
      {

        options = {
          "audience" = mkOption {
            description = "audience is the intended audience of the token. A recipient of a token\nmust identify itself with an identifier specified in the audience of the\ntoken, and otherwise should reject the token. The audience defaults to the\nidentifier of the apiserver.";
            type = (types.nullOr types.str);
          };
          "expirationSeconds" = mkOption {
            description = "expirationSeconds is the requested duration of validity of the service\naccount token. As the token approaches expiration, the kubelet volume\nplugin will proactively rotate the service account token. The kubelet will\nstart trying to rotate the token if the token is older than 80 percent of\nits time to live or if the token is older than 24 hours.Defaults to 1 hour\nand must be at least 10 minutes.";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "path is the path relative to the mount point of the file to project the\ntoken into.";
            type = types.str;
          };
        };

        config = {
          "audience" = mkOverride 1002 null;
          "expirationSeconds" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeQuobyte" = {

      options = {
        "group" = mkOption {
          description = "group to map volume access to\nDefault is no group";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the Quobyte volume to be mounted with read-only permissions.\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "registry" = mkOption {
          description = "registry represents a single or multiple Quobyte Registry services\nspecified as a string as host:port pair (multiple entries are separated with commas)\nwhich acts as the central registry for volumes";
          type = types.str;
        };
        "tenant" = mkOption {
          description = "tenant owning the given Quobyte volume in the Backend\nUsed with dynamically provisioned Quobyte volumes, value is set by the plugin";
          type = (types.nullOr types.str);
        };
        "user" = mkOption {
          description = "user to map volume access to\nDefaults to serivceaccount user";
          type = (types.nullOr types.str);
        };
        "volume" = mkOption {
          description = "volume is a string that references an already created Quobyte volume by name.";
          type = types.str;
        };
      };

      config = {
        "group" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "tenant" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeRbd" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#rbd";
          type = (types.nullOr types.str);
        };
        "image" = mkOption {
          description = "image is the rados image name.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = types.str;
        };
        "keyring" = mkOption {
          description = "keyring is the path to key ring for RBDUser.\nDefault is /etc/ceph/keyring.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "monitors" = mkOption {
          description = "monitors is a collection of Ceph monitors.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.listOf types.str);
        };
        "pool" = mkOption {
          description = "pool is the rados pool name.\nDefault is rbd.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is name of the authentication secret for RBDUser. If provided\noverrides keyring.\nDefault is nil.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeRbdSecretRef"
            )
          );
        };
        "user" = mkOption {
          description = "user is the rados user name.\nDefault is admin.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "keyring" = mkOverride 1002 null;
        "pool" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeRbdSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeScaleIO" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\".\nDefault is \"xfs\".";
          type = (types.nullOr types.str);
        };
        "gateway" = mkOption {
          description = "gateway is the host address of the ScaleIO API Gateway.";
          type = types.str;
        };
        "protectionDomain" = mkOption {
          description = "protectionDomain is the name of the ScaleIO Protection Domain for the configured storage.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef references to the secret for ScaleIO user and other\nsensitive information. If this is not provided, Login operation will fail.";
          type = (
            submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeScaleIOSecretRef"
          );
        };
        "sslEnabled" = mkOption {
          description = "sslEnabled Flag enable/disable SSL communication with Gateway, default false";
          type = (types.nullOr types.bool);
        };
        "storageMode" = mkOption {
          description = "storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned.\nDefault is ThinProvisioned.";
          type = (types.nullOr types.str);
        };
        "storagePool" = mkOption {
          description = "storagePool is the ScaleIO Storage Pool associated with the protection domain.";
          type = (types.nullOr types.str);
        };
        "system" = mkOption {
          description = "system is the name of the storage system as configured in ScaleIO.";
          type = types.str;
        };
        "volumeName" = mkOption {
          description = "volumeName is the name of a volume already created in the ScaleIO system\nthat is associated with this volume source.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "protectionDomain" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "sslEnabled" = mkOverride 1002 null;
        "storageMode" = mkOverride 1002 null;
        "storagePool" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeScaleIOSecretRef" =
      {

        options = {
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "name" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeSecret" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode is Optional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values\nfor mode bits. Defaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "items If unspecified, each key-value pair in the Data field of the referenced\nSecret will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the Secret,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeSecretItems"
              )
            )
          );
        };
        "optional" = mkOption {
          description = "optional field specify whether the Secret or its keys must be defined";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "secretName is the name of the secret in the pod's namespace to use.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#secret";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeSecretItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeStorageos" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef specifies the secret to use for obtaining the StorageOS API\ncredentials.  If not specified, default values will be attempted.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeStorageosSecretRef"
            )
          );
        };
        "volumeName" = mkOption {
          description = "volumeName is the human-readable name of the StorageOS volume.  Volume\nnames are only unique within a namespace.";
          type = (types.nullOr types.str);
        };
        "volumeNamespace" = mkOption {
          description = "volumeNamespace specifies the scope of the volume within StorageOS.  If no\nnamespace is specified then the Pod's namespace will be used.  This allows the\nKubernetes name scoping to be mirrored within StorageOS for tighter integration.\nSet VolumeName to any name to override the default behaviour.\nSet to \"default\" if you are not using namespaces within StorageOS.\nNamespaces that do not pre-exist within StorageOS will be created.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
        "volumeNamespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeStorageosSecretRef" =
      {

        options = {
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "name" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsControllerPluginVolumesVolumeVsphereVolume" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "storagePolicyID" = mkOption {
          description = "storagePolicyID is the storage Policy Based Management (SPBM) profile ID associated with the StoragePolicyName.";
          type = (types.nullOr types.str);
        };
        "storagePolicyName" = mkOption {
          description = "storagePolicyName is the storage Policy Based Management (SPBM) profile name.";
          type = (types.nullOr types.str);
        };
        "volumePath" = mkOption {
          description = "volumePath is the path that identifies vSphere volume vmdk";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "storagePolicyID" = mkOverride 1002 null;
        "storagePolicyName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsEncryption" = {

      options = {
        "configMapName" = mkOption {
          description = "LocalObjectReference contains enough information to let you locate the\nreferenced object inside the same namespace.";
          type = (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsEncryptionConfigMapName");
        };
      };

      config = { };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsEncryptionConfigMapName" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsImageSet" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsLeaderElection" = {

      options = {
        "leaseDuration" = mkOption {
          description = "Duration in seconds that non-leader candidates will wait to force acquire leadership.\nDefault to 137 seconds.";
          type = (types.nullOr (types.withMinimum 0 types.int));
        };
        "renewDeadline" = mkOption {
          description = "Deadline in seconds that the acting leader will retry refreshing leadership before giving up.\nDefaults to 107 seconds.";
          type = (types.nullOr (types.withMinimum 0 types.int));
        };
        "retryPeriod" = mkOption {
          description = "Retry Period in seconds the LeaderElector clients should wait between tries of actions.\nDefaults to 26 seconds.";
          type = (types.nullOr (types.withMinimum 0 types.int));
        };
      };

      config = {
        "leaseDuration" = mkOverride 1002 null;
        "renewDeadline" = mkOverride 1002 null;
        "retryPeriod" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsLiveness" = {

      options = {
        "metricsPort" = mkOption {
          description = "Port to expose liveness metrics";
          type = (types.withMaximum 65535 (types.withMinimum 1024 types.int));
        };
      };

      config = { };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsLog" = {

      options = {
        "rotation" = mkOption {
          description = "log rotation for csi pods";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsLogRotation")
          );
        };
        "verbosity" = mkOption {
          description = "Log verbosity level for driver pods,\nSupported values from 0 to 5. 0 for general useful logs (the default), 5 for trace level verbosity.\nDefault to 0";
          type = (types.nullOr (types.withMaximum 5 (types.withMinimum 0 types.int)));
        };
      };

      config = {
        "rotation" = mkOverride 1002 null;
        "verbosity" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsLogRotation" = {

      options = {
        "logHostPath" = mkOption {
          description = "LogHostPath is the prefix directory path for the csi log files\nDefault to /var/lib/cephcsi";
          type = (types.nullOr types.str);
        };
        "maxFiles" = mkOption {
          description = "MaxFiles is the number of logrtoate files\nDefault to 7";
          type = (types.nullOr types.int);
        };
        "maxLogSize" = mkOption {
          description = "MaxLogSize is the maximum size of the log file per csi pods";
          type = (types.nullOr (types.either types.int types.str));
        };
        "periodicity" = mkOption {
          description = "Periodicity is the periodicity of the log rotation.";
          type = (
            types.nullOr (
              types.enum [
                "hourly"
                "daily"
                "weekly"
                "monthly"
              ]
            )
          );
        };
      };

      config = {
        "logHostPath" = mkOverride 1002 null;
        "maxFiles" = mkOverride 1002 null;
        "maxLogSize" = mkOverride 1002 null;
        "periodicity" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePlugin" = {

      options = {
        "affinity" = mkOption {
          description = "Pod's affinity settings";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinity")
          );
        };
        "annotations" = mkOption {
          description = "Pod's annotations";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "containerExtraArgs" = mkOption {
          description = "Extra arguments for containers in the node plugin daemonset.\nThe key is the container name, the value is a list of CLI arguments.\nThe operator will not override the default CLI argument values it's setting.\nThis is more for customizing what comes as default with cephcsi.\nExamples of container names: csi-rbdplugin, driver-registrar, csi-addons,\nliveness-prometheus, etc.";
          type = (types.nullOr (types.loaOf types.str));
        };
        "enableSeLinuxHostMount" = mkOption {
          description = "Control the host mount of /etc/selinux for csi plugin pods. Defaults to false";
          type = (types.nullOr types.bool);
        };
        "imagePullPolicy" = mkOption {
          description = "To indicate the image pull policy to be applied to all the containers in the csi driver pods.";
          type = (types.nullOr types.str);
        };
        "kubeletDirPath" = mkOption {
          description = "kubelet directory path, if kubelet configured to use other than /var/lib/kubelet path.";
          type = (types.nullOr types.str);
        };
        "labels" = mkOption {
          description = "Pod's labels";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "priorityClassName" = mkOption {
          description = "Pod's user defined priority class name";
          type = (types.nullOr types.str);
        };
        "resources" = mkOption {
          description = "Resource requirements for plugin's containers";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResources")
          );
        };
        "serviceAccountName" = mkOption {
          description = "Service account name to be used for driver's pods";
          type = (types.nullOr types.str);
        };
        "tolerations" = mkOption {
          description = "Pod's tolerations list";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginTolerations"
              )
            )
          );
        };
        "topology" = mkOption {
          description = "Topology settings for the plugin pods";
          type = (
            types.nullOr (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginTopology")
          );
        };
        "updateStrategy" = mkOption {
          description = "Driver's plugin daemonset update strategy, supported values are OnDelete and RollingUpdate.\nDefault value is RollingUpdate with MaxAvailabile set to 1";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginUpdateStrategy"
            )
          );
        };
        "volumes" = mkOption {
          description = "Volume and volume mount definitions to attach to the pod";
          type = (
            types.nullOr (
              types.listOf (submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumes")
            )
          );
        };
      };

      config = {
        "affinity" = mkOverride 1002 null;
        "annotations" = mkOverride 1002 null;
        "containerExtraArgs" = mkOverride 1002 null;
        "enableSeLinuxHostMount" = mkOverride 1002 null;
        "imagePullPolicy" = mkOverride 1002 null;
        "kubeletDirPath" = mkOverride 1002 null;
        "labels" = mkOverride 1002 null;
        "priorityClassName" = mkOverride 1002 null;
        "resources" = mkOverride 1002 null;
        "serviceAccountName" = mkOverride 1002 null;
        "tolerations" = mkOverride 1002 null;
        "topology" = mkOverride 1002 null;
        "updateStrategy" = mkOverride 1002 null;
        "volumes" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinity" = {

      options = {
        "nodeAffinity" = mkOption {
          description = "Describes node affinity scheduling rules for the pod.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinity"
            )
          );
        };
        "podAffinity" = mkOption {
          description = "Describes pod affinity scheduling rules (e.g. co-locate this pod in the same node, zone, etc. as some other pod(s)).";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinity"
            )
          );
        };
        "podAntiAffinity" = mkOption {
          description = "Describes pod anti-affinity scheduling rules (e.g. avoid putting this pod in the same node, zone, etc. as some other pod(s)).";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinity"
            )
          );
        };
      };

      config = {
        "nodeAffinity" = mkOverride 1002 null;
        "podAffinity" = mkOverride 1002 null;
        "podAntiAffinity" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and adding\n\"weight\" to the sum if the node matches the corresponding matchExpressions; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to an update), the system\nmay or may not try to eventually evict the pod from its node.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution"
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "preference" = mkOption {
            description = "A node selector term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference"
            );
          };
          "weight" = mkOption {
            description = "Weight associated with matching the corresponding nodeSelectorTerm, in the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreference" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "A list of node selector requirements by node's labels.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "A list of node selector requirements by node's fields.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionPreferenceMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "nodeSelectorTerms" = mkOption {
            description = "Required. A list of node selector terms. The terms are ORed.";
            type = (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms"
              )
            );
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTerms" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "A list of node selector requirements by node's labels.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions"
                )
              )
            );
          };
          "matchFields" = mkOption {
            description = "A list of node selector requirements by node's fields.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields"
                )
              )
            );
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchFields" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsMatchFields" =
      {

        options = {
          "key" = mkOption {
            description = "The label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "Represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.";
            type = types.str;
          };
          "values" = mkOption {
            description = "An array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. If the operator is Gt or Lt, the values\narray must have a single element, which will be interpreted as an integer.\nThis array is replaced during a strategic merge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and adding\n\"weight\" to the sum if the node has pods which matches the corresponding podAffinityTerm; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to a pod label update), the\nsystem may or may not try to eventually evict the pod from its node.\nWhen there are multiple elements, the lists of nodes corresponding to each\npodAffinityTerm are intersected, i.e. all terms must be satisfied.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "Required. A pod affinity term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "weight associated with matching the corresponding podAffinityTerm,\nin the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinity" = {

      options = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "The scheduler will prefer to schedule pods to nodes that satisfy\nthe anti-affinity expressions specified by this field, but it may choose\na node that violates one or more of the expressions. The node that is\nmost preferred is the one with the greatest sum of weights, i.e.\nfor each node that meets all of the scheduling requirements (resource\nrequest, requiredDuringScheduling anti-affinity expressions, etc.),\ncompute a sum by iterating through the elements of this field and subtracting\n\"weight\" from the sum if the node has pods which matches the corresponding podAffinityTerm; the\nnode(s) with the highest sum are the most preferred.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOption {
          description = "If the anti-affinity requirements specified by this field are not met at\nscheduling time, the pod will not be scheduled onto the node.\nIf the anti-affinity requirements specified by this field cease to be met\nat some point during pod execution (e.g. due to a pod label update), the\nsystem may or may not try to eventually evict the pod from its node.\nWhen there are multiple elements, the lists of nodes corresponding to each\npodAffinityTerm are intersected, i.e. all terms must be satisfied.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution"
              )
            )
          );
        };
      };

      config = {
        "preferredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
        "requiredDuringSchedulingIgnoredDuringExecution" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "podAffinityTerm" = mkOption {
            description = "Required. A pod affinity term, associated with the corresponding weight.";
            type = (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm"
            );
          };
          "weight" = mkOption {
            description = "weight associated with matching the corresponding podAffinityTerm,\nin the range 1-100.";
            type = types.int;
          };
        };

        config = { };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTerm" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionPodAffinityTermNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecution" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "A label query over a set of resources, in this case pods.\nIf it's null, this PodAffinityTerm matches with no Pods.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector"
              )
            );
          };
          "matchLabelKeys" = mkOption {
            description = "MatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both matchLabelKeys and labelSelector.\nAlso, matchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "mismatchLabelKeys" = mkOption {
            description = "MismatchLabelKeys is a set of pod label keys to select which pods will\nbe taken into consideration. The keys are used to lookup values from the\nincoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`\nto select the group of existing pods which pods will be taken into consideration\nfor the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming\npod labels will be ignored. The default value is empty.\nThe same key is forbidden to exist in both mismatchLabelKeys and labelSelector.\nAlso, mismatchLabelKeys cannot be set when labelSelector isn't set.";
            type = (types.nullOr (types.listOf types.str));
          };
          "namespaceSelector" = mkOption {
            description = "A label query over the set of namespaces that the term applies to.\nThe term is applied to the union of the namespaces selected by this field\nand the ones listed in the namespaces field.\nnull selector and null or empty namespaces list means \"this pod's namespace\".\nAn empty selector ({}) matches all namespaces.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector"
              )
            );
          };
          "namespaces" = mkOption {
            description = "namespaces specifies a static list of namespace names that the term applies to.\nThe term is applied to the union of the namespaces listed in this field\nand the ones selected by namespaceSelector.\nnull or empty namespaces list and null namespaceSelector means \"this pod's namespace\".";
            type = (types.nullOr (types.listOf types.str));
          };
          "topologyKey" = mkOption {
            description = "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching\nthe labelSelector in the specified namespaces, where co-located is defined as running on a node\nwhose value of the label with key topologyKey matches that of any node on which any of the\nselected pods is running.\nEmpty topologyKey is not allowed.";
            type = types.str;
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "matchLabelKeys" = mkOverride 1002 null;
          "mismatchLabelKeys" = mkOverride 1002 null;
          "namespaceSelector" = mkOverride 1002 null;
          "namespaces" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionNamespaceSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResources" = {

      options = {
        "addons" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesAddons"
            )
          );
        };
        "liveness" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesLiveness"
            )
          );
        };
        "logRotator" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesLogRotator"
            )
          );
        };
        "plugin" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesPlugin"
            )
          );
        };
        "registrar" = mkOption {
          description = "ResourceRequirements describes the compute resource requirements.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesRegistrar"
            )
          );
        };
      };

      config = {
        "addons" = mkOverride 1002 null;
        "liveness" = mkOverride 1002 null;
        "logRotator" = mkOverride 1002 null;
        "plugin" = mkOverride 1002 null;
        "registrar" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesAddons" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesAddonsClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesAddonsClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesLiveness" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesLivenessClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesLivenessClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesLogRotator" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesLogRotatorClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesLogRotatorClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesPlugin" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesPluginClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesPluginClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesRegistrar" = {

      options = {
        "claims" = mkOption {
          description = "Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\nThis field depends on the\nDynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers.";
          type = (
            types.nullOr (
              coerceAttrsOfSubmodulesToListByKey
                "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesRegistrarClaims"
                "name"
                [ "name" ]
            )
          );
          apply = attrsToList;
        };
        "limits" = mkOption {
          description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
        "requests" = mkOption {
          description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
          type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
        };
      };

      config = {
        "claims" = mkOverride 1002 null;
        "limits" = mkOverride 1002 null;
        "requests" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginResourcesRegistrarClaims" = {

      options = {
        "name" = mkOption {
          description = "Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.";
          type = types.str;
        };
        "request" = mkOption {
          description = "Request is the name chosen for a request in the referenced claim.\nIf empty, everything from the claim is made available, otherwise\nonly the result of this request.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "request" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginTolerations" = {

      options = {
        "effect" = mkOption {
          description = "Effect indicates the taint effect to match. Empty means match all taint effects.\nWhen specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.";
          type = (types.nullOr types.str);
        };
        "key" = mkOption {
          description = "Key is the taint key that the toleration applies to. Empty means match all taint keys.\nIf the key is empty, operator must be Exists; this combination means to match all values and all keys.";
          type = (types.nullOr types.str);
        };
        "operator" = mkOption {
          description = "Operator represents a key's relationship to the value.\nValid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.\nExists is equivalent to wildcard for value, so that a pod can\ntolerate all taints of a particular category.\nLt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).";
          type = (types.nullOr types.str);
        };
        "tolerationSeconds" = mkOption {
          description = "TolerationSeconds represents the period of time the toleration (which must be\nof effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,\nit is not set, which means tolerate the taint forever (do not evict). Zero and\nnegative values will be treated as 0 (evict immediately) by the system.";
          type = (types.nullOr types.int);
        };
        "value" = mkOption {
          description = "Value is the taint value the toleration matches to.\nIf the operator is Exists, the value should be empty, otherwise just a regular string.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "effect" = mkOverride 1002 null;
        "key" = mkOverride 1002 null;
        "operator" = mkOverride 1002 null;
        "tolerationSeconds" = mkOverride 1002 null;
        "value" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginTopology" = {

      options = {
        "domainLabels" = mkOption {
          description = "Domain labels define which node labels to use as domains for CSI nodeplugins to advertise their domains";
          type = (types.listOf types.str);
        };
      };

      config = { };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginUpdateStrategy" = {

      options = {
        "rollingUpdate" = mkOption {
          description = "Rolling update config params. Present only if type = \"RollingUpdate\".";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginUpdateStrategyRollingUpdate"
            )
          );
        };
        "type" = mkOption {
          description = "Type of daemon set update. Can be \"RollingUpdate\" or \"OnDelete\". Default is RollingUpdate.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "rollingUpdate" = mkOverride 1002 null;
        "type" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginUpdateStrategyRollingUpdate" = {

      options = {
        "maxSurge" = mkOption {
          description = "The maximum number of nodes with an existing available DaemonSet pod that\ncan have an updated DaemonSet pod during during an update.\nValue can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%).\nThis can not be 0 if MaxUnavailable is 0.\nAbsolute number is calculated from percentage by rounding up to a minimum of 1.\nDefault value is 0.\nExample: when this is set to 30%, at most 30% of the total number of nodes\nthat should be running the daemon pod (i.e. status.desiredNumberScheduled)\ncan have their a new pod created before the old pod is marked as deleted.\nThe update starts by launching new pods on 30% of nodes. Once an updated\npod is available (Ready for at least minReadySeconds) the old DaemonSet pod\non that node is marked deleted. If the old pod becomes unavailable for any\nreason (Ready transitions to false, is evicted, or is drained) an updated\npod is immediately created on that node without considering surge limits.\nAllowing surge implies the possibility that the resources consumed by the\ndaemonset on any given node can double if the readiness check fails, and\nso resource intensive daemonsets should take into account that they may\ncause evictions during disruption.";
          type = (types.nullOr (types.either types.int types.str));
        };
        "maxUnavailable" = mkOption {
          description = "The maximum number of DaemonSet pods that can be unavailable during the\nupdate. Value can be an absolute number (ex: 5) or a percentage of total\nnumber of DaemonSet pods at the start of the update (ex: 10%). Absolute\nnumber is calculated from percentage by rounding up.\nThis cannot be 0 if MaxSurge is 0\nDefault value is 1.\nExample: when this is set to 30%, at most 30% of the total number of nodes\nthat should be running the daemon pod (i.e. status.desiredNumberScheduled)\ncan have their pods stopped for an update at any given time. The update\nstarts by stopping at most 30% of those DaemonSet pods and then brings\nup new DaemonSet pods in their place. Once the new pods are available,\nit then proceeds onto other DaemonSet pods, thus ensuring that at least\n70% of original number of DaemonSet pods are available at all times during\nthe update.";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "maxSurge" = mkOverride 1002 null;
        "maxUnavailable" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumes" = {

      options = {
        "mount" = mkOption {
          description = "VolumeMount describes a mounting of a Volume within a container.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesMount"
            )
          );
        };
        "volume" = mkOption {
          description = "Volume represents a named volume in a pod that may be accessed by any container in the pod.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolume"
            )
          );
        };
      };

      config = {
        "mount" = mkOverride 1002 null;
        "volume" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesMount" = {

      options = {
        "mountPath" = mkOption {
          description = "Path within the container at which the volume should be mounted.  Must\nnot contain ':'.";
          type = types.str;
        };
        "mountPropagation" = mkOption {
          description = "mountPropagation determines how mounts are propagated from the host\nto container and the other way around.\nWhen not set, MountPropagationNone is used.\nThis field is beta in 1.10.\nWhen RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified\n(which defaults to None).";
          type = (types.nullOr types.str);
        };
        "name" = mkOption {
          description = "This must match the Name of a Volume.";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "Mounted read-only if true, read-write otherwise (false or unspecified).\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "recursiveReadOnly" = mkOption {
          description = "RecursiveReadOnly specifies whether read-only mounts should be handled\nrecursively.\n\nIf ReadOnly is false, this field has no meaning and must be unspecified.\n\nIf ReadOnly is true, and this field is set to Disabled, the mount is not made\nrecursively read-only.  If this field is set to IfPossible, the mount is made\nrecursively read-only, if it is supported by the container runtime.  If this\nfield is set to Enabled, the mount is made recursively read-only if it is\nsupported by the container runtime, otherwise the pod will not be started and\nan error will be generated to indicate the reason.\n\nIf this field is set to IfPossible or Enabled, MountPropagation must be set to\nNone (or be unspecified, which defaults to None).\n\nIf this field is not specified, it is treated as an equivalent of Disabled.";
          type = (types.nullOr types.str);
        };
        "subPath" = mkOption {
          description = "Path within the volume from which the container's volume should be mounted.\nDefaults to \"\" (volume's root).";
          type = (types.nullOr types.str);
        };
        "subPathExpr" = mkOption {
          description = "Expanded path within the volume from which the container's volume should be mounted.\nBehaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.\nDefaults to \"\" (volume's root).\nSubPathExpr and SubPath are mutually exclusive.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "mountPropagation" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "recursiveReadOnly" = mkOverride 1002 null;
        "subPath" = mkOverride 1002 null;
        "subPathExpr" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolume" = {

      options = {
        "awsElasticBlockStore" = mkOption {
          description = "awsElasticBlockStore represents an AWS Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nDeprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree\nawsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeAwsElasticBlockStore"
            )
          );
        };
        "azureDisk" = mkOption {
          description = "azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod.\nDeprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type\nare redirected to the disk.csi.azure.com CSI driver.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeAzureDisk"
            )
          );
        };
        "azureFile" = mkOption {
          description = "azureFile represents an Azure File Service mount on the host and bind mount to the pod.\nDeprecated: AzureFile is deprecated. All operations for the in-tree azureFile type\nare redirected to the file.csi.azure.com CSI driver.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeAzureFile"
            )
          );
        };
        "cephfs" = mkOption {
          description = "cephFS represents a Ceph FS mount on the host that shares a pod's lifetime.\nDeprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCephfs"
            )
          );
        };
        "cinder" = mkOption {
          description = "cinder represents a cinder volume attached and mounted on kubelets host machine.\nDeprecated: Cinder is deprecated. All operations for the in-tree cinder type\nare redirected to the cinder.csi.openstack.org CSI driver.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCinder"
            )
          );
        };
        "configMap" = mkOption {
          description = "configMap represents a configMap that should populate this volume";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeConfigMap"
            )
          );
        };
        "csi" = mkOption {
          description = "csi (Container Storage Interface) represents ephemeral storage that is handled by certain external CSI drivers.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCsi"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "downwardAPI represents downward API about the pod that should populate this volume";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeDownwardAPI"
            )
          );
        };
        "emptyDir" = mkOption {
          description = "emptyDir represents a temporary directory that shares a pod's lifetime.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEmptyDir"
            )
          );
        };
        "ephemeral" = mkOption {
          description = "ephemeral represents a volume that is handled by a cluster storage driver.\nThe volume's lifecycle is tied to the pod that defines it - it will be created before the pod starts,\nand deleted when the pod is removed.\n\nUse this if:\na) the volume is only needed while the pod runs,\nb) features of normal volumes like restoring from snapshot or capacity\n   tracking are needed,\nc) the storage driver is specified through a storage class, and\nd) the storage driver supports dynamic volume provisioning through\n   a PersistentVolumeClaim (see EphemeralVolumeSource for more\n   information on the connection between this volume type\n   and PersistentVolumeClaim).\n\nUse PersistentVolumeClaim or one of the vendor-specific\nAPIs for volumes that persist for longer than the lifecycle\nof an individual pod.\n\nUse CSI for light-weight local ephemeral volumes if the CSI driver is meant to\nbe used that way - see the documentation of the driver for\nmore information.\n\nA pod can use both types of ephemeral volumes and\npersistent volumes at the same time.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeral"
            )
          );
        };
        "fc" = mkOption {
          description = "fc represents a Fibre Channel resource that is attached to a kubelet's host machine and then exposed to the pod.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeFc"
            )
          );
        };
        "flexVolume" = mkOption {
          description = "flexVolume represents a generic volume resource that is\nprovisioned/attached using an exec based plugin.\nDeprecated: FlexVolume is deprecated. Consider using a CSIDriver instead.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeFlexVolume"
            )
          );
        };
        "flocker" = mkOption {
          description = "flocker represents a Flocker volume attached to a kubelet's host machine. This depends on the Flocker control service being running.\nDeprecated: Flocker is deprecated and the in-tree flocker type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeFlocker"
            )
          );
        };
        "gcePersistentDisk" = mkOption {
          description = "gcePersistentDisk represents a GCE Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nDeprecated: GCEPersistentDisk is deprecated. All operations for the in-tree\ngcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeGcePersistentDisk"
            )
          );
        };
        "gitRepo" = mkOption {
          description = "gitRepo represents a git repository at a particular revision.\nDeprecated: GitRepo is deprecated. To provision a container with a git repo, mount an\nEmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir\ninto the Pod's container.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeGitRepo"
            )
          );
        };
        "glusterfs" = mkOption {
          description = "glusterfs represents a Glusterfs mount on the host that shares a pod's lifetime.\nDeprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeGlusterfs"
            )
          );
        };
        "hostPath" = mkOption {
          description = "hostPath represents a pre-existing file or directory on the host\nmachine that is directly exposed to the container. This is generally\nused for system agents or other privileged things that are allowed\nto see the host machine. Most containers will NOT need this.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeHostPath"
            )
          );
        };
        "image" = mkOption {
          description = "image represents an OCI object (a container image or artifact) pulled and mounted on the kubelet's host machine.\nThe volume is resolved at pod startup depending on which PullPolicy value is provided:\n\n- Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.\n- Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.\n- IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.\n\nThe volume gets re-resolved if the pod gets deleted and recreated, which means that new remote content will become available on pod recreation.\nA failure to resolve or pull the image during pod startup will block containers from starting and may add significant latency. Failures will be retried using normal volume backoff and will be reported on the pod reason and message.\nThe types of objects that may be mounted by this volume are defined by the container runtime implementation on a host machine and at minimum must include all valid types supported by the container image field.\nThe OCI object gets mounted in a single directory (spec.containers[*].volumeMounts.mountPath) by merging the manifest layers in the same way as for container images.\nThe volume will be mounted read-only (ro) and non-executable files (noexec).\nSub path mounts for containers are not supported (spec.containers[*].volumeMounts.subpath) before 1.33.\nThe field spec.securityContext.fsGroupChangePolicy has no effect on this volume type.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeImage"
            )
          );
        };
        "iscsi" = mkOption {
          description = "iscsi represents an ISCSI Disk resource that is attached to a\nkubelet's host machine and then exposed to the pod.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes/#iscsi";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeIscsi"
            )
          );
        };
        "name" = mkOption {
          description = "name of the volume.\nMust be a DNS_LABEL and unique within the pod.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = types.str;
        };
        "nfs" = mkOption {
          description = "nfs represents an NFS mount on the host that shares a pod's lifetime\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeNfs"
            )
          );
        };
        "persistentVolumeClaim" = mkOption {
          description = "persistentVolumeClaimVolumeSource represents a reference to a\nPersistentVolumeClaim in the same namespace.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumePersistentVolumeClaim"
            )
          );
        };
        "photonPersistentDisk" = mkOption {
          description = "photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine.\nDeprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumePhotonPersistentDisk"
            )
          );
        };
        "portworxVolume" = mkOption {
          description = "portworxVolume represents a portworx volume attached and mounted on kubelets host machine.\nDeprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type\nare redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate\nis on.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumePortworxVolume"
            )
          );
        };
        "projected" = mkOption {
          description = "projected items for all in one resources secrets, configmaps, and downward API";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjected"
            )
          );
        };
        "quobyte" = mkOption {
          description = "quobyte represents a Quobyte mount on the host that shares a pod's lifetime.\nDeprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeQuobyte"
            )
          );
        };
        "rbd" = mkOption {
          description = "rbd represents a Rados Block Device mount on the host that shares a pod's lifetime.\nDeprecated: RBD is deprecated and the in-tree rbd type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeRbd"
            )
          );
        };
        "scaleIO" = mkOption {
          description = "scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes.\nDeprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeScaleIO"
            )
          );
        };
        "secret" = mkOption {
          description = "secret represents a secret that should populate this volume.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#secret";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeSecret"
            )
          );
        };
        "storageos" = mkOption {
          description = "storageOS represents a StorageOS volume attached and mounted on Kubernetes nodes.\nDeprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeStorageos"
            )
          );
        };
        "vsphereVolume" = mkOption {
          description = "vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine.\nDeprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type\nare redirected to the csi.vsphere.vmware.com CSI driver.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeVsphereVolume"
            )
          );
        };
      };

      config = {
        "awsElasticBlockStore" = mkOverride 1002 null;
        "azureDisk" = mkOverride 1002 null;
        "azureFile" = mkOverride 1002 null;
        "cephfs" = mkOverride 1002 null;
        "cinder" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "csi" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "emptyDir" = mkOverride 1002 null;
        "ephemeral" = mkOverride 1002 null;
        "fc" = mkOverride 1002 null;
        "flexVolume" = mkOverride 1002 null;
        "flocker" = mkOverride 1002 null;
        "gcePersistentDisk" = mkOverride 1002 null;
        "gitRepo" = mkOverride 1002 null;
        "glusterfs" = mkOverride 1002 null;
        "hostPath" = mkOverride 1002 null;
        "image" = mkOverride 1002 null;
        "iscsi" = mkOverride 1002 null;
        "nfs" = mkOverride 1002 null;
        "persistentVolumeClaim" = mkOverride 1002 null;
        "photonPersistentDisk" = mkOverride 1002 null;
        "portworxVolume" = mkOverride 1002 null;
        "projected" = mkOverride 1002 null;
        "quobyte" = mkOverride 1002 null;
        "rbd" = mkOverride 1002 null;
        "scaleIO" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "storageos" = mkOverride 1002 null;
        "vsphereVolume" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeAwsElasticBlockStore" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (types.nullOr types.str);
        };
        "partition" = mkOption {
          description = "partition is the partition in the volume that you want to mount.\nIf omitted, the default is to mount by volume name.\nExamples: For volume /dev/sda1, you specify the partition as \"1\".\nSimilarly, the volume partition for /dev/sda is \"0\" (or you can leave the property empty).";
          type = (types.nullOr types.int);
        };
        "readOnly" = mkOption {
          description = "readOnly value true will force the readOnly setting in VolumeMounts.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = (types.nullOr types.bool);
        };
        "volumeID" = mkOption {
          description = "volumeID is unique ID of the persistent disk resource in AWS (Amazon EBS volume).\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "partition" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeAzureDisk" = {

      options = {
        "cachingMode" = mkOption {
          description = "cachingMode is the Host Caching mode: None, Read Only, Read Write.";
          type = (types.nullOr types.str);
        };
        "diskName" = mkOption {
          description = "diskName is the Name of the data disk in the blob storage";
          type = types.str;
        };
        "diskURI" = mkOption {
          description = "diskURI is the URI of data disk in the blob storage";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType is Filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "kind expected values are Shared: multiple blob disks per storage account  Dedicated: single blob disk per storage account  Managed: azure managed data disk (only in managed availability set). defaults to shared";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "cachingMode" = mkOverride 1002 null;
        "fsType" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeAzureFile" = {

      options = {
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "secretName is the  name of secret that contains Azure Storage Account Name and Key";
          type = types.str;
        };
        "shareName" = mkOption {
          description = "shareName is the azure share Name";
          type = types.str;
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCephfs" = {

      options = {
        "monitors" = mkOption {
          description = "monitors is Required: Monitors is a collection of Ceph monitors\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.listOf types.str);
        };
        "path" = mkOption {
          description = "path is Optional: Used as the mounted root, rather than the full Ceph tree, default is /";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.bool);
        };
        "secretFile" = mkOption {
          description = "secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "secretRef" = mkOption {
          description = "secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty.\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCephfsSecretRef"
            )
          );
        };
        "user" = mkOption {
          description = "user is optional: User is the rados user name, default is admin\nMore info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "path" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretFile" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCephfsSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCinder" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is optional: points to a secret object containing parameters used to connect\nto OpenStack.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCinderSecretRef"
            )
          );
        };
        "volumeID" = mkOption {
          description = "volumeID used to identify the volume in cinder.\nMore info: https://examples.k8s.io/mysql-cinder-pd/README.md";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCinderSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeConfigMap" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode is optional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDefaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "items if unspecified, each key-value pair in the Data field of the referenced\nConfigMap will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the ConfigMap,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeConfigMapItems"
              )
            )
          );
        };
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
        "optional" = mkOption {
          description = "optional specify whether the ConfigMap or its keys must be defined";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "name" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeConfigMapItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCsi" = {

      options = {
        "driver" = mkOption {
          description = "driver is the name of the CSI driver that handles this volume.\nConsult with your admin for the correct name as registered in the cluster.";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType to mount. Ex. \"ext4\", \"xfs\", \"ntfs\".\nIf not provided, the empty value is passed to the associated CSI driver\nwhich will determine the default filesystem to apply.";
          type = (types.nullOr types.str);
        };
        "nodePublishSecretRef" = mkOption {
          description = "nodePublishSecretRef is a reference to the secret object containing\nsensitive information to pass to the CSI driver to complete the CSI\nNodePublishVolume and NodeUnpublishVolume calls.\nThis field is optional, and  may be empty if no secret is required. If the\nsecret object contains more than one secret, all secret references are passed.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCsiNodePublishSecretRef"
            )
          );
        };
        "readOnly" = mkOption {
          description = "readOnly specifies a read-only configuration for the volume.\nDefaults to false (read/write).";
          type = (types.nullOr types.bool);
        };
        "volumeAttributes" = mkOption {
          description = "volumeAttributes stores driver-specific properties that are passed to the CSI\ndriver. Consult your driver's documentation for supported values.";
          type = (types.nullOr (types.attrsOf types.str));
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "nodePublishSecretRef" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "volumeAttributes" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeCsiNodePublishSecretRef" =
      {

        options = {
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "name" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeDownwardAPI" = {

      options = {
        "defaultMode" = mkOption {
          description = "Optional: mode bits to use on created files by default. Must be a\nOptional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDefaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "Items is a list of downward API volume file";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeDownwardAPIItems"
              )
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeDownwardAPIItems" = {

      options = {
        "fieldRef" = mkOption {
          description = "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeDownwardAPIItemsFieldRef"
            )
          );
        };
        "mode" = mkOption {
          description = "Optional: mode bits used to set permissions on this file, must be an octal value\nbetween 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the '..' path. Must be utf-8 encoded. The first item of the relative path must not start with '..'";
          type = types.str;
        };
        "resourceFieldRef" = mkOption {
          description = "Selects a resource of the container: only resources limits and requests\n(limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeDownwardAPIItemsResourceFieldRef"
            )
          );
        };
      };

      config = {
        "fieldRef" = mkOverride 1002 null;
        "mode" = mkOverride 1002 null;
        "resourceFieldRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeDownwardAPIItemsFieldRef" =
      {

        options = {
          "apiVersion" = mkOption {
            description = "Version of the schema the FieldPath is written in terms of, defaults to \"v1\".";
            type = (types.nullOr types.str);
          };
          "fieldPath" = mkOption {
            description = "Path of the field to select in the specified API version.";
            type = types.str;
          };
        };

        config = {
          "apiVersion" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "Container name: required for volumes, optional for env vars";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "Specifies the output format of the exposed resources, defaults to \"1\"";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "Required: resource to select";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEmptyDir" = {

      options = {
        "medium" = mkOption {
          description = "medium represents what type of storage medium should back this directory.\nThe default is \"\" which means to use the node's default medium.\nMust be an empty string (default) or Memory.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (types.nullOr types.str);
        };
        "sizeLimit" = mkOption {
          description = "sizeLimit is the total amount of local storage required for this EmptyDir volume.\nThe size limit is also applicable for memory medium.\nThe maximum usage on memory medium EmptyDir would be the minimum value between\nthe SizeLimit specified here and the sum of memory limits of all containers in a pod.\nThe default is nil which means that the limit is undefined.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir";
          type = (types.nullOr (types.either types.int types.str));
        };
      };

      config = {
        "medium" = mkOverride 1002 null;
        "sizeLimit" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeral" = {

      options = {
        "volumeClaimTemplate" = mkOption {
          description = "Will be used to create a stand-alone PVC to provision the volume.\nThe pod in which this EphemeralVolumeSource is embedded will be the\nowner of the PVC, i.e. the PVC will be deleted together with the\npod.  The name of the PVC will be `<pod name>-<volume name>` where\n`<volume name>` is the name from the `PodSpec.Volumes` array\nentry. Pod validation will reject the pod if the concatenated name\nis not valid for a PVC (for example, too long).\n\nAn existing PVC with that name that is not owned by the pod\nwill *not* be used for the pod to avoid using an unrelated\nvolume by mistake. Starting the pod is then blocked until\nthe unrelated PVC is removed. If such a pre-created PVC is\nmeant to be used by the pod, the PVC has to updated with an\nowner reference to the pod once the pod exists. Normally\nthis should not be necessary, but it may be useful when\nmanually reconstructing a broken cluster.\n\nThis field is read-only and no changes will be made by Kubernetes\nto the PVC after it has been created.\n\nRequired, must not be nil.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplate"
            )
          );
        };
      };

      config = {
        "volumeClaimTemplate" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplate" =
      {

        options = {
          "metadata" = mkOption {
            description = "May contain labels and annotations that will be copied into the PVC\nwhen creating it. No other fields are allowed and will be rejected during\nvalidation.";
            type = (types.nullOr types.attrs);
          };
          "spec" = mkOption {
            description = "The specification for the PersistentVolumeClaim. The entire content is\ncopied unchanged into the PVC that gets created from this\ntemplate. The same fields as in a PersistentVolumeClaim\nare also valid here.";
            type = (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpec"
            );
          };
        };

        config = {
          "metadata" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpec" =
      {

        options = {
          "accessModes" = mkOption {
            description = "accessModes contains the desired access modes the volume should have.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1";
            type = (types.nullOr (types.listOf types.str));
          };
          "dataSource" = mkOption {
            description = "dataSource field can be used to specify either:\n* An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)\n* An existing PVC (PersistentVolumeClaim)\nIf the provisioner or an external controller can support the specified data source,\nit will create a new volume based on the contents of the specified data source.\nWhen the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,\nand dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.\nIf the namespace is specified, then dataSourceRef will not be copied to dataSource.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSource"
              )
            );
          };
          "dataSourceRef" = mkOption {
            description = "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty\nvolume is desired. This may be any object from a non-empty API group (non\ncore object) or a PersistentVolumeClaim object.\nWhen this field is specified, volume binding will only succeed if the type of\nthe specified object matches some installed volume populator or dynamic\nprovisioner.\nThis field will replace the functionality of the dataSource field and as such\nif both fields are non-empty, they must have the same value. For backwards\ncompatibility, when namespace isn't specified in dataSourceRef,\nboth fields (dataSource and dataSourceRef) will be set to the same\nvalue automatically if one of them is empty and the other is non-empty.\nWhen namespace is specified in dataSourceRef,\ndataSource isn't set to the same value and must be empty.\nThere are three important differences between dataSource and dataSourceRef:\n* While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.\n(Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSourceRef"
              )
            );
          };
          "resources" = mkOption {
            description = "resources represents the minimum resources the volume should have.\nUsers are allowed to specify resource requirements\nthat are lower than previous value but must still be higher than capacity recorded in the\nstatus field of the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecResources"
              )
            );
          };
          "selector" = mkOption {
            description = "selector is a label query over volumes to consider for binding.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelector"
              )
            );
          };
          "storageClassName" = mkOption {
            description = "storageClassName is the name of the StorageClass required by the claim.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1";
            type = (types.nullOr types.str);
          };
          "volumeAttributesClassName" = mkOption {
            description = "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.\nIf specified, the CSI driver will create or update the volume with the attributes defined\nin the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,\nit can be changed after the claim is created. An empty string or nil value indicates that no\nVolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,\nthis field can be reset to its previous value (including nil) to cancel the modification.\nIf the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be\nset to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource\nexists.\nMore info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/";
            type = (types.nullOr types.str);
          };
          "volumeMode" = mkOption {
            description = "volumeMode defines what type of volume is required by the claim.\nValue of Filesystem is implied when not included in claim spec.";
            type = (types.nullOr types.str);
          };
          "volumeName" = mkOption {
            description = "volumeName is the binding reference to the PersistentVolume backing this claim.";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "accessModes" = mkOverride 1002 null;
          "dataSource" = mkOverride 1002 null;
          "dataSourceRef" = mkOverride 1002 null;
          "resources" = mkOverride 1002 null;
          "selector" = mkOverride 1002 null;
          "storageClassName" = mkOverride 1002 null;
          "volumeAttributesClassName" = mkOverride 1002 null;
          "volumeMode" = mkOverride 1002 null;
          "volumeName" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSource" =
      {

        options = {
          "apiGroup" = mkOption {
            description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
            type = (types.nullOr types.str);
          };
          "kind" = mkOption {
            description = "Kind is the type of resource being referenced";
            type = types.str;
          };
          "name" = mkOption {
            description = "Name is the name of resource being referenced";
            type = types.str;
          };
        };

        config = {
          "apiGroup" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecDataSourceRef" =
      {

        options = {
          "apiGroup" = mkOption {
            description = "APIGroup is the group for the resource being referenced.\nIf APIGroup is not specified, the specified Kind must be in the core API group.\nFor any other third-party types, APIGroup is required.";
            type = (types.nullOr types.str);
          };
          "kind" = mkOption {
            description = "Kind is the type of resource being referenced";
            type = types.str;
          };
          "name" = mkOption {
            description = "Name is the name of resource being referenced";
            type = types.str;
          };
          "namespace" = mkOption {
            description = "Namespace is the namespace of resource being referenced\nNote that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.\n(Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "apiGroup" = mkOverride 1002 null;
          "namespace" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecResources" =
      {

        options = {
          "limits" = mkOption {
            description = "Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
            type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
          };
          "requests" = mkOption {
            description = "Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/";
            type = (types.nullOr (types.attrsOf (types.either types.int types.str)));
          };
        };

        config = {
          "limits" = mkOverride 1002 null;
          "requests" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeEphemeralVolumeClaimTemplateSpecSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeFc" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "lun" = mkOption {
          description = "lun is Optional: FC target lun number";
          type = (types.nullOr types.int);
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "targetWWNs" = mkOption {
          description = "targetWWNs is Optional: FC target worldwide names (WWNs)";
          type = (types.nullOr (types.listOf types.str));
        };
        "wwids" = mkOption {
          description = "wwids Optional: FC volume world wide identifiers (wwids)\nEither wwids or combination of targetWWNs and lun must be set, but not both simultaneously.";
          type = (types.nullOr (types.listOf types.str));
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "lun" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "targetWWNs" = mkOverride 1002 null;
        "wwids" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeFlexVolume" = {

      options = {
        "driver" = mkOption {
          description = "driver is the name of the driver to use for this volume.";
          type = types.str;
        };
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". The default filesystem depends on FlexVolume script.";
          type = (types.nullOr types.str);
        };
        "options" = mkOption {
          description = "options is Optional: this field holds extra command options if any.";
          type = (types.nullOr (types.attrsOf types.str));
        };
        "readOnly" = mkOption {
          description = "readOnly is Optional: defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is Optional: secretRef is reference to the secret object containing\nsensitive information to pass to the plugin scripts. This may be\nempty if no secret object is specified. If the secret object\ncontains more than one secret, all secrets are passed to the plugin\nscripts.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeFlexVolumeSecretRef"
            )
          );
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "options" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeFlexVolumeSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeFlocker" = {

      options = {
        "datasetName" = mkOption {
          description = "datasetName is Name of the dataset stored as metadata -> name on the dataset for Flocker\nshould be considered as deprecated";
          type = (types.nullOr types.str);
        };
        "datasetUUID" = mkOption {
          description = "datasetUUID is the UUID of the dataset. This is unique identifier of a Flocker dataset";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "datasetName" = mkOverride 1002 null;
        "datasetUUID" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeGcePersistentDisk" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (types.nullOr types.str);
        };
        "partition" = mkOption {
          description = "partition is the partition in the volume that you want to mount.\nIf omitted, the default is to mount by volume name.\nExamples: For volume /dev/sda1, you specify the partition as \"1\".\nSimilarly, the volume partition for /dev/sda is \"0\" (or you can leave the property empty).\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (types.nullOr types.int);
        };
        "pdName" = mkOption {
          description = "pdName is unique name of the PD resource in GCE. Used to identify the disk in GCE.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "partition" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeGitRepo" = {

      options = {
        "directory" = mkOption {
          description = "directory is the target directory name.\nMust not contain or start with '..'.  If '.' is supplied, the volume directory will be the\ngit repository.  Otherwise, if specified, the volume will contain the git repository in\nthe subdirectory with the given name.";
          type = (types.nullOr types.str);
        };
        "repository" = mkOption {
          description = "repository is the URL";
          type = types.str;
        };
        "revision" = mkOption {
          description = "revision is the commit hash for the specified revision.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "directory" = mkOverride 1002 null;
        "revision" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeGlusterfs" = {

      options = {
        "endpoints" = mkOption {
          description = "endpoints is the endpoint name that details Glusterfs topology.";
          type = types.str;
        };
        "path" = mkOption {
          description = "path is the Glusterfs volume path.\nMore info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the Glusterfs volume to be mounted with read-only permissions.\nDefaults to false.\nMore info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod";
          type = (types.nullOr types.bool);
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeHostPath" = {

      options = {
        "path" = mkOption {
          description = "path of the directory on the host.\nIf the path is a symlink, it will follow the link to the real path.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = types.str;
        };
        "type" = mkOption {
          description = "type for HostPath Volume\nDefaults to \"\"\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "type" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeImage" = {

      options = {
        "pullPolicy" = mkOption {
          description = "Policy for pulling OCI objects. Possible values are:\nAlways: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.\nNever: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.\nIfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.\nDefaults to Always if :latest tag is specified, or IfNotPresent otherwise.";
          type = (types.nullOr types.str);
        };
        "reference" = mkOption {
          description = "Required: Image or artifact reference to be used.\nBehaves in the same way as pod.spec.containers[*].image.\nPull secrets will be assembled in the same way as for the container image by looking up node credentials, SA image pull secrets, and pod spec image pull secrets.\nMore info: https://kubernetes.io/docs/concepts/containers/images\nThis field is optional to allow higher level config management to default or override\ncontainer images in workload controllers like Deployments and StatefulSets.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "pullPolicy" = mkOverride 1002 null;
        "reference" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeIscsi" = {

      options = {
        "chapAuthDiscovery" = mkOption {
          description = "chapAuthDiscovery defines whether support iSCSI Discovery CHAP authentication";
          type = (types.nullOr types.bool);
        };
        "chapAuthSession" = mkOption {
          description = "chapAuthSession defines whether support iSCSI Session CHAP authentication";
          type = (types.nullOr types.bool);
        };
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi";
          type = (types.nullOr types.str);
        };
        "initiatorName" = mkOption {
          description = "initiatorName is the custom iSCSI Initiator Name.\nIf initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface\n<target portal>:<volume name> will be created for the connection.";
          type = (types.nullOr types.str);
        };
        "iqn" = mkOption {
          description = "iqn is the target iSCSI Qualified Name.";
          type = types.str;
        };
        "iscsiInterface" = mkOption {
          description = "iscsiInterface is the interface Name that uses an iSCSI transport.\nDefaults to 'default' (tcp).";
          type = (types.nullOr types.str);
        };
        "lun" = mkOption {
          description = "lun represents iSCSI Target Lun number.";
          type = types.int;
        };
        "portals" = mkOption {
          description = "portals is the iSCSI Target Portal List. The portal is either an IP or ip_addr:port if the port\nis other than default (typically TCP ports 860 and 3260).";
          type = (types.nullOr (types.listOf types.str));
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is the CHAP Secret for iSCSI target and initiator authentication";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeIscsiSecretRef"
            )
          );
        };
        "targetPortal" = mkOption {
          description = "targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port\nis other than default (typically TCP ports 860 and 3260).";
          type = types.str;
        };
      };

      config = {
        "chapAuthDiscovery" = mkOverride 1002 null;
        "chapAuthSession" = mkOverride 1002 null;
        "fsType" = mkOverride 1002 null;
        "initiatorName" = mkOverride 1002 null;
        "iscsiInterface" = mkOverride 1002 null;
        "portals" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeIscsiSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeNfs" = {

      options = {
        "path" = mkOption {
          description = "path that is exported by the NFS server.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = types.str;
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the NFS export to be mounted with read-only permissions.\nDefaults to false.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = (types.nullOr types.bool);
        };
        "server" = mkOption {
          description = "server is the hostname or IP address of the NFS server.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#nfs";
          type = types.str;
        };
      };

      config = {
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumePersistentVolumeClaim" =
      {

        options = {
          "claimName" = mkOption {
            description = "claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume.\nMore info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims";
            type = types.str;
          };
          "readOnly" = mkOption {
            description = "readOnly Will force the ReadOnly setting in VolumeMounts.\nDefault false.";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "readOnly" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumePhotonPersistentDisk" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "pdID" = mkOption {
          description = "pdID is the ID that identifies Photon Controller persistent disk";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumePortworxVolume" = {

      options = {
        "fsType" = mkOption {
          description = "fSType represents the filesystem type to mount\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "volumeID" = mkOption {
          description = "volumeID uniquely identifies a Portworx volume";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjected" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode are the mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "sources" = mkOption {
          description = "sources is the list of volume projections. Each entry in this list\nhandles one source.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSources"
              )
            )
          );
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "sources" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSources" = {

      options = {
        "clusterTrustBundle" = mkOption {
          description = "ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field\nof ClusterTrustBundle objects in an auto-updating file.\n\nAlpha, gated by the ClusterTrustBundleProjection feature gate.\n\nClusterTrustBundle objects can either be selected by name, or by the\ncombination of signer name and a label selector.\n\nKubelet performs aggressive normalization of the PEM contents written\ninto the pod filesystem.  Esoteric PEM features such as inter-block\ncomments and block headers are stripped.  Certificates are deduplicated.\nThe ordering of certificates within the file is arbitrary, and Kubelet\nmay change the order over time.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesClusterTrustBundle"
            )
          );
        };
        "configMap" = mkOption {
          description = "configMap information about the configMap data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesConfigMap"
            )
          );
        };
        "downwardAPI" = mkOption {
          description = "downwardAPI information about the downwardAPI data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesDownwardAPI"
            )
          );
        };
        "podCertificate" = mkOption {
          description = "Projects an auto-rotating credential bundle (private key and certificate\nchain) that the pod can use either as a TLS client or server.\n\nKubelet generates a private key and uses it to send a\nPodCertificateRequest to the named signer.  Once the signer approves the\nrequest and issues a certificate chain, Kubelet writes the key and\ncertificate chain to the pod filesystem.  The pod does not start until\ncertificates have been issued for each podCertificate projected volume\nsource in its spec.\n\nKubelet will begin trying to rotate the certificate at the time indicated\nby the signer using the PodCertificateRequest.Status.BeginRefreshAt\ntimestamp.\n\nKubelet can write a single file, indicated by the credentialBundlePath\nfield, or separate files, indicated by the keyPath and\ncertificateChainPath fields.\n\nThe credential bundle is a single file in PEM format.  The first PEM\nentry is the private key (in PKCS#8 format), and the remaining PEM\nentries are the certificate chain issued by the signer (typically,\nsigners will return their certificate chain in leaf-to-root order).\n\nPrefer using the credential bundle format, since your application code\ncan read it atomically.  If you use keyPath and certificateChainPath,\nyour application must make two separate file reads. If these coincide\nwith a certificate rotation, it is possible that the private key and leaf\ncertificate you read may not correspond to each other.  Your application\nwill need to check for this condition, and re-read until they are\nconsistent.\n\nThe named signer controls chooses the format of the certificate it\nissues; consult the signer implementation's documentation to learn how to\nuse the certificates it issues.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesPodCertificate"
            )
          );
        };
        "secret" = mkOption {
          description = "secret information about the secret data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesSecret"
            )
          );
        };
        "serviceAccountToken" = mkOption {
          description = "serviceAccountToken is information about the serviceAccountToken data to project";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesServiceAccountToken"
            )
          );
        };
      };

      config = {
        "clusterTrustBundle" = mkOverride 1002 null;
        "configMap" = mkOverride 1002 null;
        "downwardAPI" = mkOverride 1002 null;
        "podCertificate" = mkOverride 1002 null;
        "secret" = mkOverride 1002 null;
        "serviceAccountToken" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesClusterTrustBundle" =
      {

        options = {
          "labelSelector" = mkOption {
            description = "Select all ClusterTrustBundles that match this label selector.  Only has\neffect if signerName is set.  Mutually-exclusive with name.  If unset,\ninterpreted as \"match nothing\".  If set but empty, interpreted as \"match\neverything\".";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelector"
              )
            );
          };
          "name" = mkOption {
            description = "Select a single ClusterTrustBundle by object name.  Mutually-exclusive\nwith signerName and labelSelector.";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "If true, don't block pod startup if the referenced ClusterTrustBundle(s)\naren't available.  If using name, then the named ClusterTrustBundle is\nallowed not to exist.  If using signerName, then the combination of\nsignerName and labelSelector is allowed to match zero\nClusterTrustBundles.";
            type = (types.nullOr types.bool);
          };
          "path" = mkOption {
            description = "Relative path from the volume root to write the bundle.";
            type = types.str;
          };
          "signerName" = mkOption {
            description = "Select all ClusterTrustBundles that match this signer name.\nMutually-exclusive with name.  The contents of all selected\nClusterTrustBundles will be unified and deduplicated.";
            type = (types.nullOr types.str);
          };
        };

        config = {
          "labelSelector" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
          "signerName" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelector" =
      {

        options = {
          "matchExpressions" = mkOption {
            description = "matchExpressions is a list of label selector requirements. The requirements are ANDed.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions"
                )
              )
            );
          };
          "matchLabels" = mkOption {
            description = "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "matchExpressions" = mkOverride 1002 null;
          "matchLabels" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesClusterTrustBundleLabelSelectorMatchExpressions" =
      {

        options = {
          "key" = mkOption {
            description = "key is the label key that the selector applies to.";
            type = types.str;
          };
          "operator" = mkOption {
            description = "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.";
            type = types.str;
          };
          "values" = mkOption {
            description = "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.";
            type = (types.nullOr (types.listOf types.str));
          };
        };

        config = {
          "values" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesConfigMap" =
      {

        options = {
          "items" = mkOption {
            description = "items if unspecified, each key-value pair in the Data field of the referenced\nConfigMap will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the ConfigMap,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesConfigMapItems"
                )
              )
            );
          };
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "optional specify whether the ConfigMap or its keys must be defined";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "items" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesConfigMapItems" =
      {

        options = {
          "key" = mkOption {
            description = "key is the key to project.";
            type = types.str;
          };
          "mode" = mkOption {
            description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesDownwardAPI" =
      {

        options = {
          "items" = mkOption {
            description = "Items is a list of DownwardAPIVolume file";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesDownwardAPIItems"
                )
              )
            );
          };
        };

        config = {
          "items" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesDownwardAPIItems" =
      {

        options = {
          "fieldRef" = mkOption {
            description = "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesDownwardAPIItemsFieldRef"
              )
            );
          };
          "mode" = mkOption {
            description = "Optional: mode bits used to set permissions on this file, must be an octal value\nbetween 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the '..' path. Must be utf-8 encoded. The first item of the relative path must not start with '..'";
            type = types.str;
          };
          "resourceFieldRef" = mkOption {
            description = "Selects a resource of the container: only resources limits and requests\n(limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.";
            type = (
              types.nullOr (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesDownwardAPIItemsResourceFieldRef"
              )
            );
          };
        };

        config = {
          "fieldRef" = mkOverride 1002 null;
          "mode" = mkOverride 1002 null;
          "resourceFieldRef" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesDownwardAPIItemsFieldRef" =
      {

        options = {
          "apiVersion" = mkOption {
            description = "Version of the schema the FieldPath is written in terms of, defaults to \"v1\".";
            type = (types.nullOr types.str);
          };
          "fieldPath" = mkOption {
            description = "Path of the field to select in the specified API version.";
            type = types.str;
          };
        };

        config = {
          "apiVersion" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesDownwardAPIItemsResourceFieldRef" =
      {

        options = {
          "containerName" = mkOption {
            description = "Container name: required for volumes, optional for env vars";
            type = (types.nullOr types.str);
          };
          "divisor" = mkOption {
            description = "Specifies the output format of the exposed resources, defaults to \"1\"";
            type = (types.nullOr (types.either types.int types.str));
          };
          "resource" = mkOption {
            description = "Required: resource to select";
            type = types.str;
          };
        };

        config = {
          "containerName" = mkOverride 1002 null;
          "divisor" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesPodCertificate" =
      {

        options = {
          "certificateChainPath" = mkOption {
            description = "Write the certificate chain at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath\nand certificateChainPath, your application needs to check that the key\nand leaf certificate are consistent, because it is possible to read the\nfiles mid-rotation.";
            type = (types.nullOr types.str);
          };
          "credentialBundlePath" = mkOption {
            description = "Write the credential bundle at this path in the projected volume.\n\nThe credential bundle is a single file that contains multiple PEM blocks.\nThe first PEM block is a PRIVATE KEY block, containing a PKCS#8 private\nkey.\n\nThe remaining blocks are CERTIFICATE blocks, containing the issued\ncertificate chain from the signer (leaf and any intermediates).\n\nUsing credentialBundlePath lets your Pod's application code make a single\natomic read that retrieves a consistent key and certificate chain.  If you\nproject them to separate files, your application code will need to\nadditionally check that the leaf certificate was issued to the key.";
            type = (types.nullOr types.str);
          };
          "keyPath" = mkOption {
            description = "Write the key at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath\nand certificateChainPath, your application needs to check that the key\nand leaf certificate are consistent, because it is possible to read the\nfiles mid-rotation.";
            type = (types.nullOr types.str);
          };
          "keyType" = mkOption {
            description = "The type of keypair Kubelet will generate for the pod.\n\nValid values are \"RSA3072\", \"RSA4096\", \"ECDSAP256\", \"ECDSAP384\",\n\"ECDSAP521\", and \"ED25519\".";
            type = types.str;
          };
          "maxExpirationSeconds" = mkOption {
            description = "maxExpirationSeconds is the maximum lifetime permitted for the\ncertificate.\n\nKubelet copies this value verbatim into the PodCertificateRequests it\ngenerates for this projection.\n\nIf omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver\nwill reject values shorter than 3600 (1 hour).  The maximum allowable\nvalue is 7862400 (91 days).\n\nThe signer implementation is then free to issue a certificate with any\nlifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600\nseconds (1 hour).  This constraint is enforced by kube-apiserver.\n`kubernetes.io` signers will never issue certificates with a lifetime\nlonger than 24 hours.";
            type = (types.nullOr types.int);
          };
          "signerName" = mkOption {
            description = "Kubelet's generated CSRs will be addressed to this signer.";
            type = types.str;
          };
          "userAnnotations" = mkOption {
            description = "userAnnotations allow pod authors to pass additional information to\nthe signer implementation.  Kubernetes does not restrict or validate this\nmetadata in any way.\n\nThese values are copied verbatim into the `spec.unverifiedUserAnnotations` field of\nthe PodCertificateRequest objects that Kubelet creates.\n\nEntries are subject to the same validation as object metadata annotations,\nwith the addition that all keys must be domain-prefixed. No restrictions\nare placed on values, except an overall size limitation on the entire field.\n\nSigners should document the keys and values they support. Signers should\ndeny requests that contain keys they do not recognize.";
            type = (types.nullOr (types.attrsOf types.str));
          };
        };

        config = {
          "certificateChainPath" = mkOverride 1002 null;
          "credentialBundlePath" = mkOverride 1002 null;
          "keyPath" = mkOverride 1002 null;
          "maxExpirationSeconds" = mkOverride 1002 null;
          "userAnnotations" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesSecret" =
      {

        options = {
          "items" = mkOption {
            description = "items if unspecified, each key-value pair in the Data field of the referenced\nSecret will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the Secret,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
            type = (
              types.nullOr (
                types.listOf (
                  submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesSecretItems"
                )
              )
            );
          };
          "name" = mkOption {
            description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
            type = (types.nullOr types.str);
          };
          "optional" = mkOption {
            description = "optional field specify whether the Secret or its key must be defined";
            type = (types.nullOr types.bool);
          };
        };

        config = {
          "items" = mkOverride 1002 null;
          "name" = mkOverride 1002 null;
          "optional" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesSecretItems" =
      {

        options = {
          "key" = mkOption {
            description = "key is the key to project.";
            type = types.str;
          };
          "mode" = mkOption {
            description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
            type = types.str;
          };
        };

        config = {
          "mode" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeProjectedSourcesServiceAccountToken" =
      {

        options = {
          "audience" = mkOption {
            description = "audience is the intended audience of the token. A recipient of a token\nmust identify itself with an identifier specified in the audience of the\ntoken, and otherwise should reject the token. The audience defaults to the\nidentifier of the apiserver.";
            type = (types.nullOr types.str);
          };
          "expirationSeconds" = mkOption {
            description = "expirationSeconds is the requested duration of validity of the service\naccount token. As the token approaches expiration, the kubelet volume\nplugin will proactively rotate the service account token. The kubelet will\nstart trying to rotate the token if the token is older than 80 percent of\nits time to live or if the token is older than 24 hours.Defaults to 1 hour\nand must be at least 10 minutes.";
            type = (types.nullOr types.int);
          };
          "path" = mkOption {
            description = "path is the path relative to the mount point of the file to project the\ntoken into.";
            type = types.str;
          };
        };

        config = {
          "audience" = mkOverride 1002 null;
          "expirationSeconds" = mkOverride 1002 null;
        };

      };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeQuobyte" = {

      options = {
        "group" = mkOption {
          description = "group to map volume access to\nDefault is no group";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the Quobyte volume to be mounted with read-only permissions.\nDefaults to false.";
          type = (types.nullOr types.bool);
        };
        "registry" = mkOption {
          description = "registry represents a single or multiple Quobyte Registry services\nspecified as a string as host:port pair (multiple entries are separated with commas)\nwhich acts as the central registry for volumes";
          type = types.str;
        };
        "tenant" = mkOption {
          description = "tenant owning the given Quobyte volume in the Backend\nUsed with dynamically provisioned Quobyte volumes, value is set by the plugin";
          type = (types.nullOr types.str);
        };
        "user" = mkOption {
          description = "user to map volume access to\nDefaults to serivceaccount user";
          type = (types.nullOr types.str);
        };
        "volume" = mkOption {
          description = "volume is a string that references an already created Quobyte volume by name.";
          type = types.str;
        };
      };

      config = {
        "group" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "tenant" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeRbd" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type of the volume that you want to mount.\nTip: Ensure that the filesystem type is supported by the host operating system.\nExamples: \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#rbd";
          type = (types.nullOr types.str);
        };
        "image" = mkOption {
          description = "image is the rados image name.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = types.str;
        };
        "keyring" = mkOption {
          description = "keyring is the path to key ring for RBDUser.\nDefault is /etc/ceph/keyring.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "monitors" = mkOption {
          description = "monitors is a collection of Ceph monitors.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.listOf types.str);
        };
        "pool" = mkOption {
          description = "pool is the rados pool name.\nDefault is rbd.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly here will force the ReadOnly setting in VolumeMounts.\nDefaults to false.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef is name of the authentication secret for RBDUser. If provided\noverrides keyring.\nDefault is nil.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeRbdSecretRef"
            )
          );
        };
        "user" = mkOption {
          description = "user is the rados user name.\nDefault is admin.\nMore info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "keyring" = mkOverride 1002 null;
        "pool" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "user" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeRbdSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeScaleIO" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\".\nDefault is \"xfs\".";
          type = (types.nullOr types.str);
        };
        "gateway" = mkOption {
          description = "gateway is the host address of the ScaleIO API Gateway.";
          type = types.str;
        };
        "protectionDomain" = mkOption {
          description = "protectionDomain is the name of the ScaleIO Protection Domain for the configured storage.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly Defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef references to the secret for ScaleIO user and other\nsensitive information. If this is not provided, Login operation will fail.";
          type = (
            submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeScaleIOSecretRef"
          );
        };
        "sslEnabled" = mkOption {
          description = "sslEnabled Flag enable/disable SSL communication with Gateway, default false";
          type = (types.nullOr types.bool);
        };
        "storageMode" = mkOption {
          description = "storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned.\nDefault is ThinProvisioned.";
          type = (types.nullOr types.str);
        };
        "storagePool" = mkOption {
          description = "storagePool is the ScaleIO Storage Pool associated with the protection domain.";
          type = (types.nullOr types.str);
        };
        "system" = mkOption {
          description = "system is the name of the storage system as configured in ScaleIO.";
          type = types.str;
        };
        "volumeName" = mkOption {
          description = "volumeName is the name of a volume already created in the ScaleIO system\nthat is associated with this volume source.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "protectionDomain" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "sslEnabled" = mkOverride 1002 null;
        "storageMode" = mkOverride 1002 null;
        "storagePool" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeScaleIOSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeSecret" = {

      options = {
        "defaultMode" = mkOption {
          description = "defaultMode is Optional: mode bits used to set permissions on created files by default.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values\nfor mode bits. Defaults to 0644.\nDirectories within the path are not affected by this setting.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "items" = mkOption {
          description = "items If unspecified, each key-value pair in the Data field of the referenced\nSecret will be projected into the volume as a file whose name is the\nkey and content is the value. If specified, the listed keys will be\nprojected into the specified paths, and unlisted keys will not be\npresent. If a key is specified which is not present in the Secret,\nthe volume setup will error unless it is marked optional. Paths must be\nrelative and may not contain the '..' path or start with '..'.";
          type = (
            types.nullOr (
              types.listOf (
                submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeSecretItems"
              )
            )
          );
        };
        "optional" = mkOption {
          description = "optional field specify whether the Secret or its keys must be defined";
          type = (types.nullOr types.bool);
        };
        "secretName" = mkOption {
          description = "secretName is the name of the secret in the pod's namespace to use.\nMore info: https://kubernetes.io/docs/concepts/storage/volumes#secret";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "defaultMode" = mkOverride 1002 null;
        "items" = mkOverride 1002 null;
        "optional" = mkOverride 1002 null;
        "secretName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeSecretItems" = {

      options = {
        "key" = mkOption {
          description = "key is the key to project.";
          type = types.str;
        };
        "mode" = mkOption {
          description = "mode is Optional: mode bits used to set permissions on this file.\nMust be an octal value between 0000 and 0777 or a decimal value between 0 and 511.\nYAML accepts both octal and decimal values, JSON requires decimal values for mode bits.\nIf not specified, the volume defaultMode will be used.\nThis might be in conflict with other options that affect the file\nmode, like fsGroup, and the result can be other mode bits set.";
          type = (types.nullOr types.int);
        };
        "path" = mkOption {
          description = "path is the relative path of the file to map the key to.\nMay not be an absolute path.\nMay not contain the path element '..'.\nMay not start with the string '..'.";
          type = types.str;
        };
      };

      config = {
        "mode" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeStorageos" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is the filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "readOnly" = mkOption {
          description = "readOnly defaults to false (read/write). ReadOnly here will force\nthe ReadOnly setting in VolumeMounts.";
          type = (types.nullOr types.bool);
        };
        "secretRef" = mkOption {
          description = "secretRef specifies the secret to use for obtaining the StorageOS API\ncredentials.  If not specified, default values will be attempted.";
          type = (
            types.nullOr (
              submoduleOf "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeStorageosSecretRef"
            )
          );
        };
        "volumeName" = mkOption {
          description = "volumeName is the human-readable name of the StorageOS volume.  Volume\nnames are only unique within a namespace.";
          type = (types.nullOr types.str);
        };
        "volumeNamespace" = mkOption {
          description = "volumeNamespace specifies the scope of the volume within StorageOS.  If no\nnamespace is specified then the Pod's namespace will be used.  This allows the\nKubernetes name scoping to be mirrored within StorageOS for tighter integration.\nSet VolumeName to any name to override the default behaviour.\nSet to \"default\" if you are not using namespaces within StorageOS.\nNamespaces that do not pre-exist within StorageOS will be created.";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "readOnly" = mkOverride 1002 null;
        "secretRef" = mkOverride 1002 null;
        "volumeName" = mkOverride 1002 null;
        "volumeNamespace" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeStorageosSecretRef" = {

      options = {
        "name" = mkOption {
          description = "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "name" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecDriverSpecDefaultsNodePluginVolumesVolumeVsphereVolume" = {

      options = {
        "fsType" = mkOption {
          description = "fsType is filesystem type to mount.\nMust be a filesystem type supported by the host operating system.\nEx. \"ext4\", \"xfs\", \"ntfs\". Implicitly inferred to be \"ext4\" if unspecified.";
          type = (types.nullOr types.str);
        };
        "storagePolicyID" = mkOption {
          description = "storagePolicyID is the storage Policy Based Management (SPBM) profile ID associated with the StoragePolicyName.";
          type = (types.nullOr types.str);
        };
        "storagePolicyName" = mkOption {
          description = "storagePolicyName is the storage Policy Based Management (SPBM) profile name.";
          type = (types.nullOr types.str);
        };
        "volumePath" = mkOption {
          description = "volumePath is the path that identifies vSphere volume vmdk";
          type = types.str;
        };
      };

      config = {
        "fsType" = mkOverride 1002 null;
        "storagePolicyID" = mkOverride 1002 null;
        "storagePolicyName" = mkOverride 1002 null;
      };

    };
    "csi.ceph.io.v1.OperatorConfigSpecLog" = {

      options = {
        "verbosity" = mkOption {
          description = "Operator's log level";
          type = (types.nullOr (types.withMaximum 3 (types.withMinimum 0 types.int)));
        };
      };

      config = {
        "verbosity" = mkOverride 1002 null;
      };

    };
    "objectbucket.io.v1alpha1.ObjectBucket" = {

      options = {
        "apiVersion" = mkOption {
          description = "\nAPIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources\n";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "\nKind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds\n";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (types.nullOr (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta"));
        };
        "spec" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "objectbucket.io.v1alpha1.ObjectBucketSpec"));
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.attrs);
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "objectbucket.io.v1alpha1.ObjectBucketClaim" = {

      options = {
        "apiVersion" = mkOption {
          description = "\nAPIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources\n";
          type = (types.nullOr types.str);
        };
        "kind" = mkOption {
          description = "\nKind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds\n";
          type = (types.nullOr types.str);
        };
        "metadata" = mkOption {
          description = "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata";
          type = (types.nullOr (globalSubmoduleOf "io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta"));
        };
        "spec" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "objectbucket.io.v1alpha1.ObjectBucketClaimSpec"));
        };
        "status" = mkOption {
          description = "";
          type = (types.nullOr types.attrs);
        };
      };

      config = {
        "apiVersion" = mkOverride 1002 null;
        "kind" = mkOverride 1002 null;
        "metadata" = mkOverride 1002 null;
        "spec" = mkOverride 1002 null;
        "status" = mkOverride 1002 null;
      };

    };
    "objectbucket.io.v1alpha1.ObjectBucketClaimSpec" = {

      options = {
        "additionalConfig" = mkOption {
          description = "";
          type = (types.nullOr types.attrs);
        };
        "bucketName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "generateBucketName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "objectBucketName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "storageClassName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "additionalConfig" = mkOverride 1002 null;
        "bucketName" = mkOverride 1002 null;
        "generateBucketName" = mkOverride 1002 null;
        "objectBucketName" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
      };

    };
    "objectbucket.io.v1alpha1.ObjectBucketSpec" = {

      options = {
        "additionalState" = mkOption {
          description = "";
          type = (types.nullOr types.attrs);
        };
        "authentication" = mkOption {
          description = "";
          type = (types.nullOr types.attrs);
        };
        "claimRef" = mkOption {
          description = "";
          type = (types.nullOr types.attrs);
        };
        "endpoint" = mkOption {
          description = "";
          type = (types.nullOr (submoduleOf "objectbucket.io.v1alpha1.ObjectBucketSpecEndpoint"));
        };
        "reclaimPolicy" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "storageClassName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "additionalState" = mkOverride 1002 null;
        "authentication" = mkOverride 1002 null;
        "claimRef" = mkOverride 1002 null;
        "endpoint" = mkOverride 1002 null;
        "reclaimPolicy" = mkOverride 1002 null;
        "storageClassName" = mkOverride 1002 null;
      };

    };
    "objectbucket.io.v1alpha1.ObjectBucketSpecEndpoint" = {

      options = {
        "additionalConfig" = mkOption {
          description = "";
          type = (types.nullOr types.attrs);
        };
        "bucketHost" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "bucketName" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "bucketPort" = mkOption {
          description = "";
          type = (types.nullOr types.int);
        };
        "region" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
        "subRegion" = mkOption {
          description = "";
          type = (types.nullOr types.str);
        };
      };

      config = {
        "additionalConfig" = mkOverride 1002 null;
        "bucketHost" = mkOverride 1002 null;
        "bucketName" = mkOverride 1002 null;
        "bucketPort" = mkOverride 1002 null;
        "region" = mkOverride 1002 null;
        "subRegion" = mkOverride 1002 null;
      };

    };

  };
in
{
  # all resource versions
  options = {
    resources = {
      "ceph.rook.io"."v1"."CephBlockPool" = mkOption {
        description = "CephBlockPool represents a Ceph Storage Pool";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephBlockPool" "cephblockpools" "CephBlockPool"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephBlockPoolRadosNamespace" = mkOption {
        description = "CephBlockPoolRadosNamespace represents a Ceph BlockPool Rados Namespace";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephBlockPoolRadosNamespace" "cephblockpoolradosnamespaces"
              "CephBlockPoolRadosNamespace"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephBucketNotification" = mkOption {
        description = "CephBucketNotification represents a Bucket Notifications";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephBucketNotification" "cephbucketnotifications"
              "CephBucketNotification"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephBucketTopic" = mkOption {
        description = "CephBucketTopic represents a Ceph Object Topic for Bucket Notifications";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephBucketTopic" "cephbuckettopics" "CephBucketTopic"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephCOSIDriver" = mkOption {
        description = "CephCOSIDriver represents the CRD for the Ceph COSI Driver Deployment";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephCOSIDriver" "cephcosidrivers" "CephCOSIDriver"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephClient" = mkOption {
        description = "CephClient represents a Ceph Client";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephClient" "cephclients" "CephClient" "ceph.rook.io" "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephCluster" = mkOption {
        description = "CephCluster is a Ceph storage cluster";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephCluster" "cephclusters" "CephCluster" "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephFilesystem" = mkOption {
        description = "CephFilesystem represents a Ceph Filesystem";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephFilesystem" "cephfilesystems" "CephFilesystem"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephFilesystemMirror" = mkOption {
        description = "CephFilesystemMirror is the Ceph Filesystem Mirror object definition";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephFilesystemMirror" "cephfilesystemmirrors"
              "CephFilesystemMirror"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephFilesystemSubVolumeGroup" = mkOption {
        description = "CephFilesystemSubVolumeGroup represents a Ceph Filesystem SubVolumeGroup";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephFilesystemSubVolumeGroup"
              "cephfilesystemsubvolumegroups"
              "CephFilesystemSubVolumeGroup"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephNFS" = mkOption {
        description = "CephNFS represents a Ceph NFS";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephNFS" "cephnfses" "CephNFS" "ceph.rook.io" "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephNVMeOFGateway" = mkOption {
        description = "CephNVMeOFGateway represents a Ceph NVMe-oF Gateway";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephNVMeOFGateway" "cephnvmeofgateways" "CephNVMeOFGateway"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephObjectRealm" = mkOption {
        description = "CephObjectRealm represents a Ceph Object Store Gateway Realm";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectRealm" "cephobjectrealms" "CephObjectRealm"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephObjectStore" = mkOption {
        description = "CephObjectStore represents a Ceph Object Store Gateway";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectStore" "cephobjectstores" "CephObjectStore"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephObjectStoreAccount" = mkOption {
        description = "CephObjectStoreAccount represent the RGW user account";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectStoreAccount" "cephobjectstoreaccounts"
              "CephObjectStoreAccount"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephObjectStoreUser" = mkOption {
        description = "CephObjectStoreUser represents a Ceph Object Store Gateway User";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectStoreUser" "cephobjectstoreusers"
              "CephObjectStoreUser"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephObjectZone" = mkOption {
        description = "CephObjectZone represents a Ceph Object Store Gateway Zone";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectZone" "cephobjectzones" "CephObjectZone"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephObjectZoneGroup" = mkOption {
        description = "CephObjectZoneGroup represents a Ceph Object Store Gateway Zone Group";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectZoneGroup" "cephobjectzonegroups"
              "CephObjectZoneGroup"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "ceph.rook.io"."v1"."CephRBDMirror" = mkOption {
        description = "CephRBDMirror represents a Ceph RBD Mirror";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephRBDMirror" "cephrbdmirrors" "CephRBDMirror"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "csi.ceph.io"."v1"."CephConnection" = mkOption {
        description = "CephConnection is the Schema for the cephconnections API";
        type = (
          types.attrsOf (
            submoduleForDefinition "csi.ceph.io.v1.CephConnection" "cephconnections" "CephConnection"
              "csi.ceph.io"
              "v1"
          )
        );
        default = { };
      };
      "csi.ceph.io"."v1"."ClientProfile" = mkOption {
        description = "ClientProfile is the Schema for the clientprofiles API";
        type = (
          types.attrsOf (
            submoduleForDefinition "csi.ceph.io.v1.ClientProfile" "clientprofiles" "ClientProfile" "csi.ceph.io"
              "v1"
          )
        );
        default = { };
      };
      "csi.ceph.io"."v1"."ClientProfileMapping" = mkOption {
        description = "ClientProfileMapping is the Schema for the clientprofilemappings API";
        type = (
          types.attrsOf (
            submoduleForDefinition "csi.ceph.io.v1.ClientProfileMapping" "clientprofilemappings"
              "ClientProfileMapping"
              "csi.ceph.io"
              "v1"
          )
        );
        default = { };
      };
      "csi.ceph.io"."v1"."Driver" = mkOption {
        description = "Driver is the Schema for the drivers API";
        type = (
          types.attrsOf (submoduleForDefinition "csi.ceph.io.v1.Driver" "drivers" "Driver" "csi.ceph.io" "v1")
        );
        default = { };
      };
      "csi.ceph.io"."v1"."OperatorConfig" = mkOption {
        description = "OperatorConfig is the Schema for the operatorconfigs API";
        type = (
          types.attrsOf (
            submoduleForDefinition "csi.ceph.io.v1.OperatorConfig" "operatorconfigs" "OperatorConfig"
              "csi.ceph.io"
              "v1"
          )
        );
        default = { };
      };
      "objectbucket.io"."v1alpha1"."ObjectBucket" = mkOption {
        description = "";
        type = (
          types.attrsOf (
            submoduleForDefinition "objectbucket.io.v1alpha1.ObjectBucket" "objectbuckets" "ObjectBucket"
              "objectbucket.io"
              "v1alpha1"
          )
        );
        default = { };
      };
      "objectbucket.io"."v1alpha1"."ObjectBucketClaim" = mkOption {
        description = "";
        type = (
          types.attrsOf (
            submoduleForDefinition "objectbucket.io.v1alpha1.ObjectBucketClaim" "objectbucketclaims"
              "ObjectBucketClaim"
              "objectbucket.io"
              "v1alpha1"
          )
        );
        default = { };
      };

    }
    // {
      "cephBlockPools" = mkOption {
        description = "CephBlockPool represents a Ceph Storage Pool";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephBlockPool" "cephblockpools" "CephBlockPool"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephBlockPoolRadosNamespaces" = mkOption {
        description = "CephBlockPoolRadosNamespace represents a Ceph BlockPool Rados Namespace";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephBlockPoolRadosNamespace" "cephblockpoolradosnamespaces"
              "CephBlockPoolRadosNamespace"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephBucketNotifications" = mkOption {
        description = "CephBucketNotification represents a Bucket Notifications";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephBucketNotification" "cephbucketnotifications"
              "CephBucketNotification"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephBucketTopics" = mkOption {
        description = "CephBucketTopic represents a Ceph Object Topic for Bucket Notifications";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephBucketTopic" "cephbuckettopics" "CephBucketTopic"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephCOSIDrivers" = mkOption {
        description = "CephCOSIDriver represents the CRD for the Ceph COSI Driver Deployment";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephCOSIDriver" "cephcosidrivers" "CephCOSIDriver"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephClients" = mkOption {
        description = "CephClient represents a Ceph Client";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephClient" "cephclients" "CephClient" "ceph.rook.io" "v1"
          )
        );
        default = { };
      };
      "cephClusters" = mkOption {
        description = "CephCluster is a Ceph storage cluster";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephCluster" "cephclusters" "CephCluster" "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephConnections" = mkOption {
        description = "CephConnection is the Schema for the cephconnections API";
        type = (
          types.attrsOf (
            submoduleForDefinition "csi.ceph.io.v1.CephConnection" "cephconnections" "CephConnection"
              "csi.ceph.io"
              "v1"
          )
        );
        default = { };
      };
      "cephFilesystems" = mkOption {
        description = "CephFilesystem represents a Ceph Filesystem";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephFilesystem" "cephfilesystems" "CephFilesystem"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephFilesystemMirrors" = mkOption {
        description = "CephFilesystemMirror is the Ceph Filesystem Mirror object definition";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephFilesystemMirror" "cephfilesystemmirrors"
              "CephFilesystemMirror"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephFilesystemSubVolumeGroups" = mkOption {
        description = "CephFilesystemSubVolumeGroup represents a Ceph Filesystem SubVolumeGroup";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephFilesystemSubVolumeGroup"
              "cephfilesystemsubvolumegroups"
              "CephFilesystemSubVolumeGroup"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephNFSes" = mkOption {
        description = "CephNFS represents a Ceph NFS";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephNFS" "cephnfses" "CephNFS" "ceph.rook.io" "v1"
          )
        );
        default = { };
      };
      "cephNVMeOFGateways" = mkOption {
        description = "CephNVMeOFGateway represents a Ceph NVMe-oF Gateway";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephNVMeOFGateway" "cephnvmeofgateways" "CephNVMeOFGateway"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephObjectRealms" = mkOption {
        description = "CephObjectRealm represents a Ceph Object Store Gateway Realm";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectRealm" "cephobjectrealms" "CephObjectRealm"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephObjectStores" = mkOption {
        description = "CephObjectStore represents a Ceph Object Store Gateway";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectStore" "cephobjectstores" "CephObjectStore"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephObjectStoreAccounts" = mkOption {
        description = "CephObjectStoreAccount represent the RGW user account";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectStoreAccount" "cephobjectstoreaccounts"
              "CephObjectStoreAccount"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephObjectStoreUsers" = mkOption {
        description = "CephObjectStoreUser represents a Ceph Object Store Gateway User";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectStoreUser" "cephobjectstoreusers"
              "CephObjectStoreUser"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephObjectZones" = mkOption {
        description = "CephObjectZone represents a Ceph Object Store Gateway Zone";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectZone" "cephobjectzones" "CephObjectZone"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephObjectZoneGroups" = mkOption {
        description = "CephObjectZoneGroup represents a Ceph Object Store Gateway Zone Group";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephObjectZoneGroup" "cephobjectzonegroups"
              "CephObjectZoneGroup"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "cephRBDMirrors" = mkOption {
        description = "CephRBDMirror represents a Ceph RBD Mirror";
        type = (
          types.attrsOf (
            submoduleForDefinition "ceph.rook.io.v1.CephRBDMirror" "cephrbdmirrors" "CephRBDMirror"
              "ceph.rook.io"
              "v1"
          )
        );
        default = { };
      };
      "clientProfiles" = mkOption {
        description = "ClientProfile is the Schema for the clientprofiles API";
        type = (
          types.attrsOf (
            submoduleForDefinition "csi.ceph.io.v1.ClientProfile" "clientprofiles" "ClientProfile" "csi.ceph.io"
              "v1"
          )
        );
        default = { };
      };
      "clientProfileMappings" = mkOption {
        description = "ClientProfileMapping is the Schema for the clientprofilemappings API";
        type = (
          types.attrsOf (
            submoduleForDefinition "csi.ceph.io.v1.ClientProfileMapping" "clientprofilemappings"
              "ClientProfileMapping"
              "csi.ceph.io"
              "v1"
          )
        );
        default = { };
      };
      "drivers" = mkOption {
        description = "Driver is the Schema for the drivers API";
        type = (
          types.attrsOf (submoduleForDefinition "csi.ceph.io.v1.Driver" "drivers" "Driver" "csi.ceph.io" "v1")
        );
        default = { };
      };
      "objectBuckets" = mkOption {
        description = "";
        type = (
          types.attrsOf (
            submoduleForDefinition "objectbucket.io.v1alpha1.ObjectBucket" "objectbuckets" "ObjectBucket"
              "objectbucket.io"
              "v1alpha1"
          )
        );
        default = { };
      };
      "objectBucketClaims" = mkOption {
        description = "";
        type = (
          types.attrsOf (
            submoduleForDefinition "objectbucket.io.v1alpha1.ObjectBucketClaim" "objectbucketclaims"
              "ObjectBucketClaim"
              "objectbucket.io"
              "v1alpha1"
          )
        );
        default = { };
      };
      "operatorConfigs" = mkOption {
        description = "OperatorConfig is the Schema for the operatorconfigs API";
        type = (
          types.attrsOf (
            submoduleForDefinition "csi.ceph.io.v1.OperatorConfig" "operatorconfigs" "OperatorConfig"
              "csi.ceph.io"
              "v1"
          )
        );
        default = { };
      };

    };
  };

  config = {
    # expose resource definitions
    inherit definitions;

    # register resource types
    types = [
      {
        name = "cephblockpools";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephBlockPool";
        attrName = "cephBlockPools";
      }
      {
        name = "cephblockpoolradosnamespaces";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephBlockPoolRadosNamespace";
        attrName = "cephBlockPoolRadosNamespaces";
      }
      {
        name = "cephbucketnotifications";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephBucketNotification";
        attrName = "cephBucketNotifications";
      }
      {
        name = "cephbuckettopics";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephBucketTopic";
        attrName = "cephBucketTopics";
      }
      {
        name = "cephcosidrivers";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephCOSIDriver";
        attrName = "cephCOSIDrivers";
      }
      {
        name = "cephclients";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephClient";
        attrName = "cephClients";
      }
      {
        name = "cephclusters";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephCluster";
        attrName = "cephClusters";
      }
      {
        name = "cephfilesystems";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephFilesystem";
        attrName = "cephFilesystems";
      }
      {
        name = "cephfilesystemmirrors";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephFilesystemMirror";
        attrName = "cephFilesystemMirrors";
      }
      {
        name = "cephfilesystemsubvolumegroups";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephFilesystemSubVolumeGroup";
        attrName = "cephFilesystemSubVolumeGroups";
      }
      {
        name = "cephnfses";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephNFS";
        attrName = "cephNFSes";
      }
      {
        name = "cephnvmeofgateways";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephNVMeOFGateway";
        attrName = "cephNVMeOFGateways";
      }
      {
        name = "cephobjectrealms";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectRealm";
        attrName = "cephObjectRealms";
      }
      {
        name = "cephobjectstores";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectStore";
        attrName = "cephObjectStores";
      }
      {
        name = "cephobjectstoreaccounts";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectStoreAccount";
        attrName = "cephObjectStoreAccounts";
      }
      {
        name = "cephobjectstoreusers";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectStoreUser";
        attrName = "cephObjectStoreUsers";
      }
      {
        name = "cephobjectzones";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectZone";
        attrName = "cephObjectZones";
      }
      {
        name = "cephobjectzonegroups";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectZoneGroup";
        attrName = "cephObjectZoneGroups";
      }
      {
        name = "cephrbdmirrors";
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephRBDMirror";
        attrName = "cephRBDMirrors";
      }
      {
        name = "cephconnections";
        group = "csi.ceph.io";
        version = "v1";
        kind = "CephConnection";
        attrName = "cephConnections";
      }
      {
        name = "clientprofiles";
        group = "csi.ceph.io";
        version = "v1";
        kind = "ClientProfile";
        attrName = "clientProfiles";
      }
      {
        name = "clientprofilemappings";
        group = "csi.ceph.io";
        version = "v1";
        kind = "ClientProfileMapping";
        attrName = "clientProfileMappings";
      }
      {
        name = "drivers";
        group = "csi.ceph.io";
        version = "v1";
        kind = "Driver";
        attrName = "drivers";
      }
      {
        name = "operatorconfigs";
        group = "csi.ceph.io";
        version = "v1";
        kind = "OperatorConfig";
        attrName = "operatorConfigs";
      }
      {
        name = "objectbuckets";
        group = "objectbucket.io";
        version = "v1alpha1";
        kind = "ObjectBucket";
        attrName = "objectBuckets";
      }
      {
        name = "objectbucketclaims";
        group = "objectbucket.io";
        version = "v1alpha1";
        kind = "ObjectBucketClaim";
        attrName = "objectBucketClaims";
      }
    ];

    resources = {
      "ceph.rook.io"."v1"."CephBlockPool" = mkAliasDefinitions options.resources."cephBlockPools";
      "ceph.rook.io"."v1"."CephBlockPoolRadosNamespace" =
        mkAliasDefinitions
          options.resources."cephBlockPoolRadosNamespaces";
      "ceph.rook.io"."v1"."CephBucketNotification" =
        mkAliasDefinitions
          options.resources."cephBucketNotifications";
      "ceph.rook.io"."v1"."CephBucketTopic" = mkAliasDefinitions options.resources."cephBucketTopics";
      "ceph.rook.io"."v1"."CephCOSIDriver" = mkAliasDefinitions options.resources."cephCOSIDrivers";
      "ceph.rook.io"."v1"."CephClient" = mkAliasDefinitions options.resources."cephClients";
      "ceph.rook.io"."v1"."CephCluster" = mkAliasDefinitions options.resources."cephClusters";
      "csi.ceph.io"."v1"."CephConnection" = mkAliasDefinitions options.resources."cephConnections";
      "ceph.rook.io"."v1"."CephFilesystem" = mkAliasDefinitions options.resources."cephFilesystems";
      "ceph.rook.io"."v1"."CephFilesystemMirror" =
        mkAliasDefinitions
          options.resources."cephFilesystemMirrors";
      "ceph.rook.io"."v1"."CephFilesystemSubVolumeGroup" =
        mkAliasDefinitions
          options.resources."cephFilesystemSubVolumeGroups";
      "ceph.rook.io"."v1"."CephNFS" = mkAliasDefinitions options.resources."cephNFSes";
      "ceph.rook.io"."v1"."CephNVMeOFGateway" = mkAliasDefinitions options.resources."cephNVMeOFGateways";
      "ceph.rook.io"."v1"."CephObjectRealm" = mkAliasDefinitions options.resources."cephObjectRealms";
      "ceph.rook.io"."v1"."CephObjectStore" = mkAliasDefinitions options.resources."cephObjectStores";
      "ceph.rook.io"."v1"."CephObjectStoreAccount" =
        mkAliasDefinitions
          options.resources."cephObjectStoreAccounts";
      "ceph.rook.io"."v1"."CephObjectStoreUser" =
        mkAliasDefinitions
          options.resources."cephObjectStoreUsers";
      "ceph.rook.io"."v1"."CephObjectZone" = mkAliasDefinitions options.resources."cephObjectZones";
      "ceph.rook.io"."v1"."CephObjectZoneGroup" =
        mkAliasDefinitions
          options.resources."cephObjectZoneGroups";
      "ceph.rook.io"."v1"."CephRBDMirror" = mkAliasDefinitions options.resources."cephRBDMirrors";
      "csi.ceph.io"."v1"."ClientProfile" = mkAliasDefinitions options.resources."clientProfiles";
      "csi.ceph.io"."v1"."ClientProfileMapping" =
        mkAliasDefinitions
          options.resources."clientProfileMappings";
      "csi.ceph.io"."v1"."Driver" = mkAliasDefinitions options.resources."drivers";
      "objectbucket.io"."v1alpha1"."ObjectBucket" = mkAliasDefinitions options.resources."objectBuckets";
      "objectbucket.io"."v1alpha1"."ObjectBucketClaim" =
        mkAliasDefinitions
          options.resources."objectBucketClaims";
      "csi.ceph.io"."v1"."OperatorConfig" = mkAliasDefinitions options.resources."operatorConfigs";

    };

    # make all namespaced resources default to the
    # application's namespace
    defaults = [
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephBlockPool";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephBlockPoolRadosNamespace";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephBucketNotification";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephBucketTopic";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephCOSIDriver";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephClient";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephCluster";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephFilesystem";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephFilesystemMirror";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephFilesystemSubVolumeGroup";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephNFS";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephNVMeOFGateway";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectRealm";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectStore";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectStoreAccount";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectStoreUser";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectZone";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephObjectZoneGroup";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "ceph.rook.io";
        version = "v1";
        kind = "CephRBDMirror";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "csi.ceph.io";
        version = "v1";
        kind = "CephConnection";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "csi.ceph.io";
        version = "v1";
        kind = "ClientProfile";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "csi.ceph.io";
        version = "v1";
        kind = "ClientProfileMapping";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "csi.ceph.io";
        version = "v1";
        kind = "Driver";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "csi.ceph.io";
        version = "v1";
        kind = "OperatorConfig";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
      {
        group = "objectbucket.io";
        version = "v1alpha1";
        kind = "ObjectBucketClaim";
        default.metadata.namespace = lib.mkDefault config.namespace;
      }
    ];
  };
}
